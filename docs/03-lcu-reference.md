# League Client (LCU) reference

The League client runs a local HTTPS server with a self-signed certificate and a WebSocket event stream. This is
the only data source for the project. It is unofficial: endpoints can change on any patch. Everything that talks
to it lives in `packages/lcu`.

**Status column:** `verified` means exercised against a live client on the listed patch and the response shape is
pinned in a zod schema plus a fixture in `packages/lcu/fixtures/`. `unverified` means from memory or community
docs. **M0 exists to turn every row we need for M2 to `verified`.** Do not build on an unverified row.

## Connecting

| Item | Detail | Status |
|---|---|---|
| Lockfile | `<install dir>/lockfile`, content `LeagueClient:<pid>:<port>:<password>:https`. Default install `C:\Riot Games\League of Legends\lockfile` on Windows, `/Applications/League of Legends.app/Contents/LoL/lockfile` on macOS. M0 can run on a Mac with League installed; packaging targets Windows. Evidence so far (2026-09-08, client 16.17.812.4632, not yet read while running): this Mac's `LeagueClient.log` says the client is `Running from cwd '/Applications/League of Legends.app/Contents/LoL'`, which is where the lockfile is written. `discoverLockfile` in `packages/lcu` tries a configured override, then `LCU_LOCKFILE_CANDIDATES` (path-delimiter-separated list, replaces the defaults; tests set it so a running client is invisible), then the platform default. Verified 2026-09-08 on macOS with the client running: the default path existed, parsed as `LeagueClient:86388:50844:<password>:https`, and the pid was the running `LeagueClient` process. Windows default not yet run. | verified (16.17, 2026-09-08) (macOS) |
| Process args fallback | On Windows, the `LeagueClientUx.exe` command line contains `--app-port=` and `--remoting-auth-token=`. Use when the install dir is unknown. Also present on macOS: this Mac's `LeagueClientUx.log` (2026-09-08) shows `--app-port=60969` on the Ux command line, distinct from the Riot Client's `--riotclient-app-port`. Confirmed again 2026-09-08 on the running macOS client: `LeagueClientUx` carries `--app-port=<port>` and `--remoting-auth-token=<password>` matching the lockfile exactly. Implemented in M2.19 (`packages/lcu/src/processDiscovery.ts`, companion 0.1.1) as the fallback when every lockfile path is missing, on `win32` only: PowerShell `Get-CimInstance Win32_Process -Filter "Name='LeagueClientUx.exe'"` selecting `ProcessId,ExecutablePath,CommandLine` as JSON (`-EncodedCommand`, so no quoting), `wmic process where name='LeagueClientUx.exe' get CommandLine,ExecutablePath,ProcessId /format:list` when PowerShell cannot start. First source: `<dirname(ExecutablePath)>\lockfile`, read like any candidate. Second: `--app-port=` and `--remoting-auth-token=` off `CommandLine` (`--app-pid=` for the pid when present, else the Ux pid), each argument individually double-quoted or bare. The command line is never logged. The exact Windows output of both commands and the argument quoting are what the pending run confirms. | observed on Windows: pending (macOS observed 16.17; parsers fixture-tested, no live Windows run yet) |
| Auth | HTTP Basic, user `riot`, password from lockfile. Accept the self-signed cert (pin Riot's root, `riotgames.pem`, if practical; otherwise `rejectUnauthorized: false` on loopback only). Riot's root is vendored at `packages/lcu/certs/riotgames.pem` (fetched from `static.developer.riotgames.com/docs/lol/riotgames.pem`, SHA-256 fingerprint in `certs/README.md`). It is SHA-1 self-signed; Node's default OpenSSL level accepts that for a trust anchor (checked locally with a synthetic chain) but rejects a SHA-1 signed *leaf*, which `{ mode: 'pinned', legacyDigests: true }` (`DEFAULT@SECLEVEL=0`) allows. **Answer (16.17, 2026-09-08, macOS): plain `pinned` (Riot's root, default security level, no legacy digests) answered HTTP 200 on the first attempt for both HTTPS and the WebSocket.** Neither `legacyDigests` nor `insecure` was needed. `smoke` still probes pinned, pinned+legacy, insecure in that order and prints which one answered, so a future patch that rotates the certificate is visible. Companion default is `pinned`. | verified (16.17, 2026-09-08) |
| WebSocket | `wss://127.0.0.1:<port>` with the same basic auth. Send `[5, "OnJsonApiEvent"]` to subscribe to everything, or `[5, "OnJsonApiEvent_lol-lobby_v2_lobby"]` for one URI. Messages are `[8, "OnJsonApiEvent...", { data, eventType: "Create"|"Update"|"Delete", uri }]`. `[6, topic]` unsubscribes. Subprotocol: `packages/lcu` offers none by default (some community Node clients offer `wamp`; the Python ones offer nothing); `record-ws` prints what the server negotiated. Verified 16.17 (2026-09-08): `[5, "OnJsonApiEvent"]` subscribes, the server negotiated no subprotocol (`protocol: ""`), events arrive as `[8, "OnJsonApiEvent", { data, eventType, uri }]` with `eventType` `Create`/`Update`/`Delete` (`Delete` carries `data: null`), and `parseFrame` accepted every frame: 1310 events over 21 minutes across two custom games, 0 malformed. Two recorders (the user's and this worktree's) on the same client saw the same events. Recording in `fixtures/16.17/ws-events.ndjson`, trimmed to the URIs the companion reads (see fixtures/README.md). The empty frame after subscribe is real: on 16.17 (2026-09-08, companion live run) the client sent one empty frame within 2 ms of `[5, "OnJsonApiEvent"]`; `LcuSocket` logs it at debug as `lcu socket empty frame` and drops it, so nothing needs to change. **Ping/pong (16.17, 2026-09-08, macOS): the client answers a standard WebSocket ping with a pong in 7 ms and echoes the payload.** `LcuSocket` pings every 30 s and terminates the socket after 10 s without a pong (close reason `heartbeat`), so a socket that survives a PC sleep open-but-dead becomes a reconnect within 40 s; whether Windows sleep actually produces that state is on the M2.6 Windows run sheet. | verified (16.17, 2026-09-08) |

## Endpoints we use

| Purpose | Method and path | Notes | Status |
|---|---|---|---|
| Client version | `GET /lol-patch/v1/game-version` | A bare JSON string, but the long build form: `"16.17.8104348+branch.releases-16-17.code.public.content.release.anticheat.vanguard"`. Only `major.minor` is meaningful; `patchFromVersion` extracts it and `smoke` uses it (`16.17`) as the fixture directory name. `GameVersionSchema`. | verified (16.17, 2026-09-08) |
| Client version (fallback) | `GET /system/v1/builds` | `{ branch: "Releases/16.17", buildType: "Public", patchline: "Maclive", patchlineVisibleName: "", version: "16.17.812.4632" }`; this is where the short version lives. `smoke` falls back to it when `game-version` fails, then to `unknown-<date>`. `SystemBuildsSchema`. | verified (16.17, 2026-09-08) |
| Local player | `GET /lol-summoner/v1/current-summoner` | `puuid`, `summonerId`, `accountId`, `gameName`, `tagLine`, `summonerLevel`, `profileIconId`, `privacy`; `displayName`/`internalName` are empty strings now (Riot ID replaced summoner names). `SummonerSchema`. | verified (16.17, 2026-09-08) |
| Lookup by Riot ID | `GET /lol-summoner/v1/alias/lookup?gameName=&tagLine=` | Returns `{ puuid, alias: { gameName, tagLine } }` (`AliasLookupSchema`). Then `GET /lol-summoner/v2/summoners/puuid/{puuid}` returns the same shape as current-summoner (`SummonerSchema`), including `summonerId`; verified for the local player and for another player's puuid. | verified (16.17, 2026-09-08) |
| Own rank | `GET /lol-ranked/v1/current-ranked-stats` | `queueMap.RANKED_SOLO_5x5.{queueType, tier, division, leaguePoints, wins, losses, isProvisional, highestTier, ...}`, also `RANKED_FLEX_SR`; `queueMap` has seven queues on 16.17 (TFT ones included) and only the two SR ones are typed. Unranked: `tier: ""`, `division: "NA"`. Also `highestRankedEntry`, `highestRankedEntrySR`. `RankedStatsSchema`. | verified (16.17, 2026-09-08) |
| Rank of another player | `GET /lol-ranked/v1/ranked-stats/{puuid}` | Same shape and schema. **Works for a non-friend** (a puuid taken from a past custom game returned Silver II / Platinum III) and for a friend (Platinum II); no empty response seen. Caveat: `losses` read `0` for both other players while `wins` looked real, so only `tier/division/leaguePoints` (and maybe `wins`) are trustworthy for others. Fixture `ranked-stats-by-puuid--other.json`. The same shape is also pushed unasked over the WebSocket as `/lol-ranked/v1/cached-ranked-stats/{puuid}` `Update` events for lobby members and friends (`ranked-stats-by-puuid--ws-cached.json`, question 10); the GET is the main road, and M2.4 takes the push only as a shortcut for a puuid the server has just asked about (`ranksNeeded`), never for anyone else. | verified (16.17, 2026-09-08) |
| Gameflow phase | `GET /lol-gameflow/v1/gameflow-phase` | A bare JSON string: `"None"` idle, `"InProgress"` in game, `"EndOfGame"` on the end-of-game screen, `"Lobby"` back in the lobby (all seen by GET). The WS stream (uri `/lol-gameflow/v1/gameflow-phase`, 20 events) showed the whole custom-game sequence `Lobby -> Matchmaking -> ReadyCheck -> ChampSelect -> GameStart -> InProgress -> WaitingForStats -> PreEndOfGame -> EndOfGame` (a custom vs bots still passes Matchmaking and ReadyCheck within 25 ms) and, for a game the server dropped, `WaitingForStats -> TerminatedInError -> None` (twice). `KNOWN_GAMEFLOW_PHASES` has all eleven; the schema keeps a plain string so an unlisted phase is logged, not dropped. | verified (16.17, 2026-09-08) |
| Gameflow session | `GET /lol-gameflow/v1/session` | Captured by GET on the end-of-game screen (`gameflow-session.json`, phase `EndOfGame`) and, in a worktree run, while `InProgress`; same shape both times: `phase`, `gameData { gameId, isCustomGame: true, gameName, password (empty; never persist), queue { id: 3100, name: "SR Blind Pick Custom", type: "NORMAL", isCustom: true, gameMode: "CLASSIC", mapId }, teamOne[] / teamTwo[] { puuid, summonerId, championId, teamParticipantId, selectedPosition }, playerChampionSelections[], spectatorsAllowed }`, `map { id: 11, name, gameMode }`, `gameClient { running, visible, serverIp, serverPort }`, `gameDodge`. `gameData.gameId` is the same id match history uses; in the WS stream (58 events) it is `0` through `Lobby`/`Matchmaking`/`ChampSelect` and becomes real on the last `ChampSelect` update, 12 ms before `GameStart`, so the id is known before the game loads. `queue.id` is `-1` in `None`. `queue.type` is `NORMAL` even for customs: use `isCustomGame`. Idle: 404 (user's first smoke run; body not saved). **`Lobby` phase is not always `gameId: 0`:** in the lobby the client returns to after the end-of-game screen (`gameflow-session--in-lobby.json`, GET at 17:32 UTC, and 9 WS updates in the second window) the phase is `Lobby` but `gameData.gameId` is still the previous game's (4000969091), `teamOne` still lists only the local player with last game's champion, `gameClient.serverIp` is last game's server, and `teamTwo` is empty although a second human sat in the lobby's `customTeam200`. The session is never the lobby roster, and a `gameId` read while in `Lobby` is stale: take the id from `GameStart` onward or from the end-of-game block. `GameflowSessionSchema`. | verified (16.17, 2026-09-08) |
| Lobby | `GET /lol-lobby/v2/lobby` | `partyId`, `members[] { puuid, summonerId, summonerName, teamId, isLeader, isSpectator }`, `gameConfig { customLobbyName, customTeam100[], customTeam200[], customSpectators[], mapId, gameMode, queueId }`, `localMember`. Verified by GET in a custom lobby (`lobby.json`) and by 32 WS events across two lobbies. Real shape: `{ partyId, partyType, canStartActivity, gameConfig { queueId: 3100, gameMode: "CLASSIC", isCustom: true, mapId: 11, customLobbyName, customMutatorName: "SimulPickStrategy", customSpectatorPolicy, maxTeamSize, customTeam100[], customTeam200[], customSpectators[] }, members[], localMember, invitations[], restrictions, warnings, mucJwtDto, multiUserChatPassword }`. Members (`LobbyMemberSchema`): `puuid, summonerId, summonerName (empty), summonerLevel, summonerIconId, isBot, isLeader, isSpectator, ready, teamId, botChampionId, botDifficulty, position preferences`. No `gameName`/`tagLine` here. **`members[].teamId` is always `0` in a custom lobby; the side is which of `customTeam100`/`customTeam200` holds the puuid** (question 3). Bots are in `customTeam100/200` with `isBot: true`, `puuid: ""`, `summonerId: 0`, and are not in `members[]`. 404 `LOBBY_NOT_FOUND` when not in one. WS: `Create` on creation, two `Update`s per change, `Delete` (data null) 30-100 ms after the phase hits `GameStart`; `/lol-lobby/v2/lobby/members` fires the members array alone. Chat credentials in the body: never persist it raw. **Two humans (second capture, 17:32 UTC, `lobby--two-players.json` plus 9 WS updates):** a friend invited from the client UI joined the same party (`partyId` unchanged from `lobby.json` 28 minutes earlier) as a player, auto-placed on the empty side (`customTeam200`); `members[]` then held two entries, leader first, distinct `puuid`/`summonerId`, both `isSpectator: false`, both `teamId: 0`; `customTeam100`/`200` one puuid each; `customSpectators: []`. In every one of the 39 lobby events across both windows the human puuids in `members[]` equal the union of `customTeam100`/`customTeam200`/`customSpectators` (tested), so a member never appears without a side. The friend (not leader, `partyType: "open"`) had `allowedInviteOthers: true`, `allowedStartActivity: false`, and could move to blue and back from their own client (`customTeam100` held both humans for 1 s; `members[]` order and `teamId` unchanged). `invitations[]` is typed now (`LobbyInvitationSchema`): `{ invitationId: "", invitationType: "invalid", state: "Accepted" \| "Pending", timestamp: "0" \| "<epoch ms as string>", toPuuid, toSummonerId, toSummonerName: "" }`, one `Accepted` row per current member and one `Pending` row per outstanding invite (a `Pending` row for a third friend survived 29 minutes untouched). One `toSummonerId` was `2686822975473024` (over 2^51: a safe JS integer, not a 32-bit one; store as bigint). `summonerLevel`/`summonerIconId` come with each member, `gameName` still does not. **Spectator (third window, `lobby--spectator.json`, from the WS payload):** the friend in the spectator slot stayed in `members[]` with `isSpectator: true`, `teamId: 0`, and appeared in `customSpectators[]`; back on a team both flipped in the same `Update` (question 9). `LobbySchema`. Read end to end by the M2.2 lobby watcher on 2026-09-08 (macOS, 16.17): the connect-time GET of a UI-created custom lobby answered 200 and the mapped roster landed in the local `lobbies`/`lobby_members` with side 100 from `customTeam100`. | verified (16.17, 2026-09-08) |
| Custom game dialog data | `GET /lol-game-queues/v1/custom` | `LolGameQueuesQueueCustomGame`: `{ subcategories[] { mapId, gameMode, mutators[] { id, name, pickMode, banMode, numPlayersPerTeamOverride, ... }, numPlayersPerTeam, queueAvailability, customSpectatorPolicies[] }, queueAvailability, spectatorPolicies[], spectatorSlotLimit, gameServerRegions[] | null }` per the client's OpenAPI document for 16.17.812.4632 (2026-09-10). It is what the client's Create Custom dialog binds (`queues: { api: "gameQueues", path: "/v1/custom" }`), and the `mutators[].id` of the chosen entry is what the dialog sends as both `queueId` and `mutators.id` of the create body. Read by the corrected `verify-commands` (printed and saved as `custom-game-queues.json`) and by the companion's `create_lobby` executor; `smoke` captures it too. **Confirmed, 16.18, 2026-09-12 (fixture `custom-game-queues.json`, from the same `--verify-commands` run as the create row below):** `gameServerRegions` is **`null`**, not absent — the schema had `.optional()` alone, which rejects `null` and would have failed `CustomGameQueuesSchema.safeParse` on every real client (fixed to `.nullish()`, caught by review 2026-09-13). More importantly, **every dialog mutator on this patch carries no descriptive text at all**: `name` is just the `id` as a string (`"3100"`, `"3110"`, ...), `pickMode` and `banMode` are `""`, for blind, draft, all-random and tournament draft alike — not only blind, as the 2026-09-10 evidence (queue 3100's game-type config) had suggested. So a create body's mode can never be resolved from this endpoint's own text on 16.18; see the queue-list row below for what names it. Schema `CustomGameQueuesSchema` (loose), pinned to the fixture in `schemas.test.ts`. | verified (16.18, 2026-09-12) |
| Queue list | `GET /lol-game-queues/v1/queues` | `LolGameQueuesQueue[]`; the fields we read are `id, name, gameMode, mapId, isCustom, category, gameTypeConfig { id, name, pickMode, banMode }`. The same shape the client logs as `UpdateQueueData` (this Mac's `LeagueClient.log`, 16.17, 2026-09-08): queue `3100` "SR Blind Pick Custom", CLASSIC, map 11, `isCustom`, `gameTypeConfig { id: 19, name: "", pickMode: "" }`, `isTeamBuilderManaged: true`; queue `3220` "Howling Abyss All Random" custom, `gameTypeConfig { id: 21, name: "GAME_CFG_TEAM_BUILDER_RANDOM", pickMode: "AllRandomPickStrategy" }`; practice tool is queue `3140` (the UI hard-codes it with `mutators.id: 1`). Read by the corrected `verify-commands` to name the custom ids in the report (`game-queues.json`); `smoke` captures it. **Confirmed, 16.18, 2026-09-12 (fixture `game-queues.json`, same run):** unlike the dialog above, this list *does* name the Summoner's Rift customs, and its `id` equals the dialog mutator `id` for the same entry: `3100` "SR Blind Pick Custom", `3110` "SR Draft Pick Custom", `3120` "SR All Random", `3130` "SR Tournament Draft". Because the dialog names nothing on this patch (row above), the companion's `create_lobby` executor now reads this list too and joins on `id` to resolve the mode (`customLobbyIdsFor` in `writes.ts`; docs/04-decisions.md, 2026-09-13). Schema `GameQueuesSchema` (loose), pinned to the fixture in `schemas.test.ts`. | verified (16.18, 2026-09-12) |
| Create custom lobby | `POST /lol-lobby/v2/lobby` | **First live run, 16.17, 2026-09-09 (Windows, `Kustom.exe --verify-commands`): refused.** The community body `{ customGameLobby: { configuration: { gameMode: "CLASSIC", mapId: 11, mutators: { id: 1 }, spectatorPolicy: "AllAllowed", teamSize: 5, gameServerRegion: "" }, lobbyName: "customs-verify", lobbyPassword: "1234" }, isCustom: true }` answered `500 { errorCode: "RPC_ERROR", httpStatus: 500, implementationDetails: {}, message: "INVALID_LOBBY" }`, and so did the same body with `mutators.id: 2` (`create-lobby--legacy-blind.json`, `create-lobby--legacy-draft.json`; `GET /lol-lobby/v2/lobby` stayed 404 after both, so nothing was created). **What the 16.17 client itself sends (2026-09-10, read from the installed lobby UI plugin `rcp-fe-lol-parties` on this Mac and the client's OpenAPI document for 16.17.812.4632; not yet captured as our POST):** `{ customGameLobby: { configuration: { gameMode, gameMutator: "", gameServerRegion: "", mapId, mutators: { id: <dialog entry id> }, spectatorPolicy: "AllAllowed", spectatorDelayEnabled: true, teamSize: 5, hidePublicly: false, aramMapMutator: "NONE" }, lobbyName, hidePublicly: false, lobbyPassword: <string, or null when empty> }, queueId: <the same dialog entry id> }` — a top-level `queueId` (required by `LolLobbyLobbyChangeGameDto`; the DTO has no `isCustom`), `mutators.id` and `queueId` set to the **same** number (`getQueueId` in the UI is the identity), taken from the entry the person picked in `GET /lol-game-queues/v1/custom` `subcategories[].mutators[]` (row below). The community ids 1/2/4/6 are not what the dialog offers on 16.17: the server's party record for the user's own UI-made blind lobby (this Mac's `LeagueClient.log`, 2026-09-08) was `queueId: 3100, gameTypeConfigId: 19`, and queue 3100's `gameTypeConfig` is `{ id: 19, name: "", pickMode: "" }`. Whether the dialog's entry id for that lobby was 19 or 3100 is the one thing the evidence does not settle, so the corrected probe tries, in this order and stopping at the first 2xx: (1) the dialog body with the live entry id from `/lol-game-queues/v1/custom`; (2) the dialog body with `queueId: 3100, mutators.id: 19`; (3) the dialog body with `queueId: 3100, mutators.id: 3100`; (4) every field of `LolLobbyLobbyCustomGameConfiguration` with `mutators` and `gameTypeConfig` both `{ id: 19 }` and `queueId: 3100`; (5) the refused community body plus `queueId: 3100`. Each attempt is a fixture `create-lobby--<candidate>.json`; the accepted one is also `create-lobby.json`; the opt-in draft repeat (accepted shape, the dialog's draft entry) is `create-lobby--draft.json`. Code: `createLobbyBody` / `createLobbyCandidates` / `postCreateLobbyCandidates` in `packages/lcu/src/writes.ts`; the companion's executor takes the ids from the live dialog data joined against the queue list (`customLobbyIdsFor`, draft; see the two rows above and docs/04-decisions.md, 2026-09-13) and nacks when neither names a draft entry. Answer body still taken as `unknown`; the companion re-reads `GET /lol-lobby/v2/lobby` (verified) for `partyId`/`customLobbyName`. **Confirmed, 16.18, 2026-09-12 (Windows, `Kustom.exe --verify-commands`, second edition, one friend online):** the first candidate tried, `ui-live-3110` (the `ui` shape with mutator id 3110, body `{"customGameLobby":{"configuration":{"gameMode":"CLASSIC","gameMutator":"","gameServerRegion":"","mapId":11,"mutators":{"id":3110},"spectatorPolicy":"AllAllowed","spectatorDelayEnabled":true,"teamSize":5,"hidePublicly":false,"aramMapMutator":"NONE"},"lobbyName":"customs-verify","hidePublicly":false,"lobbyPassword":"[redacted]"},"queueId":3110}`), answered `200`; the follow-up `GET /lol-lobby/v2/lobby` read back `queueId: 3110, isCustom: true, customMutatorName: "TeamBuilderDraftPickStrategy"`. So the `ui` shape (queueId at the top equal to `mutators.id`, no `isCustom`) is the right one, and 3110 is a valid queue id for it (not only 3100 — the pair is read from the client each time, never hardcoded). **Correction (2026-09-13, reviewer-caught):** the `3110` above was **not** resolved by the code — the tool's own auto-pick for draft answered `none` that run (`custom-game-queues.json` shows every dialog mutator on this patch is wordless: `name` is just the `id` as a string, `pickMode`/`banMode` are `""`), so the person running `verify-commands` typed `3110` at the prompt by reading it off the printed subcategory list. The report line calling it "the `ui` shape with the live dialog entry from `/lol-game-queues/v1/custom`" overstated what the code did. What actually resolves 3110 with no human input, added the same day: joining the dialog's mutator ids against `GET /lol-game-queues/v1/queues` (`game-queues.json` names 3110 "SR Draft Pick Custom"), see the two rows above, `customLobbyIdsFor` in `writes.ts`, and docs/04-decisions.md, 2026-09-13. The `dto-full` and `legacy-queue` fallback shapes were never tried. Fixtures `create-lobby--ui-live-3110.json` (the full accepted-candidate envelope) and `create-lobby.json` (same lobby, envelope id `create-lobby`, note "accepted candidate ui-live-3110") in `packages/lcu/fixtures/16.18/`, plus `custom-game-queues.json` and `game-queues.json` from the same run. `LOBBY_WRITE_VERIFICATION.create_lobby` is now `{ verified: true, patch: '16.18', date: '2026-09-12' }`. | verified (16.18, 2026-09-12) |
| Invite | `POST /lol-lobby/v2/lobby/invitations` | Body `[{ toSummonerId }]`: confirmed as **what the 16.17 client's own UI sends** (`invitePlayer` in `rcp-fe-lol-parties`, read 2026-09-10; the DTO `LolLobbyLobbyInvitationDto` also has `toPuuid`, so `[{ toPuuid }]` stays the 4xx fallback). **First live run, 16.17, 2026-09-09: not exercised** — the create had failed, so both `[{ toSummonerId: 80934556 }]` and `[{ toPuuid }]` answered `404 RPC_ERROR LOBBY_NOT_FOUND` (`lobby-invitations--no-lobby.json`, `lobby-invitations--by-puuid--no-lobby.json`), which says nothing about the body. What the client shows *after* an invite is verified (see Lobby): an `invitations[]` row `{ state: "Pending", toPuuid, toSummonerId, timestamp: "<epoch ms>" }` that turns `Accepted` in the same `Update` that adds the member. `inviteWithFallback` in `writes.ts` sends `toSummonerId` first and `toPuuid` on a 4xx (a 5xx or no answer does not fall back); the companion's result `state` comes from the `invitations[]` row of a verified GET afterwards. **Confirmed, 16.18, 2026-09-12 (Windows, `Kustom.exe --verify-commands`, one friend online, against the lobby probe 1 of the same run created):** `POST /lol-lobby/v2/lobby/invitations` with `[{"toSummonerId":55838205}]` answered `200`; `[{ toPuuid }]` was never tried this run since the first attempt was already accepted. Fixture `lobby-invitations.json` in `packages/lcu/fixtures/16.18/`. `LOBBY_WRITE_VERIFICATION.invite` is now `{ verified: true, patch: '16.18', date: '2026-09-12' }`. | verified (16.18, 2026-09-12) |
| Switch side | `POST /lol-lobby/v2/lobby/team/{team}` | `team` is `TEAM1` (side 100), `TEAM2` (200) or `SPECTATOR`; no body; **the target is in the path, it is not a toggle.** This is what the 16.17 client's own UI does (`switchTeams` / `joinSpectators` in `rcp-fe-lol-parties`, read 2026-09-10) and the only team-move route in the client's OpenAPI document for 16.17.812.4632 (`PostLolLobbyV2LobbyTeamByTeam`, `team: string`). The community paths `/lol-lobby/v1/lobby/custom/switch-teams` and `/lol-lobby/v2/lobby/custom/switch-teams` appear in neither, so they were dropped from the code and the allow-list (which holds exactly `/team/TEAM1` and `/team/TEAM2`; `SPECTATOR` is never sent). **First live run, 16.17, 2026-09-09: not exercised** (no lobby). `postSwitchSide(client, side)` in `writes.ts`; the companion reads the lobby before (refusing when already on the target side, when the target side holds five, or when the local player is a spectator) and after (the ack carries the side actually read back; a 2xx that moved nobody is `client_rejected`). **Confirmed, 16.18, 2026-09-12 (Windows, `Kustom.exe --verify-commands`):** `POST /lol-lobby/v2/lobby/team/TEAM2` with no body answered `204`; a follow-up `GET /lol-lobby/v2/lobby` confirmed the local player moved from side 100 to side 200. The optional full-side repeat was not run. Fixture `lobby-team.json` in `packages/lcu/fixtures/16.18/`. `LOBBY_WRITE_VERIFICATION.switch_side` is now `{ verified: true, patch: '16.18', date: '2026-09-12' }`. | verified (16.18, 2026-09-12) |
| End of game stats | `GET /lol-end-of-game/v1/eog-stats-block` | Verified by GET on the end-of-game screen (`eog-stats-block.json`, solo custom vs five bots) and by 5 WS events (two games). Real shape: `{ gameId, gameLength (s), gameType: "CUSTOM_GAME", gameMode, queueType: "NORMAL", ranked, invalid, gameEndedInEarlySurrender, teamEarlySurrendered, endOfGameTimestamp, teams[] { teamId, isWinningTeam, isPlayerTeam, players[] }, localPlayer, mucJwtDto, multiUserChatPassword, ... }`. **No `queueId` key. No `teams[].bans`** — draft bans are only on match-history `teams[]`; a live eog row gets that list copied on later (2026-09-13). Players: `puuid, summonerId, riotIdGameName, riotIdTagLine, summonerName, championId, championName, teamId, isLocalPlayer, botPlayer, leaver, wasAfk, detectedTeamPosition, stats`. Stats hold both uppercase keys (`CHAMPIONS_KILLED`, `NUM_DEATHS`, `ASSISTS`, `GOLD_EARNED`, `TOTAL_DAMAGE_DEALT_TO_CHAMPIONS`, `MINIONS_KILLED`, `NEUTRAL_MINIONS_KILLED`, `LEVEL`, `VISION_SCORE`, `TOTAL_DAMAGE_SELF_MITIGATED`, `WIN` 0/1; not `KILLS`/`DEATHS`) and camelCase duplicates (`kills`, `deaths`, ...). **`VISION_SCORE` and `TOTAL_DAMAGE_SELF_MITIGATED` corroborated for real human 5v5 customs, patch 16.18 (inferred; M7.7, 2026-09-15):** not a captured fixture — a read against the hosted "kustom" project's `games` table (`select g.id, g.started_at, g.duration_s, (select count(*) from game_players gp where gp.game_id = g.id) as player_count, (g.raw::text ilike '%VISION_SCORE%' or g.raw::text ilike '%visionScore%') as has_vision, (g.raw::text ilike '%TOTAL_DAMAGE_SELF_MITIGATED%' or g.raw::text ilike '%damageSelfMitigated%') as has_mitigation from games g where g.raw is not null and g.duration_s > 300 order by g.started_at desc limit 15`) read `player_count = 10` — via `game_players`, which already drops bot rows before ingest (M2.10), so this is the actual per-game human count, not a guess from the blob's text — **and** both booleans true, for all 15 of the most recent games, dated 2026-09-12 to 2026-09-15 (durations 1003s-3426s), ruling out a solo-vs-bots or aborted block for every one of them. Two honest limits on this read: it does not select `games.source`, so it cannot rule out some of the 15 being backfilled match-history rows rather than live eog rows (see the Match detail row below, whose own proof does not have this problem); and the eog block carries no `gameVersion` field to read a patch off directly, so **16.18 here is inferred**, not read from this query — it is the patch this same document's M4.1 rows confirm was running on 2026-09-12, the start of this date window, with no patch change documented since. Both schemas are `z.looseObject`, so both keys already survive parsing and `scrubValue` unchanged regardless of any of the above (`EogPlayerStatsSchema` already lists `VISION_SCORE` explicitly, required; `TOTAL_DAMAGE_SELF_MITIGATED` passes through as an unlisted key, same as any of the other 182 per-player stat keys — 183 total, counted directly off `player.stats` in `eog-stats-block.json`, not the ~50 an earlier draft of this row guessed) — no schema change was needed. **Still open:** a live capture of a full 5v5 human eog block, with ten distinct real puuids and non-bot values read straight off the WS/GET response, has not been taken; the hosted-DB read only proves the keys survive ingest and storage on genuine ten-player games in production. Bots: `botPlayer: true`, puuid `00000000-0000-0000-0000-000000000000`, `summonerId: 0`, tag `BOT`. Timing (question 2): WS `Create` 0.3 s after `WaitingForStats`, `Update` 75 ms later, both before `EndOfGame`; the GET stayed 200 for the whole time the end-of-game screen was up (checked at +33 s and +4 min) and was 404 again once the client was back in the lobby (+10 min). For the server-dropped game a `Delete` came 0.2 s after `Create`. **Units and the missing start time:** `gameLength` is **seconds** (913 for the 15-minute game) and `endOfGameTimestamp` is **epoch milliseconds** (1788886380672 = 2026-09-08T16:53:00.672Z, 4 ms before the WS `Create` for this block); there is **no start time in the block at all**. So `started_at` is derived: `endOfGameTimestamp - gameLength * 1000` (M2.10), which for `eog-stats-block.json` is 1788885467672 = 2026-09-08T16:37:47.672Z — 8.8 s after the WS `GameStart`/`InProgress` for that game (16:37:38.903 / .920), because `gameLength` counts gameplay and not the load screen. The companion prefers the `InProgress` moment it observed and falls back to this arithmetic when it has none (it reconnected mid-game); the two differ by the load time only, which does not reorder the M5.2 rating fold. M2.3 (2026-09-08) builds on the WS `Create`/`Update` as the primary path — the block is mapped, written to `<configDir>/queue/<gameId>.json` and posted from there — and uses the GET exactly once, at connect time when the phase read is `EndOfGame` or `WaitingForStats`; a 404 there is logged as backfill's problem and never polled. The session's `gameData.gameId` is read once per game at the `GameStart`/`InProgress` phase event, and a session whose own `phase` is `Lobby`/`None` is treated as stale and not posted. The block carries post-game chat credentials: never persist it raw (`scrubValue`). `EogStatsBlockSchema`. Still unseen: a full 5v5 human block captured live (shape is per-player, so no difference is expected beyond the two stat keys confirmed above by the hosted-DB read). | verified (16.17, 2026-09-08); `VISION_SCORE` / `TOTAL_DAMAGE_SELF_MITIGATED` corroborated for real ten-player customs (hosted-DB read, patch 16.18 inferred, games 2026-09-12 to 2026-09-15, checked 2026-09-15) |
| Match history list | `GET /lol-match-history/v1/products/lol/{puuid}/matches?begIndex=0&endIndex=20` | `{ accountId, platformId, games { gameCount, gameIndexBegin, gameIndexEnd, games[] } }`, each game `{ gameId, gameType, queueId, gameMode, mapId, gameCreation, gameCreationDate, gameDuration, gameVersion, platformId, endOfGameResult, participants[], participantIdentities[], teams[] }`. **Custom games appear: 17 of the 21 games in the window were `gameType: "CUSTOM_GAME"`** (queueId 3100 blind, 3110 draft, 3270 custom on mapId 12). `endIndex=20` returned 21 games (inclusive). **But the list carries only the local player in `participants`/`participantIdentities` (length 1 even for a completed 5v5)**; `teams[]` is complete. Rosters and everyone's stats need the detail endpoint. Works for another puuid too (`match-history--other.json`). `MatchHistoryListSchema`. **Paging (M5.1, 2026-09-09):** `matchHistoryPagePath(puuid, begIndex, endIndex)` in `endpoints.ts` builds the window; the companion's backfill walks `begIndex` in steps of 20 with `endIndex = begIndex + 20` (the verified inclusive shape, one game of overlap deduped on `gameId`), at most 5 pages a pass and never past position 200, and treats a page shorter than 20 as the end. **Only the default window has been read on a live client**; what `begIndex=20` and deeper answer (more games, empty `games[]`, an error, or a repeat) is still M5.6's question — the companion logs `match history ends here` with the `begIndex` it stopped at and keeps `deepestBegIndex` in `backfill.json` so the first real run answers it. | verified (16.17, 2026-09-08); paging unverified |
| Match detail | `GET /lol-match-history/v1/games/{gameId}` | Same game shape as a list entry, and here `participants[]`/`participantIdentities[]` hold all ten players (`participantIdentities[].player { puuid, summonerId, gameName, tagLine }`, `participants[] { participantId, teamId 100/200, championId, stats { win, kills, deaths, assists, goldEarned, totalDamageDealtToChampions, totalMinionsKilled, neutralMinionsKilled, champLevel, visionScore, damageSelfMitigated, ...~118 keys } }`, `teams[] { teamId, win: "Win"/"Fail", bans[] }`). Stat keys are camelCase here (confirmed against the eog block, which uses the uppercase keys — see the row above). **`visionScore` and `damageSelfMitigated` verified for a real human 5v5, patch 16.17, captured 2026-09-08 (M7.7, 2026-09-15):** the primary proof is a fixture already in the repo, not the hosted-DB read below — `packages/lcu/fixtures/16.17/match-detail.json` (`patch: "16.17"`, `clientVersion: "16.17.8104348+..."`, `capturedAt: "2026-09-08T17:03:59.772Z"`) is a real `GameComplete` custom, `body.gameType: "CUSTOM_GAME"`, `body.queueId: 3110`, `body.gameVersion: "16.17.810.4348"`, 10 distinct real puuids across `participantIdentities[].player.puuid`, and every one of the 10 `participants[].stats` carries both `visionScore` (18, 29, 53, 8, 90, 18, 63, 24, 40, 110) and `damageSelfMitigated` (151130, 111916, 48955, 51175, 19814, 199376, 94610, 42904, 44456, 30154) — read directly off the fixture file, not inferred. The M7.7 hosted-DB read (games table, 2026-09-12 to 2026-09-15, `raw::text ilike '%visionScore%'` / `'%damageSelfMitigated%'` both true on 15 real ten-player customs) is **corroboration only, not primary evidence for this endpoint**: that query does not select `games.source`, and a live eog block's own camelCase duplicates (confirmed present in the row above) would produce the identical `ilike` hit from `games.raw` without a single match-history detail ever being fetched, so it cannot rule out that all 15 rows were eog-sourced. `MatchParticipantStatsSchema` is `z.looseObject`, so both keys already survive parsing regardless of whether they are named there; `visionScore` was already listed (optional), `damageSelfMitigated` passes through as an unlisted key — no schema change was needed. An aborted game (`endOfGameResult: "Abort_TooFewPlayers"`) has one participant and one team; `smoke` prefers a `GameComplete` custom for the fixture. This is the backfill path: `mapMatchDetail` in `mapper.ts` turns it into the `POST /api/companion/game` body with `source: 'backfill'` (M5.1; winner from `teams[].win === "Win"`, `startedAt` from `gameCreation`, `durationS` from `gameDuration`, camelCase stats, no `partyId`), pinned against `match-detail.json` in `mapper.test.ts`. **`role` (M5.18, 2026-09-10):** `participants[].timeline.lane`/`.role` through `MATCH_TIMELINE_ROLES` (`timelineRoles.ts`), a table that holds only pairs a live capture of the same game has proved and that is **empty on 16.17**, so every backfilled role is still `null`; the pair is the server's match-v4 guess and the one full custom in the fixtures shows it wrong for 3 of 10 players (the section "Match-history `timeline.lane` / `timeline.role`" below has the evidence, the confusion table and the capture that finishes it). `MatchDetailSchema`. | verified (16.17, 2026-09-08); `timeline` shape verified, mapping unproven (16.17, 2026-09-10); `visionScore` / `damageSelfMitigated` verified for a real ten-player custom (`match-detail.json`, patch 16.17, captured 2026-09-08; hosted-DB read from 2026-09-12–2026-09-15 games is corroboration only, checked 2026-09-15) |
| Live client data (in game) | `https://127.0.0.1:2999/liveclientdata/allgamedata` | Different server, only while in game. Answered 200 on 16.17 while in game (seen in a worktree smoke run, not kept as a fixture: `{ activePlayer, allPlayers[] { riotId, riotIdGameName, riotIdTagLine, championName, team, scores, ... }, events, gameData { gameMode, gameTime, mapNumber } }`; no puuids) and `ECONNREFUSED` otherwise. Not needed unless eog capture proves unreliable; no schema. | unverified (observed) |
| Client OpenAPI | `GET /swagger/v2/swagger.json`, `GET /swagger/v3/openapi.json` | Both 404 `{ errorCode: "RESOURCE_NOT_FOUND", message: "Invalid URI format" }` on 16.17 without `--enable-swagger`, so paths were confirmed by hand-testing, not from the client's schema. `smoke` still probes both. Neither is needed at runtime. | verified absent (16.17, 2026-09-08) |

### Match-history `timeline.lane` / `timeline.role` (M5.18)

**Status: shape verified (16.17, 2026-09-10); mapping to our roles unproven, `MATCH_TIMELINE_ROLES` is empty.**
The rule (M5.18 brief): a pair maps only when every participant of every game that exists both as a match-history
game and as a live end-of-game capture agrees with `detectedTeamPosition`; anything else is `null`. The check is
`pnpm --filter @customs/lcu timeline-roles` (reads `match-detail*.json`, `match-history*.json`,
`eog-stats-block*.json` and the eog events of `ws-events*.ndjson`; exits 1 if a mapped pair is contradicted) and
`timelineRoles.test.ts` pins the same thing in CI.

**Vocabulary seen on 16.17** (42 games: the `match-detail.json` 5v5 plus the local player's own row in the 41
list games of `match-history.json` and `match-history--other.json`): lane `TOP` / `JUNGLE` / `MIDDLE` / `BOTTOM` /
`NONE`; role `SOLO` / `NONE` / `CARRY` / `SUPPORT` / `DUO`. **Not seen: `DUO_CARRY`, `DUO_SUPPORT`** (the
match-v4 names); the bottom pairs are `BOTTOM+CARRY` and `BOTTOM+SUPPORT` here. Pairs, with where they came from:

| pair | rows | seen in |
|---|---|---|
| `BOTTOM+CARRY` | 2 | custom draft 5v5 (queue 3110) |
| `BOTTOM+SOLO` | 3 | custom draft, Kiwi (2400), ARAM (450) |
| `BOTTOM+SUPPORT` | 2 | custom draft |
| `JUNGLE+NONE` | 9 | custom draft ×5, ranked solo (420) ×2, flex (440), Kiwi |
| `MIDDLE+DUO` | 1 | custom Kiwi (3270, mapId 12) |
| `MIDDLE+SOLO` | 8 | custom draft |
| `MIDDLE+SUPPORT` | 1 | custom Kiwi |
| `NONE+DUO` | 1 | ARAM |
| `NONE+SOLO` | 1 | custom blind (3100), aborted after 100 s |
| `NONE+SUPPORT` | 15 | Kiwi ×10, custom Kiwi ×3, ranked solo, ARAM |
| `TOP+DUO` | 3 | custom draft, Kiwi, ARAM |
| `TOP+SOLO` | 1 | custom draft |
| `TOP+SUPPORT` | 5 | Kiwi ×4, custom Kiwi |

**Confusion table (16.17, 2026-09-10).** Games in both a match-history fixture and a live capture: **one**,
4000965483, the `Abort_TooFewPlayers` game (100 s, one participant). The 5v5 whose detail we hold (4000769615,
2026-09-07) was played before any recording, and the game the eog fixtures cover (4000969091, solo vs bots) was
not in the history list yet when the list was captured 37 s after it ended. So the table is one row and proves
nothing:

| pair | live `detectedTeamPosition` (count) | mapped to | agreed | disagreed |
|---|---|---|---|---|
| `NONE+SOLO` | MIDDLE 1 | null | - | - |

**Why the "obvious" rows are not in the table either.** The one full custom detail (4000769615, 49 min, draft,
ten humans) reads like this; spells 11 = Smite, 12 = Teleport, 14 = Ignite, 3 = Exhaust, 7 = Heal, 21 = Barrier:

| # | team | champion | spells | `lane+role` | lane cs | neutral cs | wards | what the stats say |
|---|---|---|---|---|---|---|---|---|
| 1 | 100 | 516 (Ornn) | Ignite/Flash | `JUNGLE+NONE` | 244 | 1 | 7 | no Smite, 1 neutral minion in 49 min: a laner, **not jungle** |
| 2 | 100 | 59 (Jarvan IV) | Smite/Flash | `JUNGLE+NONE` | 56 | 127 | 2 | the jungler |
| 3 | 100 | 90 (Malzahar) | Teleport/Flash | `MIDDLE+SOLO` | 300 | 2 | 22 | a laner; consistent |
| 4 | 100 | 119 (Draven) | Flash/Barrier | `BOTTOM+CARRY` | 214 | 14 | 7 | a laner; consistent |
| 5 | 100 | 147 (Lulu) | Heal/Flash | `BOTTOM+SUPPORT` | 90 | 0 | 38 | the support (vision score 90); consistent |
| 6 | 200 | 420 (Illaoi) | Flash/Ignite | `JUNGLE+NONE` | 277 | 4 | 7 | no Smite, 4 neutral: a laner, **not jungle** |
| 7 | 200 | 24 (Jax) | Flash/Smite | `JUNGLE+NONE` | 37 | 247 | 16 | the jungler |
| 8 | 200 | 84 (Akali) | Ignite/Flash | `MIDDLE+SOLO` | 176 | 0 | 3 | a laner; consistent |
| 9 | 200 | 45 (Veigar) | Teleport/Flash | `BOTTOM+SOLO` | 287 | 7 | 19 | a laner; the pair names no role |
| 10 | 200 | 112 (Viktor) | Exhaust/Flash | `TOP+SOLO` | 76 | 0 | 40 | 76 cs and 40 wards (vision score 110) in 49 min: the team's support, **not top** |

Each side has two `JUNGLE+NONE` rows and team 100 has no `TOP` row at all. Read by their names, the pairs give
6 right, 3 wrong, 1 unmappable — and the three wrong ones would each move a player's inferred main (M5.16)
towards jungle or top. That is why `TOP+SOLO` and `JUNGLE+NONE` are treated as **refuted**, and `MIDDLE+SOLO`,
`BOTTOM+CARRY`, `BOTTOM+SUPPORT` as **unproven** (consistent here, but the same classifier, and one game). The
reasoning above never uses the champion, only Smite, neutral minions, cs and wards; the champion names are for
reading. This is the same field Riot's public API retired (`lane`/`role` of match-v4) in favour of
`teamPosition`; the client's match history still serves the old shape.

**What finishes it.** One customs night, on a machine running the companion or `record-ws`, then the details:

1. During the night: `pnpm --filter @customs/lcu record-ws` (or just let the companion run; its queue files
   under `<configDir>/queue/<gameId>.json` carry the mapped positions but are deleted once posted, so the
   recorder is the durable capture). Every game's eog block lands in `fixtures/<patch>/ws-events.ndjson` with
   ten `detectedTeamPosition` values.
2. Afterwards (match history lags the end-of-game screen by minutes): `pnpm --filter @customs/lcu smoke
   --game-id <id1>,<id2>,<id3>` with the `gameId`s of the night (they are in the recording's eog `Create`
   events and in the companion log). The first id writes `match-detail.json`, the rest `match-detail--<id>.json`.
3. `pnpm --filter @customs/lcu timeline-roles` prints the confusion table; paste it here, add every row that
   agreed on all of its participants and disagreed on none to `MATCH_TIMELINE_ROLES`, run
   `pnpm --filter @customs/lcu test` (the table test fails on any contradiction), flip this status.
   Three games (30 participants) is enough to see whether the classifier behaves in this group's customs.

Until then the companion logs `backfill: timeline pair has no verified role; stored as null` once per distinct
pair per process, with the values and the first game id, and every backfilled `game_players.role` is null.

## Tooling (`packages/lcu`, M0.1)

- `pnpm --filter @customs/lcu smoke` finds the lockfile, probes the TLS modes, reads the version, then GETs every row in
  "Endpoints we use" (read-only ones; path parameters are filled from `current-summoner` and the first
  `CUSTOM_GAME` in the local player's match history) and writes `packages/lcu/fixtures/<patch>/<endpoint-id>.json`
  plus `manifest.json`. Exit 2: no lockfile. Exit 3: lockfile present but no TLS mode reached the client.
  `--diff` compares fresh top-level key sets and statuses with the newest saved fixtures instead of writing
  (exit 1 when anything differs). `--puuid <puuid>` and `--riot-id Name#TAG` add probes for another player
  (question 4 below). `--insecure` skips pinning. `--game-id <id>` pins the match-detail probe (the list cannot tell a 5v5
  from a solo abort); a comma-separated list writes the first as `match-detail.json` and the rest as
  `match-detail--<id>.json` overlays (M5.18). `--live-port` redirects the in-game live data probe (tests point it at a dead port).
  Bodies are scrubbed like WS events before they are written (`scrubValue`); the table says when something was
  redacted. Layout in `packages/lcu/fixtures/README.md`. `LCU_LOCKFILE_CANDIDATES` (path-delimiter-separated) replaces the platform default lockfile paths; the unit tests set it to a non-existent path so a client running on the developer's machine never leaks into a test.
- `src/schemas.ts` holds one zod schema per verified row, written from the 16.17 fixtures and parsed against them in `src/schemas.test.ts` (fixtures only, never a live client). Tolerant on unknown keys, exact on the fields we read.
- `pnpm --filter @customs/lcu timeline-roles [--patch p] [--out dir]` (M5.18) cross-checks every match-history
  fixture against every live end-of-game capture of the same game and prints the `timeline.lane`/`role`
  confusion table and the list of pairs seen (no client; see the M5.18 section above). Exit 1 when a pair in
  `MATCH_TIMELINE_ROLES` was contradicted by a live position.
- `pnpm --filter @customs/lcu record-ws` subscribes to `OnJsonApiEvent` (or `--topic` ones) and appends every
  event to `fixtures/<patch>/ws-events.ndjson` until Ctrl-C, reconnecting if the client restarts. On exit it
  prints a count per URI. Events are scrubbed before they touch disk (`src/scrub.ts`): `/lol-login`,
  `/lol-rso-auth`, `/riotclient/auth`, `/lol-chat` and any URI mentioning auth/token are written without
  `data`; credential-looking keys elsewhere become `[redacted]`. The summary says how many were redacted.
- The TLS probe only steps down from `pinned` to `pinned+legacy` to `insecure` on a certificate/handshake
  error. A refused or timed-out connection stops the probe (exit 3) so a client that is still starting is
  never recorded as "needed insecure".
- Both scripts are GET-only. Nothing in M0.1 creates a lobby, invites, or sends anything but subscribe frames.
- `pnpm --filter companion verify-commands` (packaged: `Kustom.exe --verify-commands`, or
  `CUSTOMS_NIGHT_VERIFY_COMMANDS=1`) is the **only** code that POSTs to the client while the three write rows
  above are `unverified` (M4.1, `apps/companion/src/verifyCommands.ts`). Human-run, with the client in `None` or
  `Lobby` and one friend online. **Second edition (2026-09-10)**, after the first live run on 16.17 refused the
  community create body: it first reads and saves `GET /lol-game-queues/v1/custom` and `/queues` (printing the
  Summoner's Rift dialog entries and the custom queues with their `gameTypeConfig` ids), asks which dialog entry
  id to use for blind (default: the one it can name, else the client's own known id 19), then POSTs the ranked
  create candidates listed in the create row in order, stopping at the first 2xx, one fixture per attempt
  (`create-lobby--<candidate>.json`, the accepted one also as `create-lobby.json`), then opt-in draft with the
  accepted shape; invite by the friend's Riot ID via the verified alias and summoner lookups, `[{ toSummonerId }]`
  then `[{ toPuuid }]` on a 4xx; `POST /lol-lobby/v2/lobby/team/TEAM1|TEAM2` for the side you are not on, then
  the opt-in full-side repeat. It prints each request, status and body *shape*, saves each answer through
  `scrubValue` as a fixture envelope with the request body in `request` (`packages/lcu/fixtures/<patch>/` when run
  from the repo, `<configDir>/fixtures/<patch>/` from the exe), and writes
  `<configDir>/verify-commands-<patch>-<date>.txt`. It makes no API call, needs no token, and never sends
  `DELETE /lol-lobby/v2/lobby` (not a listed-safe path): the person closes the lobby from the client. It flips
  nothing: the engineer reads the report, writes the rows above, and sets `LOBBY_WRITE_VERIFICATION` in
  `packages/lcu/src/writes.ts` in the same edit (`writes.test.ts` refuses a flag that runs ahead of its row).

## Behaviors to confirm in M0

1. Custom games in match history: **yes** (16.17, 2026-09-08: 17 of 21 games in the default window were `CUSTOM_GAME`, spanning four days of play). The default window is 21 games (`begIndex=0&endIndex=20`, inclusive); the user's first run saw 16 customs, later runs 17, so the window slides. Paging further back (`begIndex` > 20) is still untested. **The list holds only the local player per game; use match detail for rosters.**
2. Does `eog-stats-block` persist after the player clicks past the end-of-game screen? **It persists while the screen is up (GET 200 at +33 s and +4 min) and is gone (404) once the client is back in the lobby (+10 min).** The exact moment it is deleted was not captured (the `Delete` event for the played game fell outside both recordings; the server-dropped game got a `Delete` 0.2 s after `Create`). So: the companion reacts to the WS `Create`/`Update` event (which arrives before the phase even reaches `EndOfGame`) and keeps the block in memory; a GET on `EndOfGame` is a fallback, and polling later is not (16.17, 2026-09-08).
3. Does `lobby.members[].teamId` reflect sides in a custom lobby, or only `gameConfig.customTeam100/200`? **Only `gameConfig.customTeam100/200`.** `members[].teamId` and `localMember.teamId` stayed `0` while the local player switched from blue to red and back (WS events 16:33:26-16:33:31). Confirmed with a real second human (second window, 17:31:58-17:32:08): the friend's `teamId` was `0` on join (placed on `customTeam200`), while on `customTeam100`, and after moving back. **Join order for side derivation:** the first `/lol-lobby/v2/lobby` `Update` that listed the friend in `members[]` listed them in `customTeam200[]` in the same payload; there was no event with the member in one and not the other, and across all 39 lobby events of both windows the human puuids of `members[]` equal the union of the three side arrays. The `/lol-lobby/v2/lobby/members` event followed 2 ms after that `Update`. So M2.2 can derive every member's side from the lobby event it arrives in and never has to wait for a later one. **Spectators (third window, 17:38:57-17:39:27): a spectator is in `members[]` with `isSpectator: true` *and* in `customSpectators[]`, on neither team, `teamId` still `0`** (question 9 has the timestamps). Side derivation for M2.2: `customTeam100` -> 100, `customTeam200` -> 200, `customSpectators` -> spectator (`side: null`), and every human in `members[]` is in exactly one of the three. Tested in `schemas.test.ts` (16.17, 2026-09-08).
4. `ranked-stats/{puuid}` for another player: **works for a non-friend and a friend alike** (16.17, 2026-09-08). `losses` reads 0 for others.
5. Switch-side endpoint path and whether it works when the target side is full. **Path answered from the client itself (2026-09-10, not yet exercised):** the 16.17 lobby UI moves the local player with `POST /lol-lobby/v2/lobby/team/TEAM1` / `TEAM2` (and `SPECTATOR`), no body, target in the path; neither `switch-teams` path exists in the 16.17 OpenAPI document or the UI code. **Still open:** the live answer (status, body) and the full-side answer. The first live run (2026-09-09) never reached this probe because the create had failed. Next step: the corrected `verify-commands`, probe 3, then the opt-in full-side repeat (friend plus bots on the target side).
6. Invite by `summonerId` still works, or is `puuid` required now? **`[{ toSummonerId }]` is what the 16.17 client's own UI sends** (`invitePlayer` in `rcp-fe-lol-parties`, read 2026-09-10; `invitationsWithContext` with `[{ puuid, inviteContext }]` exists for Discord-flow invites and is not used here). **Still open:** the live answer. The first live run (2026-09-09) POSTed both bodies with no lobby open and got `404 LOBBY_NOT_FOUND` for each, which says nothing about the body. The two UI-sent invites in the second 2026-09-08 capture show the *result* shape: `invitations[]` gets a `Pending` row carrying both `toPuuid` and `toSummonerId` (with `invitationId: ""` and `invitationType: "invalid"`, so neither is an id to track), and it becomes `Accepted` in the same `Update` that adds the member. Next step: the corrected `verify-commands`, probe 2, with the lobby probe 1 makes; compare the resulting `invitations[]` row with `lobby--two-players.json`.
7. WebSocket event names and whether the `[5, ...]` subscription format is unchanged: **unchanged** (16.17, 2026-09-08; `[5, "OnJsonApiEvent"]` -> `[8, "OnJsonApiEvent", { data, eventType, uri }]`, no subprotocol, 1310 events, 0 malformed). URIs the companion needs, all seen with data: `/lol-gameflow/v1/gameflow-phase` (20), `/lol-gameflow/v1/session` (58), `/lol-lobby/v2/lobby` (32), `/lol-lobby/v2/lobby/members` (5), `/lol-end-of-game/v1/eog-stats-block` (5). The per-URI topics (`OnJsonApiEvent_lol-gameflow_v1_gameflow-phase` etc.) have not been subscribed to individually yet; the companion can filter the firehose by `uri` until they are.
8. Certificate: **pinning Riot's root works** (16.17, 2026-09-08, macOS; plain `pinned`, no legacy digests, HTTPS and WSS). Insecure mode is not required.
9. How does the client represent a spectator in a custom lobby (`members[].isSpectator`, `customSpectators[]`, both, or dropped from `members[]`)? **Both** (16.17, 2026-09-08, third recording window, `lobby--spectator.json`, tested). The friend was moved to the spectator slot at 17:38:57.320: the `/lol-lobby/v2/lobby` `Update` listed them **still in `members[]`** with `isSpectator: true`, `teamId: 0`, and in `gameConfig.customSpectators[]` (also `isSpectator: true`), with `customTeam200` now `[]`; the `/lol-lobby/v2/lobby/members` event at the same millisecond carried `isSpectator: true`. Back on a team at 17:39:26.874: `isSpectator: false` in both places, `customSpectators: []`, `members` event 1 ms later with `false`. `members[]` order and `canStartActivity: true` did not change either way. So the union rule of question 3 holds with spectators included, and **M1.8 needs no widening**: a spectating companion's own puuid is in the `members[]` it posts. M2.8's lobby-membership path likewise sees the spectator in `lobby_members`. Not captured: a GET of the lobby while spectating (the fixture is the WS payload, `method: "WS"`), and whether a spectator can be *on* `customSpectators` without being in `members[]` (never seen; every event of three windows keeps the two lists consistent).
10. Rank data also arrives **over the socket**: `/lol-ranked/v1/cached-ranked-stats/{puuid}` `Update` events carry another player's ranked stats in the same shape as `GET /lol-ranked/v1/ranked-stats/{puuid}` (`RankedStatsSchema` parses them; `ranked-stats-by-puuid--ws-cached.json`). Seen for the friend 0.2 s after they moved back to a team (17:39:27.171) and for several friends when the friends list loaded (second window, not kept). The companion GETs ranks itself; since M2.4 the push is a shortcut only — the connection machine routes `/lol-ranked/v1/cached-ranked-stats/{puuid}` to `onRankedStats`, and the rank sync uses it when that puuid is in the current `ranksNeeded`, dropping every other push unread (`04-decisions.md`, 2026-09-08). It is outside the WS trim rule; noted so nobody mistakes it for a third endpoint.
11. What create body does the 16.17 client accept? **Not the community one** (2026-09-09: `500 INVALID_LOBBY` for `mutators.id` 1 and 2 with no `queueId`). The client's own dialog sends a top-level `queueId` equal to `mutators.id`, both taken from `GET /lol-game-queues/v1/custom`, plus `gameMutator: ""`, `spectatorDelayEnabled`, `hidePublicly` and `aramMapMutator: "NONE"`, and no `isCustom` (read from the installed UI plugin, 2026-09-10; see the create row). **Open:** which of the five ranked candidate bodies the client accepts, and what the dialog's entry ids are on 16.17 (19? 3100?). Next step: the corrected `verify-commands`, probe 1, which reads the dialog data and tries the candidates in order.

## Riot policy notes

- The LCU is not an official API. Riot tolerates read and lobby automation and explicitly forbids automating
  gameplay. Register the project in the Riot developer portal as a personal app that uses the LCU; it is free and
  is what Riot asks.
- Vanguard does not interfere with local API reads (16.17: every read and the WebSocket worked with Vanguard running).
- The event stream also carries `/lol-champ-select/*`, `/lol-lobby-team-builder/champ-select/*` and `/lol-matchmaking/*` events with full payloads. They are recorded because we subscribe to everything; the companion must never act on them, subscribe to them on purpose, or POST to those paths. That is the gameplay line.
- `/riot-messaging-service/*` is server push the companion never reads. Its `lol-gsm-server` `game-update` messages carry the player's game credentials (`encryptionKey`, `spectatorKey`, `observerEncryptionKey`, `packetCopMetadata`, server ip and port) inside a JSON *string* (`payload`), a second delivery of `/lol-gameflow/v1/player-credentials`. `scrub.ts` treats the whole prefix as sensitive and, since 2026-09-08, parses JSON strings before key-scrubbing so that path cannot leak again.
- Do not display other players' custom match history publicly beyond our own group's site. Keep the site unlisted.

## Community references

- https://riot-api-libraries.readthedocs.io/en/latest/lcu.html
- https://github.com/topics/lcu-api (schema dumps such as `lcu-schema` and `LCU-Explorer` show the live OpenAPI on a running client at `/swagger/v3/openapi.json` when `--enable-swagger` is set)
- A community dump of the client's own OpenAPI document for 16.17.812.4632 (dysolix/hasagi-types `swagger.json`, generated from the client's `/help`; the client itself answers 404 on `/swagger/*` without `--enable-swagger`). Used 2026-09-10 for `LolLobbyLobbyChangeGameDto`, `LolLobbyLobbyCustomGameConfiguration`, `LolGameQueuesQueueCustomGame` and the `/lol-lobby/` path list. Not committed (multi-MB); re-fetch per patch.
- The client's own lobby UI, `Plugins/rcp-fe-lol-parties/assets.wad` in the install (a Riot WAD v3.4 archive; entries are zstd frames, `zstd -d` decodes them; the lobby code is the one 1.7 MB JS entry). Grepping it for `customGameLobby`, `/v2/lobby/team/` and `/v2/lobby/invitations` is the strongest evidence short of a capture: it is exactly what the client sends. Re-read it after a patch when a write breaks.
- The client's own OpenAPI: `GET /swagger/v2/swagger.json` on a running client, when enabled. M0 should try it first; it is the fastest way to verify paths.
