# Decisions

Append-only. One row per decision that the code or docs would otherwise leave ambiguous. Newest at the bottom.

| Date | Decision | Why | Alternatives rejected |
|---|---|---|---|
| 2026-09-08 | Build our own instead of using In-House Queue | Existing bots need manual reporting and equal-start ratings; we want zero-input and rank seeding. | In-House Queue (free, good baseline, still the fallback), MatchBalancer ($5.99/mo, no Discord) |
| 2026-09-08 | Companion app is phase 1, not a later add-on | It is the only way to get results and rosters without a human step, and it replaces the Riot public API. | Manual `/result` command with vote confirmation |
| 2026-09-08 | Supabase for database, auth, realtime | Hosted Postgres with the rest included; Discord OAuth is a checkbox. | Bare Postgres on Neon plus separate auth and pub/sub |
| 2026-09-08 | API lives in Next.js route handlers, not Supabase edge functions | One runtime and deploy; `packages/core` imports cleanly. | Edge functions (Deno toolchain), separate Fastify service |
| 2026-09-08 | Companion is a Node CLI packaged as an exe; tray wrapper deferred to M6 | Keeps the project all TypeScript, shares `packages/lcu`. | Tauri or Electron from day one, Overwolf |
| 2026-09-08 | Players keyed by PUUID; Discord link optional and admin-managed | Riot IDs are renamable; balancing and stats need no Discord identity. | Require Discord OAuth before playing |
| 2026-09-08 | No check-in. Roster comes from the custom lobby | The lobby is the truth about who is playing. A check-in is a form people skip. | Discord button check-in, web check-in as the primary flow |
| 2026-09-08 | Discord webhook in M3, bot process only in M4 for voice | Posting needs no process; voice moves do. | Full bot from the start |
| 2026-09-08 | OpenSkill for ratings, balance on mu, leaderboard on mu - 2 sigma, display mu * 60 | Team-aware rating with uncertainty; display scale reads like familiar MMR numbers. | Elo, Glicko-2 per player, raw TrueSkill port |
| 2026-09-08 | Role multipliers 1.00 / 0.93 / 0.85 for main / secondary / fill | Simple, explainable, tunable in one constant with tests. | Per-role ratings (too sparse for our game count) |
| 2026-09-08 | Backfill (M5) is the recovery path for missed games, never manual entry | Keeps ratings honest; manual entry invites disputes. | Admin manual result entry |
| 2026-09-08 | Workspace packages are `@customs/*`; apps keep bare names (`web`, `companion`, `discord`) | One prefix, no collision with npm, and `pnpm --filter web dev` stays exactly as CLAUDE.md documents it. | `@customs-night/*` (long), unscoped `core`/`db` (collides with real packages) |
| 2026-09-08 | Workspace packages ship TypeScript source: `main`, `types` and `exports` all point at `src/`, and no package emits anything | No build step to forget before `dev`, `test`, `typecheck` or a Vercel deploy; every consumer types from source. vitest and Turbopack transform the source directly, `transpilePackages` in `next.config.ts` covers the Next side. | Building each package to `dist` first (an extra step in front of every command, and a broken `next build` on a fresh checkout) |
| 2026-09-08 | Typecheck is a plain `tsc --noEmit` per package, no composite projects and no TypeScript project references | References only pay off when consumers read `.d.ts` from `dist`, and we type from source instead. With references and `types` pointing at `dist`, a fresh checkout failed `next build` with TS6305 before anything had been built, which is exactly the Vercel case. `pnpm -r` already runs packages in dependency order. `apps/web` runs `next typegen && tsc --noEmit`. | Composite projects + `tsc -b` emitting declarations into `dist/` (rejected: broke `next build` on a fresh checkout) |
| 2026-09-08 | TypeScript pinned to 5.9.x, not the 7.0 native port | Next 16, Biome and vitest are proven on 5.9 today; a foundation task is the wrong place to absorb a compiler rewrite. Revisit once the toolchain has shipped 7.x support. | TypeScript 7.0 (current `latest`) |
| 2026-09-08 | One vitest config per package, no root workspace/projects file | `pnpm -r test` already runs every package in dependency order, each with its own environment; the workspace file has moved twice in three vitest majors. | Root `vitest.workspace.ts`, root `test.projects` |
| 2026-09-08 | `next.config.ts` sets `agentRules: false` | Next 16 writes its own `AGENTS.md` and `CLAUDE.md` into `apps/web` on first dev run. Agent instructions for this repo live in the root `CLAUDE.md` and `docs/`; a second generated set would compete with them. | Let Next generate them and commit them |
| 2026-09-08 | The M1.4 worked example uses settled `mu` values, not raw seeds | Seeds are multiples of 0.25, so any seed-only roster has a gap that is a multiple of 15 display points; the accepted criterion "gap of 100" is unreachable from seeds alone. Ten friends a few weeks into a season have drifted `mu` anyway, so the example is also the more honest picture. Criterion left unchanged. | Softening the criterion to "gap of at most 100" or moving it to 105; both hide the real constraint |
| 2026-09-08 | The worked example's three splits are all-main with gaps 100, 170 and 220 | Any split with someone off-role costs at least 240 (2 x 120) before its gap counts, so keeping all three under 240 makes the top three stable and the test deterministic without a tie-break. `01-architecture.md`'s sample string "gap 300" stays as a format illustration only. | Designing for a 300-point next best; every candidate roster then let an off-role split into the top three |
| 2026-09-08 | Players are sorted by PUUID before enumeration, and the lowest PUUID is always on blue | Makes output byte-identical for the same ten in any order, fixes side colour without an extra rule, and still lets red be the favoured side. | Blue is always the stronger side (then "Red favored" never prints); input order decides (not reproducible) |
| 2026-09-08 | `gap` and `score` are computed on effective (role-adjusted) skill in display units; `gap` is rounded once for display | Keeps `score = gap + penalties` true, so the explanation's number is the number that chose the teams. | Gap on raw `mu` (then the printed gap does not match the decision) |
| 2026-09-08 | A `roleOverride` becomes the main; the player's usual main becomes their secondary; the declared secondary drops to fill | One sentence to a friend: "the role you tapped is your role tonight, your usual role is your backup." Never treats someone's real main as a random fill. | Override replaces the main outright (punishes the player if the balancer needs them elsewhere) |
| 2026-09-08 | A player with no known main role is flexible: every role is a main and they are never counted off-role | A new face must not make the night worse or produce a misleading "1 off-role" line before anyone has set their roles. | Treat unknown roles as fill on everything (constant penalty, misleading explanation); refuse to balance until an admin sets roles (a manual step, against the whole product) |
| 2026-09-08 | Duo locks are a filter, not a penalty; if no 5/5 partition satisfies them, `balance` throws | An "infinite penalty" number is a footgun in float arithmetic. Throwing with the blocks named lets the API tell the group which duo to drop. | Infinite penalty constant; silently dropping the duo that does not fit |
| 2026-09-08 | The repeat-split penalty matches the same five people on either side, colour-agnostic | Swapping blue and red is not a different night to the people playing. | Comparing blue to blue only |
| 2026-09-08 | Explanation wording: numerals throughout, "N off-role: Name at role", "Next best: swap A and B" for one change and "N swaps" for more, next-best off-role count appended when it differs, clause omitted when there is no next split | Discord-length, friend-toned, and it stops the "next best has a smaller gap" line from reading as a bug. | "Omar on secondary (jungle)" (jargon); spelled-out numbers (inconsistent with "Gap 100") |
| 2026-09-08 | `rateGame` has no draw path; `winningSide` is `100` or `200` | A League custom cannot draw, and a remake is not a game — the API drops it before rating. | Modelling draws for completeness |
| 2026-09-08 | Workspace dependency direction is one-way: `core` depends on no workspace package; `db` depends on `core`; `web` depends on `core` and `db`; nothing may import into `core` | Keeps `core` pure and testable with no I/O anywhere near it, and keeps the import graph acyclic so a package can be reasoned about alone. `lcu` is imported only by `apps/companion`; the web app must never import it. | `core` importing schemas from `db` (would drag zod and the database shape into the pure layer) |
| 2026-09-08 | `lastSplit` is the most recent chosen split whose lobby had the same ten players, else null | The repeat penalty exists to stop "same teams as last time" for the people in the room; a split from a lobby with a different roster is not the same night and would penalise teams nobody has played. Same ten in a different order still counts, since the lobby is a set of puuids. Derived by the API (M2.7), never by core. | Always the previous chosen split whatever the roster (penalises unrelated teams); the previous split of the same lobby row only (rebalance after a member change would wipe the memory); a time window such as "tonight" (an arbitrary clock rule in a pure-ish path) |
| 2026-09-08 | The sample explanation string in `01-architecture.md` is the worked example's split 1, verbatim | Two different sample strings in two docs is how a doc goes stale unnoticed; the architecture doc now shows the string the tests pin (M1.4). Supersedes the earlier row that kept "gap 300" as a format illustration. | Leaving "gap 300" with a footnote (already caused one question); dropping the sample from the architecture doc (the format is worth showing where the balancer is described) |
| 2026-09-08 | `seedFromRank` matches tier and division case-insensitively; a missing or unrecognised division on a ranked tier (`null`, `'NA'`, `'V'`) seeds at the tier base (IV) | The client sends uppercase, but the value passes through the companion and the database on the way here; a casing slip must not seed a whole night at 20. "Your division was unreadable so you start at the bottom of your tier" is one sentence to a friend. | Strict-string lookup (silent 20.00 for `'gold'`); treating a bad division as unranked (throws away a known tier) |
| 2026-09-08 | `rateGame` throws unless both sides have exactly five ratings | A League custom is 5v5 and the API only rates games with a full roster; an empty or short team makes OpenSkill produce nonsense quietly. Throwing keeps a malformed ingest out of the rating history. | Rating whatever arrives (silent bad data); padding a short side |
| 2026-09-08 | Tuning constants live in `config.rating.*` in `packages/core/src/config.ts`; M1.4 adds `config.balance.*` beside it | One object, namespaced per module, so a tuning change is a one-line diff plus a test update and consumers import a single `config`. | One flat object (name collisions once balance weights arrive); constants scattered per module |
| 2026-09-08 | The M1.3 convergence test rates P0's team against the next five players in the same rotation; the tenth player sits that game | The brief's setup is P0 plus ten others, so "the remaining five" is really six and `rateGame` rejects a 6-player side. Rotating the opponents keeps every game 5v5 and the sequence deterministic. Results reported to the lead, not tuned: `mu` passes 23.00 in game 4 and is 34.09 after game 10; `sigma` is 6.47 after game 10 and first drops below 5.00 in game 36. | Nine other players (changes the brief's numbers); relaxing `rateGame` to accept six |
| 2026-09-08 | The unions in the schema are Postgres enums (`lobby_status`, `player_role`, `game_source`, `companion_command_kind`, `companion_command_status`), not check constraints | `supabase gen types typescript` turns an enum into the same string union `packages/core` declares, so `LobbyRow['status']` is `'open' \| 'balanced' \| ...` and the state machine typechecks. A check constraint generates `string`. `side` stays a smallint with a check because 100/200 are the client's numbers, not a vocabulary of ours. | Check constraints everywhere (loses the union in the generated types); a `sides` lookup table |
| 2026-09-08 | `discord_id` is hidden with a `players_public` view; the `players` table itself is service-role only | Column-level grants make PostgREST's `select=*` fail outright for anon, which breaks every public page. A view with `security_invoker` off is the only shape that keeps `select=*` working and still returns `401 permission denied` for `discord_id`. Consequence for the web and bot: read `players_public`, never `players`. | Column grants on `players`; an RLS policy that cannot hide a column; keeping `discord_id` in a side table (an extra join on every page) |
| 2026-09-08 | `ratings.ordinal` is a stored generated column (`mu - 2 * sigma`) with an index on `(season_id, ordinal desc)` | The leaderboard has to sort in SQL, so the formula exists in the database whether we write it down or not; generating it means one definition instead of an expression index every query must match. `packages/core` stays the only place that *computes* a rating. | Expression index plus `order=mu-2*sigma` in every query; sorting in the app after fetching every row |
| 2026-09-08 | The first admin is `public.bootstrap_admin(puuid)`, an idempotent SQL function the API calls on start with `BOOTSTRAP_ADMIN_PUUID`, not a value baked into a migration | A migration cannot read Vercel's environment, and a hard-coded PUUID in applied SQL can never be changed. The function inserts the player if the PUUID is new and sets `is_admin` either way, so calling it on every boot is a no-op. Service role only. | A PUUID literal in `0001_init.sql`; a one-off admin script someone has to remember to run |
| 2026-09-08 | `0001_init.sql` inserts Season 1 (active, fixed id `00000000-...-0001`, exported as `SEASON_ONE_ID`) and `games.season_id` defaults to `public.active_season_id()` | Ratings and games are per season from the first row, so there must be a season before the first game; a fixed id makes local resets and tests deterministic and gives the API a default it never has to look up. | Creating the season by hand in Studio; nullable `season_id` until M5.3 |
| 2026-09-08 | `splits` gains `explanation`, `created_at` and `roster_key`, and a rebalance appends a new set of three rather than replacing the old one | The explanation is built in core and must not be recomputed by the tonight page or the reroll. `roster_key` (the ten puuids sorted and joined) is how the API finds "the last chosen split for these same ten" to pass as `lastSplit`, in one indexed lookup instead of a jsonb set comparison. A partial unique index keeps at most one `is_chosen` split per lobby. Deviation from `01-architecture.md` "Data model", which lists neither column. | Recomputing the explanation on read; comparing `blue`/`red` jsonb arrays to find the last split; unique `(lobby_id, rank)` (would forbid keeping the previous split as history) |
| 2026-09-08 | `companion_tokens` gains `revoked_at`; command statuses are `pending`, `sent`, `acked`, `failed` | Revoking by deleting the row throws away `last_seen_at` and the audit trail of a token that may have been leaked; a timestamp is a soft delete the auth path already filters on with a partial index. The four statuses are exactly the poll/execute/ack loop in M4.1 plus its failure. | Deleting the token row to revoke; a boolean `is_revoked` (no when); adding an `expired` status nothing sets |
| 2026-09-08 | `game_players.mu_before/sigma_before/mu_after/sigma_after` are nullable | The API inserts the game and its ten players, then rates: the rating columns are written a step later, and a rebuild (M5.2) overwrites them. Not-null would force one giant statement or a placeholder that lies. | Not null with a 0 placeholder; a separate `game_ratings` table |
| 2026-09-08 | `packages/db` integration tests hit PostgREST with `fetch` and the CLI's keys, and skip themselves when the local stack is unreachable | Testing through PostgREST is testing the path the API and the browser actually take, RLS and all, and it keeps `@supabase/supabase-js` out of this package's dependencies. Skipping (not failing) keeps `pnpm -r test` green on a machine without Docker. | `@supabase/supabase-js` in `packages/db`; direct `psql` (bypasses RLS and PostgREST, so it proves nothing about anon); no integration test at all |
| 2026-09-08 | Supabase Storage is disabled in the local `config.toml` | The project stores no files. Leaving it on also made `supabase db reset` exit 1 on a 502 from the Storage container's restart, which would have made a documented command fail for everyone. | Keeping Storage enabled and telling people to ignore the failure |
| 2026-09-08 | `0001_init.sql` adds `lobbies`, `lobby_members`, `splits`, `games`, `game_players` and `ratings` to the `supabase_realtime` publication | Supabase creates that publication empty, and a table that is not in it never emits a `postgres_changes` event — silently, with a subscription that looks healthy. The tonight page (M3.4) and the bot (M4.4) are built on those events, so publishing belongs in the migration that creates the tables. `players` is left out because it is not publicly readable. | Adding tables to the publication by hand in Studio (invisible, and lost on every `db reset`); polling instead of Realtime |
| 2026-09-08 | Balancer scores closer than `1e-9` display points are a tie and fall through to the next tie-break key (`offRoleCount`, then the sorted blue puuids) | Two partitions can have the same arithmetic gap but sum their five effective skills in a different order, and floating point then differs in the last bit. Without a tolerance the brief's tie-break would be decided by that noise instead of by the stated key. Real differences are at least 0.6 points (`mu` carries two decimals), so `1e-9` cannot merge two genuinely different scores. | Exact float comparison (tie-break silently driven by summation order); rounding scores before comparing (changes the number the explanation prints) |
| 2026-09-08 | A `roleOverride` on a flexible player (`mainRole: null`) makes the override their only main; every other role is fill and off-role for them tonight | Literal application of the override rule: the usual main becomes the backup, and a null usual main is no backup. The player tapped a role, so honouring it is the only outcome they would not call rigged. A flexible player who taps nothing stays flexible. | Ignoring the override for flexible players (they tap top, get support, and the line still says "everyone on a main role"); keeping every other role at 1.00 (same problem) |
| 2026-09-08 | Each side of a `Split` is listed in lane order: top, jungle, mid, adc, support | The Discord embed, the tonight page and the "off-role" clause all read in lane order, so storing it that way means nobody sorts twice and the stored shape matches the printed one. Determinism does not depend on it (the sorted puuid list is the tie-break key), it is purely the shape consumers get. | Puuid order (the enumeration order, meaningless to a reader) |
| 2026-09-08 | `explain(split, next, players)` is exported from `@customs/core` beside `balance`, and `balance` builds its `explanations` with that same function | One code path for the sentence, so the string stored in the `splits` table and any string recomputed later (rebuilds, tests, a future copy change) cannot drift apart. `players` supplies names and role settings; the split itself carries no names because names get renamed and puuids do not. | Explanation built privately inside `balance` only (tests would have to reach around the API to check the aligned-side logic); storing names in the split |
| 2026-09-08 | `players_public` keeps `is_admin`; only `discord_id` is withheld | Who can press reroll is not a secret from the group, and the tonight page needs the flag to decide whether to draw the reroll button. Reading it from the public view keeps that page on the anon key instead of adding a service-role call to a public page. | Dropping `is_admin` from the view (the tonight page then needs a privileged round trip just to show or hide a button); exposing the whole `players` row (leaks `discord_id`) |
| 2026-09-08 | `packages/lcu` TLS default is `pinned`: verify the chain against the vendored `certs/riotgames.pem` only, with hostname verification skipped because the peer is always 127.0.0.1. `legacyDigests` (OpenSSL `SECLEVEL=0`, for a SHA-1 signed leaf) and `insecure` (`rejectUnauthorized: false`) are explicit opt-ins; the smoke script probes the three in that order and reports which worked | Pinning is nearly free and turns "anything on loopback" into "Riot-signed on loopback". Hostname checks cannot be kept until M0.2 shows what SAN the leaf carries, and a synthetic chain showed Node accepts a SHA-1 root but not a SHA-1 leaf at the default level, so the fallback ladder has to be in the code before the first live run | `rejectUnauthorized: false` as the default (the documented fallback; kept as the last rung); pinning with hostname checks on (would fail if the leaf has no `127.0.0.1` SAN) |
| 2026-09-08 | Fixtures are envelopes: `{ id, method, path, status, capturedAt, patch, clientVersion, contentType, body | bodyText }` under `fixtures/<major.minor>/<endpoint-id>.json`, with `body` the raw JSON untouched | A 404 for `lobby` and a non-JSON reply are data too, and M0.3 schemas need the status next to the body. `major.minor` matches how Riot names patches | Raw body only (loses the status; a 404 body looks like a real response); full version as the directory name (one directory per hotfix) |
| 2026-09-08 | `LcuClient` never throws for anything the client does: results are `{ ok: true, status, json }` or `{ ok: false, reason: 'http' \| 'malformed' \| 'schema' \| 'network' }`, each logged with `METHOD path`. `LcuSocket` remembers subscriptions and re-sends them on every `connect()`, rejects `connect()` only before the socket opened, and afterwards reports everything as `close`/`dropped` events; it has no backoff (M2.1 owns the loop) | The companion must survive a patch restart, a sleeping PC and a malformed frame without a single uncaught path; making the failure a value forces every caller to handle it | Throwing typed errors (one forgotten `try` crashes the watcher); putting backoff inside the socket (hides the policy the companion needs to log and tune) |
| 2026-09-08 | `packages/lcu` scripts (`smoke`, `record-ws`) run through `tsx`, pinned as a dev dependency | Relative imports use `.js` per the repo convention and Node's built-in type stripping does not map `.js` to `.ts`; `tsx` does, and there is still no build step | Node `--experimental-strip-types` (import specifiers would have to become `.ts`); a `dist` build for scripts only |
| 2026-09-08 | Every League client path lives in `packages/lcu/src/endpoints.ts` (`READ_ENDPOINTS`, `WRITE_ENDPOINTS`, `LIVE_CLIENT_DATA`); the smoke script walks that catalogue rather than its own list | The catalogue and the reference-doc table drift otherwise, and "the post-patch check hits every endpoint we use" is only true if there is exactly one list | Paths inline where they are called |
| 2026-09-08 | Recorded WebSocket events are scrubbed by URI deny-list plus key deny-list before they are written: `/lol-login`, `/lol-rso-auth`, `/riotclient/auth`, `/lol-chat` and any URI containing auth/token/credential/password/secret are written as `{ ts, topic, uri, eventType, redacted: true }` with no `data`; inside every other payload, keys matching token/password/secret/cookie/authorization/credential/jwt/bearer become `[redacted]`. Frames the parser refused get the same treatment (`{ ts, reason, dropped: true, redacted: true, uri }` for a sensitive URI, a key-scrubbed `frame` otherwise, and a 200-character text-scrubbed `preview` when the frame is not JSON), never the raw text. The reader of `ws-events.ndjson` therefore always sees which events fired, never a credential | `OnJsonApiEvent` includes the login session (`idToken`) and RSO access/refresh tokens, and `fixtures/` is committed. Redacting `data` instead of dropping the event keeps the event-order evidence M0.3 needs (which URIs fire around EndOfGame) | Dropping sensitive events entirely (loses the timeline); trusting the reviewer to skim the file before committing (the README still asks for that, as a second line) |
| 2026-09-08 | The smoke TLS probe steps down only on a TLS error (`isTlsError`); ECONNREFUSED/ETIMEDOUT stop it with exit 3 | Otherwise a client that is still starting would time out on pinned modes, answer on insecure, and the manifest would falsely record that pinning fails, corrupting the answer to reference question 8 | Stepping down on any network failure (simpler, dishonest) |
| 2026-09-08 | `checkServerIdentity` is skipped for `127.0.0.1` only; any other `host` gets Node's normal hostname check | `LcuClient`/`LcuSocket` accept a `host` for tests; a blanket skip would silently weaken TLS for whoever passes a real hostname later | Blanket skip (original M0.1 draft); removing the `host` option (tests need it for the localhost case) |
| 2026-09-08 | Relative imports drop the `.js` extension (`from './types'`, not `'./types.js'`) in every package `apps/web` imports — that is `packages/core` and `packages/db`. `packages/lcu` is deliberately exempt: the web app must never import it, and its scripts run through `tsx`, which does map `.js` to `.ts` (see the `tsx` row above) | Turbopack has no `extensionAlias`, so it cannot resolve a `./x.js` specifier to `x.ts`. `next build` and `next dev` both failed on `apps/web/app/page.tsx` importing `@customs/core` — the whole app was 500 before M1.5 touched it, and every route that imports `@customs/db/schemas` would have been too. With source-shipping packages and `moduleResolution: "Bundler"`, extensionless is the specifier every consumer of core and db (tsc, vitest, Turbopack) agrees on. | `experimental.extensionAlias` (webpack only, Next 16 defaults to Turbopack); `next build --webpack` (deprecated path, and dev would still need the flag); building packages to `dist` (already rejected: breaks a fresh checkout); applying the rule to `lcu` too (churn in a package no bundler ever sees) |
| 2026-09-08 | Companion auth is a handler wrapper, `withCompanionAuth(schema, handler)`, not Next middleware; it authenticates before it parses the body | Middleware cannot be unit tested without a server and would hand the handler a header to re-read instead of a typed identity. Authenticating first means an unauthenticated caller learns nothing about the payload shape we expect. | Next middleware on `/api/companion/*`; a `requireCompanion(request)` call at the top of every handler (easy to forget) |
| 2026-09-08 | `companion_tokens.last_seen_at` is written at most once every 5 minutes per token | The companion polls; writing a timestamp on every request would be one write per poll for a field only a human reads on the admin page. The lookup already returns `last_seen_at`, so the throttle costs nothing. | Writing on every request; not tracking `last_seen_at` at all (an admin needs to see which companions are alive) |
| 2026-09-08 | `bootstrap_admin` runs lazily on the first companion request of a process, memoised, and never fails the request | A build-time or cold-start hook (`instrumentation.ts`) would have to cope with the database being unreachable during `next build`, and nothing needs the admin row to exist before the first request. The SQL function is idempotent, so "once per process" is a cost optimisation, not a correctness rule. | `instrumentation.ts register()`; calling it from a migration (cannot read the environment); a script someone has to remember to run |
| 2026-09-08 | `POST /api/companion/rank` accepts any PUUID from any valid token, and only a `RANKED_SOLO_5x5` payload writes the rank columns | The companion reports the rank of unknown lobby members so a first-time player can be seeded before their first game (M2.4), so restricting it to the token's own player would break seeding. A rank is a lookup the client performed, not a claim about who the caller is. One player row holds one rank, and the seed table is a solo-queue table, so a flex reading must not overwrite it; it is accepted and reported as `stored: false`. | Only the token's own PUUID (kills M2.4 seeding); storing whichever queue arrived last (a flex rank would silently reseed a player) |
| 2026-09-08 | An end-of-game block whose `gameType` is missing, null or anything but `CUSTOM_GAME` is refused with 422 | The companion reads `gameType` straight off the block, so its absence means we are looking at something we do not understand — and a ranked game landing in the customs ratings is worse than a dropped game we can backfill. | Ingesting when `gameType` is absent (optimistic); accepting and flagging the row for later |
| 2026-09-08 | Ingest diffs before it writes: a repeated identical lobby or player post updates no column at all | `lobbies.updated_at` has a trigger, so a blind upsert would bump it on every poll and "updated" would stop meaning "something changed" — which M2.5's stability rule and the tonight page both read. It also makes "post the same lobby twice, nothing changes" literally true rather than true of row counts only. | Blind upsert on every post; comparing in SQL with a `WHERE ... IS DISTINCT FROM` clause per column (PostgREST cannot express it) |
| 2026-09-08 | `lobbies.reported_by_player_id` is the first companion that reported the party, and is not reassigned | Five companions in one lobby posting the same roster would otherwise fight over the column on every event. It means "who told us about this lobby", which is only interesting for the first answer. | Last reporter wins (a write on every post from every companion); a `lobby_reporters` join table (nothing needs it yet) |
| 2026-09-08 | One response envelope for the whole API: `{ ok: true, ... }` or `{ ok: false, error, issues? }`, with 400 validation, 401 auth, 403 non-participant, 422 non-custom | The companion branches on one field and can log `error` verbatim; `issues` carries the zod paths so a payload bug is one log line rather than a debugging session. | Bare 4xx with an empty body; throwing and letting Next render its own error page (leaks stack traces) |
| 2026-09-08 | `POST /api/companion/game` accepts `phase: 'in_progress'` and stores nothing until M2.5 | The companion (M2.3) posts it as soon as the client enters the game and needs a 200, but moving the lobby to `in_game` is a state-machine transition and the state machine has exactly one home (M2.5). Answering 404 or 501 would make M2.3 wait on M2.5 for no reason. | 501 until M2.5; writing the `games` row early with a null result |
| 2026-09-08 | The API integration tests call the route handlers in-process against the Supabase CLI local stack, and skip when it is not running | Calling `POST(request)` exercises the real handler, the real service-role client and the real SQL constraints without a dev server in the test harness, so the suite is one `vitest run`. Skipping (not failing) keeps `pnpm -r test` green on a machine without Docker, exactly as `packages/db` does. | Spawning `next dev` and using `fetch` (a second process to wait for and kill); mocking the Supabase client (proves nothing about the constraints that make ingest idempotent) |
| 2026-09-08 | `apps/web/scripts/mint-token.ts` is run straight by Node's type stripping (`pnpm --filter web mint-token`), which is why `apps/web/tsconfig.json` sets `allowImportingTsExtensions` | The companion needs a token before `/admin` exists (M1.6), and a script that imports the real `mintCompanionToken` cannot drift from the hash the API checks. Node 22+ strips types, so no `tsx` dependency is needed; it only requires the `.ts` extension on the two relative imports. | A `tsx` dev dependency; a self-contained script that reimplements the SHA-256 hashing (drift is silent until a token stops authenticating) |
| 2026-09-08 | The "a companion may only report a game it was in" check is participant membership: the token's PUUID must appear in `participants`. `01-architecture.md` "Security" says the block's `localPlayer` must match the token's player; the companion flattens the end-of-game block before it posts, so `companionGamePayloadSchema` (M1.2) carries no `localPlayer` field | The two checks are the same strength — a companion that was in the game is on the scoreboard, and one that was not cannot forge a scoreboard it is on without forging the whole block, which `localPlayer` would not stop either. Membership needs one fewer field to trust, and it is checked against the ten rows we are about to store rather than against a claim beside them. The architecture sentence needs updating; routed to product. | Adding `localPlayer` back to the payload (a second identity field the API must distrust, and it can disagree with `participants`); trusting the block's own `localPlayer` without cross-checking the scoreboard; no check at all (any token could post any game) |
| 2026-09-08 | The admin gate reads the Discord snowflake from `user.identities[]` where `provider === 'discord'` (`UserIdentity.id`, GoTrue's `auth.identities.provider_id`), never from `user_metadata.provider_id` | Both carry the same snowflake, but `user_metadata` is writable by the signed-in user through `auth.updateUser({ data })`, so a gate that read it would let any Discord account claim an admin's id. Verified against the local stack: signing in a user with a discord identity row returns `identities[].id` = the provider id, and the email identity's id = the user id. `user_metadata` is read for the display name only. | `user_metadata.provider_id` (the shorter path, and a privilege escalation); the JWT `sub` (that is the Supabase user id, not the Discord one) |
| 2026-09-08 | Admin auth is `withAdminAuth(schema, handler)` for routes and `requireAdmin()` for server components, mirroring `withCompanionAuth`; 401 means no session and 403 means every other refusal, and auth failures are always the JSON envelope even for a form post | The companion wrapper already proved the shape, and the same order (authenticate, then parse) keeps an anonymous caller from learning the payload. Answering auth failures with the envelope rather than a redirect makes "401 anonymous, 403 non-admin" one assertion whichever way the request arrived, which is exactly what M1's acceptance asks for. | Next middleware as the gate (cannot be unit tested, and would be a second copy of the rule); redirecting a signed-out form post (the status would then depend on the content type) |
| 2026-09-08 | `/admin` pages are server components with plain HTML forms posting to `/api/admin/*`; the routes accept both JSON and `application/x-www-form-urlencoded` and answer a form post with a 303 back to the page carrying `?notice=` or `?error=` | One mutation path instead of two: the route handlers are the API, and the pages are their first client. It keeps the admin area working with no client JavaScript on a phone in the dark, and it means the same zod schema validates the browser and a curl. Empty strings are the form's way of saying null, which is how "clear a main role back to flexible" is expressible at all. | Server actions (a second write path the acceptance criteria's 401/403 tests could not reach); a client-side fetch UI (JavaScript for five buttons) |
| 2026-09-08 | A freshly minted companion token is returned in the JSON response, and for a form post in a one-off HTML page the route handler renders itself, never in a redirect URL, a cookie or a flash message | A token in a query string lands in browser history and every proxy log; a cookie would still be readable after a refresh. The mint response is the one place the raw token exists, which is what "shown once" has to mean when only its SHA-256 is stored. | Redirect with `?token=` (leaks it); a short-lived cookie the page renders (still re-readable, and it puts a live credential back on the wire); storing it for later display (defeats hashing) |
| 2026-09-08 | `BOOTSTRAP_ADMIN_DISCORD_ID` links a Discord snowflake to `BOOTSTRAP_ADMIN_PUUID` on the first request that needs it, idempotently, and refuses to move a link that already exists | The first admin is seeded by PUUID but `/admin` matches sessions by `players.discord_id`, so the bootstrap admin could never sign in and only an admin can do the linking. An env variable breaks the loop with the same properties the PUUID one already has: optional, idempotent, safe to leave set. It only ever fills a null, so it cannot steal an id from another player. | A first-run "claim this account" page (an unauthenticated privilege escalation by design); linking by Discord *username* (renamable); doing it by hand in Studio (undocumented and forgotten by the next deploy) |
| 2026-09-08 | An admin may not remove their own `is_admin` flag (403, and the button is disabled) | The last admin demoting themselves locks the whole group out of `/admin`, and the only way back is a redeploy with `BOOTSTRAP_ADMIN_PUUID` set. Demoting *someone else* is always fine, so this is a one-line rule rather than a "last admin" count that would race. | Counting remaining admins (racy, and it still lets the last two lock each other out); no guard at all |
| 2026-09-08 | Starting a season is `public.start_season(name)` (migration `0002`), one transaction that closes the active season and opens the new one | `seasons_one_active_idx` allows exactly one active season, so two PostgREST calls could leave zero: `active_season_id()` would return null and every `games` insert would fail on its not-null `season_id`. PostgREST has no transactions, so the transaction has to be a function. Service role only, like `bootstrap_admin`. It does not carry ratings forward — that is M5.3, and the page says so. | Deactivate-then-insert from the app (a failure between them breaks ingest); allowing several active seasons (the index exists for a reason) |
| 2026-09-08 | The admin session is refreshed in `proxy.ts` (Next 16's `middleware.ts`), matched to `/admin/:path*` only, and it gates nothing | Server components cannot write cookies, so without it the access token expires after an hour and an admin is silently signed out mid-session; this is the only place `@supabase/ssr` can hand refreshed tokens back to the browser. `/api/admin/*` is deliberately outside the matcher: those handlers read cookies off the request and must never depend on middleware having run for their 401/403 to be correct. | No refresh at all (re-login every hour); matching `/api/admin/*` too (a gate that looks like it is in two places); gating in middleware (a weaker copy of `players.is_admin`) |
| 2026-09-08 | The Discord webhook URL is shown masked on `/admin/discord`, an empty field means "keep the stored one", and clearing it takes an explicit checkbox | The URL is a bearer credential for posting as the bot, and a masked value cannot be round-tripped through a form — so "empty means null" would silently wipe it on every unrelated save. The response schema returns `webhookSet` and the mask, never the URL. | Rendering the URL in the input (a live credential in the page source and in every screenshot); empty means clear (destroys it by accident) |
| 2026-09-08 | `discord_config` writes are keyed by `guild_id` (upsert on the primary key); the page renders the oldest row and warns when there is more than one | The first version read "the oldest row" and rewrote it, so a leftover config for another guild would have been silently renamed into the one being saved — a config the bot then posts through. Keying on the primary key makes the write say exactly which row it touches, and a second row becomes a visible edge state an admin resolves deliberately instead of a silent overwrite. | Rename-the-oldest-row (the silent overwrite); deleting other rows on save (destroying data the admin did not mention); a `singleton` check constraint (would need a migration for a case that should not happen) |
| 2026-09-08 | `apps/web` runs vitest with `fileParallelism: false`, and `set_active_season(id)` (migration 0002) restores Season 1 in one transaction after the season tests | The integration files share one Supabase local stack, and the active season is a singleton that cannot be namespaced with a run id: in parallel workers the admin file's `start_season` moved the active season out from under the companion file's game ingest, which then failed with a null `season_id` (about half of full runs). Serialising files removes the overlap, and an atomic restore removes the window where no season is active at all. Cleanup now throws on error rather than leaving test seasons behind. | Namespacing seasons per run (the singleton is the problem, not the naming); a separate database per file (a second stack to start and keep in step); retrying the flake |
| 2026-09-08 | Starting a season from `/admin/seasons` requires typing the name of the season being ended; the copy on the page states the consequence in plain words rather than in milestone ids (M3.9) | It is the only control in the app that destroys, in the eyes of everyone who looks at the leaderboard, a month of the group's history, and there is no undo: `ratings` is keyed `(player_id, season_id)` and nothing carries over until M5.3. It sat two form fields away from "set a role", one click, on a page an admin opens on a phone. Friction here is the point; every other admin control is reversible in one more click, so none of them get this. | A confirm dialog (needs client JavaScript, which the admin area deliberately has none of); no guard and a louder warning (a warning is not a guardrail); hiding the page until M5.3 (the group may genuinely want a fresh season before then) |
| 2026-09-08 | The leaderboard shows two numbers per player: **Proven** (`round(ordinal * 60)`) is the primary column and the sort key, and **Rating** (`round(mu * 60)`) is the secondary line and the number the embeds print beside a name. Where only one number fits, as in the nightly Discord embed, it is Proven. Both names are fixed on every surface (product, M3.5 brief) | Sorting on `ordinal` while printing `mu * 60` puts a visibly out-of-order list on the page, which is the exact complaint M3.8 exists to prevent; printing `ordinal * 60` alone means nobody recognises their own number. Naming both settles "which number am I" in voice and gives the still-settling sentence two things to point at | Printing `mu * 60` alone and sorting on it (the board stops being cautious and a smurf tops it in a week); printing `ordinal * 60` alone; the designer's placeholder label `Board` (circular as a column name on the board itself) |
| 2026-09-08 | A displayed rating change is `displayRating(muAfter) - displayRating(muBefore)` — both rounded, then subtracted — computed at the display boundary by one shared helper that the result embed, the tonight page and the player page all call (product, M3.3 brief) | The row has to add up in front of ten people: `1469 (+43)` beside a new rating of `1512` is only true under this rule. One helper means the Discord message and the web page cannot print different numbers for the same game, and `packages/core` stays free of a display concern beyond `displayRating` | `round((muAfter - muBefore) * 60)` (off by one often enough to be noticed, and the row stops adding up); storing a delta column on `game_players` (a second source of truth for a subtraction) |
| 2026-09-08 | The five-rating side sums stay bare numbers with no label, in the team card header and in the teams embed field name (`Blue · 7695`); the web number carries visually-hidden text `sum of the five ratings`. The word "gap" appears only in the explanation line (product, from designer finding 3) | The sum sits directly above or beside the five ratings that make it, so it needs no noun. Any label short enough to fit would be a second name for a quantity used nowhere else in the product, and a named number invites people to subtract the two and compare the result with `Gap`, which is computed on role-adjusted effective skill and is a different thing | Labelling it `Total` or `Sum` (a new noun for a number we never reuse); labelling it `Gap` (wrong, they are not the same quantity); dropping the sums entirely (they are the fastest read of "are these two sides close") |
| 2026-09-08 | Seven named colour tokens per theme (`bg`, `surface`, `text`, `dim`, `blue`, `red`, `accent`) and nothing else; every other value is derived with `color-mix()`. `accent` is brass and is the one colour that belongs to neither side (designer, M3.0) | An eighth colour is an eighth thing to keep in step across two themes. Deriving hairlines and tints from the seven means the light theme follows the dark one for free. Live state, off-role, "you" and the reroll control all need a colour that does not read as "blue team" or "red team" | A full scale per hue; a single saturated accent used everywhere; using `blue`/`red` for anything but side 100 and side 200 |
| 2026-09-08 | Rating deltas are never coloured by sign: a gain is `text` at weight 600, a loss is `dim`, and both always print their sign (designer, M3.0) | Green-for-good would collide with `red` already meaning side 200, and a red loss printed next to the red team is unreadable in both senses. The sign is the signal and it is always there | Green up and red down (every other scoreboard); colouring only losses |
| 2026-09-08 | Discord embeds are built from short self-contained lines (a role in inline code, a name, a middot, a number), never fenced code blocks and never aligned columns (designer, M3.0) | Discord's proportional font will not align names, and a code block that does align is a grey slab that scrolls sideways on a phone and kills every other colour in the message. Inline fields re-wrap to one column on mobile, so every field has to make sense read alone and in any order | A fenced code block with padded columns; one field per player (25-field limit and unreadable on a phone); an image render of the teams |
| 2026-09-08 | The teams embed bar is `accent` (brass, neither side); the result embed bar is the winner's side colour (designer, M3.0) | A teams embed tinted blue would read as a prediction before anyone got to the win chance. On a result the winner is a fact, so the bar can carry it and the two messages stay distinguishable at a glance while scrolling | Side colour on both (teams post looks rigged); accent on both (the result loses its one free signal) |
| 2026-09-08 | The tonight page renders exactly one primary block, chosen from `lobbies.status` for the newest non-abandoned lobby today, and a state change replaces that block in place with a 150 ms opacity fade — the page never appends, never scrolls itself and never animates anything else (designer, M3.0) | The page is read on a phone, one-handed, by someone who is about to be in a game and may be mid-tap on their role. A page that appends or scrolls under a thumb loses the tap and the reader's place. One always-mounted header strip means a phone reopened mid-night answers "where are we" without scrolling | A feed of blocks that accumulates through the night; per-section spinners and skeletons; slide or scale transitions |
| 2026-09-08 | `apps/web/app/admin/admin.css` does not adopt the design tokens and keeps `color-scheme: light dark` and native controls; its `.admin { font-family: system-ui }` rule must stay once the root layout sets a font (designer, M3.0) | The admin area is five people on a laptop with no client JavaScript and every control a native form control. Skinning them means maintaining a second set of button, input and select styles for an audience already inside the building. The `.admin` font rule is what keeps the admin plain after the root layout sets `--cn-font-sans` on `body` | Adopting the tokens in the admin (a second UI to keep in step); a shared component library across both areas |
| 2026-09-08 | Two typefaces, split by meaning: Archivo for anything read as language, IBM Plex Mono with `font-variant-numeric: tabular-nums` for anything read as data — ratings, deltas, gap, percentages, durations, rank numbers and role labels (designer, M3.0) | A column of ratings in a proportional font wobbles, and a scoreboard that wobbles looks wrong before anyone can say why. Plex Mono also gives a distinguishable 1 and l and a real minus sign, which a column of signed deltas needs | One family throughout (numbers wobble); a mono display face for headings (dashboard template); rendering numbers in the body font with only `tabular-nums` |
| 2026-09-08 | A companion may only report a lobby it is in (M1.8): the token's player PUUID must be in the posted `members` (any `isSpectator` value), or the caller must already be the lobby's `reported_by_player_id`. Anything else is 403 before a single row is written, unknown `partyId` included | The posted list replaces the roster, deletions included, so with no check any valid token could shrink another lobby to one member — verified against the local stack on 2026-09-08, HTTP 200 and ten rows became one. The owner fallback keeps two real cases working that the member test alone would refuse: the "everyone left" empty list (an empty list cannot contain the caller) and a companion whose client has stopped listing it once it is only spectating (M0.3 is still open on whether the client lists spectators at all) | Members-only with no fallback (the owner could not report an empty lobby); trusting a `reporter` field in the body (the token decides, never the payload); silently ignoring a foreign post (a 200 that changed nothing is indistinguishable from a bug) |
| 2026-09-08 | `lobby_members` is frozen once the lobby reaches `in_game` and stays frozen in `finished` (M2.9): a post for that party is still 200 and still idempotent, the lobby name and password still refresh, and the response says `rosterFrozen: true` with the stored `memberCount`, but no member row is inserted, updated or deleted. `open`, `balanced` and `abandoned` keep the replace semantics | From `in_game` on the roster is history, not live state: M2.7 matches tonight's ten against it and M5.5 lists lobbies that started and never finished. Verified 2026-09-08 that an empty `members` array emptied a lobby, so the last companion to shut down could erase who was around. `abandoned` is excluded because a lobby that dissolves without ever starting has nothing worth keeping (M2.9 brief) | Freezing only deletes and still refreshing `side` (the M2.9 brief's first phrasing; dropped because the same brief requires two companions posting different lists after `in_game` to change nothing, and the side that matters after the game starts is the one on `game_players`); 409 on a post to a frozen lobby (the companion is not doing anything wrong and would retry forever); freezing from `balanced` (people still join and leave while teams are being shown) |
| 2026-09-08 | `players.display_name` follows the Riot `gameName` automatically (M1.7): set to the reported `gameName` when the row is created, and refreshed on a later report only while it still equals the `game_name` we stored, or is null. `ensurePlayers` is the only writer on the ingest side | The name on every surface has to be the one the group uses, and M1.4's explanation string reads "swap  and " when it is null. "Display name still equals the stored game name" is the cheapest test for "nobody has overridden it" that needs no extra column: a rename follows through, an admin's override survives every rename after it, and because null also counts as automatic, clearing the admin field (which posts `""` and stores null) puts the row straight back on automatic at the next report | A `display_name_source` column (a migration and a second thing to keep in step for a rule two comparisons express); never overwriting a non-null display name (a cleared field would stay null forever, and a rename would never follow); writing the display name from the admin route only (every row would start blank, which is the M1.7 bug) |
| 2026-09-08 | The client-shape-to-companion-payload mapper lives in `packages/lcu`, which takes a dependency on `@customs/db` for the payload schemas; `apps/companion` calls it and holds no copy | Client-shape knowledge belongs where CLAUDE.md already confines client calls, and one mapper tested against the 16.17 fixtures keeps the companion and the API on one contract. Dependency direction stays acyclic: lcu -> db -> core. | Mapper in `apps/companion` (a second place that knows client shapes); mapper in `packages/db` (would drag client shapes into the schema package) |
| 2026-09-08 | The companion talks to the client with TLS pinned to Riot's vendored root (`packages/lcu/certs/riotgames.pem`), and `insecure` stays a diagnostic flag on the smoke and record-ws scripts, never a companion fallback | Verified on 16.17 (macOS): plain pinning answered HTTPS and WSS on the first attempt, no legacy digests needed. The credential in the lockfile is the real secret, so pinning mostly guards against a wrong local port answering with something else; it costs nothing now that it is known to work. If a patch rotates the certificate, `smoke` shows which mode answered and the fix is a new `riotgames.pem`. | `rejectUnauthorized: false` on loopback (the community default); automatic step-down to insecure in the companion (would hide a certificate change until someone reads a log) |
| 2026-09-08 | `LCU_LOCKFILE_CANDIDATES` replaces the platform default lockfile paths; `packages/lcu` tests set it (or inject `readFile`) so they never look at the real install | Two tests failed on a machine with League running because "no lockfile" assumed the developer had no client. Unit tests must pass both with and without League installed, and the CLI subprocess in `smoke.test.ts` cannot be mocked from outside, so an environment override that the CLI honours is the smallest change. Unset for users, so `--lockfile` and the defaults behave as before. | Skipping the tests when a lockfile exists (hides the bug on exactly the machines that matter); mocking `node:fs` (does not reach the subprocess); a `--no-defaults` flag (one more flag to document for a test-only need) |
| 2026-09-08 | Match history backfill goes through `GET /lol-match-history/v1/games/{gameId}` for each custom game; the list endpoint is only used to find game ids | On 16.17 the list carries only the local player in `participants`/`participantIdentities` (length 1, even for a completed 5v5); the detail carries all ten with puuids. So a backfill is one list call plus one detail call per custom game. | Reading rosters from the list (impossible on this patch); using the live client data server (no puuids, only while in game) |
| 2026-09-08 | Schema tests in `packages/lcu` are pinned to an explicit fixture directory (`16.17`) rather than the newest one | Fixtures are captured in whatever state the client was in (this capture: mid custom game, so `gameflow-session` is a 200 with a game id). A test that followed "newest" would assert a state it cannot know; a new patch gets a new capture, its rows re-verified, and its own tests. | Tests over the newest directory (state-dependent, breaks on every capture); fixtures hand-trimmed into a canonical state (forbidden: never hand-edit a fixture) |
| 2026-09-08 | The companion takes the end-of-game block from the WebSocket `Create`/`Update` event and keeps it in memory; a GET on `EndOfGame` is only a fallback | On 16.17 the event arrives 0.3 s after `WaitingForStats`, before the phase reaches `EndOfGame`, and the GET is 404 again once the player is back in the lobby. Polling would race the player's click; the event cannot be missed while the socket is up, and on reconnect the GET covers the case where the screen is still open. | Polling `eog-stats-block` on `EndOfGame` (loses the game if the player clicks fast); reading match history afterwards (works, one detail call, but delays the result by however long the client takes to index the game) |
| 2026-09-08 | A lobby member's side is derived from `gameConfig.customTeam100`/`customTeam200` membership; `members[].teamId` is ignored | On 16.17 `teamId` is `0` for everyone in a custom lobby, before and after switching sides. | Trusting `teamId` (always blue); asking the user |
| 2026-09-08 | The committed `ws-events.ndjson` is trimmed to the URIs the companion reads; the full stream stays out of the repo | The full 21-minute stream is 1.9 MB, and 1092 of its 1310 lines are chat, friend presence, clash, champion select and skin payloads that the companion must never act on. The trimmed file (225 lines, 0.7 MB) still holds every lobby, gameflow, end-of-game and matchmaking event, which is what the schemas and the fake client need. Trimming is a URI filter run through the same scrub code, not an edit. | Committing the full file (bloat, and champion-select payloads in the repo); committing nothing (no WS fixture for the companion tests) |
| 2026-09-08 | Bots are filtered out of every roster by `botPlayer` (end-of-game) / `isBot` (lobby), never by puuid shape | Bots carry the all-zero puuid in the end-of-game block and an empty puuid in the lobby; both would otherwise reach the API as a player. The flags are explicit; the puuid conventions are incidental. | Rejecting on the API side only (a companion would still post them); treating a zero puuid as a bot (misses the lobby's empty string) |
| 2026-09-08 | Starting a season requires typing the name of the season being ended, compared exactly after trimming and case-sensitively (M3.9) | The one control in the app with no undo sits two fields away from "set a role", on a page an admin opens on a phone. Trimming forgives a stray space from a copy-paste, which costs nothing; matching case-insensitively would let `season 1` end `Season 1`, and the friction is the entire point of the field. A wrong confirmation and a missing one are the same 400 and `start_season` is never called, so nothing is written either way. | Case-insensitive compare (removes the friction the field exists for); no trim (a trailing space from a copy-paste reads as a bug, not a guardrail); a checkbox or a browser `confirm()` (one click, which is what M3.9 exists to stop); a typed literal like `END SEASON` (does not make the admin read which season they are ending) |
| 2026-09-08 | `startSeason` skips the confirmation entirely when no season is active | The confirmation asks for the name of the season being ended, and there is nothing to end. Demanding a name for a season that does not exist would be unanswerable, and the destructive half of the action — emptying a leaderboard — is not happening. The page says so in place of the field: "No season is active, so there is nothing to end." | Requiring the new season's own name as a confirmation (a nonsense prompt to satisfy a uniform rule); refusing to start a season with none active (the recovery path out of exactly that state) |
| 2026-09-08 | `players.display_name` is capped at 40 characters; longer is a 400, not a truncation (M1.7); enforced in `adminPlayersRequestSchema`, not only by the form's `maxLength` | Verified 2026-09-08 that the cap today is the `maxLength={40}` attribute on the admin input and nothing else — there is no `.max(40)` in the zod schema and `players.display_name` is plain `text` — so a JSON caller bypasses it entirely; the server check is owed. The name goes in a Discord embed column beside a rating and in a table cell on a phone. 40 is past any Riot ID (16 characters plus a tag) and short enough that no row can be pushed out of shape by one person. Refusing beats truncating because a silently shortened name is a name the admin did not choose and will not notice until it is on the board. | Truncating to fit (silent, and the admin never sees what shipped); no cap (one paste breaks every layout that shows a roster); a database `varchar(40)` (the same rule, enforced where the error message cannot explain itself) |
| 2026-09-08 | Admin response schemas validate season ids with `z.guid()`, not `z.uuid()` | The seeded `SEASON_ONE_ID` is a fixed, hand-written constant with no version nibble, so it is a well-formed GUID but not a valid RFC 9562 UUID; zod's `uuid()` rejects it and every seasons response would 500 on a fresh database. `guid()` is the honest description of what the column holds. | Reseeding Season 1 with a v4 uuid (a migration to make a test pass, and every existing local database drifts); dropping the id validation (the schema stops catching a genuinely wrong shape) |
| 2026-09-08 | `playerLabel` renders the Riot ID rung as `gameName#tagLine`, and a whitespace-only `display_name` is treated as absent when rendering | The tag disambiguates two friends with the same game name; whitespace-only names come only from direct database edits and must not print as blanks. | Bare `gameName` (ambiguous), printing whatever is stored |
| 2026-09-08 | The season-start confirmation is read-then-write (`getActiveSeason` then `start_season()`), not atomic; two submits inside the same few milliseconds could both pass | Closing it needs a new SQL function taking the confirmation name, and the group has one or two admins; recorded as known, revisit if a second admin ever shows up. | A `start_season_confirmed(name)` function now (migration for a race nobody can hit) |
| 2026-09-08 | A fixture for an endpoint already captured, taken in another client state, is an overlay named `<endpoint-id>--<state>.json` beside the base file (`lobby--two-players.json`, `gameflow-session--in-lobby.json`, the existing `--other`); a later `record-ws` window is appended to the one `ws-events.ndjson` in time order and listed in `manifest.overlays`, and tests that count lobbies or games select a window by `ts` | The base file is what `smoke --diff` compares against and must stay the state its tests pin; the second capture overwrote it in the main tree and had to be restored. A named overlay keeps both states and says which one a test reads. One WS file keeps the fake client and the schema tests reading a single stream, and the file is 0.94 MB after three windows; the README says to start `ws-events--<state>.ndjson` when the next window would cross 1 MB | Overwriting the base file (loses the solo state and breaks its tests); a second patch-like directory per capture (`16.17-b`: the patch dirs are sorted numerically and mean "this client version"); a separate `ws-events-2.ndjson` now (two readers to keep in step for 220 KB) |
| 2026-09-08 | `apps/companion` keeps the `.js` extension on relative imports and runs through `tsx` (`pnpm --filter companion dev`), the same exemption as `packages/lcu`; only `packages/core` and `packages/db` drop the extension | The companion is never imported by `apps/web` (Turbopack is the only consumer that cannot map `.js` to `.ts`), and it is the one workspace that imports `@customs/lcu`, which already runs this way. M2.6 bundles it, and every bundler resolves `.js` to `.ts`. | Extensionless imports (would need a separate tsconfig `moduleResolution` from lcu for no consumer that needs it); Node type stripping (would force `.ts` specifiers, which bundlers and `tsc` handle less uniformly) |
| 2026-09-08 | The companion config carries an optional `lockfilePath` beside `{ apiBase, companionToken }`, passed to `discoverLockfile` as the override tried before the platform defaults | The reference says discovery must handle a configured override, and a friend with League on `D:\` has no other way to point the companion at it short of the Windows process-args fallback (still unimplemented). Optional, so the documented two-field shape is still the whole first-run story. | An environment variable only (`LCU_LOCKFILE_CANDIDATES` exists but is a test/CI knob and replaces the defaults); a `--lockfile` flag (a tray exe has no command line) |
| 2026-09-08 | `DEFAULT_API_BASE` in `apps/companion/src/config.ts` is `http://localhost:3000` until M2.6 stamps the deployed origin; the first-run prompt offers it as the default and accepts any `http(s)://host[:port]` origin without a path | There is no deployed origin yet (M1.10 is open) and inventing one would be a wrong default that persists in every friend's config file. The prompt checks the origin with `GET /api/health` and lets the person keep an unreachable one, since the API may simply be down and every later call retries. | A hard-coded Vercel URL guessed today; refusing an unreachable origin (locks a friend out during a deploy) |
| 2026-09-08 | The connection machine has four states, the three documented ones plus `stopped`, reached only by `stop()` (SIGINT/SIGTERM) and left by nothing; the transition table `TRANSITIONS` in `connection.ts` is the whole machine and an unlisted move is logged and ignored, never thrown | "Never exits on an error" needs the exit path to be a state the loop can see, not an exception; a table makes the reconnect-forever property reviewable in six lines and testable without a client. | A boolean `stopping` flag beside three states; throwing on an illegal transition (would end the loop it exists to protect) |
| 2026-09-08 | The companion subscribes to the firehose (`OnJsonApiEvent`) and routes by `uri` to three hooks (`/lol-lobby/v2/lobby`, `/lol-gameflow/v1/gameflow-phase`, `/lol-end-of-game/v1/eog-stats-block`); every other event is a debug line with URI and type only, and payloads are never logged | The per-URI topics are still unverified on 16.17 (reference, question 7), and one subscription is one frame to re-send on reconnect. Hooks run one at a time in arrival order so a lobby watcher can never post members out of order. | Per-URI subscriptions (unverified); logging ignored payloads at debug (the stream carries chat and login credentials) |
| 2026-09-08 | `@customs/lcu` exports its fake client to other workspaces through the package subpath `@customs/lcu/test-support/fake-lcu`, and the companion's state-machine tests run against it plus an in-process fake API (`apps/companion/src/test-support/fake-api.ts`) | The fake client already exists for lcu's own tests; a second copy in the companion would drift from the real socket behaviour it imitates. A subpath keeps it out of the main export surface so production code cannot import it by accident. | Copying `fake-lcu.ts` into the companion; exporting it from the package root |
| 2026-09-08 | Companion logs are JSON lines in `<configDir>/logs/companion-<YYYY-MM-DD>.log` (UTC date, file level `debug`, 14 files kept) plus one human console line at `info` (`CUSTOMS_NIGHT_LOG_LEVEL` overrides); fields are scrubbed with lcu's `scrubValue` and every registered secret (the token, the lockfile password) is string-replaced with `[redacted]` in the serialised line | Two layers because they fail differently: key-based scrubbing catches a credential under a new key name only if the name looks like one, and string replacement catches the two values we know regardless of where they land (a URL, a message, a nested list). Tested by grepping the written files. | One layer; logging the token's prefix for support (a prefix is still a secret) |
| 2026-09-08 | The 10-second lobby stability rule is measured on the posts we already get, not by a timer: the roster's identity is `rosterKey()` over the non-spectator, non-bot puuids, the clock is the lobby row's own `updated_at` (written only when that identity changes), and the lobby response carries `recheckInMs` telling the companion when to re-post an unchanged roster (M2.5, M2.2) | Vercel route handlers have no timers and no queue, and Hobby cron runs once a day, so a rule expressed in seconds has to be evaluated by a request. Keeping the rule and the number on the server means a change ships with the API rather than with a new exe on ten friends' PCs; the companion only obeys a number it is handed. | A Vercel cron (once a day on the free plan); pg_cron in Supabase (puts the state machine in SQL, away from `packages/core`); a blocking wait inside the request; a debounce in the companion (every friend's exe would carry its own copy of the rule) |
| 2026-09-08 | A game is rated only when the stored row has ten `game_players`, five per side, and `duration_s > 300`; 300 exactly is not rated (M2.5) | M1.5 stores every `CUSTOM_GAME` block, remakes and 4-minute surrenders included. Five minutes is the shortest thing anyone in the group would call a game, and rating a remake would move ratings for a game nobody played. | Rating everything and filtering on the leaderboard; trusting `gameEndedInEarlySurrender` alone (a real game can end in a surrender) |
| 2026-09-08 | The rating fold runs exactly once per game because the update that writes `mu_before/sigma_before/mu_after/sigma_after` carries `where game_id = ? and mu_after is null`; zero rows affected means it was already rated (M2.5) | Two companions post the same game and both requests reach the fold. Postgres row locks make the null column a claim, so the guarantee needs no new column, no advisory lock and no transaction spanning the whole request. | A `games.rated_at` column (a migration for something the existing nullable columns already say); an application-level mutex (does not survive two serverless instances) |
| 2026-09-08 | The 2-hour idle sweep moves `open` and `balanced` lobbies to `abandoned`; `in_game` is never swept and stays `in_game` for good (M2.5) | `abandoned` keeps the M2.9 replace semantics, so ageing an `in_game` lobby out would unfreeze the record of who played. A game the server dropped leaves a lobby that M5.5 lists as "reached in_game, never finished", which is the honest description of it. Corrects the second half of point 6 of the M2.10 brief. | Sweeping `in_game` too (loses the frozen roster); adding a sixth status for a dropped game (a vocabulary word for a thing that happens twice a year) |
| 2026-09-08 | "Tonight", for the sit-out rotation, is the calendar day containing the balance in the timezone named by `CUSTOMS_NIGHT_TZ` (an IANA name, default `UTC`), counted over `games.started_at` (M2.5) | "Fewest games tonight" needs a boundary and a server in UTC is not where the group lives. One environment variable keeps the definition in configuration instead of in code. | A hardcoded UTC day (splits a night that runs past midnight for most of the world); a rolling 12-hour window (nobody can predict who sits); a 06:00-to-06:00 night (better behaved, but it is a second concept to explain and the calendar day is what the docs already say) |
| 2026-09-08 | When more than ten non-spectators are in a lobby, the ones who sit are ordered by most games tonight, then least recent sit-out, then puuid; and a "sit-out" is derived, not stored: a `lobby_members` row of a lobby that reached `in_game` or `finished` with no `game_players` row for that lobby's game (M2.5) | The nightly loop promises "who sits next game based on who sat last" and the data to answer it already exists in two tables. A derived answer cannot drift from the games it is derived from, and it needs no column, no table and no companion change. | A `sit_outs` table (a row nobody writes when the companion is off); random selection (the one thing a referee may never do); ordering by fewest games (would sit the person who has played least) |
| 2026-09-08 | The companion lobby response gains `ranksNeeded: string[]` — the puuids in the posted lobby whose `rank_updated_at` is null or older than 7 days — and that is the whole definition of "an unknown player" for M2.4 | "Once, then weekly" is a staleness rule about our data, so it belongs where our data is. The companion keeps only an in-process de-duplicator; a puuid drops off the list the moment its rank POST lands, so the schedule needs no clock on the client at all. | A local cache file in the companion (ten friends running ten different staleness rules, and a fix means shipping a new exe); asking on every lobby event (hammers the client) |
| 2026-09-08 | `companionRankPayloadSchema` gains optional `gameName` and `tagLine`, so one POST carries a player's rank and their name (M2.4, flagged into the M2.10 contract pass) | Lobby members carry no Riot ID on 16.17, so a name needs its own lookup; the rank sweep already visits exactly the PUUIDs whose name we are missing. `ingestRank` hands them to `ensurePlayers`, which already refreshes `display_name` and already refuses to overwrite an admin's override (M1.7). | A fourth companion route just for names; putting names only on the lobby payload (leaves a player whose name changed while they were not in a lobby with a stale name forever) |
| 2026-09-08 | The companion posts a rank or a name only for its own PUUID or for a PUUID the server listed in `ranksNeeded`; `/lol-ranked/v1/cached-ranked-stats/{puuid}` WebSocket events are used only to satisfy a puuid already on that list (M2.4) | The firehose pushes ranked stats for the whole friends list unasked. This database is a record of one group's customs, not a scrape of somebody's friends, and the narrow rule is also the one that is easy to check in review. | Storing every cached-ranked-stats event that arrives (free data, but data about people who never agreed to be in here) |
| 2026-09-08 | The lobby watcher reads only `/lol-lobby/v2/lobby`; `/lol-lobby/v2/lobby/members` is dropped at the URI filter, and a lobby `Delete` posts nothing (M2.2) | The `members` event carries no `gameConfig`, so it cannot say which side anyone is on, and it fires 1-2 ms after the full event that can. The `Delete` fires 30-100 ms after `GameStart`: posting an empty roster there would wipe the ten people the game post is about to need. A lobby that truly dissolved is caught by the 2-hour idle rule instead. | Treating the members event as a second source of truth (two shapes to keep in step); posting an empty roster on `Delete` (deletes tonight's roster at the exact moment it starts mattering) |
| 2026-09-08 | `lobbies.lobby_password` stays null until M4.1: the 16.17 lobby body carries no custom-game password (M2.2) | Checked against all three lobby fixtures — the only `*assword` key is `multiUserChatPassword`, a chat credential that is scrubbed and never sent. The companion can only know the password when it is the one that set it, which is M4.1's lobby creation. | Reading `gameflow-session.gameData.password` (empty on 16.17, and the reference says never persist it); asking a friend to type the password somewhere (a typed step in a zero-input product) |
| 2026-09-08 | A `lobbies` row is one game cycle, not one party: migration `0003` replaces the plain unique on `lobbies.lcu_party_id` with a partial unique index `where status in ('open','balanced','in_game')`, and a lobby post whose latest row for that party is `finished` or `abandoned` starts a new row with the same party id (M2.14, sequenced right after M2.10) | The client keeps one party id for the whole night — fixture evidence: party `e3c69392` was created at 16:36:41, played 16:37:39 to 16:53:05, and was still emitting lobby events at 17:39:27 with no new id. With a plain unique key and the M2.9 freeze, the first game of the night closes the row and every later game gets silence. Nothing is rewritten or deleted, so this is a migration rather than a judgement call. | Recycling the finished row back to `open` (destroys the frozen record of who played game one, which M2.7 and M5.5 both read); keying lobbies on party id plus a sequence number the companion invents (a second identity for the same thing); leaving it and telling the group to remake the lobby between games (a typed step, in a zero-input product) |
| 2026-09-08 | Supersedes the calendar-day row above: a night runs **06:00 to 06:00** in the timezone named by `CUSTOMS_NIGHT_TZ`, an IANA name defaulting to `Africa/Cairo` and overridable per deployment (M2.5) | The group plays late; a midnight boundary cuts one session into two nights and resets "games tonight" while people are still in the lobby, which is exactly when the sit-out rotation is being read. 06:00 is an hour nobody is playing. The default names where the group actually is instead of pretending a server's UTC is a place. | Midnight to midnight (the earlier row); a rolling 12-hour window (nobody can predict who sits); hardcoding the zone (a second group would have to change code) |
| 2026-09-08 | Supersedes the sit-out row above on one point: the candidate pool is **everyone around, spectators included** — every non-bot lobby member — and the lobby's stability key is `rosterKey()` over all of them, not only the non-spectators. When the chosen ten include someone in the spectator slot, the teams post pairs each sitter with the spectator taking their slot and asks them to swap (M2.5, copy in M2.15) | A custom lobby caps each side at five, so the eleventh friend has nowhere to stand but the spectator slot. Treating that as "volunteered to sit forever" means one person watches every game and the referee never notices — the opposite of the rotation step 6 of the nightly loop promises. `01-architecture.md` already counts lobby spectators as "around". The ordering rule itself is unchanged. | Balancing only the non-spectators (leaves the rotation dead on an ordinary eleven-person night); having the companion move people into seats (M4.3 switches only the local player's side, and lobby seating is not something to automate for other people); asking the group to sort seating out themselves before the bot will speak |
| 2026-09-08 | The companion wire contract is `packages/db/src/schemas/companion.ts` and the raw-client-to-payload mapping is specified, as commented code, in `packages/db/src/schemas/companion.contract.test.ts`, which reads `packages/lcu/fixtures/16.17/*.json` from disk rather than importing `@customs/lcu` (M2.10) | The mapper itself lives in `packages/lcu` (it is client-shape knowledge) and `db` must not depend on `lcu` — the dependency runs `lcu -> db`. Reading the fixture JSON keeps the specification fixture-backed without inverting the graph, and puts the field-by-field rules next to the schema they constrain, where the API half can see them. | Importing the mapper into the db test (a dependency cycle); a copy of the mapping in `apps/companion` (two truths); prose in the reference doc only (nothing fails when it drifts) |
| 2026-09-08 | `members[].summonerId` is accepted as a JSON number **or** a digit string and normalised to a decimal string; `0`, `""` and absent all normalise to `null`. No migration: `players.summoner_id` stays `text` | The client sends a number (`47890856`) and it is **not** 32-bit — an invitee's read `2686822975473024`, above 2^51 and still a safe JS integer — so a bigint column would be the only alternative and text already holds it exactly. `0` is what a bot slot reports, and storing it would make every bot the same summoner. Rejecting non-digits keeps a Riot-ID-shaped value out of the column. | `z.number().int().safe()` only (the transport may stringify; some clients do); a `bigint` column (migration 0003 for no gain, and JSON numbers above 2^53 would already be lost before Postgres sees them); keeping `optionalText`, which refuses a number and would fail the whole roster |
| 2026-09-08 | A lobby member's `side` is derived only from `gameConfig.customTeam100`/`customTeam200`; `side: null` plus `isSpectator: true` is a spectator, `side: null` alone is "not placed yet", and neither is an error | `members[].teamId` is always `0` in a custom lobby (question 3), and the 16.17 captures show every human in `members[]` is in exactly one of `customTeam100`/`customTeam200`/`customSpectators` **in the same payload they first appear in**, so a side is always derivable from one event and never needs a second. `invitations[]` is not a roster: a `Pending` invitee is nobody yet and is not posted. | Reading `teamId` (always 0: everyone would land on one team); waiting for a later event to place a member (a lobby post that never happens); treating an unplaced member as a 400 (a valid client state) |
| 2026-09-08 | `puuidSchema` refuses the empty string and the all-zero PUUID; the lobby payload **drops** such members before validation (counting them in `droppedMembers`, which the route logs) instead of refusing the roster, and the eog payload refuses them outright | PUUID is the identity: one `players` row keyed on `00000000-0000-0000-0000-000000000000` would be every bot in every game, forever. But a bot leaking through an old companion binary must never cost the group the other nine members, so the roster is filtered and the night goes on; a scoreboard, by contrast, is posted once by a mapper that already filtered `botPlayer`, so a leak there is a bug worth a 400. | 400 on any bot (one stale companion silences the whole lobby); accepting the placeholder and de-duplicating later (the one bad row this product cannot tolerate) |
| 2026-09-08 | The end-of-game payload's `winningSide` is required and nullable; `null` parses and the route answers **422 `no winning team; remake or terminated`**, writing nothing. `startedAt` is required and mapper-derived (the observed `InProgress` moment, else `endOfGameTimestamp - gameLength * 1000`) | The real terminated block in the 16.17 capture (game 4000965483, `WaitingForStats -> TerminatedInError`) has one team, `isWinningTeam: false`, and no other signal, so "nobody won" must be expressible rather than a forgotten field; refusing it at the route keeps remakes out of `games` and out of the rating fold. The block has no start time at all, and M5.2 folds games in `started_at` order, so a derived value is mandatory rather than optional. | An `abandoned`/`terminated` game row (a status nobody asked for); moving the lobby anywhere on a refusal — it stays `in_game`, because M2.5's 2-hour sweep covers `open` and `balanced` only and a frozen roster must stay frozen, so M5.5 is the surface that lists a lobby whose game was dropped; defaulting `startedAt` to the ingest clock (reorders history for a companion that posts a day late) |
| 2026-09-08 | `games.raw` is scrubbed by `scrubRawEogBlock` (`mucJwtDto`, `multiUserChatPassword` -> `"[redacted]"`, any depth, any casing) in **both** the game route and `ingestEogGame`, and the function is idempotent | `games` is public-read under RLS and the block carries live post-game chat credentials, so this is a leak and not hygiene. Two call sites because the route is today's only writer and `ingestEogGame` is tomorrow's (M5.1 backfill), and a second pass over an already-scrubbed block costs one object copy. | Scrubbing only in the companion (old binaries run in people's trays for months); scrubbing only in the route (backfill would write the block raw); dropping the keys instead of redacting them (a scrubbed block should still parse as the block it was) |
| 2026-09-08 | The rank payload normalises `tier` `""`/`NONE`/`UNRANKED` and `division` `""`/`NA`/`NONE` to `null`, a null tier forces a null division **and** a null `lp`, and neither `wins` nor `losses` exists in the payload | 16.17 reports unranked as `tier: ""`, `division: "NA"`, which would otherwise print "NA" as a division on the player page, and `losses` reads `0` for every player but the local one, so it is not truth. Wins and losses come from our own `games` rows. | Carrying `losses` for the local player only (two shapes for one endpoint); storing `"NA"` and hiding it in the UI (every future reader has to know) |
| 2026-09-08 | Companion **response** schemas live in `packages/db/src/schemas/companionResponses.ts` beside the request schemas (the route-local `schema.ts` files are gone), and `GET /api/companion/me` returns `{ ok: true, puuid, playerId, displayName }` | The companion parses what the route validated, from one definition, so a response it cannot read is a compile error rather than a 3 a.m. surprise in a friend's tray. `me` exists so a mistyped token is a sentence on the friend's screen on first run instead of a silent 401 on the first lobby of the night (M2.1); it is the only companion route with no body, hence `withCompanionIdentity` beside `withCompanionAuth`. | Route-local response schemas (the companion cannot import them); no `me` route (token errors surface as a failed lobby post, which looks like the client being down) |
| 2026-09-08 | `recheckInMs` ships in the lobby response now (M2.10) and is hard-coded `null` until M2.5 measures the stability window; `ranksNeeded` ships filled, with the 7-day rule as `RANK_STALE_MS` in `apps/web/lib/ingest/lobby.ts` and computed from the members just posted, spectators included, even when the roster is frozen | A response field that appears later is a companion release; a field that is always there and sometimes null is a comment. M2.2 can be written and tested against the final shape today. `ranksNeeded` is one select over at most twenty rows, so there was no reason to defer it, and a frozen roster (M2.9) says nothing about whose rank we are missing — those people are about to play the next game. | Adding both fields with M2.5 (M2.2 would ship twice); making them optional (every reader needs a fallback and forgetting one is silent); excluding spectators (the friend sitting out this round is the one whose rank we most often lack) |
| 2026-09-08 | The companion never posts an end-of-game block with no winning team and never queues one: a remake or `TerminatedInError` block is dropped at capture with one log line naming the `gameId`, and a queued file that comes back 422 `no winning team` is deleted rather than retried (M2.3) | The block is real and complete — game `4000965483` in the 16.17 capture is a `CUSTOM_GAME` block whose only team has `isWinningTeam: false` — so the choice is ours to make, and posting something the API is contractually required to refuse forever is not a choice. One rule, at the earliest point: no winner, no file, no POST. | Posting it and letting the 422 be the record (a permanent refusal on every retry, and a queue file whose only exit is a rule that already exists); inventing a lobby status for it (M2.10 point 6 forbids it); posting it as an `abandoned` marker (the server has an idle sweep for that) |
| 2026-09-08 | The mapped, scrubbed end-of-game payload is written to `<configDir>/queue/<gameId>.json` (atomic rename, owner-only, capped at 50 files with the oldest dropped) **before** the first POST attempt, replayed on every start, and deleted on any 2xx or on a permanent 4xx (422, 403, 400, 404) (M2.3) | The client keeps the block only while the score screen is up, so an in-memory retry loses the game to a crash, a closed lid, or an API that is down for the two minutes that matter — and by the time the companion is back the block is gone for good. This widens `01-architecture.md`'s "Nothing else is written to disk": that line needs `queue/` beside `logs/`. | Retrying in memory only (loses the game the one time it matters); polling the client's GET later (404 forever once someone clicks through); leaving it all to backfill (M5, a day late, and only while it is still in somebody's match history) |
| 2026-09-08 | The packaged companion carries the deployed API origin as a build-time constant (esbuild `--define`, falling back to `http://localhost:3000` when absent), and a first run whose built-in origin answers `GET /api/health` skips the address question entirely and asks only for the token (M2.6) | The friend-facing promise is download, paste token, leave it running. `DEFAULT_API_BASE` is a dev localhost today, so without this every friend is asked to type a URL — one more question, and one typo per friend, on the single step we get from them. A `config.json` that names its own `apiBase` still wins, so a second deployment needs no new exe. | Shipping localhost and telling friends to type the URL (a typo per friend); an environment variable (nothing sets one for a double-clicked exe); a config file shipped next to the exe (a second file to lose) |
| 2026-09-08 | The API deployed on Vercel is a precondition for the M2 acceptance night, not an M3 task | Friends' companions have to reach the API from their own PCs; a dev server on the organiser's laptop is not an address anyone else can use, and M2.6 has to bake a real origin into the exe before it is handed out. | Running the API on the organiser's PC behind a tunnel for one night (a throwaway URL baked into every friend's exe); postponing the test night to M3 (M2's acceptance would then never have been run) |
| 2026-09-08 | The 2-hour abandon sweep keeps its opportunistic run at the start of every companion post **and** gains `GET /api/cron/sweep`, a bearer-`CRON_SECRET` route running the same single statement (M2.5) | The opportunistic sweep only fires while somebody is posting, so a night that ends with everyone closing their companion leaves a lobby `open` until the next night's first post. One route, one secret, no second copy of the rule, and `CRON_SECRET` on Vercel now has a reader. | Only the opportunistic sweep (a stale lobby survives until someone plays again); pg_cron in Supabase (the state machine moves into SQL); an unauthenticated route (anyone could hammer it) |
| 2026-09-08 | The packaged exe is published to a **public Supabase Storage bucket `releases`** on the hosted project — one object per version plus a `latest` copy, linked from `/admin` and later the tonight page (M2.6) | The download has to be one click for a friend with no GitHub account, and a release asset on a private repo is a login; making the repo public to avoid that is a bigger change than hosting one binary. The project already has Supabase, public read is a bucket setting, and a versioned object keeps the old build reachable when a new one misbehaves. | A GitHub release asset (a login per friend, or a public repo); serving the exe from a Next.js route on Vercel (a 100 MB response through a function); a personal file-share link (dies, and nobody can re-issue it) |
| 2026-09-08 | The companion calls `GET /api/companion/me` on every start and right after the first-run prompt, printing `signed in as <displayName>` or one plain sentence pointing at the admin page on 401, and never blocking on it (folded into M2.3) | `main.ts` pings `/api/health` with no token, so a mistyped or revoked token is invisible until a night goes silent. The route was built for exactly this and had no caller. Non-blocking because a token refused mid-season must not stop the queue from draining. | Exiting on 401 (the queue still has games to deliver); checking only on first run (a revoked token is the common case); inferring it from the first lobby POST's 401 (nobody is reading the console by then) |
| 2026-09-08 | The lobby watcher posts only lobbies with `gameConfig.isCustom: true`; a normal-queue lobby is logged once per party and never posted (M2.2) | The product is customs only, and every ARAM or ranked lobby a friend opens would otherwise become a `lobbies` row on a public-read table that only the 2-hour sweep cleans up. `isCustom` is a required, verified field of `LobbySchema`, so the filter costs one line and no extra read. | Posting every lobby and filtering on the server (a junk row per queue lobby, and a second place to know what a custom is); reading `queueId` (3100/3110/3270 are the customs we know of, and a new one would be dropped) |
| 2026-09-08 | `mapEog` in `packages/lcu` scrubs `mucJwtDto` and `multiUserChatPassword` out of `raw` with `scrubValue` before it returns; the server scrubs again (M2.10, point 11) | The payload leaves the PC the moment it is built, and M2.3 writes it to disk before posting. Scrubbing in the one mapper means neither the queue file nor the wire ever carries the credential, and `scrubRawEogBlock` on the server is idempotent so the double pass is free. | Scrubbing only in M2.3's capture path (a second caller of `mapEog` could forget); trusting the server alone (old binaries keep running for months) |
| 2026-09-08 | The connect-time `GET /lol-lobby/v2/lobby` in the lobby watcher is not awaited by `onConnected`, and its result is dropped if a lobby event arrived while it was in flight (M2.2) | The machine waits for `onConnected` before opening the socket, so a slow client answer would delay the subscription; and a WebSocket event that lands first is newer than the GET by definition, so posting the GET after it would reorder the roster. | Awaiting the GET (delays the socket by up to the request timeout); posting both in arrival order (the older one can land last) |
| 2026-09-08 | A lobby post that fails on a network error or 5xx after `ApiClient`'s own attempts is re-tried by the watcher with its own backoff (1 s to 60 s) for as long as it is still the newest payload; a newer event cancels it, and a 4xx other than 403 or a body that fails the response schema is logged and dropped (M2.2) | The brief says a failed post is retried with the existing backoff only while it is the newest; `ApiClient` gives up after four attempts, which is a couple of minutes, and an API that is down for the whole join window would otherwise lose the roster until the next event. A 4xx is our bug and re-sending it cannot help. | Retrying forever inside `ApiClient` (blocks the coalescing slot for every caller); queueing lobby posts to disk like the eog block (a stale roster is worse than none) |
| 2026-09-08 | Name lookups in the lobby watcher: one at a time, at most one per 200 ms, once per puuid per process; a lookup that could not start because the client went away is re-armed for the next connection, and the names re-post is skipped when it would be byte-identical to the last payload (M2.2) | The brief fixes the rate and the once-per-process rule; the two refinements keep the guarantees honest: a disconnect before the GET was ever sent is not a failed lookup, and a re-post that changes nothing would only add a request. | A second attempt after a failed lookup (the brief forbids it); a cache file of names (M2.4's sweep is the durable route) |
| 2026-09-08 | The queued end-of-game file is posted with **one** API attempt per pass and retried on the game watcher's own backoff (30 s to 15 min, jittered) for as long as the process runs; it is deleted on any 2xx and on 400/403/404/422, and **kept on a 401** as on a network error, 5xx, 408 or 429 (M2.3) | `ApiClient`'s inner retries would hold the serial queue for a minute per file (the M2.2 lesson), and the file on disk is the retry that matters. The brief names 400/403/404/422 as permanent and says keep anything else; a 401 means the token was revoked or replaced, and a game held until the friend pastes a new token is worth more than a clean directory — the other companions in the game land it meanwhile, and dedupe makes the late copy a no-op. | Deleting on 401 (loses the game for that companion for good); using the client's 4 inner attempts before the outer loop (holds the queue) |
| 2026-09-08 | The `in_progress` post is skipped when the gameflow session says `isCustomGame: false`, and a session whose own `phase` is `Lobby` or `None` at the moment of the read is treated as stale and not posted (M2.3) | `GameStart` fires for a friend's ranked game too; posting its id with the last custom `partyId` this process saw could move a real custom lobby to `in_game` after M2.5. The phase guard is belt and braces on the brief's "never read the id in any other phase": the read is only ever triggered by a `GameStart`/`InProgress` event, and the 16.17 recording shows the session's `phase` moving with the event, so the guard costs nothing on the happy path and refuses `gameflow-session--in-lobby.json` outright. | Posting every game's id and letting the server sort it out |
| 2026-09-08 | The game watcher holds `partyId` only from **custom** lobby events (`gameConfig.isCustom`), captured at `GameStart`; an end-of-game block for a game whose `GameStart` this process did not see goes out with `partyId: null` and the derived `startedAt`, never with a guessed party (M2.3) | The lobby watcher never posts a non-custom lobby, so a party id from one is unknown to the server anyway; and guessing the last party for a game whose start was missed could attach a game to the wrong cycle after M2.14. `partyId: null` is a state the contract already accepts (`games.lobby_id` null, still rated). | Using the last party id seen for any block with no held start |
| 2026-09-08 | The rank sync takes `ranksNeeded` through an `onResponse` callback on the lobby watcher, keeps a one-hour in-memory "asked" guard that is set on every attempt (success or failure), posts a rank with at most two API attempts, and re-posts the own rank on a reconnect only when the 6-hour timer fired while disconnected (M2.4) | A callback keeps the two watchers independent (`main.ts` wires them; neither imports the other). Marking a puuid asked on failure is what makes "logged once per puuid per hour, left for the next lobby response" one rule instead of two. Two attempts is one quick retry without a storm: a rank that fails is re-listed by the server within the week. A flaky client must not re-post the own rank on every reconnect; a timer that fired in the gap is honoured on the next connection. | Polling `watcher.ranksNeeded`; the client's default four attempts; re-posting the own rank on every connect |
| 2026-09-08 | An end-of-game post resolves its party id to the **newest `lobbies` row that already existed when the game started** (`created_at <= startedAt`), falling back to the party's live row and then to its newest row of any status; a lobby post only ever resolves to the live row (M2.14) | With one row per game cycle, a party id no longer names one lobby, and a block can arrive minutes late — after the group has opened the night's next lobby with the same id. `started_at` is already on the payload and is the only thing that says which cycle the game belongs to, so game one's result stays on game one's lobby instead of attaching itself to game two. The fallbacks keep a companion with a skewed clock on the behaviour the approach note describes (live row, then newest): a companion whose clock runs behind Postgres reports a `startedAt` older than every row for its party, no row matches the containment test, and the resolution falls back to the live row — the same answer it would have given without `startedAt` at all, so the edge costs nothing and never picks an older cycle by accident. A row that resolves to `abandoned` is treated as no lobby at all: the two-hour sweep gave up on it, so the game is stored with `lobby_id: null`, still rated, rather than claiming the group played a lobby that dissolved. | Always taking the live row (a late block lands on the next game's lobby, and M5.5 then reports a lobby that reached `in_game` and never finished); always taking the newest row (same, one step worse); a `lobby_id` on the wire (the companion would have to know our primary keys) |
| 2026-09-08 | The two-hour idle sweep is triggered **opportunistically at the start of every companion lobby and game post** (one cheap conditional `update`), plus `GET /api/cron/sweep` behind a bearer `CRON_SECRET` for a scheduler; with `CRON_SECRET` unset that route answers 503 rather than running unauthenticated (M2.5) | Vercel Hobby cron runs once a day, which is not a two-hour rule, and a lobby that matters is one somebody is posting about — the next post sweeps it. The endpoint exists for the other half: everybody closed the client, nothing will post again tonight, and the row should still stop looking live on the tonight page. Closed-by-default keeps a route that writes rows from being open because a deployment forgot a variable. | pg_cron in Supabase (the state machine moves into SQL, away from the one place that owns it); a daily Vercel cron alone (a two-hour rule evaluated every 24 h); sweeping from a read on the tonight page (a public GET that writes); no sweep at all until somebody posts (a lobby that reads `open` for a week) |
| 2026-09-08 | `POST /api/companion/game` accepts an end-of-game block when the token's player is among the participants **or** is a `lobby_members` row of the lobby that game was played from, `is_spectator` included; anyone else still gets 403 (M2.8) | M2.13 confirmed on 16.17 that a spectator stays in the client's `members[]` with `isSpectator: true`, so the lobby half of this rule has something to match. The friend who sits out a round is often the one running the companion, and under the participants-only rule their post was refused and the game was lost until backfill. The lobby is resolved the same way ingest resolves it, so the check and the write agree about which cycle the game belongs to. | Participants only (the spectator's night is silently dropped); trusting any valid token (a companion could post a game it was nowhere near); an admin allow-list (a typed step, in a zero-input product) |
| 2026-09-08 | `lobby_members` and `players` are written on **every** accepted lobby post — sides, the spectator flag and Riot IDs included — while only a change to the roster's *identity* writes the `lobbies` row and so restarts the ten-second clock (M2.5) | The clock is `lobbies.updated_at` and nothing else touches it, so keeping the member rows current costs no stability: a friend swapping to red or into the spectator slot lands in the table the seat plan reads, and a Riot ID that changed still follows M1.7's rule, neither of which is a reason to make ten people wait another ten seconds. **Known window (accepted, M2.5 review):** the member rows are written before the `lobbies` row that restarts the clock, so for one round trip a concurrent post can see the new roster against the old `updated_at` and balance the new ten without the full ten seconds. It needs two companions posting a changed roster in the same few milliseconds, it produces a valid split of the people who are actually there, and the next real change restarts the clock properly; reordering it would need the write and the member replace in one statement, which PostgREST cannot do. | Skipping the member write when the identity is unchanged (a rename never lands and the seat plan goes stale); treating a side swap as a roster change (the clock restarts every time somebody fidgets) |
| 2026-09-08 | Ingest announces `balanced` and `finished` through a hook list in `apps/web/lib/ingest/hooks.ts` (`registerLobbyHook({ onBalanced, onFinished })`), carrying the chosen split, core's explanation string, the sitters and the seat pairing; a hook that throws is logged and the request goes on (M2.5, the seam M3.1 and M3.3 fill) | Every M2.5 acceptance check has to pass with the webhook switched off, so posting to Discord cannot be inline in the state machine. Handing the event everything the embeds need means M3.1 renders rather than re-derives, and M2.15's copy has one source for who sits and who swaps. | Calling the webhook from the route (a Discord outage costs the group its teams); a `discord` module imported by ingest (the dependency runs the wrong way); Realtime only (the bot is M4 and the webhook is M3) |
| 2026-09-08 | A player with no `display_name` and no `game_name` balances as `"Unknown"` in the explanation string, as a placeholder until M3.10 decides the fallback for every surface (M2.5) | Core's `BalancePlayer.name` is a required string and the explanation is built from it, so the balancer needs *something* the first night a friend appears. A PUUID fragment in a Discord sentence is worse than a word, and M3.10 is the task that picks the real answer for the embed, the tonight page and the leaderboard at once. | A puuid fragment (a Discord line nobody can read); refusing to balance without a name (ten people wait for a name lookup the lobby payload cannot do); the summoner id (not a name either) |
| 2026-09-08 | A first-time friend is told to **join one of our custom lobbies before asking for a token**, in step 2 of the companion README (M2.6); `/admin/tokens` keeps minting against an existing player only | `players` rows appear lazily from the first lobby, game or rank post that names a PUUID, and there is no "add player" anywhere in `/admin` — so until somebody's companion has seen a friend in a lobby, there is nobody to mint a token *for*. The cheapest fix is one sentence in the copy the friend is already reading, not a new admin surface: the friend's first custom is a step they were taking anyway, and PUUID stays the identity nobody has to type. The M2 test-night sheet (`docs/06-test-night.md`) makes it precondition 4 for the same reason. | Minting by raw PUUID (a friend does not know their PUUID, and the person minting would have to ask them for a string neither of them can read back); an "add player" form keyed on Riot ID (renamable, and it would be the one place in the product where identity is typed); auto-minting a token per Discord member (a token for everyone who never installs anything, and a secret handed out before it is needed) |
| 2026-09-09 | **Supersedes the `releases` bucket row above.** The packaged exe ships as a **GitHub release asset on the public repo `suyaser/kustom-releases`** — one tag `v<version>` per build carrying `CustomsNight.exe` and `README.txt`, with the group-chat link being the stable `https://github.com/suyaser/kustom-releases/releases/latest/download/CustomsNight.exe` (M2.6). The code repo stays private; the Supabase `releases` bucket keeps `README.txt` only, until someone removes it | The bucket decision assumed the binary would fit: the project's Supabase plan caps a Storage object at **50 MB** and the exe is **90 MB**, so the chosen host cannot hold the thing it was chosen for. A separate public repo that contains no code answers the objection that killed GitHub the first time — a friend downloads the asset with no account and no login, and the source stays private. GitHub's `/releases/latest/download/` path is a stable link that follows the newest tag, so the group chat keeps one URL forever, and old tags stay reachable when a new build misbehaves. | Splitting the exe across two bucket objects (an unpack step, and a step is a step); paying for a Storage tier to host one file a month; making the code repo public (a bigger change than moving one binary); serving it through a Vercel route (a 90 MB response through a function) |
| 2026-09-08 | The webhook post is one `fetch` with a **5 s timeout and at most two attempts**: one retry on a network error, a timeout or a 5xx, one wait-and-retry on a 429 (`retry_after` from the body, then the `retry-after` header, capped at 5 s), and no retry at all on any other 4xx. It never throws — every caller is a lobby hook — and it never logs the URL (M3.1) | M3.1 says "log one line and no retry loop": the tonight page is the other surface and must not depend on Discord. One retry covers the only failure worth covering (a dropped connection or a Discord blip); anything longer and the teams post lands after the game has started, which is worse than not landing. | A retry queue or a job table (a night's teams are worthless ten minutes late); no retry at all (a single dropped socket would silently cost the group the post); retrying a 401/404 (a revoked webhook stays revoked) |
| 2026-09-08 | The webhook URL is read from **the oldest `discord_config` row that has one**, and a process with no configured webhook logs one line and skips every post after that | The table is keyed by guild and this group has one guild; picking the oldest means a leftover row written later by a second guild cannot silently take over the posting. `/admin/discord` already says when there is more than one row. "No webhook" is a configuration state, not an incident, so it is one line per process rather than one per lobby. | Newest row (a stale second config would hijack the posts); a `WEBHOOK_URL` environment variable (the admin page already owns this value, and two sources of truth for a secret is one too many) |
| 2026-09-08 | A player with no `display_name` and no `game_name` renders as **`Someone`** in the Discord embeds, computed in `renderName()` at the display boundary and never written to any row (M3.10's fallback, first used by M3.1/M3.3). Display names are truncated at 32 characters with `…` at the same boundary | M3.10 needs one word on every surface and this is the first surface to need it. Rendering it means the moment a name arrives — the M2.4 rank sweep, the first end-of-game block — every later post says the real name with no backfill and no repair job. The `"Unknown"` in a *stored* explanation string is a different thing and is left exactly as core wrote it: a stored sentence is a record of what was said. | Writing `"Someone"` into `players.display_name` (a lie in the database that a rename would have to undo); leaving the line blank (a five-line field with a hole reads as a bug); rewriting the stored explanation at render time (it is quoted verbatim by three surfaces and must not be edited by any of them) |
| 2026-09-08 | The teams embed's `Lobby` field shows `` `name` · password `1234` `` when both are known, `` `name` `` alone when the client reported no password (which is every lobby before M4.1), and **is absent entirely** when neither is known | M3.1's acceptance check 5: absent, not empty and not the word `unknown`. A field that says `password unknown` teaches the group to distrust the field; a field that is simply not there says nothing and is never wrong. | `password: —` or `unknown` (noise on every post for months); keeping the field with just a label (an empty Discord field renders as a stray bold word) |
| 2026-09-08 | The Discord embed's `url` is the origin of the request that triggered the transition (`siteOrigin(request)`, which already prefers `NEXT_PUBLIC_SITE_URL`), threaded to the hook as `requestOrigin`, and **dropped entirely when it is a localhost host**. The companion routes set it; ingest only passes it along. **With `NEXT_PUBLIC_SITE_URL` unset the request's own host is trusted** — a companion that sets `x-forwarded-host` decides what the embed links to — which is acceptable only because the link is cosmetic and the deployment that matters (Vercel) pins the variable; a self-hosted or dev instance that cares should set it too | M3.1: no domain exists yet, never hardcode a host, and never post a `localhost` link to Discord. The request knows the origin and the hook does not, so the route is the only honest place to read it; dropping localhost in one function (`tonightPageUrl`) means a dev machine's post is missing a link rather than carrying a broken one. | A `SITE_URL` constant (a hardcoded host, which the brief forbids); posting the localhost link anyway (a link that works for exactly one person in the channel); making the embed `url` mandatory (it is optional in Discord and optional here) |
| 2026-09-08 | Discord is registered as a lobby hook by importing `apps/web/lib/ingest/discord.ts` for its side effect from the two companion route modules; that file is the only one in `lib/ingest` that names Discord | The M2.5 seam's promise is that every acceptance check passes with the webhook switched off, and the cheapest proof of that is a single import line whose removal switches it off. A route module is loaded once per process, so registration happens exactly once with no framework hook to configure. | Registering inside `lobby.ts` (ingest would import the poster, which is the coupling the seam exists to prevent); Next's `instrumentation.ts` (does not run in vitest, so the integration test would prove nothing about the real path); registering per request (a growing hook list) |
| 2026-09-08 | `isOffRole` is re-exported from `@customs/core`'s entry point so the teams embed can mark an off-role line with the same predicate the scorer and the explanation use | The alternative is a second copy of the role model in the web app, which is the one thing `packages/core` exists to prevent. It is a re-export of an already-tested pure function; no logic moved. | Recomputing `role !== mainRole` in the embed (silently wrong the day `roleOverride` or a null main matters); parsing the stored explanation string for names (fragile, and it is prose) |
| 2026-09-08 | The teams embed ships **M2.15's** sit-out copy as two independent fields — `Sitting out: {names} — most games tonight.` (or `— longest since they last sat out.` when everyone is level) and a `Seats` field of `Swap: {sitter} out, {mover} in.` / `{Name} is playing — take the open slot.` — which supersedes the earlier single-sentence field 3 sample in `05-design.md`. The web sit-out strip keeps its own longer sentence | Two questions, two fields: who is not playing, and who has to physically move. They are not the same list — ten around with one spectator means somebody moves and nobody sits — and one sentence cannot answer both. The design doc's sample predates M2.15 and was updated in the same session. | One combined sentence (says nothing about the swap, which is the only part anyone has to act on); reusing the web strip's paragraph in the embed (two lines of channel text should not read like a page) |
| 2026-09-08 | A rating change on any surface is `displayRating(muAfter) - displayRating(muBefore)` from **one shared helper**, `displayDelta` in `apps/web/lib/ratingDisplay.ts`. `packages/core` keeps `displayRating` and gains no delta concept (M3.3) | Product's rule: round both, then subtract, so `1469 (+43)` and a new rating of `1512` agree on the screen. One helper is what stops the result embed and the tonight page printing different numbers for the same game, and keeping it out of core keeps core about the model rather than about presentation. | `round((muAfter - muBefore) * 60)` (the row does not add up); a `delta` column on `game_players` (a derived number that a rebuild would have to maintain); a delta helper inside `packages/core` (a display concern in a package that has no display) |
| 2026-09-08 | The result embed posts **only for a game the fold rated**. A remake, a sub-five-minute surrender, a scoreboard that is not five a side, and the second companion's re-post of an already-rated game all produce no message (M3.3) | The whole embed is what the game did to ten ratings; with no ratings there are no lines to print. The game route already narrows its `onFinished` to the post that changed something, so "one game, one message" needs no dedupe of its own. | Posting a result with no deltas (a scoreboard nobody asked for, and it invites "why did my rating not move?"); posting on every eog block (ten companions, ten messages) |
| 2026-09-08 | The Windows exe is a **Node single-executable application** (blob from `node --experimental-sea-config`, injected with `postject` into a stock `node.exe`), not `pkg`; the Node release is pinned in `apps/companion/build/config.ts` (24.20.0 LTS) and the build downloads that exact release from nodejs.org for both the blob (a host copy) and the target (`win-x64/node.exe`), verified against `SHASUMS256.txt` (M2.6) | Least machinery that produced a working exe: the bundle is one esbuild pass, the blob is one Node flag, the injection is one npm package with no native code, and the runtime is Riot-irrelevant stock Node rather than a patched fork. The docs require the blob and the binary to be the same Node version, which is why the version is pinned and downloaded rather than taken from PATH (this Mac runs Node 25). Cross-building from macOS worked on the first attempt (`useCodeCache`/`useSnapshot` off); the same script's `--target host` builds a macOS binary that was run against the live client here as the pipeline check. `node.exe`'s Authenticode signature is stripped before injection (the docs' `signtool remove /s`, done by zeroing the PE security directory) so the result is unsigned rather than badly signed. | `pkg`/`@yao-pkg/pkg` (a patched Node from a third-party release feed, a virtual filesystem, and bytecode that cannot be generated for a cross target); building on the Windows PC (kept as the fallback host; the script runs there unchanged and uses the one `node.exe` for both sides) |
| 2026-09-08 | `LcuSocket` pings the client every 30 s and terminates the socket when no pong arrives within 10 s (`heartbeatMs`, `heartbeatTimeoutMs`; the close event then carries reason `heartbeat`), so a connection that is open but dead after a PC sleep or a killed client is a reconnect within 40 s instead of a silent night (M2.6) | The state machine already catches a lockfile that changes or disappears, but a socket that stays open and delivers nothing is invisible to it. The client answers WebSocket pings (16.17, 7 ms, payload echoed), so a standard ping/pong is the cheapest liveness check there is, and it costs nothing when the connection is healthy. | Re-reading a GET on a timer (a poll against the client every few seconds for the life of the process); trusting the OS to close the socket after sleep (loopback sockets often survive sleep on both ends) |
| 2026-09-08 | The console budget is enforced by level, not by a counter: connection transitions, the client library's own socket open/close, `local player` and `no lobby open at connect` are `debug` (file only); a friend sees the starting line, `api reachable`, `signed in as`, `waiting for`/`connected to the League client`, `watching`, and then only things that change (M2.6) | The brief's idle-hour cap is 20 lines; measured on the packaged build against the live client this is 6 to 8 lines to `watching` and 0 per idle hour, with the daily `debug` file keeping everything. | A rate limiter on the console sink (would drop lines that matter on a bad night); leaving the lines at info and raising the cap |
| 2026-09-08 | A double-clicked exe that fails at startup (unreadable config, an exception before the loop) waits for Enter before exiting when stdin and stdout are a TTY; the version is stamped from `apps/companion/package.json` at build time and reads `0.0.0-dev` under `tsx`; `--version`/`--help` exit before touching the config directory (M2.6) | On Windows a console process that exits takes its window with it, so the one sentence saying why is never read. The flags give the build a host-independent smoke test (`build/bundle.test.ts` runs the bundle under plain `node`) and give a friend a way to check which build they have. | Exiting silently (the window flashes and closes); a `--pause` flag nobody would know to pass |
| 2026-09-08 | The exe upload to `releases` is held until the project's upload limit is raised: the hosted project answers `413 EntityTooLarge` ("The object exceeded the maximum allowed size") to the 90.1 MB exe, and Supabase's global file size limit cannot exceed 50 MB on the Free plan (Pro: configurable, up to 500 GB). `latest/README.txt` is published; `pnpm --filter companion release` is ready to publish `latest/CustomsNight.exe` and `v<version>/CustomsNight.exe` the moment the limit allows it (M2.6, engineer) | A Node runtime is 93 MB before our 1 MB of code, and nothing in our control shrinks it below 50 MB (a UPX-packed `node.exe` under SEA is unverified and would be tested on a friend's PC first). The choice is the user's: raise the limit (Pro plan, Storage settings) and re-run `release`, or host the exe elsewhere and change the URL in the brief and README; until then the Windows verification uses the file copied from `apps/companion/dist/`. | Splitting the exe (an unpack step, ruled out by the brief); a `.zip` (same); serving it through Vercel (rejected earlier) |
| 2026-09-09 | The sign-in destination travels in a 10-minute HttpOnly `SameSite=Lax` cookie scoped to `/auth/callback` (`cn-auth-next`), and `redirect_to` is the bare `<siteOrigin>/auth/callback`; the callback re-validates the value as a path on this site and clears the cookie on every exit (M1.11) | Supabase compares `redirect_to` against the allow-list as **exact** URLs, so `?next=/admin` matched nothing and the whole round trip fell back to the project's Site URL — a hosted sign-in landed on somebody's localhost. A cookie is the only carrier that does not change the URL Supabase has to match, and it removes the query parameter an open-redirect check had to police. Lax, not Strict: the callback is a cross-site top-level navigation and Strict would drop the cookie exactly when it is needed. | Allow-listing one URL per destination (a Supabase dashboard edit for every new admin page); a `**` wildcard entry (a standing open-redirect allowance — the interim workaround this removes); encoding the destination in the OAuth `state` (Supabase owns that value); always landing on `/admin` (a link to a specific admin page could never survive sign-in) |
| 2026-09-09 | With no active season `POST /api/companion/game` answers **503** with the sentence "No season is active, so games cannot be saved. Start a season on the Seasons page.", checked before any write; the admin index and the seasons page print the same string from `apps/web/lib/season.ts` (M2.18) | `games.season_id` defaults to `active_season_id()`, so the night failed on a not-null constraint after the game, on a serverless function, with nothing but a Postgres message. 503 is deliberate: the companion deletes a queued game on 400/403/404/422 and keeps it on anything else, so a missing season is recovered by starting one and letting the queue drain rather than by backfill. One exported constant, because the value of the task is that the log line, the admin index and the seasons page say the identical sentence. | 422 or 409 (permanent to the companion: the queue file is deleted and the night is lost); letting the insert fail and mapping the Postgres error (a message about a column, and only after a partial write path); auto-starting a season (a season is a thing the group decides, `00-product.md`) |
| 2026-09-09 | The teams embed posts the `Sitting out` and `Seats` fields before `Blue` and `Red`, not after (designer, M3.1 review) | The web puts the sit-out strip above the team cards on the rule "if you are sitting out, everything under it is not about you"; in Discord ten rating lines plus a wrapped explanation is about one phone screen, and the swap line is the one line that must happen before anyone can play. Discord groups only consecutive inline fields, so a block field in front does not break the side pairing, and on a ten-person night the embed is unchanged. | Leaving them last (the instruction lands below the fold exactly when it exists); folding them into the description (core's explanation is verbatim, not ours to append to); a second message per balance |
| 2026-09-09 | Display names are escaped for Discord markdown (backtick, `*`, `_`, `~`, `\|`) inside `renderName`, applied last, after the 32-character truncation (designer, M3.1 review) | Riot IDs carry underscores and asterisks, and one stray backtick closes the role code span and swallows the rest of the field, mangling four other lines. Escaping after the cut means a backslash is never sliced from its character and the visible length stays 32. | Escaping before truncation (can split an escape pair); stripping the characters (a friend's name is not ours to edit); leaving it |
| 2026-09-09 | The result embed's coin-flip clause is `Neither side was favored.`, not core's `Even 50%.` (M3.11) | Core's fragment is present tense in a present-tense list before the game. In the result embed it sits under `Red wins · 34:12` next to a full past-tense sentence, where *even, 50%* reads as a scoreline before it reads as a prediction — and on a first night, every player unrated and every split gap 0, it is the first result sentence the group ever sees. The percent goes with it: 50 is what "neither" means. | Keeping `Even 50%.` word for word across both messages (the tense already differs in the 54% case); `It was even, 50%.` (still reads as the score); dropping the clause entirely (the bot does not get to quietly forget what it predicted) |
| 2026-09-09 | A third sit-out reason clause for the first balance of a night: `nobody has sat out before, so somebody had to be first.` (M3.12) | Tied on games and nobody with a sit-out on record, the comparator falls through to PUUID order, which is arbitrary. `— longest since they last sat out.` is true and vacuous there, and it sends the friend looking for a night they sat out that never happened. The clause says what actually happened in the words a friend would use. | Keeping the vacuous clause; saying "random" or "the bot drew a name" (it is deterministic — the same person is picked until somebody plays a game, and a friend told it was a draw asks for another draw); no clause at all (a sit-out with no reason is the argument this product exists to end) |
| 2026-09-09 | Reroll is a promotion by split id, refused unless the lobby is `balanced`, and its Discord message is a new post titled `Teams are set · reroll 1 of 2` / `· reroll 2 of 2` (M3.2) | Naming the target split makes a double tap on a slow phone a no-op instead of a skip to split 3, which a "next" endpoint could not. A new message rather than an edit matches M3.1's rule that two messages are the honest record, and the title tells someone scrolling up how far down the list the group went and that the second one is the last. | A `next` endpoint (double taps skip a split); editing the original message (hides that a reroll happened); a random reroll (no such thing exists — core returns a ranked three); allowing reroll during `in_game` (the teams are already on the rift) |
| 2026-09-09 | A role tap on a lobby that is already `balanced` or `in_game` is stored but never rebalances or reposts; the control says `Saved for the next game. Teams are already set.` (M3.6) | A rebalance on a role tap is an unlimited reroll that any one of ten people can pull, and by then people have moved to their side in the client. The referee does not reopen a decision because one player changed their mind. The two doors that do change teams tonight — reroll, and changing who is in the lobby — both already exist and both are named in the copy. | Rebalancing on every tap (unlimited reroll, and teams churn while people are moving); refusing the tap outright (the friend is told no for a choice that costs nothing next game); queueing a rebalance for when the lobby reopens (the same thing, with a delay nobody can see) |
| 2026-09-09 | A role override lasts the **night**, not the lobby row: a new cycle for the same party copies `role_override` forward when the previous cycle started after `nightStart`. Nothing is cleared on `finished`; a new night has nothing to copy (M3.6) | Since M2.14 a `lobbies` row is one game cycle, so "cleared when the lobby finishes" would make every friend re-tap between every game — four or five extra steps a night in a product whose whole claim is that nobody does anything. "For tonight" is what the task always said it meant. The closed row keeps its value as the record of what the teams were built from. | Clearing on `finished` as originally written (a tap per game); a night-scoped preferences table (a second source of truth for a value that already has a column); a permanent preference on `players` (that is the profile role `/admin` already sets) |
| 2026-09-09 | A third route class: a Supabase session matched to a `players` row by `discord_id`, with no `is_admin` requirement. First used by the role tap; an admin may act on anybody, a player only on their own row (M3.6) | Until now every write was either a companion token or an admin session, and the role tap is neither: it is a friend touching one column of their own `lobby_members` row. The identity path is the one this project already has (`discord_id` → player), so nothing new is trusted. A `puuid` in the body is honoured only for an admin; from anyone else it is a 403, never a silent write to their own row. | Making the role tap admin-only (one person doing ten people's typing is the manual step this product removes); an unauthenticated tap keyed on PUUID (anybody could set anybody's role); RLS write policies on `lobby_members` (every write in this project goes through a route handler that has already checked something) |
| 2026-09-09 | The tonight page follows the newest non-`abandoned` lobby row of the night (06:00 boundary) and ships no "last game" block: when the night's next cycle opens, the finished game's result is replaced by the member list (M3.4) | One row is one game cycle (M2.14), so a night has several. The page's job is "where are we now", which the newest row answers; the finished game already has the Discord result embed and the leaderboard. Adding a second block for history would be a component nobody has asked for on the one screen people read in a hurry. | Keeping the result up until the next game is balanced (the page then lies about a lobby that is filling); a persistent last-game strip (new component, and the design's one-primary-block rule exists to stop exactly this); a games history page (M5's, if anyone asks) |
| 2026-09-09 | A signed-in visitor with no linked player picks themselves out of tonight's lobby members once (`That's me`), which writes `players.discord_id`; only tonight's members are offered, a player already linked is refused (409), and an admin undoes a link on `/admin/players` (lead, M3.6) | `discord_id` is set nowhere but the admin page today, so every friend's first role tap would wait on somebody else doing a chore before the first night. The claim is safe because it can only name somebody who is in the lobby in the room with them, it can only happen once per player, and it is reversible in one click by an admin. | Admin-only linking (a chore before every first night, and the one person who has to do it is the one person already running the companion); linking by Riot ID lookup (needs the League client, and the client is the companion's, not the browser's); trusting the Discord display name to match a Riot ID (renamable, and wrong the first time two friends share a nickname) |
| 2026-09-09 | The third press is refused by rank, not by a counter: with the lobby's last split already chosen, a promotion of any split of rank above 1 answers 409 with `No more splits. Change who is in the lobby to rebalance, or play these.`, while split 1 stays promotable (M3.2) | The route is by split id and stores no reroll count, so "there is no third press" has to be a rule about the board, not about history. Split 1 is not a reroll — it is the teams the balancer chose, and the brief's acceptance check has an admin putting them back from split 3 under the plain title. Every other backwards promotion is a fourth message showing a split the group has already seen and rejected. | A per-lobby reroll counter (a column, and a second thing to reset when the lobby rebalances); refusing every promotion once split 3 is up (contradicts the brief's "POST rank 1 after that: it is promoted again"); allowing rank 3 to rank 2 (a third press by another name) |
| 2026-09-09 | The reroll title counts the splits the lobby actually stored (`reroll {rank - 1} of {splitCount - 1}`) rather than the literal `of 2` (M3.2) | Core returns three splits today, so the copy in `05-design.md` comes out byte for byte. The day a duo lock leaves a lobby with two, the title must not promise a reroll that does not exist — and the alternative is a message that says `of 2` beside a button the group cannot press. | A hardcoded `of 2` (lies the first time core returns fewer); leaving the count off the title (`of 2` is what tells the reader the second one is the last) |
| 2026-09-09 | A nameless player is `Someone` in the **stored** explanation too: `lib/ingest/balance.ts` hands core `NAMELESS_PLAYER`, the same constant `renderName` prints (M3.15). Supersedes the second half of the 2026-09-08 row above, which left `Unknown` in the stored string as "a record of what was said" | The stored sentence is quoted verbatim by the embed and the tonight page and may never be recomposed, so the word inside it is a display decision made early rather than a record of anything: one message could read `Someone` on a team line and `Next best: swap Unknown and Hana` in the sentence above it. Nothing is written to `players`; the name columns stay null for the sweep to fill in. | Rewriting the stored explanation at render time (three surfaces quote it and none may edit it); keeping `Unknown` (M3.10's "one agreed fallback on every surface" broken in the one string nothing may recompose); writing `Someone` into `players.display_name` (a lie in the database) |
| 2026-09-09 | The tonight page reads with the **anon key** on both sides through one loader (`lib/tonight/load.ts`): the server component renders the first paint with it and the browser re-runs the same function on every Realtime event, rather than applying the `postgres_changes` payload (M3.4) | Every state on the page is five joins wide — the newest lobby, its members, its promoted split, its game — and a payload is one row of one table, so applying events would be a second, thinner set of rules about what the page shows. Re-reading is one round trip on a page nobody is scrolling, and using the anon key for the first paint makes "with the anon key only, every state renders" true by construction instead of by review; `players.discord_id` cannot reach the page even by mistake. | Assembling state from event payloads (a second code path, and it drifts on the night nobody is watching); server-rendering with the service-role client (RLS then proves nothing about what a phone can read); an API route for the page's own data (a public read that RLS already allows) |
| 2026-09-09 | Component tests run in a second vitest project, `dom`: jsdom, `**/*.test.tsx` only, `@testing-library/react` and `jest-dom` pinned exactly; the existing `node` project keeps the node environment and `fileParallelism` stays false for both. Both projects set `oxc.jsx` to the automatic runtime (M3.4) | Route-handler and ingest tests must keep running on node globals — a jsdom `fetch`/`Response` under a route test would be testing a runtime Vercel does not have. Splitting by file extension rather than by directory means a `.tsx` test cannot land in the wrong environment by living in the wrong folder. The JSX transform has to be named because `apps/web/tsconfig.json` sets `jsx: preserve` for Next, and the test runner has no Next in front of it. | One jsdom environment for the whole app (changes the runtime every existing test proved something about); `environmentMatchGlobs` (removed in vitest 3); no component tests at all (the sit-out strip, the `-0` delta and the reroll's disabled state would then only be checkable by running a night) |
| 2026-09-09 | The tonight page's reroll control posts JSON with `fetch` and never navigates; it is a real `<form>` whose submit is intercepted, so with JavaScript off it still posts and lands on `/admin` with the route's notice (M3.4, M3.2) | The page is already live: the promoted split arrives as a `splits` event and the strip re-renders in place, which is what the acceptance check ("cards and explanation become the promoted split's, in place, with no scroll movement") asks for. A 303 back to `/` would remount the page and lose the reader's scroll position for no gain, and `rerollRoute` hardcodes `redirectTo: '/admin'` in a file `web-engineer` does not own. | A plain form post with `redirectTo: '/'` (needs a change to `app/api`, and it reloads the page a subscription was about to update anyway); a control that is disabled without JavaScript (a button that does nothing is worse than one that works slowly) |
| 2026-09-09 | The `Names fill in after someone's first game.` hint appears once under **whichever** primary block has a nameless row — the member list as well as the team block — and disappears with the last `Someone` (M3.4, M3.10) | M3.10 wrote the rule for the team block because that is where the case was first noticed, and the same row reads `Someone` while the lobby is still filling; a hint that appeared only after the balance would explain a word the reader met five minutes earlier. Still once per page and never per row, which is the part of the rule that matters. | Only under the team block (the filling state then prints an unexplained word); one hint per nameless row (ten hints on a first night) |
| 2026-09-09 | `companion_commands` gains `sent_at`, `attempts`, `result jsonb`, `error text` and `expires_at` in a new migration, with a time to live per kind: `create_lobby` 60 s, `invite` 5 min, `switch_side` 3 min (M4.1) | The table as shipped in `0001_init.sql` can say what to do and that it was acked, but not what came back, why it failed, or when it stops being worth doing — and a `create_lobby` handed out an hour late opens a lobby nobody asked for. The TTLs are how long each command is still the right answer: a minute is how long somebody stares at a button, three is how long a side is still your side, five is how long an invite is worth a popup. | A single global expiry (wrong for all three kinds); putting the result back in `payload` (a column that means two things); a `dead_letter` table |
| 2026-09-09 | The commands poll carries `clientConnected`; with `false` the server hands out nothing, moves no row and **does not write `last_seen_at`**, so `companion_tokens.last_seen_at` comes to mean "this friend was at their PC with League open" (M4.1) | The companion should ask for work only when it can do it: a queue drained into a client that is not there spends a command on nobody and burns its delivery count. The side effect is the one signal M4.2 needed and did not have — who is around — for free, with no new column and no presence system. | Nacking every command while disconnected (a friend launching League loses their invite); a `client_connected` column on the token (state that goes stale the moment a process dies) |
| 2026-09-09 | Execute-once is a local file of executed command ids (`<configDir>/commands-done.json`, 200 entries, 24 h) plus a read-before-write executor per kind, not a server-side lock (M4.1) | An ack can be lost after the client call has already happened, and no amount of server bookkeeping can see that. The file lets the companion re-ack from its own record without touching the client, and reading the lobby before every write makes a genuine double execution a no-op anyway: a second create finds a lobby open, a second invite finds a pending invitation, a second switch finds the player already there. | A server lease with a lease id (still cannot tell "executed, ack lost" from "never executed"); no dedupe at all (two lobbies on one client) |
| 2026-09-09 | A command kind whose `03-lcu-reference.md` row is `unverified` is disabled at **both** ends — the server does not queue it, the companion answers `endpoint_unverified` — and `pnpm --filter companion verify-commands` is the only code allowed to POST to the client before a row is green (M4.1) | `CLAUDE.md`'s "verify before you claim" with a mechanism instead of a good intention. Two flags rather than one because companion binaries keep running for months: a stale exe must never be the thing that POSTs an unverified path. The verify mode is human-run, prompts before each probe and writes a report that becomes the reference row. | Shipping the handlers on the community-documented bodies and fixing it on the night (the failure mode is somebody's client doing something unexpected in front of ten people); a smoke script that POSTs unattended |
| 2026-09-09 | **Supersedes the 2026-09-08 `lobbies.lobby_password` row.** The password is generated by the API, carried on the `create_lobby` command, and re-sent by the creating companion on every lobby post for that party; a `lobbyPassword: null` **never clears** a stored password (M4.2) | The client's lobby body has no password field, so the only knowledge of it is in the process that set it. The never-clear rule is not optional: the second companion in the lobby, and the host's own companion after a restart, both post null and neither may erase what the group is reading off the screen. | Storing the password only on the command row (the tonight page and the embed read `lobbies`); having a friend type it somewhere (a typed step in a zero-input product) |
| 2026-09-09 | A generated lobby is `Customs <dd Mon> #<n>` with a four-digit password, blind pick (`mutators.id: 1`), map 11, five a side, spectators allowed (M4.2) | `#n` is the night's cycle number, so the client's own lobby list and the Discord embed say which game of the night this is. Four digits because somebody joining by hand types it and the group reads it out in voice. Blind matches the lobbies the group actually opened in the 16.17 capture (queue 3100); the balancer assigns the roles either way. | A mode picker or a name field on the page (each one is a step, and this product's claim is that there are none); a long random password (unreadable aloud) |
| 2026-09-09 | "Around" for M4.2, until voice presence exists: every player whose companion token was seen in the last 60 minutes, **plus** every player who appeared in a custom on one of the last 7 nights, minus the host and minus anyone already in the lobby, capped at 19 invites and ordered most recently active first. M4.5 replaces the second clause with voice presence (M4.2) | `00-product.md` says one or two friends run the companion, so a companion-only definition would invite two people and call it a night. The asymmetry decides the rest: an invite that reaches somebody who is not around costs them one popup that expires by itself, while a missing invite costs a person their game. | Companion-seen only (invites nobody); everyone who has ever played (a popup for people who left the group); an "I'm in tonight" tap on the page (a manual check-in, which is the exact step principle 2 forbids, two tasks before M4.5 removes the need for it) |
| 2026-09-09 | `Start a lobby` is open to any signed-in **linked player**, not admins only, and one route (`POST /api/lobbies/start`) serves both the tonight page and the admin control (M4.2) | Opening a lobby is what any of these ten people does today; making it an admin chore puts a human in the way of a night starting, which is the problem this project exists to remove. One route because two implementations of the same rules drift, and the refusal sentences have to be identical on both surfaces. | Admin-only (a bottleneck, and the admin is often the one not at their PC); anonymous (a public page that writes) |
| 2026-09-09 | Auto side switch is gated on one server constant plus the companion's own per-kind flag, is never queued for a player whose `lobby_members.side` is null, and ships with fixed copy either way: `Move to your side in the lobby.` when off, `You'll be moved to your side — if not, move yourself.` when on. The tonight page shows the line only while one of the ten is still mismatched (M4.3) | The teams embed is posted at the moment of balancing, before any companion has polled, so it can never truthfully say everyone is on their side — it can only say what is about to happen. The page is live and can be accurate, so it is the surface allowed to stop nagging. A spectator has no side to toggle from, and a toggle cannot seat them; they get the line like anybody without a companion. | Naming who is on the wrong side (stale the second somebody moves, four extra lines on a phone); editing the embed after the switches land (M3.1: two messages are the honest record, and this would be one message that rewrites history); orchestrating two commands to swap a pair |
| 2026-09-09 | A game posted with `source: 'backfill'` is stored and **not rated inline**; ratings for it come from `pnpm --filter web rebuild-ratings`, which is why M5.1 and M5.2 ship together (M5.1) | Ratings are a fold in `started_at` order and backfill delivers games out of order by definition. Rating them as they land would write numbers the first rebuild throws away, and would move everybody's leaderboard position twice for no reason. The route already knows the source; the gate is one condition. | Rating backfilled games inline and relying on the rebuild to fix them (a leaderboard that is visibly wrong between the two); refusing backfill until a rebuild is scheduled (a step nobody would take) |
| 2026-09-09 | Backfill approval is `players.backfill_approved_at` / `backfill_requested_at` (new migration) with a three-state `Backfill` column on `/admin/players`, and for `source: 'backfill'` the participant check has **no** lobby fallback (M5.1) | An end-of-game post is one game the server can see a lobby for; a backfill batch is dozens of games from nowhere, and one bad or mis-mapped batch moves every rating in the group. One person looking once, before the first batch, is cheap. M2.8's lobby fallback exists for the friend sitting out a round, which cannot happen for a game months old. | Approving per token (a re-mint would re-approve silently); no approval (the architecture doc's Security section already promised one); approving each batch (a chore every day) |
| 2026-09-09 | Backfilled participants carry `role: null` (M5.1) | Match detail has no `detectedTeamPosition`; `timeline.lane`/`role` is a different vocabulary and nothing verified maps it onto ours. Role is display-only — the rating fold never reads it — so a null is an honest blank rather than a guess that shows up as a fact on a player page. | Inferring the role from the champion (`03-lcu-reference.md` already forbids it for the eog mapper); mapping `timeline.lane` on a guess |
| 2026-09-09 | The rating rebuild takes no lock: a guard that refuses to start while a lobby is live, a fold held entirely in memory with one batched write at the end, and a fence that re-reads the game set afterwards and exits 2 with "run it again" if it grew (M5.2) | There is no transaction to hold — the API writes through PostgREST one statement at a time — so a lock table would be a migration plus a lock nobody releases after a crash. Because `ratings` is untouched until the final pass, a concurrent end-of-game post can only be overwritten by the ordered answer, never blended with it; and the command is idempotent, so "run it again" costs seconds. | A lock table or an advisory lock (does not stop the service-role writer); a fold-version column on every row (a migration and a second code path forever); refusing all companion posts during a rebuild (a night that silently loses a game) |
| 2026-09-09 | The companion validates the token's shape (43 base64url characters, the exact output of `mintCompanionToken`) at the prompt and when reading `config.json`; a wrong shape re-prompts (three tries at the prompt, then exit; a saved wrong shape is "no token" and keeps `apiBase`) rather than being sent to the API (M2.19) | The first Windows run saved `[200~<token>[201~` and got a 401 forever with no way to know the paste was the problem. Every minted token has one shape, so a value of any other shape cannot be a credential; refusing it with a sentence at the prompt is the only place the person is still at the keyboard. The shape is pinned in the companion because it must not import the web app; `config.test.ts` checks the pin against `randomBytes(32).toString('base64url')`. | Sending whatever was pasted and letting the 401 explain (that is 0.1.0); a probe request to the API before saving (the API may be down at first run, and the prompt already says the file can be fixed later); a looser check such as "no whitespace" (would have accepted the bracketed-paste wrapper) |
| 2026-09-09 | The hidden token prompt parses terminal escape sequences (`ESC [ ... final`, `ESC O x`, `ESC` + one character) as units and drops them, even across chunk boundaries, and every token input path (hidden, piped, `--show-token`, the config file) is cleaned the same way; `--show-token` / `CUSTOMS_NIGHT_SHOW_TOKEN=1` echoes to the console only (M2.19) | Raw mode hands the application the bytes a terminal with bracketed paste emits around a paste; dropping only the `ESC` byte keeps the rest of the sequence, which is the 0.1.0 bug. One cleaner for every path means a token quoted by hand in `config.json` or pasted with a trailing CR works everywhere. `--show-token` is for a console that cannot paste into a raw-mode prompt; the logger never sees the prompt, so the flag cannot leak the token into a log that gets sent around. | Turning bracketed paste off (`ESC[?2004l`) before the prompt (the terminal that emits it unasked would not honour that either); readline with `terminal: true` and a mute output (still raw mode underneath, same bytes); always echoing the token (the README promise "you will not see it as you type" is the one privacy line a friend reads) |
| 2026-09-09 | Lockfile discovery on `win32` falls back to the process list when every path is missing: PowerShell `Get-CimInstance Win32_Process` for `LeagueClientUx.exe` sent as `-EncodedCommand`, `wmic` when PowerShell cannot start; the lockfile beside `ExecutablePath` first, `--app-port`/`--remoting-auth-token` off `CommandLine` second. It is off whenever `candidates` or `LCU_LOCKFILE_CANDIDATES` replaced the defaults, only ever runs when the real host is Windows, is rate-limited to one shell-out per 15 s by `createLockfileDiscovery`, and remembers the path it found so a custom install is polled like a default one (M2.19) | The first Windows PC has League outside `C:\Riot Games`, and "add `lockfilePath` to config.json" is a support step in a product whose companion is meant to be double-clicked. The process list is the one source that knows where a running client lives without asking anyone. `-EncodedCommand` sidesteps `powershell -Command` quoting entirely; `wmic` is kept only as the fallback because Windows 11 24H2 removes it. A PowerShell start every 5 s poll on an idle PC is a real cost, hence the rate limit and the remembered path; the gating on `candidates`/env is what keeps every test and the smoke script away from a real client. The command line carries the client password, so it is parsed and never logged or quoted in `tried`. | Reading `LeagueClient.exe` instead of `LeagueClientUx.exe` (its command line has no port or token); the registry uninstall key for the install dir (Riot's installer is not the only way League gets installed, and the running process is what matters); `tasklist` (no executable path or command line); shelling out on macOS too (the default bundle path has been right on every Mac so far, and the macOS row stays observed-only); scanning the Riot Client's `RiotClientInstalls.json` (a second file format to verify for one PC) |
| 2026-09-09 | The `finished` tonight page is the result card plus the explanation line of the split they played, and nothing else: the result card's own two team cards carry the after ratings and the deltas, there is no second pair with the before numbers, and the result card's headers carry no side sums (M3.16, designer) | The state table and the Result card component read together put two ratings for one player on one screen. The sums answer "are these teams even?", a question the game has just answered, and a reader who saw 6000 before and 6465 after has computed a team total of deltas by subtraction, the number no surface may print. | Keeping the secondary team cards with before ratings; dropping the explanation line after the game (it is the record the group argues with); keeping the sums with a label |
| 2026-09-09 | A player with no declared main role reads **`flexible`** on the tonight page's member list, the word `/admin` and M1.4 already use for a null `main_role` (M3.4) | `05-design.md` gives the row's shape — name, main role and backup, rating — but no word for the case where there is no main, and an empty cell in a list of ten reads as a bug the way a blank name does. `flexible` is not new vocabulary: it is what the admin page calls it (`roles, including back to flexible`) and what the balancer means by it — every role is a main. One word in both places, borrowed rather than invented. | `—` or an empty cell (looks like missing data on the one screen people read in a hurry); `fill` (a different thing: the balancer's third tier, for a role that is neither main nor secondary); `any` (reads as a request rather than a setting); a design-doc addition for a word product already owns |
| 2026-09-09 | The tonight page says `No season is active, so tonight's games are not being saved. An admin can start one.` It is its own exported constant beside `NO_ACTIVE_SEASON_MESSAGE` in `apps/web/lib/season.ts`, rendered at the top of `main` under the header strip, in every state, and absent whenever a season is active (M3.17) | M2.18's sentence ends "Start a season on the Seasons page.", which is right for `/admin`, the seasons page and a companion log line and wrong for the one page the whole group opens: the tonight link is pasted in WhatsApp, so nineteen of the twenty readers are being sent to a page they cannot open, and a page that ends in an instruction you cannot follow reads as broken rather than as informative. The fact behind it matters to everybody — tonight is not being saved — so the page keeps the fact and drops the instruction. Two constants in one file rather than two inline strings, so both stay product-approved and reviewable side by side. Supersedes the line in M2.18's acceptance check that had the tonight page printing the admin sentence. | Sharing `NO_ACTIVE_SEASON_MESSAGE` on the tonight page (the accepted plan until now: one string, three surfaces — rejected because the third surface has a different reader); saying nothing on the tonight page (the group finds out the next day that a night went unrated); showing it only in the idle state (it can be true while ten people are mid-game); linking the Seasons page for everyone (a sign-in wall on a page that has never had one) |
| 2026-09-09 | Backfill posts go through the game watcher's durable queue (`GameWatcher.enqueue`, the same `<configDir>/queue/<gameId>.json` file, drain and dedupe as an end-of-game block), not a separate post-and-forget path (M5.1, companion half) | One code path posts games, so `created: false`, the permanent-refusal list and the outer backoff are not duplicated, and the dedupe on `gameId` runs across both sources by construction: a game captured live in this process is never posted again from a detail. The cost is the queue's 50-file cap: a backfilled file evicted while the API is down is not fetched again until `backfill.json` is deleted, which is logged when it happens. This overrides the M5.1 brief's "no disk queue for backfill posts" on the lead's direction. | A second, queue-less poster in `backfill.ts` (the brief's text); a separate queue directory for backfill |
| 2026-09-09 | `backfill.json` is `{ version, lastRunAt, deepestBegIndex, knownGameIds, pendingGameIds, resumeBegIndex }`: the brief's shape plus the ids scanned-but-not-fetched and the walk cursor, and a game dropped for good locally (aborted, fewer than ten, no winner, schema) is remembered in `knownGameIds` (M5.1) | Without a cursor the steady-state stop rule ("a page whose customs are all known") would end every pass at page 0 and a fresh install would never reach position 100; without `pendingGameIds` the ids beyond the 20-detail cap would be forgotten by a restart. Remembering local drops keeps an aborted custom from being logged and re-checked every pass. Deleting the file still costs bandwidth and nothing else. | Persisting only the brief's four fields and re-walking from 0 every pass; a cursor that only advances when every candidate on the walked pages was fetched (stalls on the all-known rule) |
| 2026-09-09 | The backfill scan contract lives in one place: `companionBackfillScanRequestSchema` / `companionBackfillScanResponseSchema` in `packages/db/src/schemas/companionResponses.ts` and the doc comment there (request `{ gameIds }` of 1..100 positive ints; answer `{ ok: true, approved, unknown }`; not approved is `approved: false` with `unknown: []` **or** an HTTP 403; any other non-2xx, network error or non-matching body is "scan failed", one log line, back in 10 min; the game post is the unchanged eog body with `source: 'backfill'`, no `partyId`, `role: null`, and the route answers 2xx with `created` true or false for a stored-but-unrated game so the queue file is deleted). Ids still pending are re-scanned on every pass (M5.1) | Two restatements of one contract had already drifted between a comment and a schema. A 404 must never read as a refusal: today's server has no scan route and the walk must simply wait for it. Re-scanning pending ids is one round trip and is what lets a friend's backfill of the same nights save this companion its detail fetches. | Companion-local scan schemas; treating every 4xx as "not approved"; scanning an id once and trusting the answer forever |
| 2026-09-09 | The match-history walk treats an HTTP 4xx on a page past `begIndex` 0 as the end of history (logged with the index for M5.6), and drops the deep cursor after three consecutive failed passes on the same page so the steady-state walk from 0 resumes; a queue write the game watcher refuses (`'refused'`) leaves the id pending for the next pass instead of marking it known (M5.1 review) | A deterministic client refusal at a deep page would otherwise pin the cursor there forever and no new custom would ever be backfilled; a full disk must not turn a game into "handled". | Retrying a refused page every 10 min indefinitely; remembering a refused write as a drop |
| 2026-09-09 | The backfill pass runs 60 s after the first connect and then every 6 h, with a 10-min put-back when it stops with work left or the client is not idle, as the M5.1 brief says; the task text's "daily" is read as the brief's 6 h | The brief is the spec and 6 h keeps a night's games within hours of the first idle client; the interval is one constant (`PASS_INTERVAL_MS`). | 24 h |
| 2026-09-09 | The command-queue wire contract lives in one doc comment in `packages/db/src/schemas/companionResponses.ts` (M4.1, companion half, on the lead's direction), and differs from the M4.1 brief in shape, not in rules: `GET /api/companion/commands?clientConnected=` answers `{ ok, commands[≤10], nextPollInMs? }`; ack and nack are two per-id routes, `POST .../commands/{id}/ack { result }` and `POST .../commands/{id}/nack { error, retryable }`; an unknown or foreign id is 404, an already-settled row is **409** (the brief said a 200 no-op), a result that fails its kind's schema is 422. The companion treats 409 as "already recorded" and 404 as "nothing more to do" | The server half is built against what this comment says, so one place had to say it; the brief's `{ id, outcome }` body and its `changed: false` no-op were the same idea with a different spelling. 409 is louder than a silent 200 and costs the companion one branch. `retryable` is the one new bit: it separates "nothing ran" (`not_connected`, a client that gave no HTTP answer) from "ran or refused", so the server may re-offer the former inside the TTL and must fail the latter. The reason words, TTLs, page order, `clientConnected=false` semantics and result shapes are the brief's verbatim | The brief's single `/ack` route with `outcome` (fine, just not what the lead specified); a 200 no-op for a repeat (indistinguishable from a first ack in the companion's log) |
| 2026-09-09 | The per-kind verification gate is one constant in `packages/lcu` (`LOBBY_WRITE_VERIFICATION` in `writes.ts`), read by the companion, and `writes.test.ts` fails the build if a kind reads `verified: true` while its row in `docs/03-lcu-reference.md` is not `verified (<patch>, <date>)` with the same patch (M4.1) | "Verify before you claim" as a test, not a convention: the flag cannot run ahead of the doc, and flipping it is the same edit as writing the row. It lives beside the writes rather than in the companion because the exe bundles both and the gate belongs with the paths it guards. The server keeps its own flag (M4.2/M4.3, `apps/web/lib/commands.ts`), so a stale exe and a fresh server can never disagree in the dangerous direction | A flag in `apps/companion` (two places for one fact); reading the doc at runtime (the exe has no docs) |
| 2026-09-09 | The write answers of the three lobby POSTs are taken as `unknown` and never depended on; every executor re-reads `GET /lol-lobby/v2/lobby` (verified) after the write and builds the ack result — `partyId`, `customLobbyName`, the invitation row's `state`, the side actually read back — from that. A switch that answered 2xx without moving the player is `client_rejected` (M4.1) | The POST bodies are community knowledge and the answer shapes are guesses; the lobby GET is the one shape on 16.17 we have fixtures for. Reading after writing also makes "did it work" a fact rather than a status code, which is what the tonight page will show a person | Zod schemas on the POST answers (invented shapes marked verified); trusting the status code |
| 2026-09-09 | The command runner polls every `nextPollInMs` (5 s) while the client is up and every 60 s with `clientConnected=false` while it is not; a retryable nack is **not** written to `commands-done.json`, everything else (done, every non-retryable nack, `endpoint_unverified`, `expired`, `malformed_payload`) is (M4.1) | The disconnected answer is empty by contract, so a 5 s cadence there is 17 000 empty calls a day per companion on a free tier for nothing; 60 s keeps the letter of "keeps polling" at a twelfth of the cost, and the connect hook polls at once so a command queued while the client was away runs within a second of connect. Not recording a retryable nack is what lets the same row run for real on its next delivery; recording every other outcome is what lets a re-delivery re-answer without re-evaluating anything | 5 s always (the brief's letter); recording retryable nacks (a `not_connected` would then be re-nacked forever from the file) |
| 2026-09-09 | `--verify-commands` runs one POST per kind by default: the draft (`mutators.id: 2`) and full-side repeats the brief lists are opt-in prompts defaulting to no; a lobby the player is already in is never replaced without an explicit `y`; fixtures go to `packages/lcu/fixtures/<patch>/` when the repo is present and to `<configDir>/fixtures/<patch>/` from the exe, with the request body kept in a new optional `request` field of the fixture envelope (M4.1) | One POST per kind is the smallest run that answers the three questions, and every extra POST is one more thing to explain if the client does something unexpected in front of a friend; the person can say yes to the extras when the first answers look right. The exe cannot write into a repo it does not have; the config directory is where everything else of the companion's lives and the report names the files to paste back | Running every probe unprompted (the brief says prompt before each); a separate fixture format for writes |
| 2026-09-09 | The companion never compares a command's `expiresAt` with its own clock. Its only local expiry is `isStale`: the local time elapsed since the poll that handed the row out, against the row's own TTL (`expiresAt - createdAt`, two server instants), which catches a PC that slept between the poll and the execution and nothing else. After a 2xx `create_lobby` POST, a failed read-back is retried once and then nacked as a recorded, non-retryable `client_rejected`, never `not_connected` (M4.1 review) | A PC clock ahead by more than the TTL would otherwise nack every fresh command as `expired`; the server sweeps before every hand-out, so it owns expiry, and the difference of two server timestamps against the difference of two local ones is skew-free by construction. A retryable nack after the lobby already exists would make the server re-offer the row, the re-run would find the lobby and answer `already_in_lobby`, and the password would never be remembered | Comparing `expiresAt` with `Date.now()` (the first cut); a 30 s skew tolerance (still breaks at 31 s); no local check at all (a laptop that slept for an hour would open a lobby nobody wants any more) |
| 2026-09-09 | An `invite` is refused with `client_rejected: the local player may not invite (not the leader)` only when `localMember.isLeader` is false **and** `allowedInviteOthers` is false; a non-leader with `allowedInviteOthers: true` (what the 16.17 capture showed for a friend in an open party) is allowed to try (M4.1) | The brief's reason vocabulary has no "not leader" word and the capture says non-leaders can invite in an open party, so a hard leader check would refuse invites the client would accept. The client's own flag is the better oracle, and a wrong guess costs one `client_rejected` with the client's message rather than a silent skip | Refusing every non-leader (the task text's letter); adding a `not_leader` reason (widening a closed vocabulary the page renders) |
| 2026-09-09 | `POST /api/companion/game` answers with two optional extra fields, `rated: boolean` and `reason: string | null`, added to `companionGameResponseSchema`; a backfill post is always `{ rated: false, reason: 'backfill' }` and an end-of-game post carries M2.5's gate reason when it did not rate (M5.1, server half) | The M5.1 brief specifies that answer for a backfilled game, and the companion must still see the ordinary `created` field, which is what deletes its queue file. Optional keys keep every older companion parsing the same body, and one uniform shape beats a second response schema for one source. The same fields make an end-of-game skip visible in a log instead of only in the server's console. | A separate backfill response schema (two shapes for one route); required fields (an old companion's parse fails); saying nothing and letting the companion infer (it cannot see `game_players`) |
| 2026-09-09 | A backfill game post is gated by the **participant check only**; `players.backfill_approved_at` is enforced at `POST /api/companion/backfill/scan`, not on the game route (M5.1, server half) | This is what the M5.1 brief specifies and it loses nothing: the token's player must be on the scoreboard with no lobby fallback, so an approved-or-not companion can only ever post games that player actually played — exactly the rule an end-of-game post already lives under. Approval exists to stop a *batch* being walked out of match history, and the scan is the only way a companion learns which ids to fetch. Adding a second gate on the game route would also break the M5.1 case of a game already stored from an eog block. | Refusing a backfill post from an unapproved player (a rule the contract doc does not state, and one the companion would hit as a permanent 403 that deletes a queue file); approving per token |
| 2026-09-09 | `pnpm --filter web rebuild-ratings` runs as `node --env-file-if-exists=.env.local --import tsx scripts/rebuild-ratings.ts`, and `tsx` is a devDependency of `apps/web` (M5.2) | Plain `node` with type stripping cannot resolve the **extensionless relative imports** inside `packages/core` and `packages/db` (`./balance/index`, `./rosterKey`), so any script that imports a *value* from a workspace package dies on `ERR_MODULE_NOT_FOUND`. `mint-token` survives only because its cross-package imports are types, which are erased. The rebuild needs `rateGame` and `seedFromRank`, so it needs a resolver; `tsx` is already in the lockfile for `apps/companion` and adds no network install. | Adding `.ts`/`.js` extensions to every relative import in `packages/core` and `packages/db` (a repo-wide change, for one script); a build step for the script; duplicating the fold in the script with no core import (the one thing M5.2 exists to prevent) |
| 2026-09-09 | The rebuild treats two rating values within `RATING_EPSILON` (1e-9) as the same number and does not rewrite a row inside the tolerance (M5.2) | PostgREST prints `double precision` to fifteen significant digits, so a rating written by the live fold and read back is that double **rounded**; the live fold then folds the next game from the rounded read while the rebuild folds the season in memory from seeds, and no amount of care makes the two chains agree bit for bit. Without a tolerance every run would rewrite every row, `ratings.updated_at` (a trigger) would move, and "two runs are byte-identical" would be false. 1e-9 of mu is 6e-8 of a display point, which is below anything the product shows. | Exact equality (an idempotent command that changes the table every run); rounding every stored rating to N decimals (a migration's worth of rewriting, and a lie about what the model computed); always rewriting and ignoring `updated_at` (the acceptance check is a byte-identical dump) |
| 2026-09-09 | The rebuild is scoped to one season and is the only writer allowed to overwrite the live fold's `mu_after is null` claim; its integration test therefore runs inside a season of its own (M5.2) | `ratings` is keyed `(player_id, season_id)` and the fold is per season, so a rebuild cannot be namespaced by row the way every other integration test here is — it necessarily touches every game in the season it folds. Starting a season for the test file and handing Season 1 back afterwards is the only isolation that is actually true, and it exercises `--season` at the same time. | Rebuilding the shared active season in tests (rewrites every other test's leftovers and makes the assertions depend on them); a `--only-these-games` flag (not a fold) |
| 2026-09-09 | A `source: 'backfill'` post **fills names, never patches them**: `ensurePlayers` gains a `fillOnly` mode that creates a row for a PUUID we have never met with the names the detail carries and touches no existing row's `game_name`, `tag_line`, `display_name` or `summoner_id`, and a backfill post of a game already stored claims no name at all (M5.1 review) | A match detail reports who somebody was **when the game was played**. The walker reads history newest-first, so the ordinary M1.7 refresh would settle everybody's name on the *oldest* game in the batch, and a second friend backfilling the same nights would do it again tomorrow — display names on every surface silently rolling backwards, with no admin override to blame. The good half survives untouched: backfill is still where a friend the database has never met gets a row and a name, because that name arrives on the insert. | Refreshing names from backfill like an end-of-game block (the defect); dropping names from backfill entirely (loses the unknown-player case, which is the commonest good outcome of the whole feature); preferring the newest `startedAt` per puuid across a batch (state in ingest, and still wrong across two friends' batches) |
| 2026-09-09 | `/leaderboard` lists **every** `players_public` row, and a player with no `ratings` row for the season is seeded from rank in memory with `seedFromRank` — the same rule `loadPool` and the tonight page use — so they appear with `0 games`, a Rating, a Proven and the `settling` chip (M3.5) | The brief's "zero games this season" edge case is explicit that a seeded player must not be filtered out: "a friend who was seeded last night and cannot find themselves will ask why". Reading the players and left-joining the ratings is also the only shape that keeps one rule for what a player's rating is; a query over `ratings` alone would put a different set of people on the board than the balancer works from. The group is twenty rows, so reading them all costs nothing. | Selecting from `ratings` and ordering by the generated `ordinal` column (the seeded newcomer is invisible, and the printed number would come from SQL while every other surface's comes from core); inserting a `ratings` row at seed time (a write from a page, and a rating nobody has earned) |
| 2026-09-09 | Three strings `05-design.md` and the M3.5 brief do not spell: `No games this season yet.` (an empty board, and a player with no games), `No season is active, so there is no board yet. An admin can start one.` (no active season on either board page), and `Won` / `Lost` on a recent game. All three live in `lib/board/copy.ts`; product owns them and may replace them without anything else moving | The brief requires "one line saying the season has no games yet" and names no line; the design doc designs the tonight page's empty states and none of these. The empty line copies the tonight page's `Nobody in the lobby yet.` construction; the no-season line copies M3.17's shape (the fact, then who can fix it, and nothing to tap) rather than `NO_ACTIVE_SEASON_MESSAGE`, which ends by naming a page most readers cannot open. `Won`/`Lost` is the viewed player's own result: `Red wins` on their page makes them work out which side they were on first. | Reusing `NO_ACTIVE_SEASON_TONIGHT_MESSAGE` on the board (it is about tonight's games not being saved, which is not what an empty board is about); an empty page or a spinner (the brief forbids both); `Blue wins` / `Red wins` per row (the result card's copy, for a page that is about one person) |
| 2026-09-09 | The `Rating` chart is a server-rendered `<svg>` with a stretched `viewBox` (`preserveAspectRatio="none"`) and `vector-effect: non-scaling-stroke` on both shapes, and the `seed` label is an HTML element positioned from a CSS custom property, not an SVG `<text>` (M3.5) | The plot has to fill whatever width a phone has while the 1.5px line stays 1.5px, and the same stretch that does that would smear an SVG label horizontally. Passing the position as `--cn-seed-pos` lets the stylesheet `clamp()` it inside the plot, so a reference line at the very top of the range does not put its label over the chart's title. No charting library, per the task; the geometry is a pure function with its own tests. | A charting library (a dependency, client JavaScript, and a tooltip the design forbids); a fixed-width chart (wrong on every phone); `<text>` in the stretched box (distorted); computing the label's `top` inline without a clamp (the label escapes the plot at the extremes) |
| 2026-09-09 | The recent-games list on `/p/[puuid]` shows, per game, the player's **own five in lane order** (`lib/laneOrder.ts`) with their row marked, plus the result, the duration and `displayRating(muAfter)` with the delta computed at render. Five games (M3.5) | The design doc gives the chart "the recent-games list underneath is the detail view" and specifies nothing else, and the lane order is what makes "my row" sit where it did in the teams block, the result card and both embeds. The delta is `displayDelta` at render like every other surface, so `-0` cannot be flattened by a `JSON.stringify` and `1512 (−24)` adds up. | A bare list of results (answers nothing the chart does not); both teams per game (ten rows a game, five games, on a phone); carrying formatted deltas in the loaded snapshot (`-0` does not survive JSON) |
| 2026-09-09 | The nightly leaderboard post is a pure builder (`leaderboardEmbed`) plus `postNightlyLeaderboard`, exposed on `GET /api/cron/leaderboard` behind a bearer `CRON_SECRET` — the same door as `/api/cron/sweep` — and **no schedule ships with M3.5**: something outside the app calls it at a configured time, and no `vercel.json` cron entry is added. The embed carries a `timestamp`, which the design doc's field list does not mention | Vercel's hobby plan allows very few cron entries and the time the group wants the board in the channel is an ops decision, not a code one; a route that does exactly what it is asked, twice if asked twice, is the shape a scheduler can be debugged against. The timestamp is what says *which* night's board a scrolled-back post is, and both shipped embeds carry one — its absence from the block in `05-design.md` reads as terseness rather than as a rule, since that block also omits `description`. | A Vercel cron entry in this milestone (picks a time nobody has agreed and spends the plan's one slot); dedupe inside the route ("did we already post tonight" state for a thing a scheduler already controls); no timestamp (a board post scrolled back to next week has no date) |
| 2026-09-09 | `apps/web/app/board.css` is standalone and does not share `tonight.css`; the leaderboard row's **name** is the tap target that opens `/p/[puuid]` (44px, styled as the row spec's text, not as a link); the bare `Proven` number carries visually-hidden text; and the streak column reads the season's most recent 200 games, fetched in chunks of 90 game ids (M3.5) | `tonight.css` couples `.cn-member` and `.cn-seat` in one rule because the ten-row height reservation depends on it, so pulling the shared primitives out is a change to a page in review; the repeated shell rules are three-line token references. The row spec has no link in it, and the name is the only thing on the row a thumb would aim at. The visually-hidden `Proven` follows the precedent `05-design.md` sets for the team card's bare side sum. A season is thousands of `game_players` rows and PostgREST caps a response at a thousand: a bounded window read in chunks is honest where reading everything is silently truncated in an order nothing controls. | Extracting a shared stylesheet now (touches the tonight page's row rule and its test); a whole-row link (swallows the row's two lines into one enormous target and breaks the two-group layout); no accessible name for Proven (a screen reader gets an integer and no noun); one unbounded read of `game_players` (truncated at 1000 rows, silently) |
| 2026-09-09 | Season carry-over runs **inside** `start_season`, in SQL, in the same transaction that closes one season and opens the next; a new migration `0005` drops `start_season(text)` and creates `start_season(p_name text, p_confirm_name text, p_carry_sigma double precision)` (M5.3) | `0002` exists because two PostgREST calls can leave the group with zero active seasons. A carry-over run as an API step after it has the same failure with a worse shape — a live new season with an empty board, no undo, and no retry that is safe to press twice — and the button already has no undo. One function, one transaction: close, open, carry. | A second API call after `start_season` (the failure mode the function was written to remove); a trigger on `seasons` (invisible, and it would fire for `set_active_season` too); carrying lazily at first read (a leaderboard that changes because somebody looked at it) |
| 2026-09-09 | "Reset sigma" on carry-over is the **ranked** starting sigma `8.33` for every carried player, whatever their rank, and the API passes it into the SQL function as an argument rather than the migration hard-coding it (M5.3) | The 8.33 / 10 split exists because an unranked player's seed `mu` of 20 is a guess about a stranger. A carried player is not a stranger: their `mu` is a season of our own games and is the best number in the database, so it gets the confident sigma. It is also one number to explain — everyone starts the season equally unsure. Passing it as an argument keeps `config.rating.rankedSigma` in `packages/core` the only definition, so a tuning change cannot miss a migration. | The player's tier seed sigma (8.33 or 10 — punishes a friend for a rank the client never read, using information a whole season has superseded); a fixed wider value like 10 or 12 (a slower, noisier first month for no stated reason); copying sigma forward unchanged (then the season is not a season) |
| 2026-09-09 | The carried `{ mu, sigma }` is stored on the new season's `ratings` row as `seed_mu` / `seed_sigma` (two nullable columns, `0005`), and the M5.2 rebuild seeds a player from those columns when they are non-null and from `seedFromRank` otherwise (M5.3) | The rebuild folds a season from seeds. Without a stored seed, the first `rebuild-ratings` of the new season would re-seed every carried player from their rank and silently erase the carry-over — the exact line in `rebuild.ts` that already carries the comment "when M5.3 makes a new season carry `mu` forward, this line is what changes". Storing it makes "a rebuild reproduces the carry-over" a fact about the data rather than a hope. The columns are the ones M5.7 wants, so M5.7 shrinks to filling them for rank-seeded players. | Re-deriving the carry at rebuild time from the previous season's final `ratings` (they are not final — a backfill plus a rebuild of the old season moves them, so the new season's history would change retroactively); a separate `season_seeds` table (a table for two columns whose key is exactly `ratings`'); no rebuild support at all (one command silently wipes the group's history) |
| 2026-09-09 | Only `ratings` rows with `games > 0` carry into the next season, and `rebuild-ratings --prune` never deletes a carried row with zero games (M5.3) | A zero-game row is itself a carry nobody played on, or a seed from a lobby that never became a game. Carrying it again walks a stale number forward season after season and fills the board with people who left in March. One game brings anybody straight back with a fresh rank seed. The prune exception is the same fact from the other end: a carried row with no games yet is the carry-over, not an orphan. | Carrying every row (a board of ghosts, growing every season); carrying rows with games above some threshold like 5 (a second arbitrary number, and it would drop a friend who played four games in a month); pruning carried rows (a rebuild on night one would empty the season) |
| 2026-09-09 | The typed season-start confirmation moves into SQL: `start_season` takes `p_confirm_name` and raises unless it equals the active season's name, trimmed and case-sensitive (M5.3). **Supersedes the 2026-09-08 row that recorded the read-then-write race as known and acceptable.** | The race stops being cosmetic the moment carry-over exists: two submits in the same few milliseconds start Season 2 **and** Season 3, and Season 3 carries nothing, because Season 2's rows all have `games = 0`. The board is empty and the reason is invisible. The check is one `if` inside a function that is already reading the active season, and `lib/admin/seasons.ts` keeps its read-then-write check so the friendly 400 sentence does not change. | Leaving the race recorded as known (it now destroys a season's ratings); a lock or an advisory lock (a lock nobody releases after a crash, for one button pressed twice a year); a client-side disable on the submit button (not a gate) |
| 2026-09-09 | Starting a season is refused with 409 `A lobby is live. Start the season when nobody is playing.` while any lobby is `open`, `balanced` or `in_game`, with no `--force` equivalent (M5.3) | A season that starts between the teams post and the end-of-game block rates that game into a season whose ratings the split never saw, so the result embed's numbers disagree with the game that produced them and the night's rating movement is unexplainable. It is the rebuild's guard, in the same words, for the same reason. Nobody starts a season in a hurry, so there is no escape hatch to add. | A warning the admin can click through (the confirmation field is already the friction, and a second one trains people to click past both); attributing the in-flight game to the old season (season attribution by `started_at` is M5.9, and it would still not fix the split's ratings); allowing it and explaining afterwards |
| 2026-09-09 | Season-aware pages are a `?season=<uuid>` query parameter on `/leaderboard`, `/p/[puuid]` and `/stats`, defaulting to the active season, with an unknown id a 404 and a past season strictly read-only (M5.3) | A query parameter is a link somebody can paste in the group chat, it needs no client state, and the default keeps the zero-input promise: nobody picks a season to see tonight's board. 404 rather than a silent fallback because an unknown id can only come from a hand-edited URL, and a page that quietly shows something else is how people misread a number. | A path segment `/leaderboard/[season]` (a second URL for the same page and a redirect to maintain); a cookie or client state (a friend's link would show a different board to whoever opens it); falling back to the active season with a notice (more code, and the notice is the thing nobody reads) |
| 2026-09-09 | Every number on `/stats` counts a game only when the rating fold counted it — `gateGame` from `apps/web/lib/ingest/fold.ts`, imported, not re-implemented in SQL (M5.4) | The games number on `/stats`, the games number on `/leaderboard` and `ratings.games` have to be the same number for the same player, or every statistic under them is argued with. Sharing the predicate makes that true by construction, the same way sharing `foldGame` makes the rebuild reproduce the live fold. | A SQL `where duration_s > 300` plus a participant-count join (a second definition of a rated game, drifting from the first); counting every stored game including remakes (two different game counts for one player on two pages); counting only games with a lobby (drops every backfilled game) |
| 2026-09-09 | `/stats` and the per-player stats sections are computed at request time from `games` and `game_players`, capped at the most recent 2000 games of the season, with `revalidate = 300`; nothing is precomputed and no table or view is added (M5.4) | Twenty friends produce a few hundred games a season, which is a few thousand rows and a fold in milliseconds. A precomputed table is a second copy of the truth that can disagree with `game_players` — and `game_players` is the truth, rewritten by every rebuild. If the page ever gets slow the fix is the cap, not a cache. | A materialised view refreshed on game insert (a migration, a refresh nobody watches, and stale numbers after a rebuild); a stats table written by the ingest (the ingest gains a second job it can half-finish); no cap at all (one page load paging tens of thousands of rows in three years) |
| 2026-09-09 | There is no group-wide win rate by role on `/stats`; role win rates are per player only, with a 5-game minimum before a percentage is printed, and `/stats` shows a ranked list per role instead (M5.4) | Every game has a blue top and a red top, so any group-level win rate by role is 50.0% by construction — a number that costs a query, means nothing, and reads like a bug. The question the group actually asks is who is best at a role, which is the ranked list. The 5-game minimum stops "100% mid" off one game. | Printing the 50% table anyway (a page of noise); computing role rates against a role-neutral baseline (a statistic nobody asked for and nobody could check); no minimum (the top of every role list is somebody's single game) |
| 2026-09-09 | Awards (most improved, best off-role, cursed duo) appear only on a season that has ended, are computed at request time from that season's games, are never stored, and use higher minimums than the browsing stats: 15 counted games, 10 off-role games, 8 games together (M5.4) | An award that changes every night is a statistic, not an award; holding them until `ends_at` is what gives the last night of a season something to read together. Computing rather than storing means a late backfill or a rebuild corrects an award instead of freezing a wrong one forever. The higher minimums are the difference between a table that invites you to look and a line that crowns somebody: the duo table's five games is fine for browsing and thin for a title. | Live awards on the active season (a leaderboard by another name, changing nightly); snapshotting the winners into a table at season start (wrong forever the first time a backfill lands); one shared minimum everywhere (either the tables go empty or the awards go silly) |
| 2026-09-09 | Off-role, everywhere in the product, means the role played is not the player's `main_role` — a secondary role counts as off-role — and a player with no main role is not eligible for the off-role award (M5.4) | It is the balancer's own definition: `offRolePenalty` is charged for anyone not on a main role, and the explanation line says "Everyone on a main role". A second definition of off-role on the stats page would let the page and the embed disagree about the same game. A flexible player has no main to be away from, so the award has nothing to measure. | Off-role meaning neither main nor secondary (disagrees with the balancer and with the explanation string); using the balancer's assigned role from `splits` instead of the role actually played (no role at all for backfilled games, and it measures the plan rather than the night) |
| 2026-09-09 | `/admin/games` is one read-only admin page with two lists — lobbies that reached `in_game` and never finished, and the last 200 captured games with source, participant count, rated flag, season and lobby — and it issues no write of any kind (M5.5) | "The games we did capture" and "the lobbies we did not" are the same query over the same two tables, and one page answers both; it also retires the by-hand row counting in `docs/06-test-night.md`. Read-only because the stuck row is the evidence, and a button that clears evidence on the only page that shows it is the wrong shape — the cost of a stuck row is fixed by a server rule (M5.11), not by an admin chore at 21:30. | Two separate pages (the same query written twice); a public page (operational noise on a page friends open for teams); a "mark abandoned" button (erases the record this page exists to show, and adds a human step to a zero-input product) |
| 2026-09-09 | A lobby stuck at `in_game` past the two-hour sweep interval becomes a new terminal status `dropped` rather than `abandoned` or a second live row, so the party's next post starts a clean cycle; `dropped -> finished` stays legal (M5.11, found while briefing M5.5) | `lobbies_active_party_idx` is unique per party across `open`, `balanced` and `in_game`, so a new row cannot simply be started beside the stuck one; and the client keeps one `partyId` all night, so today one missed end-of-game block silently costs the group every remaining game of that night — the posts land on the frozen row and answer `balanced: null`. `abandoned` would free the index but means "dissolved before it ever started", keeps replace semantics, and would erase the one fact the missed-game report exists to show. `dropped` says exactly what happened, and keeping `dropped -> finished` legal means a queued block from days ago still closes its own lobby. | An admin "unstick" button (a human step at 21:30 with nine friends waiting); reusing `abandoned` (loses the record and unfreezes the roster); sweeping `in_game` to `finished` with no game (invents a result); dropping the partial index (two live rows per party is the bug M2.14 fixed) |
| 2026-09-09 | The web adopts Floodlit: nine palette tokens per theme (a second surface `raise`, a real `line` colour), `accent` renamed `brand` and pushed to amber `#FFB13C` on ink `#0B0E14`, cards built from fill + 1px line + a lit top edge, Archivo's `wdth` axis at 800 as the display cut. Supersedes the v1 palette, type and spacing sections of 05-design.md | The user's calibration is "an actual gaming product, modern, something like Blitz, with its own character". v1's seven flat tokens and 42rem column render faithfully and read as unfinished. Amber is the only strong colour that is neither side, so the brand can be loud without looking like a prediction; measured contrast keeps blue and red within 0.15 of each other in dark. | A violet or cyan brand (the generic AI-page look; cyan is a step from side 100); a third font family for display; keeping v1 and adding chrome only (the complaint was the style, not just the missing header) |
| 2026-09-09 | The filling state renders ten seat rows always, unfilled ones recessed and labelled `open`, instead of a list under a 449px min-height | The no-layout-shift guarantee is kept as content: a rack of ten seats filling up is the information in the shape the group thinks in, and it retires the ten progress bars that said the same thing twice. | A compact "N of 10" line (loses the shift guarantee below); letting the list grow (a join moves the footer); keeping the blank reserve |
| 2026-09-09 | Exactly one gradient and one glow exist in apps/web: a 5% amber radial at the top of the shell and a 14% amber ring on the live pill, both named in 05-design.md | A flat near-black field at 1400px is the desktop complaint, and a dark UI with no light source reads as a document. Naming both instances and their percentages is what stops one glow becoming a glow on every card. | No gradients or glows at all (v1); a shadow elevation system; per-card accent glows |
| 2026-09-09 | The tonight page's "Get the companion" link points at the GitHub releases page, not the direct exe asset; /admin and the group chat keep the direct link | The tonight page is opened on a phone from WhatsApp, where a direct link starts a 90 MB Windows download on a device that cannot run it. | One link everywhere; no link on the tonight page |
| 2026-09-09 | Floodlit status strip says FINAL only after a game; the winner is named once, on the result card headline; "how this works" is a footer disclosure, not a page | Two winners on one screen is the redundancy M3.16 removed for ratings; four lines of explanation need no route. | Winner in the strip with a smaller card headline; a /how page |
| 2026-09-09 | The tonight page's idle headline is `NOBODY IN YET` and its finished headline is `GAME OVER`, and the status strip's sentence never repeats the headline above it (`Teams in a moment.`, `Ratings move when it ends.`). **Supersedes the wording half of the 2026-09-09 row that said the strip reads FINAL**; that row's substance — the winner is named once, on the result card headline, not twice — stands unchanged (product, M3.18 copy pass) | `Nothing tonight` is the 19:00 screen for a group that plays every night, and it reads as *the night is off*; it is also false after an abandoned lobby, where idle still applies. `FINAL` is the broadcast lower-third word this document's own tone line forbids, and `GAME OVER` is the group's vocabulary at the same length. The repeated clauses spent the page's one live line saying what 44px type had already said. | Keeping `Nothing tonight` and `Final` (shipped, and both wrong for a different reason); `NO LOBBY YET` (false when a lobby was abandoned); moving the winner into the strip and shrinking the card headline (M3.16's offer — refused: the big line is the answer to "what happened") |
| 2026-09-09 | With a lobby open and nobody in it, the page says `Nobody in the lobby yet.` **once**, in the status strip's sentence slot; nothing is rendered under the seat rack in that state (product, M3.18 copy pass) | The Floodlit proposal had `Nobody has joined yet.` in the strip and the shipped sentence under the rack — one fact, twice, 40px apart, on the emptiest screen in the product. The settled string wins and it goes in the strip, because the strip is mounted in every state and its two lines are reserved anyway; ten `open` seats are the picture the sentence would otherwise draw in words. | Two sentences (the duplicate); dropping the shipped string entirely (it is on the do-not-rewrite list); a new shorter line for the rack (a third string for one fact) |
| 2026-09-09 | Friend-facing web copy has one name for the desktop app — **the companion** — so the Floodlit nav tab is `Companion ↗`, not `Get the app`; the footer keeps `Get the companion` and the card keeps `Run the companion` (product, M3.18 copy pass) | The name is already fixed by a shipped sentence this redesign may not rewrite ("…in a custom lobby with the companion running"), and the group hears the word in Discord and in the README. `Get the app` was the only place on the page inventing a second word for the same download, which is how a friend ends up asking whether they are two things. A destination noun also matches the three tabs beside it. | `Get the app` everywhere including the shipped sentence (rewrites settled copy for a tab label); `Get the app` in the nav and `the companion` elsewhere (two names, one thing); `Download ↗` (says nothing about what it is) |
| 2026-09-09 | The footer's four `How this works` lines must cover, and be checkable against, four facts: the companion reads the lobby, teams are balanced on ratings that move with results, reroll is admin-only and never random, and a rating starts from your rank with Proven catching up after about 30 games (product, M3.18 copy pass) | Those four are the whole answer to "is this rigged", which is the question the product exists to end, and the proposal's four lines named neither the reroll nor where a rating comes from. Fixing the content of the disclosure rather than its length keeps it four lines and keeps it true; each claim is mapped to the code that makes it true in `05-design.md` so a later change to the system fails the mapping instead of quietly making the page lie. | Leaving reroll out (the one human control in a zero-input product, unexplained); a fifth line (the disclosure stops being four short lines); a `/how` page (a route for a paragraph) |
| 2026-09-09 | Every write on `/admin` and on the tonight page submits **in place** — React form actions or `fetch` to the existing route, an inline notice or error beside the control, no navigation — with the plain form post kept only as the no-JS degraded path (M3.20). **Supersedes the 2026-09-08 rows that made the 303-with-`?notice=` redirect the admin's only mutation path and that ruled out client JavaScript in the admin area**; `admin.css`, its native controls and its `color-scheme: light dark` are untouched | The user's report is the acceptance criterion: "experience sucks, page refreshes on any button I press". A full document load per button costs the scroll position, the focus ring and half a second on every one of the dozen controls an admin touches while nine friends wait. The route handlers stay the API and keep their 401/403 tests, so the reason those rows chose one mutation path survives — the page just stops navigating to read the answer. | Keeping the redirects (the reported experience); server actions replacing the routes (the second write path the 2026-09-08 row rejected, and the auth tests could not reach it); a global client store (these are server-rendered reads with a handful of writes; per-control state is enough); skinning the admin to match Floodlit (out of scope, and the 2026-09-08 plain-admin row still stands) |
| 2026-09-09 | The product is called **Kustom**; `Customs Night` stays the codename in `CLAUDE.md`, `docs/`, the package names and the `%APPDATA%/customs-night` config path. Every friend-facing surface says Kustom: wordmark, `<title>`, Discord embed footers, the companion README, its console and `--help`, the release title, and the exe, which becomes `Kustom.exe` (M2.20, M3.21) | The group already calls it kustom (the user, 2026-09-09), the releases repo is already `kustom-releases`, and a product should be called what the people using it call it. Six letters is also a better wordmark at 800 weight than thirteen. Keeping the codename costs nothing — no reader of `CLAUDE.md` is a friend on a phone — while renaming it would touch every doc, every package name and an installed config path for no user-visible gain. | Renaming the repo, packages, docs and config path too (churn with no user-visible gain, and an existing install would ask for its token again); keeping `Customs Night` on the wordmark and Kustom nowhere (the name nobody says); shipping both names on different surfaces (two products) |
| 2026-09-09 | The three board strings M3.5/M3.8/M3.10 wrote against no doc stand as written — `No games this season yet.`, `No season is active, so there is no board yet. An admin can start one.`, and `Won` / `Lost` on a recent game — and every word `/leaderboard` and `/p/[puuid]` say is now a row in `05-design.md`, "Copy — the board pages, final" (product copy pass) | Each was already the right sentence: the empty-season line is the tonight page's `Nobody in the lobby yet.` shape and is true both of a season with no games and of a player with none, so one constant serves both slots; the no-season line is built like M3.17's tonight sentence (the fact, then who can fix it, nothing to tap) and is correctly a **third** constant, because the admin one ends by naming a page nineteen of the twenty readers cannot open and the tonight one is about tonight's games not being saved, which is not what an empty board is about; `Won`/`Lost` is the player's own result on a page that is about them, where `Red wins` would make them work out which side they were on first. Writing them into the copy table is what stops the next engineer inventing a fourth voice for the same fact. | Rewriting for the sake of a review (churn on sentences that read correctly); reusing `NO_ACTIVE_SEASON_MESSAGE` or `NO_ACTIVE_SEASON_TONIGHT_MESSAGE` on the board (one sends friends to `/admin`, the other is about game capture, not the board); a separate longer empty state for a player with no games ("You have not played yet this season" — a third empty-state voice for one fact); `Win`/`Loss` to match `13W 15L` (a tally's letters on a line that is one game that happened) |
| 2026-09-09 | The board's destination has **one name on every surface: `Leaderboard`**. The `/leaderboard` heading reads `Season 1` with a dim `Leaderboard` beside it, the back link on `/p/[puuid]` reads `← Leaderboard`, both `<title>`s use it, and the nightly embed title becomes `Season 1 · leaderboard`. `STANDINGS_LABEL` is renamed `LEADERBOARD_LABEL` in `apps/web/lib/board/copy.ts` and the embed snapshot is regenerated (M3.5, designer's review of the rendered pages) | One destination was carrying three names: the route and the Floodlit nav tab said `Leaderboard`, the page heading and the back link said `standings`, and the embed title said `standings` again — so a friend who taps the tab, reads the page and scrolls back to the post sees two words for one place. `Leaderboard` is already product-approved friend-facing copy (the nav row of the tonight copy table) and is the noun inside a shipped sentence this redesign may not rewrite, `Ratings are updated. The leaderboard has the rest.`; `standings` was invented by `05-design.md`'s own embed section and appears in no other surface's vocabulary. Same rule as the `Get the app` → `Companion ↗` row: one thing, one name. | Keeping `standings` for the content and `Leaderboard` for the destination (the distinction is invisible to twenty friends with one board, and it is exactly the two-names-one-thing failure the companion row rejected); renaming the route and the nav tab to `/standings` (churn, and it breaks a link already posted in the channel); leaving the embed title alone and changing only the web (the post and the page it links to would disagree in the title bar) |
| 2026-09-09 | With no active season, `/p/[puuid]` renders the player's name and the sentence `No season is active, so there is no board yet. An admin can start one.` and **nothing else** — no numbers, no `settling` chip, no still-settling sentence, no chart, no `No games this season yet.`, no recent games, no by-role block. `/leaderboard` is unchanged. `loadPlayerBoard` returns a **discriminated union** (`{ kind: 'no-season', puuid, name }` or the `kind: 'season'` arm that carries every number), not one shape with zeros in it (product, after the M3.5 review) | Ratings are keyed `(player_id, season_id)`, so with no season this player has no rating, no Proven and no history — and the first cut filled those fields with `0` and printed `Rating 0 · Proven 0 · settling` directly above a sentence saying there was no board. Zero is a number the model never produced, and it is the one number on that page a reader would believe. Making it a union rather than a conditional in the view means the page cannot regress into printing numbers for the case: there are no fields to print. | Keeping one interface and hiding the block in the view (the zeros stay in the payload and the next component to render it prints them); `rating: number \| null` everywhere (four nullable numbers and a nullable check at every call site, for one state); showing the seeded rank as the rating (a number that is not this season's and that nothing else on any surface would agree with) |
| 2026-09-09 | The nightly leaderboard post also skips, with `no games this season`, when **every** row on the board has `games === 0`; the three skip rules live in one pure `nightlyLeaderboardSkip(board)` (M3.5, product) | The board seeds every known player from their rank, so on the first morning of a new season the post would be `Season 2 · leaderboard` over ten real names with real Proven numbers and `0 games` against every one of them — a ranking of a season that has not happened — while `/leaderboard` correctly says `No games this season yet.` It is the result embed's rule (`a message that says nothing is worse than silence`) applied to the third message. One rated game and it posts exactly as before. Pure, because a season nobody has played is a state no test can arrange against a shared local stack. | Posting the seeded board (the defect); suppressing it by a date window such as "not in the first 24 hours" (a clock rule for a question the data already answers); requiring N games before the first post (a second threshold nobody has agreed, next to M3.8's 30) |
| 2026-09-09 | **Proven is floored at zero where it is displayed**, on every surface — `/leaderboard`, `/p/[puuid]` and the nightly Discord embed — as `Math.max(0, displayRating(ordinal(rating)))` in `provenRating`. The **sort** stays on the raw `ordinal` from core, carried on the row as `sortKey` and never printed (`provenSortKey`; the designer's review, M3.5) | `ordinal = mu - 2σ` is negative for anybody whose uncertainty outweighs half their skill estimate, which is the ordinary state of a new player and not an edge case: an Iron IV seed is `14 - 2 × 8.33 = -2.66`, so `-160` on the board, and a low seed after two losses lands there too. A minus sign on a scoreboard reads as a penalty a friend has been given, when the true statement is "the board has not seen you play yet" — which the `settling` chip beside them already says. Sorting on the raw ordinal is what keeps rows that all display `0` in their true order instead of falling through to the name tie-break; `Math.max` is monotonic, so the printed column is still non-increasing down the page and product's rule (the order matches the number shown) holds. | Printing the negative number (the defect: it reads as a punishment); flooring the sort key too (every unproven player then ties at zero and the board orders them alphabetically, which is a shuffle a reader cannot explain); a different floor such as 1 (a number with no meaning); hiding Proven until 30 games (the board would then sort on a number it does not show — the exact complaint the two-number rule exists to prevent) |
| 2026-09-09 | The board's word is **`Leaderboard`**, not `standings`: the page heading (`Season 1` + dim `Leaderboard`, and `Leaderboard` at full weight when no season is active), the back link (`← Leaderboard`), the `<title>`, and the nightly embed's title lower-cased in place (`Season 1 · leaderboard`). One constant, `LEADERBOARD_LABEL` (designer, 2026-09-09) | It is what `docs/01-architecture.md` and every milestone have called this page since M0, and what the group says; `standings` was a second name for one page, which is the drift the two number names are already policed against. The no-season heading changed with it: `cn-strip-sub` demotes the noun beside the season name it belongs to, and with nothing beside it the page's only heading was a grey afterthought. | `standings` (a second name); `Board` (already rejected as a column name in the 2026-09-08 row); leaving the embed title capitalised (`Season 1 · Leaderboard` reads as two proper nouns) |
| 2026-09-09 | `/p/[puuid]` prints the player's record (`37 games · 19W 18L`) under the two numbers; the `No games this season yet.` line is gated on **`games === 0`**, not on an empty chart series; each recent game carries its date (`9 Sep`) formatted server-side by the one formatter in `lib/night.ts` (`formatDayMonth`, `en-GB`, `CUSTOMS_NIGHT_TZ`); and `loadSeasonGames` now reads a season **newest first** (designer's review, M3.5) | The record was on every board row and missing from the one page a friend opens to read about themselves. The gate was the real bug behind it: `history.length === 0` also means "this player's games were not in the slice the season read returned", so the page could tell somebody with thirty-seven games that the season had none — the root cause being that the season read took the *oldest* 1000 games while the player read took their newest, so past the cap the two sets stopped overlapping at the recent end. Both reads now start at the same end and callers sort in memory, which they already did. The date is one formatter and a fixed locale for the same reason the night's slug line is: a date formatted in the visitor's locale differs between the server render and the browser, and the line changes under the reader. | Gating on the chart (the defect); a second `Intl.DateTimeFormat` beside the board (two timezones, one page); relative dates such as "2 days ago" (a clock in a server render, and it goes stale in a cached page); dropping the cap instead of aligning the direction (a season's `game_players` is thousands of rows against a 1000-row response limit) |
| 2026-09-09 | The companion rename to Kustom (M2.20) stops at the surfaces a friend sees: the exe (`Kustom.exe`, `Kustom.exe.sha256`), the console banner, `--help`, the first-run prompt, the verify-commands banner, the release title and the README. It leaves as codename the `User-Agent` (`customs-night-companion/<version>`), the API's `service: customs-night` health literal, the `CUSTOMS_NIGHT_*` environment variables and esbuild defines, and the `%APPDATA%\customs-night` config directory, which is not moved and gets no fallback. The build's intermediates (`dist/kustom.cjs`, `dist/kustom.blob`, the `build:host` output `kustom-<version>-<platform>-<arch>`) follow the exe because they are named after it, not after the codename | A wire value nobody reads is not a surface, and each of those has a reader on the other side (`GET /api/health` answers `customs-night`, and a companion pinned to it would refuse a renamed server for no gain). The config path is the task's own line: moving it would make every installed companion ask for its token again. The environment variables are documented in `--help` under their `CUSTOMS_NIGHT_` names and in `docs/06-test-night.md`; renaming them would break the shortcuts and dev commands written down for the test night. | Renaming everything including the config path with an old-path fallback (two directories to explain in the "If something looks wrong" section, for no friend-visible gain); `KUSTOM_*` environment variables with the old names as aliases (two names for one thing); a Kustom user-agent (the one place a server-side grep for the companion would silently stop matching) |
| 2026-09-09 | **Supersedes the 2026-09-08 row that made `in_game` unswept and terminal.** A lobby that has been `in_game` for two hours with no result becomes **`dropped`**, a sixth `lobby_status` (`0005_lobby_dropped.sql`): roster frozen for good like `in_game`, outside `lobbies_active_party_idx` like `finished`, and `dropped -> finished` still legal so a late end-of-game block closes its own lobby. The same sweep statement that abandons a stale `open`/`balanced` row does it, so `GET /api/cron/sweep` alone is enough (M5.11) | The old row optimised for the frozen roster and paid with the night: the client keeps one `partyId` all night (M2.14) and the partial index allows one live row per party, so a single missed eog block left the party's live row at `in_game` and every later post of that night answered `rosterFrozen: true`, `balanced: null`, `recheckInMs: null` — no teams, no split, and nothing a friend in Discord could diagnose. One missed block cost every remaining game of the night. `abandoned` cannot be the door out (it keeps the M2.9 replace semantics, so it would unfreeze the record of who played, and it claims the lobby dissolved before it ever started, erasing the one fact M5.5 exists to show), so the row needs its own terminal status. Two hours is the same number as the existing sweep and no game of League runs that long. M5.5's missed list becomes `status in ('in_game', 'dropped')`: still playing, versus nobody will ever see a result. | Leaving `in_game` unswept (the defect); sweeping it to `abandoned` (unfreezes the roster, and says the wrong thing); a second live row per party (forbidden by `lobbies_active_party_idx`, and it would be two lobbies for one party); an admin "unstick" button (a step in the scene at 21:30 with nine friends waiting, in a product whose whole claim is that there are no steps); a shorter timeout such as one hour (a long game plus a slow post-game lobby is real, and being wrong here costs a frozen roster) |
| 2026-09-09 | `dropped` gains **no friend-facing word on any surface**: the tonight page's loader keeps its `.neq('status', 'abandoned')` filter and the row falls through `tonightState`'s `default` to `idle`, so a dropped lobby reads `Nothing tonight`, and `/admin`'s reroll refusal reuses the `in_game` sentence (`the game has started, so the teams on the rift are the teams`) rather than inventing one (M5.11, acceptance 5) | The status exists so the *server* can tell a game whose result never landed from a lobby that dissolved; a reader is told nothing by the distinction and would have to learn a sixth word to read a page that says the same thing either way. The tonight page treating it as idle is also true: there is nothing live to show and the next post will open a real lobby. M5.5 is the one surface that will name it, and it is an ops page. | A `Dropped` state on the tonight page (a new word, for the emptiest screen in the product); reusing the `abandoned` copy path by widening the loader's filter (a change in `lib/tonight`, which the M3.18 rebuild owns, for no visible difference); a distinct reroll sentence (a second sentence for the same refusal) |
| 2026-09-09 | The M3.12 case in `discord.integration.test.ts` posts its game under a clock **pinned to 06:00:30 in `CUSTOMS_NIGHT_TZ`** — thirty seconds into a night — with `started_at` computed as "a minute ago, or the night's own start, whichever is later" (M3.24) | The case failed on 2026-09-09 at 06:00:41 Cairo: `Date.now() - 60_000` was on the other side of the 06:00 night boundary, `gamesTonight` was 0 for everybody, and the sit-out clause fell through to longest-since. Pinning the clock to the failing minute rather than steering around it means the boundary is exercised on every run at any hour, and the case now fails if the night rule regresses instead of failing once a day. The clock only ever moves backwards, which matters on a local stack other agents share: the idle sweep every companion post runs takes its cutoff from it, so an earlier clock can only sweep fewer rows. | Starting the game an hour into the night (still a clock the case does not control, and a game an hour in the future when the suite runs at 06:00); injecting `now` through the route (the route reads its own clock, and the seam would exist only for a test); leaving it (a test that fails for one minute a day teaches everyone to re-run the suite) |
| 2026-09-09 | The companion's default lobby mode for M4.1's `create_lobby` is **draft** pick, not blind (the user's answer, 2026-09-09). | The group plays draft; blind was only the safer default while nobody had said. | Blind pick as default; asking per night. |
| 2026-09-09 | **Seven rules from the designer's review of the rendered Floodlit build** (M3.18), all in `05-design.md`: (1) `How this works` and `Run the companion` render in the rail **or** inline, never both — one card, one place, per width; (2) at ≥720px the seat rack's name column is capped, `minmax(0, 16rem) · 10rem · minmax(4.5rem, 1fr)`; (3) the role column is 10rem above 480px and the second role is CSS-hidden, not unmounted, below it; (4) the sit-out strip has no header bar; (5) the three lane icons are drawn 8→16 inside a 30% frame; (6) card titles are Archivo `t-sm` 600 `text` — mono is for legends and states only; (7) light `raise` becomes `#DAE2ED` | Every one was measured on the running page, not eyeballed: the idle page at 1080px printed the same two cards twice; the rack's name column was 566px carrying 124px of text, leaving ~450px of nothing before the role; `jungle · support` needed 149px in a 136px column and hard-cut to `suppo` at every width ≥480 with no ellipsis, because `text-overflow` does not apply to that inline-flex box; the sit-out card's header repeated the first three words of its own sentence for 45px above the fold, on the one screen where the second team card already starts below it; the lane path at 6→18 left under a pixel of air inside the frame at 14px, so `top`, `mid` and `adc` rendered as one filled square; the two card titles were sentences set in 12px tracked mono, quieter than the prose they introduced; and light's three layers sat 1.08 apart against dark's 1.24, so the top bar and every card header nearly vanished. | Shipping the desktop rack as drawn (the sparse look v2 exists to end); widening the role column at every width (names ellipsise instead, and a name is what the page is for); dropping the second role entirely above 480px; keeping the sit-out header and cutting the settled sentence instead (product's copy, not the designer's to edit); dropping the lane frame altogether (clearest of the three, but the frame is what says "these three are lanes"); numbering the ten seats (declined by the lead for now); going dark-only (nothing in light is broken — it was under-separated, not wrong) |
| 2026-09-10 | The lobby writes are modelled on **what the 16.17 client's own UI sends**, read from the installed `rcp-fe-lol-parties` plugin and the client's OpenAPI document, not on community docs: create carries a top-level `queueId` equal to `mutators.id` (both from `GET /lol-game-queues/v1/custom`) and no `isCustom`; switch side is `POST /lol-lobby/v2/lobby/team/TEAM1|TEAM2` with the target in the path; the community `switch-teams` paths are dropped from the code and the allow-list (M4.1 follow-up) | The first live run refused the community create body with `500 INVALID_LOBBY`; the community shapes date from 2021 and the 16.17 schema has neither `isCustom` nor `switch-teams`. The UI code is the one source that cannot be stale for the running patch. | Keeping the community bodies and adding fields by trial; keeping `switch-teams` as a 404 fallback (a dead POST per command) |
| 2026-09-10 | The corrected `verify-commands` tries a **ranked list of create bodies in one prompted step**, stopping at the first 2xx and saving every attempt as `create-lobby--<candidate>.json` (the accepted one also as `create-lobby.json`); the 2026-09-09 refusals are kept as `create-lobby--legacy-blind.json` / `--legacy-draft.json` and the no-lobby invite answers as `lobby-invitations--no-lobby.json` / `--by-puuid--no-lobby.json` (ids renamed by script on ingest, bodies untouched) | One prompt per write step keeps the person in control while a refusal costs nothing but a second POST; the renamed ids keep the base names free for an accepted answer and keep the refusal on record. | One prompt per candidate (five prompts for one lobby); overwriting the refusals on the next run |
| 2026-09-10 | The companion's `create_lobby` executor reads `/lol-game-queues/v1/custom` every time and takes the Summoner's Rift entry it can name **draft** by its `name`/`pickMode` words (blind falls back to the client's own known id 19 when the words are empty); when nothing matches it nacks `client_rejected` with the list and posts nothing. No mutator or queue id is a constant in the runtime path. | The ids are per patch and per server; the dialog data is what the client itself uses; refusing is safer than opening a wrong-mode lobby. | Hard-coding 3100/19 (or 20) once verified; a per-night admin choice of ids |
| 2026-09-10 | `verify-commands` never sends `DELETE /lol-lobby/v2/lobby`; the person closes the lobby from the client | The reference lists no DELETE as safe and the allow-list is POST-only; the UI's `leaveLobby` does use it, but adding a write kind for a convenience is not worth widening the allow-list. | A final prompted DELETE |
| 2026-09-09 | The Floodlit shell is mounted by a new `app/(site)/layout.tsx` route group rather than by the root layout: `app/page.tsx`, `app/leaderboard/` and `app/p/` move into `(site)`, `/admin` stays outside it, and every URL is unchanged (M3.18) | `05-design.md` says the shell wraps `{children}` "with `/admin` opting out", and a root layout has no way to know the route: opting out would take a client component with `usePathname()` above every page, including the admin area the design deliberately keeps plain. A route group is the framework's own answer, costs nothing at runtime, and makes the opt-out structural instead of conditional. | Shell in `app/layout.tsx` with a `usePathname()` guard (a client component above every page, and one more thing to get wrong); mounting the shell per page (three copies of the same wrapper) |
| 2026-09-09 | On the seat rack a member's **second** role is hidden below 480px; the role column is `6.5rem` there and `8.5rem` above it (M3.18) | `jungle · support` beside a name does not fit in a 390px row: the column clipped to `jungle · t…`, which is worse than saying the one role somebody actually plays. The design's `top · mid` pair is kept everywhere there is room for it, and the team cards — which print one role per seat — are unchanged. | A wider role column at every width (names ellipsise instead, and a name is what the page is for); shrinking the role type below 12px (the scale's floor for anything that carries meaning) |
| 2026-09-09 | `/admin`'s in-place success sentences are a **second copy** of the route handlers' words, in `apps/web/lib/admin/notices.ts`, guarded by a test that greps the handler sources for each one (M3.20) | The routes are the API and M3.20 keeps them untouched, but their JSON envelope carries no sentence — only the 303 path does. Composing the sentence in the browser is the only way to show it beside the control without a second round trip to re-render the whole page; the grep test is what stops the two drifting. Refusals are **not** duplicated: they come back as `{ ok: false, error }` and the page prints the route's own words. | Following the 303 with `fetch` and reading the notice out of `response.url` (a full page render thrown away on every press); a server action (a second write path from a page, which `CLAUDE.md` forbids); moving the sentences into `lib/` and importing them from the handlers (an edit to `app/api`, which M3.20 rules out) |
| 2026-09-10 | The product name goes in the **teams** embed footer only (`Kustom · more on the tonight page`, or `Kustom`). The result embed footer stays `<season> · game <n>` and the nightly leaderboard footer stays the settling sentence. | M3.21's acceptance asked for the name on all three, but the other two footers carry information a reader uses (which game this was; why Proven lags) and the teams post is the one a friend sees first every night. One name per night is enough. | Naming all three footers (drops two useful lines); a second footer line (Discord has one). |
| 2026-09-10 | **Two rules for the off-role marker on a team card, both counted per card and not per page** (designer, from the Floodlit re-check; specified in full in `05-design.md`, "Teams"): (1) a card with at least one marked seat carries a `· off-role` **legend in its header bar**, after the side name inside a new `.cn-team-heading` leading group so the sum does not move, mono `t-xs` `dim` `0.08em` with the rows' own 6px `brand` dot as its key, an `aria-hidden` middot separator, accessible name `off-role seats in this card`; it never renders on a result card's header, in the seat rack or in an embed. (2) at **three or more** marked seats in one card the role icon and word go back to `dim` — one class, `.cn-team-many-off .cn-off { color: var(--cn-dim) }` — while the dotted underline, the dot before the name, the hidden `off-role` and the legend all stay. Lands with M3.19 in `TonightView.tsx` and `tonight.css` | The marker was inside the rows only, so the header a reader scans first said nothing about it; and on a ten-mid-mains night four amber role words plus four amber icons in one card were more lit area than that card's 4px side rule and side name together, so the card read as *the amber one* instead of as blue or red — a marker outranking the identity of the thing it marks, on a page whose first question is "which side am I on". Three of five is the majority: below it the marked seats are the minority and colour is the fastest way to find them; at or above it colour covers most of the card and points at nothing. Nothing is lost when the colour goes, because the underline is a shape rather than a hue, the dots the legend keys are ~36px² a row, the hidden word is what a listener hears, and the explanation line still prints `4 off-role: Hana at support, …` verbatim. Per card, because a card answers "which of *these five* seats", and a page-wide count would let a red seat change colour because of a blue one. | Leaving the header as name + sum (the defect); an amber legend word (three amber elements in a 32px bar, the same fight the threshold settles); a legend with the count (`2 off-role`) (the explanation line under the cards owns that number); the legend as a third flex child of the header (it would push the sum, and the two cards' sums would stop sharing an edge); `aria-hidden` on the whole legend (a listener gets no warning before five rows, where a reader gets one); no threshold (the amber card); a threshold at 2 or at 4 (neither is the majority line, so neither can be explained); inverting the mark at the threshold to colour the unmarked seats instead (a marker that means the opposite thing on the card next to it); dropping the dot or the underline as well at the threshold (the marker would then exist in the hidden text alone, and colour would be the only signal it ever had) |
| 2026-09-10 | `/p/[puuid]` lists unrated games in `Recent games` with `not rated` in the rating column, counting toward the five; refused games and not-yet-rebuilt backfills read identically; the chart, By role and the record stay rated-only; one hint sentence under the list when any row is unrated (product, M3.23). | A friend who played a nine-man at 2am and sees nothing on their page reads the page as broken; "every game played with a companion present is in the database" is a promise the page keeps on the night, not after a command runs. A blank cell in a column of numbers is a rendering bug to the reader. | Omitting unrated games (the incidental behaviour); a second vocabulary for refused vs backfilled; a blank cell. |
| 2026-09-10 | `0006_command_queue.sql` adds exactly the five columns the 2026-09-09 row named (`sent_at`, `attempts`, `result`, `error`, `expires_at`) and **no `lobby_id`**; a command is scoped to a lobby through its target player's `lobby_members` row instead (M4.1, server half) | A player is in at most one live lobby (`lobbies_active_party_idx`), so "this lobby's commands" is "these players' live commands" with no new column, no backfill for the rows already in the table and no second key to keep in step when a lobby cycle rolls over. The cost is one ten-row select before a supersede, on a transition that happens a few times a night. One consequence to know: `replaceMembers` runs before `restartClock` in `ingestLobby`, so a friend who leaves is already out of `lobby_members` when the lobby drops back to `open` and their own pending row is not superseded -- it sits until its TTL, which is the right answer anyway, since they are no longer in the lobby it would move them in. | A `lobby_id` column with an index (a schema decision the milestone did not make, for one query); superseding every live row of every player in the party by party id (the same select, spelled less directly) |
| 2026-09-10 | The server's per-kind gate is `COMMAND_KIND_ENABLED` in `apps/web/lib/commands/gate.ts`, all three kinds **off**, and `lib/commands` is a **directory** with an `index.ts` rather than the `lib/commands.ts` the M4.1/M4.3 briefs name (M4.1) | The gate is one table of booleans that must be flipped in the same commit as the reference row, so it gets its own file and its own doc comment rather than living halfway down a 400-line module. `@/lib/commands` still resolves to the same import path the briefs wrote, so nothing downstream changes; M4.3's `SWITCH_SIDE_ENABLED` is exported by name from there. | One flat `commands.ts` (the queue rules, the gate, the hook seam and the `balanced` writer in one file); a `lib/commands.ts` beside a `lib/commands/` directory (two modules one character apart) |
| 2026-09-10 | A lobby leaving `balanced` supersedes its pending `switch_side` rows **inside `moveLobby`** — plus the two exits that do not go through it, `restartClock` in `ingest/lobby.ts` and the two-hour sweep — with `error = 'superseded: lobby is now <status>'`. `create_lobby` and `invite` are never superseded by a transition and expire on their own TTL (M4.1) | The rows were written for a split that is no longer on the board, and three minutes is long enough to be somebody's champion select; `moveLobby` is the one function with the transition table, so a caller cannot forget the cleanup. The other two kinds are how a lobby gets filled in the first place — a lobby going `in_game` says nothing about whether an invite is still worth popping up. | A `lobby_left_balanced` hook (a seam with exactly one listener, and one more thing to register); letting the three-minute TTL do it (a friend dragged across the lobby in champion select); superseding every kind (an invite cancelled by the game it was for) |
| 2026-09-10 | `GET /api/companion/commands` parses its query string with `companionCommandsQuerySchema` (`clientConnected` required, `'true'`/`'false'`, a 400 otherwise), and `withCompanionIdentity` gains a `shouldTouch(request)` dependency so the `false` poll writes **no** `last_seen_at` (M4.1) | The query string is a boundary like a body, and the two answers differ in what they write, so a missing parameter must be a refusal rather than a default. `last_seen_at` means "at their PC with League open" (2026-09-09) and only the auth wrapper can decide not to write it — the handler runs after the touch. The flag is read from the URL before the token is looked up, so a poll that says the client is down touches nothing at all. | Defaulting the missing parameter to `false` (a silently empty answer forever if the companion ever drops the parameter); clearing `last_seen_at` back afterwards (a write is a write); a second route for the disconnected poll (two routes with one difference) |
| 2026-09-10 | A retryable nack leaves `attempts` where the hand-out put it, so three of them still fail the row with `not acked after 3 deliveries` rather than the TTL being the only limit (M4.1) | The wire contract's word is "`attempts` unchanged", and refunding the delivery would make a `not_connected` loop free: a companion whose client keeps dropping between the poll and the call would be handed the same row every five seconds for the whole TTL. Three tries and a five-minute worst case is the same shape as every other giving-up rule here. | Decrementing `attempts` on a retryable nack (the row can only be given up on by the sweep); not storing `error` for a retryable nack (the log line is the only trace of a client that will not answer) |
| 2026-09-10 | `promoteSplit` (M3.2's reroll) is the **second** caller of the switch-side queue writer, right after it flips `is_chosen`: it supersedes the previous split's rows with `superseded: another split was chosen` and queues the promoted split's, from the lobby's own `lobby_members` seats. Failures are one log line and never undo the promotion (M4.1, review finding) | A reroll never leaves `balanced`, so no transition fires and `moveLobby`'s cleanup never runs. With only the `onBalanced` hook wired, a reroll with the gate on would drag the ten to the sides the group had just rerolled away from for up to the three-minute TTL and send nobody to the new ones — the exact failure the queue exists to prevent. The writer's parameter was widened from `LobbyBalancedEvent` to "a split's two sides plus who is seated where" so the reroll path does not have to rebuild a `PoolMember`. | Emitting a synthetic `onBalanced` from `promoteSplit` (Discord would post the teams twice); a `balanced -> balanced` self-transition through `moveLobby` (a status write that changes nothing, and `lobbies.updated_at` moving would restart the stability clock on a reroll) |
| 2026-09-10 | The still-settling sentence on `/leaderboard` and `/p/[puuid]` becomes `The board sorts on Proven: your rating, minus how unsure the board still is about you. That gap shrinks as you play and settles after about 30 games.` (product, ruling on the designer's M3.19 finding; one constant, `SETTLING_SENTENCE` in `apps/web/lib/board/copy.ts`, with `30` still interpolated from `SETTLING_GAMES`) | The shipped second sentence, `New players start low on purpose and climb as they play.`, is false about the board it sits under. Every recognised tier seeds the same sigma (`config.ts`), so on a season's first board Proven orders exactly as rank does: nobody is low, nobody has climbed, and a Master-seeded friend with 0 games is rank 1 above someone with 40. The designer's diagnosis is taken and the replacement is rewritten twice over: the gloss is joined with a colon, like the M3.23 hint sentence, because no shipped board string uses an em-dash; and `It catches up after about 30 games` is refused because it repeats the old sentence's defect in a bigger way — Proven never catches Rating, and this document's own worked example prints Lena at 41 games with Rating `2088` and Proven `1548`. `settles` is what actually happens: sigma falls from 8.33 to about 5.00 between game 26 and 36 (M1.3) and then flattens. The new sentence is true on the first board, true for a 0-game Master, and true at game 200. | Keeping the shipped sentence (false, and the page it explains is the evidence); the designer's proposal verbatim (em-dash, and `catches up` promises a convergence that never happens); explaining sigma by name (jargon for a page twenty friends read on a phone); dropping the second sentence and shipping the gloss alone (nothing then says the gap moves, which is the only reason a new player cares) |
| 2026-09-10 | The nightly leaderboard embed's field is named `Top ten` only when ten lines print and `The board` when fewer; the rule keys on lines printed, not on how many players the season has (product, M3.22; `05-design.md`, "Nightly leaderboard embed") | With eight friends seeded, `Top ten` over eight lines names a slice that does not exist — the list is everyone, and a field name that is wrong on the group's own small nights is the kind of detail that makes the rest of the post look guessed at. `The board` is already friend-facing vocabulary: it opens the settling sentence both web pages print, so the embed invents no word. Keying on printed lines keeps it one comparison in one place and is unambiguous at exactly ten, where both names would be true. | `Top eight` and friends, spelled per count (a number in a heading that a reader must check against the list below it); `Standings` (retired 2026-09-09 — one thing, one name); `Leaderboard` (the embed title already ends in that word, one line above); dropping the field name (Discord requires one, and the list would then start under the title with nothing over it) |
| 2026-09-10 | **Two rules for a recent game's block on `/p/[puuid]`** (designer, from the M3.19 review; specified in `05-design.md`, "The player page"): (1) the block shows its side as the 3px leading rule **only** — no 10% side tint behind it; (2) the four teammate names carry a `line`-coloured underline at rest, raised to `currentColor` on hover and focus, the viewed player's row stays plain text, and the `brand` inset rule marks exactly one row, the player whose page it is, never also the signed-in viewer's seat. Lands with M3.19 in `app/board.css` and `app/_board/PlayerView.tsx` | The block's headline is `Won` / `Lost`, so a pink wash behind the word `Won` teaches a reader that red means lost — and side is the one fact this page deliberately does not lead with (`Red wins` beside your own delta makes you work out which side you were on first). The tint keeps its job on the tonight page's team cards, where "which side?" is the question. The links were `text-decoration: none` with a `:hover` affordance on a page opened from WhatsApp on a phone, where nothing hovers: four of five names went somewhere, one did not, and all five looked identical, so the one screen built for hopping between friends hid the hop. The `you` rule was drawn on both the page's subject and the viewer, which on a normal night is two amber rules in one five-seat lineup with no way to tell them apart. | Keeping the tint and colouring `Won` / `Lost` instead (two colour systems on one row, and a green win is not in the palette); a dotted underline for the links (that mark is the off-role seat's, and it is per-seat vocabulary on the tonight page); an arrow or `↗` after each name (four arrows in a five-row list is decoration, and the row is already the tap target); marking the viewer's seat with a second colour (a third meaning for an inset rule, on the one page that has no side to explain it) |
| 2026-09-10 | The board row is one component, `apps/web/app/_leaderboard/BoardRow.tsx`, wrapped by `BoardCard` / `TopOfBoard`, and its stylesheet `app/board-parts.css` is imported by the **component**, not by either page. `board.css` keeps only the page rules; `lib/board/load.ts` gains `loadTopPlayers(client, { limit })`, which is `loadBoard` plus a slice (M3.19) | The tonight page's rail renders the board's first five, so a second copy of the row would drift the first time a number moved. It cannot import `board.css`, though: that file defines `.cn-strip`, `.cn-block`, `.cn-notice`, `.cn-hint`, `.cn-duration` and `.cn-delta`, all of which `tonight.css` also owns, and the last stylesheet loaded would win at random. Splitting the shared pieces into a file the component imports is the framework's own answer and keeps one definition per rule. `loadTopPlayers` re-uses `loadBoard` rather than issuing its own ordered query so a rail that disagrees with the page it links to about who is first is impossible; the group is twenty rows and there is nothing to save by reading fewer. | Duplicating the row markup in the rail (two rows to keep in step); importing `board.css` on the tonight page (a second `.cn-strip` over `tonight.css`'s); extracting every board rule into `tonight.css` (couples the two pages' row heights, which the M3.5 decision row already refused); a separate `select ... limit 5` for the rail (a second ordering of the same board) |
| 2026-09-10 | `/leaderboard`'s and `/p/[puuid]`'s column is capped at **44rem from 720px up** in `board.css`, not stretched to the shell's 76rem; the two-column grid stays the property of the page that has a rail (M3.19) | `05-design.md` gives three widths and no fourth, and the 76rem shell is the *shell's* width, not a page's. A two-line row stretched to 1216px puts a name and its Proven number 1100px apart, which is the sparse look Floodlit exists to end. The board has no rail of its own — the rail's one board card is the tonight page's, and a `Top of the board` beside the board would be the same rows twice on one screen. | Stretching the board to 76rem (a row whose two ends are a screen apart); giving the board pages the 1fr/20rem grid and a rail (the board beside the board); a fourth breakpoint for wide boards |
| 2026-09-10 | On `/p/[puuid]` the two numbers are **labels on one line with the numbers hanging under them** (`.cn-numbers { align-items: baseline }`), `Proven` at `t-display` and `Rating` at `t-md`, in the shipped order `Rating` then `Proven`; the `settling` chip sits on the labels' line (M3.19) | `05-design.md` fixes the sizes (`the two numbers are t-display and t-md`) and the content order is M3.5's, which this task may not move. Bottom-aligning two numbers that differ by 25px puts their labels at two different heights and the smaller label reads as a caption for the bigger number — measured on the first render of this task. Aligning on the labels' baseline is the only arrangement where both names sit on one line and each number is unambiguously under its own. | Bottom-aligned numbers (the rendered defect); putting `Proven` first (moves M3.5's settled content order); both numbers at `t-display` (two 44px numbers with no primary); the labels after the numbers (`1434 Rating` reads as a sentence, and the board row already owns that shape) |
| 2026-09-10 | A nameless board row's link carries `, rank <n>` in visually-hidden text, with a comma; the visible `Someone` stays one word (M3.19, the designer's M3.5 review) | Two nameless players are two links called `Someone`, and a screen reader's link list then has two identical entries. The rank is already on screen beside the name, so nothing new is shown. The comma is not decoration: an accessible name concatenates its parts with no separator, so a leading space is trimmed and ` rank 1` is announced as `Someonerank 1` — measured against Testing Library's accname implementation. | The puuid as the disambiguator (M3.10 forbids a puuid on the page); a visible rank suffix (a second rank on a row that already has one); no disambiguator (two identical links) |
| 2026-09-10 | The chart's seed line is padded by **0.15 of the span** on the side it widened, against the series' own 0.05 (M3.19, the designer's M3.5 review), and the chart's accessible label names the count: `Rating over 1 game, from … to …, seed ….` | At 5% a seed outside the series lands two or three pixels inside a 140px plot, so the hairline draws under the edge of the 1.5px stroke and the `seed` label sits half off the box — a reference line the reader cannot see is a chart with no reference. The label's count comes from `history.length - 1`, because the series is the rating carried *into* the first game plus the rating out of every game since; `gamesLabel` is the same function the record beside it uses, so `1 game` never reads `1 games`. | Padding both sides by 0.15 (the series stops filling its own plot); clamping the label inside the box only (the hairline is still under the stroke); a fixed pixel pad (a number that stops being right the first time the height changes) |
| 2026-09-10 | The nightly leaderboard embed's field is `Top ten` **only when ten lines print** and `The board` otherwise, from one pure `leaderboardFieldName(lines)` (M3.22, product 2026-09-09) | With eight players seeded the post read `Top ten` over eight lines, in a channel where the whole group can count them. `The board` is true at any length and is the destination's own noun in a shipped sentence (`Ratings are updated. The leaderboard has the rest.`), so it invents no fourth name for the page. A pure function rather than a ternary in the builder because the rule is a rule, not a formatting detail, and it is asserted at eight, ten and eleven entries. | `Top 8` / a count in the name (a field name that changes every night); `The board` always (ten lines is the post the design doc worked out and the group knows); padding the list to ten with seeded rows (a ranking of a season that has not happened) |
| 2026-09-10 | `Recent games` on `/p/[puuid]` lists the last five games the player played, **rated or not**; an unrated one counts toward the five and its rating column reads `not rated` — no delta, no em-dash, no visually-hidden `Rating` — with one sentence under the list while any row reads it. Everything the fold makes stays rated-only: the two numbers, the chart, the seed, `By role` and the `37 games · 20W 17L` record. `lib/board/recent.ts` is the pure half (M3.23, product 2026-09-10) | The loader filtered `mu_after !== null` before taking the five, so a backfilled game — every one of them until `rebuild-ratings` runs — was missing from the list with nothing saying why, while it sat in the database and on `/admin/games`. A gap in a list of five is a page disagreeing with the night the reader remembers. One vocabulary for a refused game and a not-yet-rebuilt one because the reader's question is the same either way, and the explanation belongs in one sentence under the list rather than five words per row. The record may now count fewer games than the list shows, which is the honest reading: it counts what moved the number. | Omitting unrated games (the defect); an em-dash or a blank cell (a column that fails to load); a second wording for backfilled games (two vocabularies for one fact); counting unrated games in the record (a games total that disagrees with `ratings.games` and with `/stats`) |
| 2026-09-10 | A team card whose seats include one off their role carries a `· off-role` legend in its header bar — the middot in its own `aria-hidden` span, the same 6px `brand` dot the rows carry, the word as a mono micro-label in `dim`, and `seats in this card` hidden for a listener — inside a new leading group so the sum stays the header's last flex child. At **three or more** marked seats in that card, `.cn-team-many-off` puts the role icon and word back to `dim` while the underline, the dot, the hidden word and the legend all stay (M3.19, the designer's Floodlit re-check) | The header is what the eye reads first on a card with a 4px side rule and a display-cut name, and it said nothing about the amber dots below it; the legend is the key to the mark. It is per card, never per page, because the question a card answers is `which of these five seats`. The threshold is the same arithmetic: three of five is the majority, and colour spread over the majority points at nothing — four amber role words are more lit area than the card's own side rule, so the card starts reading as *the amber one* rather than as blue or red, and side is the first question this page answers. Nothing is removed, because the underline is a shape rather than a hue and the marker's non-colour signals were always the pair to its colour. | The legend as a third header child (the sum moves when the legend appears); a CSS `::before` middot (generated content is announced); the bare word `off-role` as the accessible name (reads as a property of the side after `RED`); inverting the mark onto the unmarked seats at three (a marker that means the opposite on the card beside it); a page-wide count (a red seat changing colour because of blue); dropping the dot or the underline as well (colour would be the only signal, and the legend would be a key to nothing) |
| 2026-09-10 | `/api/me/*` is the product's **third route class**: a Supabase session with a linked player and no `is_admin`. `lib/me/identity.ts` resolves session → Discord identity → `players.discord_id` → the player row and hands the handler `player: null` rather than a 403, because `POST /api/me/link` exists for exactly that visitor; `lib/me/route.ts` is its `withAdminAuth`. A body may name another player's PUUID and it is honoured **only** for an admin — 403 otherwise, checked before any read, and never a silent write to the caller's own row (M3.6) | The brief settled who may set a role and the route had to have somewhere to live. It is `withAdminAuth` step for step — client, session, zod parse, envelope for JSON and 303 for a form — with one step removed, so there is one shape of route in this app and not two. The unlinked case cannot be a refusal at the gate without making the self-link impossible for the person who needs it. The 403 comes first because "names somebody else" is a fact about the body alone: answering it before the lobby read means a stranger's probe learns nothing about which lobbies exist. | Reusing `withAdminAuth` and flagging every friend as an admin (the whole point is that they are not); one route that takes a session *or* a companion token (two auth models in one handler); trusting `puuid` and falling back to the caller's own row when it does not match (a body that silently writes somewhere else); putting the rules in the route handler rather than behind a small store interface (untestable without a database) |
| 2026-09-10 | The role control is a card **below the rack and last in the main column**, in every state, rather than five words inside the viewer's own 44px rack row (M3.6, and `05-design.md` has no entry for it) | The rack is ten rows at every count and a row is exactly 44px (`rowHeight.test.tsx` guards the arithmetic); five 44 × 44px targets do not fit inside one, and a row that grew when the viewer signed in would move the block under a thumb — the one thing the tonight page's layout rules forbid. Last in the column means the card can appear, change and disappear without touching a pixel above it, and it is the same position in `filling`, `balanced` and `in_game`, so a friend finds it in the same place all night. The `you` rule on the rack row is what ties the card to its owner. | Inside the viewer's row (does not fit, and moves the rack); above the primary block (a control over the teams, on the screen people open to read the teams); a floating bar (the design has no overlays); a separate page (a tap that costs a navigation is not a tap) |
| 2026-09-10 | The seat rack prints **tonight's** roles: `lib/tonight/roles.ts` asks core which role it does not call off-role (`isOffRole` over `ROLES`) instead of restating `resolveRoles`, and a row with an override shows that role **alone** — no ` · backup` (M3.6) | The row had to stop saying `top · mid` beside a player whose override the bot is about to build teams from, and the web app may not hold a second copy of the role rule. `resolveRoles` is not on `@customs/core`'s public surface and the task forbade changing `packages/core`, so the main is read out of the one predicate that is exported — a question about core's rule rather than a copy of it, which cannot drift. The backup cannot be asked for the same way, because `isOffRole` is true for a secondary and a fill alike; showing the override alone is also the honest reading of what the player chose. | Restating `resolveRoles` in `apps/web` (two definitions of what an override means); exporting `resolveRoles` from core (a change to `packages/core`, left to the lead); leaving the row on the profile's roles (the page contradicting the balancer on the same screen) |
| 2026-09-10 | A role override is written by `POST /api/me/role-tonight` and **carried onto the party's next cycle by lobby ingest** (`lib/ingest/roleCarry.ts`), keyed on the previous cycle having started inside the current 06:00-to-06:00 night; migration `0007` rewrites the column comment `0001` left behind. Nothing is cleared on `finished` (M3.6, implementing the 2026-09-09 decision) | The decision row of 2026-09-09 settled the rule and left the mechanism open. Ingest is the only place that knows a new cycle has been opened for a party, so it is the only place the copy can happen without a scheduled job or a second source of truth. The rule itself is a pure function over the previous cycle's timestamp and the new cycle's members, so "a new night starts flexible" is a unit test rather than a night of waiting; the I/O around it is one read and at most five grouped updates. | A night-scoped preferences table (a second source of truth for a value that has a column); clearing on `finished` (the retired rule — a re-tap between every game); copying in the balancer (a balance is not the only thing that reads the column, and a rebalance would re-apply a value a friend had just cleared) |
| 2026-09-10 | M4.2's press is `POST /api/admin/lobbies/start` on the **admin** route class (Supabase session + `players.is_admin`), not the brief's `/api/lobbies/start` open to any linked player; the rules live in `apps/web/lib/admin/lobbyStart.ts` beside `reroll.ts` rather than in `lib/lobbyStart.ts` (M4.2, server half) | The brief hangs "a signed-in visitor whose Supabase session matches a `players` row" off M3.6's third route class, and M3.6 has not landed: there is no such class to hang it on, and inventing a second one for one route would be the thing M3.6 then has to unpick. Everything that decides anything is in the helper and the wrapper is the only thing that knows who may press, so widening it when M3.6 lands is an auth swap and a moved file, not a rewrite. Admin-only also matches what is renderable today — `currentViewer()` already tells the tonight page whether the reader is an admin, and the reroll control beside it is drawn on exactly that. | A third auth class invented here (M3.6 owns that decision); leaving the route unbuilt until M3.6 (M4 is the milestone in progress and the queue under it is already wired); shipping it open to any session (a stranger with a Discord account could open a lobby on a friend's PC) |
| 2026-09-10 | Every read the press and the fan-out make is bounded by `now` **at both ends** — the night's 06:00 in `CUSTOMS_NIGHT_TZ` and `now` itself — and the double-tap lock matches a live `create_lobby` on `expires_at ∈ (now, now + the kind's TTL]` rather than on `created_at` (M4.2) | "A lobby of tonight" and "a create that is still pending" are both statements about one night, and an open-ended `expires_at > now()` also answers yes to a stuck row from three nights ago. `created_at` cannot carry the bound because `enqueueCommands` leaves it to the database's own `now()` while the writer's clock is injected, so the two disagree the moment a test moves the clock. The upper bound is a no-op in production — nothing is created in the future — and it is what makes an injected instant name exactly one night, which is why `start.integration.test.ts` can run at a fixed 2019 instant on a stack a dozen other runs are writing to and see only its own rows. | `expires_at > now()` alone (another agent's pending create refuses the press, and a shared stack makes the test flaky); bounding on `created_at` (the column the writer does not set); a `night_id` column on `companion_commands` (a schema decision M4.1 deliberately did not make) |
| 2026-09-10 | The refusals are decided in the order **gate → live lobby → pending create → nobody around**, and `create_lobby` and `invite` gate the route together (M4.2) | Not the brief's table order, and the swap matters: two people tapping two seconds apart must read `A lobby is already being opened.` rather than `Nobody has the companion running right now.` when the chosen host's poll has not landed yet — the pending row *is* the lock, so it outranks the host check. The gate goes first so a deployment whose reference rows are not green does not read the database at all. The two kinds gate together because a lobby that opens and then invites nobody is worse than no lobby: the friend who tapped would have to invite nine people by hand, from a client they did not open. | The brief's literal order (a double tap answered with the wrong sentence); gating only on `create_lobby` (a lobby with no invites); checking the gate last (four queries for an answer that was fixed before the request arrived) |
| 2026-09-10 | The invite fan-out is `apps/web/lib/commands/invites.ts`, registered on M4.1's `onAcked` seam by `lib/commands/register.ts`, which the two ack routes now import for the side effect; a player already holding a live `invite` for that host is skipped, and the fan-out's "most recently active first" is the writer's order, not a column (M4.2) | The fan-out writes queue rows, so it belongs with the queue — putting it in `lib/admin` would make `lib/commands/register.ts` import `lib/admin`, which already imports `lib/commands`. Registering it in `register.ts` keeps the one seam rule M4.1 wrote: with that import removed, an ack behaves identically and nobody listens. The skip is the second half of idempotence (the compare-and-set ack is the first): two `create_lobby` acks in one night add no second popup for anybody. The order cannot live in `created_at` because every row of one insert shares it to the microsecond, so the queue's oldest-first hand-out cannot reproduce it; `fanOutInvites` returns the order it wrote and the test asserts that, which is the honest claim. | The fan-out in `lib/admin/lobbyStart.ts` (an import cycle); the ack route calling it directly (the queue would learn what a command is for); staggering `created_at` per row to make the order a column (rewriting M4.1's writer for a popup order nobody can perceive) |
| 2026-09-10 | `startLobbySentence` maps a `create_lobby` row to the page's line: pending or sent → `Opening a lobby on <Name>'s PC…`, acked → **nothing**, failed with `already_in_lobby` → `<Name> already has a lobby open — everyone can join that one.`, and **every other failure** → `Nobody's client answered. Try again.` (M4.2) | Product wrote the last sentence for the expiry and wrote no other failure line, and there are nine more words in `commandFailureReasonSchema`. From the friend holding the phone a `wrong_phase` (the host is in champion select) and a client that refused the POST are the same fact — nothing was created, press it again — so one sentence is true of all of them and the alternative is the queue's vocabulary printed on the tonight page. It is a pure function rather than a ternary in a component so `/` and `/admin` cannot end up saying two different things about one row. | A sentence per failure reason (nine strings product did not write, and `not_on_a_team` on the tonight page); printing the nack text (it can carry a client status code); saying nothing on a failure (the button comes back with no explanation for why the lobby never appeared) |
| 2026-09-10 | `resolveRoles` is re-exported from `@customs/core`'s entry point, with its input and output named `RoleProfile` (`Pick<BalancePlayer, 'mainRole' \| 'secondaryRole' \| 'roleOverride'>`) and `ResolvedRoles` (`{ main, secondary }`), so the tonight page prints an overridden row as `<override> · <old main>` from the same rule the balancer scores with (M3.6 support) | Same reasoning as the `isOffRole` row of 2026-09-08: the page needs tonight's effective pair, and restating "override becomes main, old main becomes backup, equal override is a no-op, null main is flexible" in the web app is a second copy of the role model. No logic moved; the two types only give names to what the function already took and returned. The role model's public surface is now exactly two functions and stays that way — `roleTier` remains internal because no surface needs the three-way tier. | Exporting `roleTier` too (nothing renders `secondary` vs `fill`); having the web app compute `roleOverride ?? mainRole` (wrong on the equal-override and null-main cases, and silently so) |
| 2026-09-10 | M3.6's nine refusal sentences are product's, not the route's: eight are replaced and one (`No player with that id.`) is kept because the page cannot reach it, and every M3.6 string now sits in `05-design.md`'s copy table beside the tonight page's. The rule the replacements follow — **say what happened, then what to do next or who can undo it; never the database's vocabulary; never apologise; never name a page the reader cannot open** — governs the next refusal too, and rendered strings use the ASCII apostrophe | A refusal is friend-facing copy that happens to travel over HTTP, and three of them told a friend on a phone about a `row` in `lobby_members` — a table this product exists to never make anybody fill in. Four more were dead ends (`Nothing to set a role on.`) where the fix was one clause away, and `Pick yourself out of the lobby first` reads as *leave the lobby* to somebody standing in one. `Only somebody in tonight’s lobby can be picked.` was also the one rendered string in the app with a curly apostrophe, two files from `That's me`. Putting them in the copy table makes the next engineer copy a settled sentence instead of writing a ninth. | Keeping the engineer's nine (`row` on a friend's phone, and no fix in any of them); one generic `That did not work.` for all of them (the reader learns nothing and taps again into the same wall); rewriting `Someone is already linked to that player.` for consistency with its neighbours (it is product's own from the M3.6 brief and is pinned by acceptance check 3, so a reword would be a silent change to an acceptance criterion); leaving refusals to the route and out of the design doc (the split that produced them) |
| 2026-09-10 | The role control's `balanced`/`in_game` hint becomes `Teams are already set. A role you pick now is what the bot tries for in the next game.` (was `Saved for the next game. Teams are already set.`), the signed-out card splits into the settled sentence plus a new button label `Sign in with Discord`, and the viewer's own heading reads `Your role tonight · <name>` when that player has a display name (M3.6, the designer's review) | One hint per state instead of two stacked ones changes what each sentence has to carry alone. `Saved for the next game.` was a receipt: as the state's only line it greets a friend who has tapped nothing with the claim that a role was saved for them, and it lost the limiter the sentence above it used to supply. The replacement is a rule rather than a receipt, true before and after a tap, and it borrows the `open` hint's own verb so the two states speak one vocabulary; the receipt stays what the brief made it, the role word turning `brand`. A button is named for the act and the sentence above it carries the reason, which is why the OAuth label is three words. The name in the heading is for the phone that gets passed around so somebody can see the teams: without it a tap lands on the lender's row and the screen gives no way to tell. **M3.6's acceptance check 3 quotes the old hint and needs the same edit before the task is judged.** | Keeping `Saved for the next game. Teams are already set.` unstacked (a save nobody made); keeping both sentences stacked in every state (the designer's card, and two hints where one state has one fact); `Sign in with Discord to pick your role.` as the button label (a sentence in a button, and the reason lost when the card has no body); `Your role tonight · Someone` for a nameless player (answering *who is you* with the word that raised the question); the name repeated on `/admin/players`' role tap (M3.25), where every row already carries its own name |
| 2026-09-10 | The Start-a-lobby lock is a **global** partial unique index — `companion_commands_one_create_lobby_idx` on `(kind) where kind = 'create_lobby' and status in ('pending', 'sent')` (`0008`) — not per host and not per night; the read-first refusal in `decideStart` stays as the friendly path, `enqueueCommands` turns a `23505` on that index into `skipped: [{ kind: 'create_lobby', reason: 'conflict' }]` instead of throwing, and `startLobby` maps it to the same 409 and the same `A lobby is already being opened.` (M4.9) | M4.2's refusal table names no host and no night, and the read it describes never filtered by player. Per host would leave the worse half of the race — two admins with two companions, two lobbies, two invite fan-outs — wide open, which is what the reviewer found. Per night cannot be an index predicate at all: a night boundary is `06:00` in `CUSTOMS_NIGHT_TZ` and `timestamptz at time zone <text>` is STABLE, not IMMUTABLE. It also buys nothing, because a `create_lobby` lives 60 seconds and a minute never spans two nights. | Per `target_player_id` (leaves the two-lobby case); a night-bucket expression index (not immutable); an advisory lock or `serializable` in the route (a second concurrency model for one row); leaving the read-then-insert and accepting the misleading `already_in_lobby` sentence |
| 2026-09-10 | `startLobby` runs `sweepExpiredCommands` **before** it reads the night, after the verification gate (M4.9) | `expires_at` cannot be in the index predicate, so an expired-but-unswept `create_lobby` would hold a table-wide lock. The sweep normally rides on every companion poll; a host whose client went away between its last poll and this press is exactly the case where it has not run, and that must not cost the group its next lobby. One statement, the same one the poll runs, and it settles nothing still in date. | Leaving expiry to the poll only (a dead lock survives up to the host window); putting `now()` in the predicate (not immutable); a `create_lobby` TTL short enough not to matter (60 s already is, and it still leaves the hole) |
| 2026-09-10 | **The M3.6 control is a card, last in the main column, carrying the rack's own 2px `brand` "you" rule; its title is Archivo `t-sm` 600 (not the mono micro-label the first build shipped); role chips are capped at `9rem` and left-aligned, `text` at rest with a `dim` icon, and the chosen one gains `inset 0 0 0 1px var(--cn-brand)`; `That's me` is a rack-shaped row list whose target is a discrete button, `minmax(0, 16rem) auto` from 720px; and the product gains one focus ring, 2px `brand` at 2px offset** (designer 2026-09-10, the M3.6 review; specified in `05-design.md`, "`Your role tonight`, and picking yourself") | A rack row is 44px and the rack may not shift, so five 44px targets cannot live in it — but a card floating at the bottom of the column was tied to nobody, and the 2px inset rule is the mark the page already uses for "you". At rest the chips were `dim` mono role words with `dim` icons, byte for byte the treatment the rack uses fifty pixels above for a fact nobody can press, so nothing on the card looked tappable; `1fr` chips stretched to 245px at 1280, five bars around a 12px word. In light the unselected `raise` chips out-weighed the chosen one's 12% wash, which made the product's only receipt the palest thing in its row. The pick list's target stays a button rather than the whole row because a claim is a one-way door — 409 on the second, undone only by an admin on `/admin/players` — and `space-between` at 1280 put a name and its button 1000px apart, nine times. The ring lands here because this is the first screen with a cluster of targets, and `brand` at `:focus-visible` needs no fifth colour. | Role taps inside the rack row (the row is 44px and the rack is the one block that may not move); the control in the rail (the rail never carries state); the 3px leading rule as the tie (that mark means "the bot's own sentence" on the explanation and sit-out strips); amber on all five chips (five lamps on one card); a full-row target on the pick list (a one-way identity claim under a thumb); a chip grid of names (a wall of unequal words with no scanning edge); the sign-in moved to the top bar (a control with no object, on a bar that carries identity and destinations); the browser's own focus outline (white in one engine, blue in another, neither theme's) |
| 2026-09-10 | **No Discord voice split.** M4.4 (move linked members into blue and red voice on `balanced`, merge on `finished`) and M4.6 (deploy the bot) are dropped at the user's request; M4.5 presence is deferred until asked for. `apps/discord` stays a placeholder; Discord remains webhook-only (teams, result, nightly board). | The user: "i'm not interested on auto separating players into channels". The group talks in one voice channel and does not want to be moved. | Building it anyway (a bot needs a token, a host and Move Members permission for a feature nobody asked for); keeping presence alone (a bot for one message). |
| 2026-09-10 | **Ratings never reset. A "season" becomes a time window the same board is read through**: `This week` and `Last week` (Monday 06:00 to Monday 06:00 in `CUSTOMS_NIGHT_TZ`), `This month` and `Last month` (calendar month on the same 06:00 boundary), `All time`. A game belongs to the week and month its `started_at` falls in. The window changes who is on the board and their record and climb over those days; the sort stays Proven descending and every rating is the one running fold. `/leaderboard` defaults to `This week`, `/p/[puuid]` to `All time`, `/stats` to `This month`. Every Monday and every 1st, the closed window's board and its three awards post to Discord from a cron route, no button. Award minimums scale with the window: 6/4/4 games in a week, 15/10/8 in a month (M5.3 dropped; M5.9, M5.12, M5.10, M5.13) | The user asked for "a weekly or daily automatic session" and for season creation to go. A window is a filter over games, not a rating event, so it can close every week without touching anybody's number — which is the only way a weekly rhythm and a trustworthy rating can both exist on a group that plays one to three games a night. It also gives the week the thing seasons were wanted for: something that ends by itself and something to win by Friday. `Last week` and `Last month` are on the picker because the Monday post links to a board and the awards have to be readable somewhere a reader can navigate back to. | **Daily** (one to three games is not a board and three awards on two games is noise with a trophy on it); **a weekly sigma reset** (with carry-over's reset everyone would be "settling" every week, so the board would never mean anything); **sorting a window by who climbed most in it** (a second ranking with a second meaning, on a product whose whole point is one number to argue about); keeping manual seasons alongside windows (two things that both claim to end the month) |
| 2026-09-10 | **Season creation is removed** (M5.14): `/admin/seasons` loses its name field, its typed confirmation (M3.9 retires with it) and its `Start` button, and `POST /api/admin/seasons` goes. The one `seasons` row is the one `0001_init.sql` already inserts (fixed id, active), and after this nothing can make a second; `start_season()` and `set_active_season()` stay in the database unreachable, because an applied migration is never edited. The three no-season sentences are rewritten to promise no button | The user: "remove session creating." With M5.3 dropped, the button does nothing anybody wants and one thing nobody does — it empties the board with no undo, two fields away from "set a role". The safest version of a destructive button nobody needs is the one that is not there. No migration is needed: `0001` already seeds one and `start_season` was the only thing that ever closed it, so a database can no longer end up with none — which matters because M2.18 refuses every game insert without an active season. | Leaving the button and documenting it (M3.9 already tried that); dropping the table (every `games.season_id` points at it); renaming the existing production row `gamesd` (its name is never printed to a friend again, so it costs nothing where it is) |
| 2026-09-10 | **A player's main and backup are inferred from their own games** (M5.16, M5.17): the most and second-most frequent `game_players.role` over their last 20 counted games, ties to the more recent, `flexible` under 3 games; **a game where the balancer put them off both their then-roles does not count**; recomputed after every rated game and after every rebuild into the same `players.main_role` / `secondary_role` the balancer already reads. `/admin/players` loses its two role selectors and shows the pair read-only with the count behind it; hand-set M1 roles are overwritten by the first recompute | The user asked for roles assigned by frequency and changing over time, and for the admin selector to go. Roles were the last hand-entered data in a product whose claim is that nobody types anything, and a hand-set role goes stale silently. The off-role exclusion is the whole design: without it a support who gets filled into jungle twice becomes a jungle main and is never a support again — the product would invent a role for somebody and then insist on it. The role-for-tonight tap (M3.6) stays as the deliberate way to change: tap it, play it on-role, and it counts. | A lifetime frequency count (a role you played all last year outranks the one you played all month); counting every game (the feedback loop above); a longer window than 20 (a real change would take two months to show); keeping the admin selectors as an override (an override nobody can see the effect of, on data that recomputes itself) |
| 2026-09-10 | **Per-role ratings rejected for now** (M5.15). One rating per player stays; `/p/[puuid]` explains it instead — the win chance the balancer gave your side, the result and the change, per game, plus the seed line | Five ratings each needing ~30 games to settle is months of nightly play per person on a group playing one to three games a night, so every per-role number would sit permanently in the `settling` state and the board would get less trustworthy, not more. The balancer already charges for playing off your main, and "how do I do on jungle" is a win rate on `/stats`, not a second rating. **Revisit if** several players' per-role win rates diverge sharply after 100+ games each — that evidence would already be on `/stats`. | Building five ratings now; a role modifier on the one rating (the same settling problem with a smaller number and no way to explain it) |
| 2026-09-10 | `game_players.counts_for_role_inference` is a boolean written at fold time (M5.17): true when the balancer put the player on their then-main or backup, or when the game was not balanced by us (backfill). | Whether a player was filled cannot be reconstructed later from roles that have since changed; recording the fact at the moment it is known is the only honest source for the feedback-loop guard (lead, approving product's M5.17 ask). | Deriving it at recompute time from the split's role plan (breaks the moment the inferred roles move); counting every game (the loop product's decision row refuses). |
| 2026-09-10 | `inferRoles` (M5.16) **orders the games by `startedAt` itself**, newest first, instead of trusting the caller's order; `startedAt` is `string \| number` (epoch millis or the ISO-8601 text the database hands over, parsed with `Date.parse`, an unparseable string sorting as the oldest); and a count tie whose two newest games share the same instant falls to **lane order** (`ROLES`), never to list order. The window and threshold live in `config.roles = { inferenceWindow: 20, minGames: 3 }`. | The brief's tiebreak is "the role whose most recent game is later", which is a fact about timestamps, not about array positions, so the function reads the timestamps: a caller that hands the list oldest-first, or a rebuild that folds in a different order, gets the same pair, and "your last 20 games by date" is the one sentence a friend who thinks the bot is rigged can check against their own history. `Date.parse` on ISO-8601 is deterministic and no clock, so core stays pure. The equal-instant tie cannot happen for one player (one game at a time) and is pinned only so the answer is never insert order. | Trusting the caller's order and tie-breaking by index (`startedAt` carried but unread, and a reordered list silently changes a main); accepting only numbers (every caller converts, and Supabase already hands ISO text); a random or alphabetical tie for the same instant (alphabetical is arbitrary in a product that speaks lane order everywhere else). |
| 2026-09-10 | **Backfilled roles stay null on 16.17: the `timeline.lane`/`timeline.role` table (`MATCH_TIMELINE_ROLES`, M5.18) is empty until a live 5v5 capture proves a pair.** The mechanism ships — `mapMatchDetail` reads the table, the companion logs each unknown pair once per process, `timeline-roles` prints the cross-check from fixtures, `smoke --game-id a,b,c` saves several details — and a row is added only with the confusion table beside it in `03-lcu-reference.md`. | The fixtures hold one overlapping game (aborted, one participant, `NONE+SOLO` vs `MIDDLE`), and the one full custom detail contradicts the plain reading of the pairs the brief expected to be safe: two of its four `JUNGLE+NONE` players had no Smite and 1 and 4 neutral minions in 49 minutes, its one `TOP+SOLO` player had 76 cs and 40 wards. A classifier wrong 3 times in 10 moves a main (M5.16) silently; the brief says null over a guess. | Mapping `TOP+SOLO` / `JUNGLE+NONE` / `MIDDLE+SOLO` on their names (the fixture refutes two of them); reading Smite, wards and cs into a role (a heuristic product has not approved — raised as a question, not built) |
| 2026-09-10 | The **admin's per-row role tap on the tonight page is struck from M3.6** (lead, 2026-09-10). `POST /api/me/role-tonight` keeps its admin `puuid` support and its tests; the control moves to `/admin/players` with M3.25 | A rack row is exactly 44px and the rack is ten rows at every count, so five 44px targets do not fit in one and the design system has no per-row affordance to borrow. The friend-facing half — the case in the brief's own scene — ships now rather than waiting for a control only two people would ever press, and the route being ready means the admin half is a rendering task and not a second design round. | Shipping a cramped per-row control against no design; blocking M3.6 on it; dropping admin `puuid` support from the route (then the admin control would need a second endpoint later) |
| 2026-09-10 | An override is put back on **every row lobby ingest creates**, not only when it opens a cycle, from two sources in order: a row the same post is deleting (read before the delete) and the party's previous cycle inside the night. Rows that already exist are never written to (M3.6, the reviewer's 2026-09-10 defect) | The first version ran only for a lobby row it created, so a friend who tapped a role, dropped out of the client lobby for a moment and rejoined lost their choice: `replaceMembers` deletes the row and the next post inserts a fresh one. The brief's own edge case says a row recreated inside the night's party gets the carry like any other. Never touching an existing row is the other half of the rule: a re-post must not re-apply an old value over a tap that has just landed, and the upsert already leaves `role_override` alone. | Running the carry on every post for every row (clobbers a fresh tap); keeping the departed row in the table (a member who has left would be balanced, seated and rendered); a night-scoped preferences table (rejected on 2026-09-09) |
| 2026-09-10 | The `That's me` list is built from a **server-side** list of tonight's members whose `discord_id` is null, carried in the `unlinked` viewer state (M3.6, the reviewer's 2026-09-10 defect) | The brief says a player somebody is already linked to is *not offered*, and the tonight page reads with the anon key, which cannot see `discord_id` at all — the first version offered everybody and let the route refuse. Deciding it in `lib/me/claimable.ts` with the service role keeps the rule where the data is and sends the page nothing but the PUUIDs it may draw: no Discord id, and no fact about who is linked, reaches a browser. The route re-checks both rules, so this decides what is drawn and never what is allowed. | Sending `discord_id` (or a boolean per row) to the page (leaks who is linked); letting the route refuse a tap the page invited (a list where most rows fail); a public view exposing "is linked" (a new relation for one screen) |
| 2026-09-10 | `ACTIVE_LOBBY_STATUSES` and `isActiveLobbyStatus` move from `lib/ingest/lobby.ts` to `lib/lobbyState.ts`, and ingest re-exports them unchanged (M3.6) | The tonight page's role control asks the same question in the browser — is this lobby still live — and importing the ingest module for a predicate would pull the whole ingest, and the service-role client with it, into the client bundle. `lobbyState.ts` is already the state module and is already in that graph. Every existing importer keeps the name it had. | A second copy of the three statuses in the component (two definitions of "live"); importing `lib/ingest/lobby` from a client component (ships the ingest to the phone); a new tiny module for one array |
| 2026-09-10 | **Supersedes the "two sources" row above.** A role for tonight is a **preference of the player for the night**: `players.role_tonight` with `players.role_tonight_until` set to 06:00 local at the end of the night it was picked in (migration `0009`, real DDL — the earlier comment-only 0007/0009 is superseded with it). The tap writes that pair **and** `lobby_members.role_override` on the live row; lobby ingest gives every member row it creates `role_override = players.role_tonight` while the expiry is in the future; clearing writes null to both; nothing is swept and nothing is carried across nights (M3.6, the reviewer's second pass) | The carry that read the previous cycle and the rows a post was deleting could not work, and the reviewer proved it on the stack: `replaceMembers` computes `inserted` and `departed` from the same read, so they are disjoint by construction and the pre-delete source never contributed. A friend who tapped `mid` in the night's **first** cycle, closed the client lobby for a moment and rejoined got `null`, because the deleted row was the only copy. `lobby_members` rows are per game cycle and are replaced by every roster post, so they can hold what the balancer reads but never what the player chose. One column on `players` plus an expiry makes "a new cycle", "a rejoin" and "a re-created row" one rule, makes a new night free (an expired pair is simply not selected), and keeps the balancer path untouched. | Keeping the two-source carry (does not work, and is three code paths for one rule); a night-scoped preferences table (a table for two columns, and a second row to keep in step with the player); clearing on `finished` (the retired rule — a re-tap between every game); a nightly sweep of the column (a cron job to express `where role_tonight_until > now()`); writing only `players` and having the balancer read it (every reader of `lobby_members.role_override` — core, the embeds, the page — would need the same join) |
| 2026-09-10 | **The inferred pair is stored in `players.main_role` / `secondary_role` with two new columns beside it** (M5.17, migration `0010_inferred_roles.sql`): `roles_inferred_at timestamptz` (null means never) and `roles_counted integer not null default 0`, so `/admin/players` can print `support · jungle · from 17 games`, `flexible · from 2 games` under the threshold, or `flexible · no games yet`, and no page has to handle a null int. `game_players.counts_for_role_inference boolean not null default true` is in the same migration | The pair goes in the columns the balancer already reads, so nothing downstream moves and the M1-era hand-set roles are simply overwritten by the first recompute. The count is stored rather than recomputed on page load because the page is a plain server render of `players` and a per-row `inferRoles` over everybody's history would be twenty selects to print twenty short strings. `default 0` rather than a nullable int because "never computed" and "no rated game yet" are the same sentence to a reader (`flexible · no games yet`) and there is no third case. | A separate `inferred_roles` table (a join for two enums the balancer reads on every balance); recomputing the count in the page loader (twenty extra selects for a caption); a nullable `roles_counted` (two null cases, one sentence) |
| 2026-09-10 | **The recompute runs in exactly two places and is never fatal** (M5.17): after a *rated* fold in `rateStoredGame`, for the ten who played, wrapped in a try/catch that logs; and at the end of `rebuildRatings`, for everybody with a game in the season, before the fence. Only rows whose pair, count or stamp actually move are written | A failed recompute must not 500 the end-of-game post: the game is already rated and the companion's retry answers `already-rated`, so the retry would never reach the recompute again — the pair would be lost until the next game these people play, and the post would have been lost for nothing. Writing only what moves is what makes a second `rebuild-ratings` byte-identical: `roles_inferred_at` is part of the row, so an unconditional write would make an idempotent command produce a different table every run. The rebuild recomputes *everybody in the season* rather than only the players whose ratings moved, because a game that stopped qualifying has just taken a counted game away from ten people. | Recomputing on page load (a pair changing is not an event, and `/admin` would be the only writer of a column the balancer reads); a cron (nothing to schedule: the fold is the event); letting the recompute throw (a lost end-of-game post to fix a caption); rewriting every row every run (the idempotency claim would be a lie) |
| 2026-09-10 | **Inference reads a player's rated games across every season**, and `POST /api/admin/players` with `action: 'set-roles'` answers **410** with `Roles are worked out from the games people play, so there is nothing to set here. Reload the page to see the current pair.` — the variant stays in the zod union, with its two role fields optional, purely so a stale open tab gets that sentence (M5.17) | Roles are a fact about a person, not about a season — seasons are gone as a rating boundary (M5.14) and a player who mained support all of last season is a support on the first night of this one. A removed union variant would answer a tab that has been open since before the deploy with `that form was not valid`, which is both untrue and unactionable; 410 is the status for "this existed and is gone" and the sentence names what replaced it. | Scoping inference to the active season (a whole group flexible on the first night of a season, and the balancer filling ten people at random); deleting the variant (a lie to the one caller that can still reach it); leaving the route working as an override (`04-decisions.md` 2026-09-10 already refused an invisible override) |
| 2026-09-10 | **The tonight page's admin-only `Set roles` link is removed** (M5.17, lead): `SET_ROLES_LINK` goes from `lib/tonight/copy.ts` and the `<Link>` from `app/_tonight/SeatRack.tsx`, and the all-flexible hint — `Nobody has set a role tonight, so the bot can put anyone anywhere.` — stands alone for everybody, admin or not. The two `05-design.md` copy rows are struck through rather than deleted. **The code hunk lands on `main`, not on the M5.17 branch**: M3.6 added the link after that branch was cut, so the branch's `SeatRack` renders the hint alone already and a merge would otherwise bring the link back | The link took an admin to a control that no longer exists — `/admin/players` shows the inferred pair read-only — so it was a signpost to a blank wall on the one page a friend opens on a phone. The hint itself is still true and still worth saying: it explains why no role column is drawn. | Repointing the link at the M3.6 role card (it is already on this page, under the rack); leaving it and letting an admin find out on `/admin`; deleting the design rows outright (a copy table that forgets what it used to say cannot explain the next change) |
| 2026-09-10 | **`rebuild-ratings` recomputes the inferred pair for every `players` row, not only the players with a game in the season it folded** (M5.17): `selectAllPlayerIds` feeds `recomputeInferredRoles`, whose id lists are chunked at 200 | A hand-set M1 pair on somebody who has not played since is exactly what "overwritten by the first recompute" means, and no game-driven pass would ever visit that row — they would stay a support to the balancer forever on data nobody can see. Rated games are read across seasons, so folding one season can legitimately move somebody with no game in it. It stays idempotent because only rows that actually move are written: the run after the first writes nothing, which is what the rebuild's own test asserts. | Recomputing only the season's players (the row that never plays is never fixed); a separate one-off backfill script (a command nobody would remember to run, for a column that already has a writer); leaving the stale pair and documenting it (an invisible input to the balancer) |
| 2026-09-10 | **The result embed's footer is `Kustom · game 47`** (`` `Kustom · game ${gameNumber}` ``, and `Kustom` alone when the count is null), replacing `Season 1 · game 47`. The count keeps the meaning it already had and the doc now says it out loud: **the all-time game number** — every game stored with a `started_at` at or before this one, rated or not, taken when the post is written. Supersedes the clause in **M3.21**'s acceptance that kept the season name; lands with **M5.12**. | Acceptance check 8 of M5.12 says no friend-facing string in `apps/web/lib/discord` says *season*, and this was the last one. The count is the half of that footer that carries information, so it stays; the container it was named after is no longer a thing the product has, and on the deployment that exists it would have read `gamesd · game 47`. `Kustom` is the word the teams embed's footer already puts in that position (M3.21), so both messages sign the same way and both degrade to the bare name when their second half is unavailable — one fallback rule, already written. A count needs a sayable scope: this one is the group's odometer, and the night's count is not it. | `Game 47` bare (a number with no stated scope, and on the first night `Game 3` reads equally as tonight's third); `Game 47 tonight` (false — it is all time); `Game 3 tonight · 47 all time` (two numbers and a second query per post, for a fact the tonight page already shows, in the one line of an embed nobody is reading for data); keeping a season name a friend never sees anywhere else |
| 2026-09-10 | **The window picker's accessible name is `Time window`** (M5.12) — confirmed as the engineer wrote it, on screen nowhere, as the `aria-label` of the `<nav>` holding the five links. It is now a row in `05-design.md`'s copy table, and if **M5.8** gives the control a visible heading, that heading is this string verbatim. | A `<nav>` with five links and no name is announced as "navigation" next to the one that says `Leaderboard`, which leaves a listener to tab through the options to find out what they change. `Time window` is `00-product.md`'s own noun for the thing ("a window the same board is read through"), it is two plain words that resolve the moment the first option says `This week`, and it is true on all three pages the control appears on. Screen-reader text is friend-facing text, so it belongs in the copy table like every other string. | `Window` alone (a browser window); `Time range` / `Period` (a second name for a thing the docs already name, the drift `Leaderboard` was ruled against); `Leaderboard window` (wrong on `/p/[puuid]` and `/stats`); leaving it out of the copy table because it is invisible (the table is the code half's contract, and an unlisted string is one nobody reviews) |
| 2026-09-10 | **The window slot under the picker** (M5.12, the designer's slot; copy in `05-design.md`): `` `${range} · ${gamesLabel(count)}` `` with one range per window — `Monday 8 Sep to Sunday 14 Sep`, `Monday 1 Sep to Sunday 7 Sep`, `September`, `August`, `Since 8 Sep 2025`. Sentence case in the DOM, upper-cased by the dress like the tonight strip. Formatted by `lib/night.ts`'s formatters in `CUSTOMS_NIGHT_TZ` and `en-GB` with the existing `Sept`→`Sep` cut, on the server. An empty window shows its own sentence there instead, never both; `/p/[puuid]` prints the range half alone. | Nothing else on the page says which Monday `Last week` is, and two boards that differ only in their heading are two boards a reader cannot tell apart. The week form is **M5.10's approved post description, byte for byte**, so the Monday post and the page it links to say the same words from one formatter — a reader who taps the post sees its own sentence again at the top of the board. Months are named, not ranged, because a day range spells out what a calendar already says and `this-month` would be claiming 29 days nobody has played yet. `Since 8 Sep 2025` gives all time a line too, so the slot never vanishes on one of five options, and it is the honest statement of how far back the group's record actually reaches (`00-product.md`, backfill). The count goes through `gamesLabel` because a one-game week must not read `1 games`. | `1 Sep to 30 Sep` for a month (a range for a thing everyone can already name); nothing at all on `All time` (a slot that disappears on one tap reads as broken); a bare count (`312 games` answers a question nobody asked and drops the fact that our history has a floor); printing the count on `/p/[puuid]` too (M5.15's seed line already ends `, 6 games since.`); the slug and the empty sentence together (`Monday 1 Sep to Sunday 7 Sep · 0 games` is a board that looks broken rather than empty) |
| 2026-09-10 | **The window picker is a wrapping row of chips with Archivo `t-sm` 500 labels, under the h1, and it is five links with `aria-current="page"`** (M5.12, designer; spec in `05-design.md`, "The window picker"). 3 + 2 at 390, one row from 720px; the chosen chip keeps the role chips' `brand` word, tint and doubled edge; the climb on a row's window line renders at `t-xs` like the run it sits in, and the still-settling note moves below the board card. | Five two-word labels do not fit one 358px row at 390 without scrolling or breaking the 44px rule, so a segmented control would have to hide options or shrink below the tap floor, and tabs would put a second underlined tab row 44px under the shell's — two navigations in one visual language on a phone. Archivo is the shell nav's own ruling (*"these are destinations, so Archivo"*) and the type rule everywhere else (`number or role → mono`): `This week` in mono is a terminal string, and mono micro-labels are lower case, which product's capitalised labels are not. `aria-current="page"` on a link matches `TopBar` 100px above — one convention for "the destination you are on", and no hole in the tab order where the current window is. The climb shipped at `t-sm` inside a `t-xs` meta line, which put two type sizes in one row. | A segmented control (cannot hold five two-word labels at 390 without a scroll or a sub-44px target); tabs under the title (a second tab row under the shell's); a `<select>` (hides four of five options on the one control that tells two identical boards apart); scrolling one row (saves 52px of fold by cutting the fifth option off-screen); mono labels (breaks the type rule twice — language in mono, capitals on a mono label); colouring the climb by sign (banned product-wide) |
| 2026-09-10 | `pnpm -r test` runs **one workspace at a time**: `workspaceConcurrency: 1` in `pnpm-workspace.yaml`, and the same limit spelled out as `--workspace-concurrency=1` on the root `test` script (M3.28) | **What it protects.** pnpm's recursive order is topological, so `apps/web` already waits for `packages/db` (web depends on `@customs/db`) and those two never overlapped. What did overlap is every pair with no dependency between them: measured on 2026-09-10, `apps/web` and `packages/lcu` started **1 ms apart** under the default concurrency and 750 ms apart with this setting. `packages/lcu` and `apps/companion` touch no database today, so the setting is insurance rather than a fix for a live defect — and it is the insurance that keeps the next package that does drive the stack from being started beside `apps/web` by surprise. **What it cannot do.** Nothing in this repo can serialise two *worktrees*: the five-case failure the milestone row describes was another worktree running its (then afterAll-less) tonight suite against the same local stack, and the ten green runs recorded for this task ran while that leak was in the database, not after it was cleaned. The real fix for cross-worktree collisions is M3.27's `afterAll` landing in every integration file, so a suite cannot leave rows for a suite in another checkout to trip over; a second stack per worktree is the only mechanical answer and nobody wants five Dockers. **Where it lives.** `pnpm-workspace.yaml` and not `.npmrc`: on pnpm 12.3.4 `workspace-concurrency` in `.npmrc` is inert — `pnpm config get workspace-concurrency` answers `undefined` and the packages still start together — while the camelCase key in the workspace file is honoured. It has to be configuration and not only a flag because `pnpm -r test` is the spelling `CLAUDE.md` tells everyone to type, and that spelling reads no script of ours. | `workspace-concurrency=1` in `.npmrc` (inert on this pnpm; the version that shipped first and was caught in review); the root script's flag alone (bypassed by the documented spelling); moving `packages/db`'s integration tests onto their own schema (every migration replayed into it, and `games.season_id` still defaults to the one global `active_season_id()`, so the singleton is not separated); one stack per worktree (five Supabase stacks on one laptop) |
| 2026-09-10 | **Every integration test that injects a clock anchors it to `nightStart(now) + 1 hour`, never to a plain `new Date()`** — `roleCarry.integration.test.ts` joins `discord.integration.test.ts` (M3.24) in doing so (M3.28) | A role for tonight expires at the next 06:00 in `CUSTOMS_NIGHT_TZ`, and the carry cases post their second cycle at `now + 20` and `now + 30` minutes: run between 05:30 and 06:00 local, that second post lands in the *next* night and the preference is legitimately gone. Found 2026-09-10 at 05:34 Cairo, where two cases failed and the same two pass at every other hour. An anchor an hour into the night leaves 22 hours of headroom for every offset in the file and stays inside the same night as the database's own `now()` | Faking the clock for the whole file (the database keeps writing its own `now()`, so half the comparison would still be real); skipping the cases near the boundary (a suite that is green because it did not look); shortening the offsets (the same defect at 05:55 instead of 05:30) |
| 2026-09-10 | `/admin/players` **pages at 50 with an explicit `.range()`, an exact `count`, and a search box** over display name, Riot game name (both *contains*) and PUUID (*prefix*); paging and searching **navigate** — a `GET` form and plain links — while every write on the page still posts in place. `?page=` must be a **safe** integer (`1e21` is an integer to `Number.isInteger` and an offset of 5e22 to `.range()`), `_` is escaped to `\_` at the filter rather than stripped from the box (it is a real character in a Riot ID, and an unescaped one is `LIKE`'s any-character wildcard — a search for `it_` matched every `it-` row), and `/admin/tokens`, whose mint dropdown reads one 1000-row page, prints `showing the first N of M` when that page is not everybody (the last three found in review, 2026-09-10) (M3.25) | The query had no `range` at all, so PostgREST answered with its `max_rows` (1000) and no error: past a thousand rows players silently were not there, which is how `admin.integration.test.ts` failed on the shared stack. One form per editable field per row also means a thousand players is ~4800 client components and a page 85,000px tall. Paging and searching are *reads*, so a URL that can be bookmarked, opened in a new tab and gone back from is the right shape, and it does not weaken M3.20 — that rule is about the writes, which still never navigate. A `?page=` past the end (PostgREST answers `PGRST103`) clamps onto the last page instead of showing an empty table | Infinite scroll (JavaScript, on the page that deliberately has almost none); a client-side filter over every row (the 1000-row truncation is upstream of it, so it would filter rows that never arrived); keeping the whole list and only adding a search box (the 85,000px page stays); `count: 'planned'` (the page prints the number, and an estimate in a sentence is a lie); searching the PUUID with an unanchored `ilike` (a sequential scan over 78-character random strings, and nobody quotes the middle of a PUUID) |
| 2026-09-10 | `/admin/games` (M5.5) is built as the brief specifies with three small additions the brief is silent on: every name on it — the reporter and each of the frozen roster — **links to `/p/<puuid>`**; the `Missed` cap is an option (`cap`) the page never passes and the integration test does; and `Captured`'s `rated` is computed as "every `game_players` row of this game carries a `mu_after`" | The names are the only thing on the page an admin wants to follow anywhere, `/p/[puuid]` is where they would go, and a link is a read — it breaks none of the page's read-only rule. The cap is injectable because proving "stops at the cap and still prints the real total" the honest way would mean seeding 101 `in_game` lobbies into a **shared** local stack, where every other suite reads "the live lobby of tonight"; with the option, three rows prove the same truncation. `rated` is read off the rating columns rather than any flag because a game the fold refused and a backfilled game nobody has rebuilt yet are the same fact to a reader, and this page is the first surface where either becomes visible | Linking nothing (a page of names an admin has to copy-paste into a URL); a `?page=` on the missed list (the brief caps it at 100 and says to read the total, not to walk it); trusting `games.source` or a `rated` column (there is none, and inventing one would put a second copy of the fold's answer in the schema); seeding 101 lobbies in the test (breaks every other suite's "live lobby" read on the shared stack) |
| 2026-09-10 | The window helpers live in `apps/web/lib/night.ts` beside `nightStart` and answer four things: `windowRange(kind, now, tz)` (half-open `[start, end)`, both ends 06:00 local, `all-time` = `{ start: null, end: null }`), `isInWindow(startedAt, range)`, `weekStart`/`monthStart`, and `closedWindow(kind, now, tz)` → `{ kind, start, end, key }` where `key` is `start.toISOString()` — the `window_posts.window_start` M5.13 dedupes on (M5.9) | Every windowed board, page and post asks "is this game inside this week" and there has to be exactly one answer to it. `all-time`'s nulls give every caller one predicate and no branch, which is what stops five surfaces each inventing an "except all time" clause. `closedWindow` derives the cron's key from the same bounds the page reads, so "which week did we already post" and "which week are we about to post" cannot be computed two different ways. | Putting it in `packages/core` (the zone comes from `CUSTOMS_NIGHT_TZ` and core takes no environment); building the week off the raw instant rather than off `nightStart` (a 02:00 Monday game would open a new week six hours before the night it was played in ended); a stored `week_id` column (a game backfilled three weeks late has to land in the week it was played, on every page and in every past post, with nothing rewritten) |
| 2026-09-10 | **`All time` keeps M3.5's membership: every player the database knows, seeded from rank when they have no `ratings` row.** A window's membership is the counted games inside it (`mu_after is not null`), and only there. `BoardRow.games/wins/losses` are the window's counts — the fold's own totals on `All time` — while `settling` is always the all-time count and `streak` is `All time` only, replaced in a window by the climb (M5.12) | The M5.12 brief says "`All time` is every player with a `ratings` row", which would drop the seeded friend M3.5 deliberately keeps on the board ("a board that hides its newest players is the board M3.8 exists to explain") and would change numbers the board tests pin. One count per row, because a row carrying both the window's games and the all-time games would print two game counts on one line and the reader would have to be told which. The chip stays all-time because it is a fact about the rating: a player with one game this week and two hundred behind them has not become unsettled by the calendar. | Filtering `All time` to rated players (changes shipped numbers, hides new players); two game counts per row; a per-window settling rule; keeping the streak in a window (product fixed the line as `6 games · 4W 2L · +58`, with no streak in it) |
| 2026-09-10 | `/p/[puuid]` is **one shape, not a union**: the `no-season` arm and its sentence are deleted with the button behind them, and a window scopes the whole page — chart, record, role breakdown and `Recent games`. With no counted game in the window the two numbers fall back to the player's current rating and the window's empty line carries the explanation (M5.12, M5.14) | `NO_SEASON_BOARD` is deleted and a deployment with no season row has no games either, so the honest page is the ordinary one with an empty line on it. Windowing `Recent games` too, though the brief only names the chart and the record: a `Last week` page that listed this week's games would be one block disagreeing with every other block on the screen. The numbers never fall back to zeros — that was the defect the union was built to prevent — they fall back to the number `/leaderboard` already shows for that player. | Keeping a second page shape with nothing on it; printing no numbers for a window a player missed (the page is a person, not a board row); leaving `Recent games` all-time inside a window |
| 2026-09-10 | The window picker is **five links in the page header**, `?window=<kind>` on every option including the page's default, the selected one a marked `<span>` and not a link, an unknown value `notFound()`. Dress is provisional and M5.8's: 44px mono chips in a wrapping flex row (3 + 2 at 390px, one row from 720px), the chosen one in `brand` (M5.12) | Product fixed the behaviour — a plain link, no client state, works with JavaScript off — and `05-design.md` has no picker spec yet, so the code borrows the tonight page's role-chip recipe rather than inventing a control. `next/link` keeps the tap an in-place render. Every option names its window so the URL in the address bar is the link somebody pastes into the group chat. A 404 for an unknown value because it can only come from a typed URL, and a page that quietly showed a different window than the URL names is a page whose links cannot be trusted. | A `<select>` (hides four options behind a tap, and the picker is the difference between two boards that otherwise look identical); a client component with fetch (nothing to gain over a link, and it would be the first client component on these pages); a parameterless default (two URLs for one board); falling back to the default on an unknown value |
| 2026-09-10 | Every `in (…)` filter the board reads through is chunked at 90 ids — the players on a window's board, their `ratings` rows and the scoreboard rows, joining the game-id list that already was (`inChunks`, M5.12) | A PostgREST filter is a URL and a long enough `in` list is answered `414 URI too long` by the gateway before Postgres sees it. Not theoretical: the first render of a windowed board against a database with a few hundred players failed exactly that way. The group is twenty people, so production would not have hit it for years — which is why it is a unit test and not a page nobody opens. | Leaving it (works until the board is opened against a big database); one query per player (twenty round trips a page); a `.or()` string (the same URL, longer) |
| 2026-09-10 | Three readings the slot needed and the copy table does not fix (M5.12): **`Since …` on `/p/[puuid]` is dated from that player's own first counted game**, not the group's, because the page is a person's history; **the settling note is not drawn when no board card is**, since it explains a column that is not on the screen; and a window's game count is `count: 'exact'` over `games` with a `game_players!inner(mu_after)` filter — PostgREST counts the **parent**, so it is a count of games and not of scoreboard rows (35 games behind 350 rated rows, checked against the local stack) | The alternative for the first is a group-wide date on a page about one person, which reads as a claim about them that is not true. For the second, an empty window draws nothing for the sentence to explain and it would sit alone under a heading. For the third, the honest alternatives were counting rated rows and dividing by ten (right only while every counted game has exactly ten rated rows) or enumerating game ids (truncates at PostgREST's row cap on any real history) | Dating a person's `Since …` from the group's first game; keeping the settling note over an empty window; `sum(ratings.games) / 10`; a stored per-window count (a window is a filter over games, and nothing about it is stored) |
| 2026-09-10 | **`window_posts` (migration `0011`) records a *claim*, not only a post**: `(kind, window_start)` primary key, `claimed_at` from the route's clock, `posted_at` **null until Discord takes the message**, `attempts`, `reason`. `GET /api/cron/window` inserts before it posts (`on conflict do nothing`) and stamps `posted_at` after, so a duplicate call at any cadence is a no-op and a failed post stays retryable. M5.13's brief said to *delete* the row on a webhook failure; the null column does the same job without a delete that could race the next call, and it leaves the failure readable (`reason`) instead of erasing it. A window with **no games** is stamped posted with `reason = 'no games in the window'`, so an empty week is not retried hourly for seven days. | The failure mode this task exists to prevent is a duplicate post in a channel ten friends read; the one it must not create is a week that silently never posts. Insert-then-post makes the first impossible and null-`posted_at` makes the second recoverable. | Post-then-insert (a crash between the two posts the week twice). Deleting the row on failure (the delete is a second write that can fail, and the row is the only record that anything was tried). An `is_posted` boolean (a timestamp answers "when" for free). |
| 2026-09-10 | **The retry is a lease plus a compare-and-swap, not a bare `posted_at is null`** (M5.13, `apps/web/lib/discord/windowPosts.ts`): a claim is only taken over once it is `WINDOW_POST_RETRY_MS` (10 minutes) old, and the takeover updates `where claimed_at = <the value it read>`. Every clock in it is the **route's injected `now`**, never the database's, so a faked Monday in a test and the lease agree. | Without the lease, the second of two calls that arrive while the first is still talking to Discord would see `posted_at` null and post the same week again — the exact duplicate the table exists to prevent. Ten minutes is longer than any post can take (two five-second attempts) and shorter than the hourly cadence, so an outage at 06:00 on a Monday is recovered by the next call. | No lease (duplicates whenever a post is slow). A database-side `now()` comparison (a test with an injected clock could not reach it). A row lock or advisory lock (a lock held across an outbound HTTP call). |
| 2026-09-10 | **`GET /api/cron/window` considers `last-week` on every call and `last-month` only in the 24 hours after a month closes.** The week has no freshness rule — a deployment that was down all Monday posts last week on Tuesday, late and correct — and the month has one, so the first ever call against a year of history posts one week and, unless it happens to be the 1st, no month at all. | "On the 1st" is a freshness question, not a dedupe one, and the dedupe table cannot answer it: with no rows at all, every closed month looks unposted. Deriving it from the closed month's own `end` reuses M5.9's boundary instead of adding a second calendar rule. | Considering both kinds always (posts a month on the 15th on a fresh database). Reading the civil day number in `CUSTOMS_NIGHT_TZ` (a second calendar rule beside `lib/night.ts`'s, and wrong for the six hours before 06:00 on the 1st). |
| 2026-09-10 | **The weekly post ships as the closed window's board with an `awards` seam** (M5.10, M5.13): `windowSummaryEmbed` takes `awards?: WindowAward[]` and prints an `Awards` field only when it is given some. M5.4 has not shipped, so nothing fills it yet and the post is the board, the dates and the link. | M5.13 is the task that makes the week end by itself, and it does not need the awards to be true; shipping the post without them costs one array argument the day M5.4 lands and no other change. An empty `Awards` field with three "not built yet" lines would be a worse message than no field. | Blocking M5.13 on M5.4. Printing a placeholder block. Building the awards here (they are M5.4's numbers and this route may not re-derive them). |
| 2026-09-10 | **A missing Discord webhook makes `GET /api/cron/window` a 200 skip that writes nothing** — `{ ok: true, posted: [], skipped: [{ kind, reason: 'no webhook configured' }] }` — and not a 503. The unset `CRON_SECRET` stays a 503, because that is the door. | Nothing must be claimed while there is nowhere to post: a `window_posts` row says "the group has been told", and one written with no webhook would silently eat the first week after somebody finally configures Discord. Answering 200 matches `GET /api/cron/leaderboard`'s existing behaviour for the same condition, so a pinger does not alarm on a deployment that is merely half configured. | 503 (an uptime pinger would page somebody for a Discord setting). Claiming the window anyway (loses the first real week). |
| 2026-09-10 | **`rebuild.integration.test.ts` cleans up by season, not by game id** (M3.29): `afterAll` restores Season 1, deletes **every game of the season it started** (not only the ids it named), then the ratings, the season and the players, running each step even if an earlier one throws, and finally **fails the file if the season is still there**. | The two `it-<run> rebuild` seasons left on the shared stack were not a hook that did not run: the hook ran, deleted games by id, and a case that failed before its own inline cleanup left one game the list did not name. `games.season_id` has no `on delete cascade`, so the season delete failed on a foreign key and said nothing. Verified by making a case fail on purpose: `select count(*) from seasons` is unchanged. | A `try/finally` around each case (the leak is the hook's, not a case's). Adding `on delete cascade` to `games.season_id` (a migration that makes deleting a season delete its games is a footgun in production for a test's convenience). |
| 2026-09-10 | **The schedule lives in `apps/web/vercel.json`: Vercel Cron calls `/api/cron/leaderboard` and `/api/cron/window` daily at `30 4 * * *`** (04:30 UTC — 07:30 Cairo in summer, 06:30 in winter, both after the 06:00 night boundary). Daily and not hourly because the Hobby plan allows one run a day per cron; both routes are safe at any cadence, so a paid plan tightens the schedule and nothing else changes. **Vercel sends `Authorization: Bearer <CRON_SECRET>` by itself** when that variable is set on the project, which is exactly the door both routes check, so there is no second credential. `/api/cron/sweep` gets no entry: it already runs at the start of every companion post. | The route was written so the schedule could not get it wrong, and this is the cheapest caller that exists — it is in the repo, it deploys with the app, and it needs no third-party account. After the boundary in both halves of the year, so the week the post names is always the week that has closed. | An uptime pinger (a second account and a second place to look when a Monday is silent). Hourly (not available on the plan the project is on). Midnight UTC (02:00 or 03:00 Cairo, which is *inside* the night and would name the wrong week). |
| 2026-09-10 | **`window_posts` is at-least-once, not exactly-once, and the header of `0011` says so.** If Discord answers 2xx and the `posted_at` stamp then fails, the row stays unposted and a call after the lease posts that week again. | Exactly-once across a webhook and a database needs a distributed transaction, which is not on offer here. Given the choice the group would rather see a week twice in a year than never see one — and the stamp is the very next statement after the webhook returns, so the window is milliseconds wide. | Stamping before the post (a failed post would then be a week nobody ever sees, which is the failure this whole table exists to prevent). |
| 2026-09-10 | **Five copy rulings on M5.15 and the `Start a lobby` control** (`05-design.md`, the board table and the new `Copy — Start a lobby` table). Four readings the web engineer made where the copy table was silent are **confirmed** and written into it: the per-game change is `formatWebDelta`'s own string with the **U+2212** minus (`Lost as the 58% side, −31`); the seed line drops its clause at zero games (`Seeded from Gold II at 1469.`); a window with no counted game prints **no** start line; and `That did not reach the server. No lobby was opened — tap it again.` stands byte for byte. The month windows read `Started the month at …`, confirmed as one interpolation off `WindowKind` rather than a fifth product string. The fifth ruling **replaces** a reading: the tonight page **does** print the live lobby's name and password — `Missed the invite? The lobby is Customs 09 Sep #1, password 4821.` in the `filling` and `balanced` states — as **M4.10**, and `/admin` keeps its own line. `Sign in with Discord to start a lobby.` is marked suspended, not deleted, until the press widens past admins | The four confirmations are the document agreeing with itself: "Rating delta" already fixes U+2212 for every number on the web and the sentence shares `formatWebDelta` with the column beside it, so the table's ASCII hyphen was a slip in prose; M5.15's own acceptance check 5 forbids a bare `0` on the newest player's page; an empty window's reference falls back to the seed, so a start line there would name a number the week never saw; and the offline sentence is the shape product fixed twice already (`ROLE_TAP_OFFLINE`, `LINK_OFFLINE`). The fifth is the M4.2 brief's explicit ruling — *"It is not a secret: it goes in the Discord embed and on the tonight page"* — which the control shipped without because the copy table, not the brief, had no string for it. It matters at the bar this product is judged at: the invite fan-out runs once, with no second wave, so a friend who walks into voice late or dismisses the popup must otherwise have the password read out in voice, which is a step. One lobby's exposure, a fresh password per lobby, and the line is gone once the game starts | Retyping the table's ASCII hyphen (one number spelled two ways on one row); `, 0 games since.` (a zero standing in for "nothing has happened yet"); `Started the week at <seed>` in an empty window; a fourth refusal from the route for a press it never received; a per-window product sentence (four strings where one shape does); keeping the password off the tonight page (reverses a brief with no decision row, and leaves the latecomer interrupting nine people); gating the line behind sign-in (a step, for the one person who has the least time to take it); deleting the suspended sign-in string (the widening would have to invent it again) |
| 2026-09-10 | **Two amendments after the designer's review** (`05-design.md`, both copy tables). (a) The per-game line on `/p/[puuid]` is the **chance clause alone** — `As the 58% side.`, with the full stop — because the row head beside it already reads `Lost … 1392 (−42)`. A game with **no stored chance prints no line**, and the `Won, +43` form is deleted. (b) The tonight page's `Missed the invite? The lobby is Customs 09 Sep #1, password 4821.` renders **only for a signed-in viewer the page has matched to a player row** — never for an anonymous one, and not for a signed-in visitor with no player yet | (a) `Won as the 42% side, +43` said the result twice and spelled the delta twice in one row; the only thing the head cannot say is the chance, so that is all the line keeps. With the result and the delta gone, the no-chance form had nothing left in it but the head repeated. Full stop because it is a caption, not a label, and an unstopped fragment in `dim` under a row of numbers reads as text that got cut off. The U+2212 ruling of the row above now lands on the row head alone, where "Rating delta" already governed it, and the Discord embeds are untouched. (b) The designer's point and product's both hold, and the gate is what makes them hold together: a forwarded link with a live four-digit password on it is a credential, and a late friend is by definition one of the twenty who has picked himself out of the list once already — being linked is what the page needed of him anyway to know who he is | (a) Keeping the full sentence (the same fact three times on one row); dropping the full stop (a fragment that reads as truncated); printing `Won, +43` for a backfilled game (the row head, retyped); changing what the embeds print (a page ruling reaching into a message nobody complained about). (b) Leaving the line open to anonymous visitors (the link is passed around WhatsApp and would carry the password out of the group); dropping the line entirely (the latecomer goes back to interrupting nine people in voice); showing it to any signed-in visitor (the `That's me` picker is two blocks up and is the thing to do first) |
| 2026-09-10 | **The `Start a lobby` control is drawn in `idle` only, above the rack, with no card and no mark; in `filling` the same block renders its readout without the button** (`05-design.md`, M4.7 (a)). The same section fixes the type rule the whole page runs on: **a quantity inside a sentence stays Archivo, a token you have to transcribe is mono** — which is why `Invited 7 friends` and `As the 58% side.` are Archivo throughout while the lobby name and the four-digit password in `Missed the invite? …` are mono spans inside an Archivo sentence, and the password span is `user-select: all`. M4.10's line is not part of the control: it is the last line of the **primary block** in `filling` and `balanced`, so it does not move between two blocks when the teams land | `filling` means a live `lobbies` row exists and `decideStart` refuses on exactly that, so the button there could only ever answer `There is already a lobby open.` — the rule that already kept it off `balanced`, one state earlier. Above the rack because ten 44px `open` rows put it at y≈799 on a 390 × 844 phone, under the fold, on the one screen where it is the point of the page. No card because at 1280 a bordered card around one button renders as 1300 × 110px of empty surface with a 200px button in a corner. No mark because the 2px inset means "you" and the 3px rule means "the bot's own sentence", and an "admin" mark would have to be removed the day the press widens. The mono exception is the Type table's own `lobby password` entry: a distinguishable `1`/`l` and `0`/`O` is the entire reason those two spans exist, and they are the only place in the product somebody is expected to copy something | Keeping the button in `filling` (a control whose only answer is a refusal); `disabled` while a create is pending (moves focus off the button that was just pressed, which M3.20 exists to prevent — `aria-disabled` plus the `:disabled` dress plus a client-side short-circuit does the same job and keeps the focus); a filled amber button (more lit area than the live pill, a winner's ring and a side rule together, and a warning banner in light); putting the M4.10 line inside the start block (it would have to appear in a state the block is not drawn in); all-mono or all-Archivo numbers (one spells a quantity as code mid-sentence, the other spells a password you have to retype in a proportional face) |
| 2026-09-10 | The three readings M5.15's copy table does not fix: the per-game sentence's change is **`formatWebDelta`'s own string**, U+2212 and all (`Lost as the 58% side, −31`), never a second spelling of the number in the column beside it; the seed line **drops its `, N games since` clause at zero games** (`Seeded from Gold II at 1200.`); and a window with **no counted game prints no start line at all** | The brief pins the change to `displayDelta(mu_before, mu_after)` and says it "is the same string `formatWebDelta` already prints beside it" — the table's `-31` is prose, and two glyphs for one number is exactly the drift the one-helper rule exists to stop. `, 0 games since.` puts a bare zero on the page of the newest player in the group, which acceptance check 5 forbids in the same breath as `NaN`; the record line above it already disappears at zero for the same reason. And in an empty window `reference` falls back to the player's **seed**, so `Started the week at 1200` would name a number that week never saw — the window's own empty sentence is already on the page and is true. | Retyping the copy table's ASCII hyphen (a page and a Discord message spelling one number two ways); `, 0 games since.` (a zero standing in for "nothing has happened yet"); `Started the week at <seed>` in an empty window (a sentence about a week that did not happen) |
| 2026-09-10 | `Started the **month** at 1469, 14 games since.` on `This month` and `Last month`, by the same shape as product's week form (M5.15) | Product wrote one window sentence and the picker has four windows. `Started the week` on `Last month` names the wrong calendar, and the page's other window strings — the empty lines, the slot's ranges — already say `week` or `month` per kind. One interpolation off `WindowKind`, in `lib/board/copy.ts` with the rest of the table's code half. | A single `Started at 1469` for all four (drops the one word that says which window is being read); a fifth product string per window (four sentences where one shape does) |
| 2026-09-10 | The win chance a `Recent games` row prints is read **for the five rows on screen**, as one `splits` query keyed by the games' `lobby_id` with `is_chosen`, not folded into the season read (M5.15) | It is a sentence on five rows and nothing else on the page needs it: reading a season's splits to print five percentages would put a table nobody looks at into the page's critical path. `splits` is publicly readable and is the same row the tonight page and the teams embed print, so the percentage on a row is the one the group was shown that night. A game with no lobby, or a lobby with no chosen split, simply has no entry and the row drops the clause. | Denormalising `blue_win_prob` onto `games` (a migration for a sentence, and M5.15 adds none); reading it in the component (a page that queries); defaulting a missing chance to 50% (a number nobody computed) |
| 2026-09-10 | The tonight page **polls** tonight's `create_lobby` — `router.refresh()` every 5 s while it is `pending` or `sent` — instead of subscribing to it, and reads it on the server with the **service role**, only for a viewer the session already said is an admin (`lib/tonight/lobbyStart.ts`, M4.2's control). **Widened by M4.13, 2026-09-15: the gate is `linked`, not `isAdmin`** — every linked viewer's page now makes this round trip and polls while pending, not just the one who pressed the button, since M4.13 draws the whole control for any linked viewer and M4.10 already ruled the password is not a secret among them. | Not a choice: `companion_commands` has no RLS policy at all and is in no `supabase_realtime` publication (`0001_init.sql`), so the browser may neither read that table with the anon key nor ever be sent an event about it. Five seconds is the companion's own poll interval, so the page is at most one companion tick behind the client, and the row lives sixty seconds, so the timer runs a dozen times at most — the same shape as the 60 s name re-read the page already runs for `players`, which is unpublished for the same reason. | Publishing `companion_commands` (a queue with player ids and lobby passwords readable by anyone with the anon key); a policy for `authenticated` (a second auth model for one sentence); no feedback at all until the lobby appears (a button that answers nothing is a button people press twice) |
| 2026-09-10 | The `Start a lobby` control is drawn **for an admin only**, in the `idle` and `filling` states only, as a plain `raise` card directly under the seat rack — and an anonymous visitor is shown **nothing**, not the brief's `Sign in with Discord to start a lobby.` (M4.2's control; the layout is M4.7's and `05-design.md` has no spec for it yet) | The route is admin-gated until M3.6's third route class exists (the 2026-09-10 row above), so the sign-in sentence would promise a button that a signed-in non-admin still could not press — and this page's one sign-in already lives on the role card, where its object is forty pixels below it. From `balanced` on the only answer the route can give is `There is already a lobby open.`, and a control whose only outcome is a refusal is not a control. Under the rack because the control and the seats it is about are one thought; **not** the role card's 2px `brand` inset rule, which means "this is about you" and this is about the night. | The sign-in sentence for anonymous visitors (a promise this deployment cannot keep); the control in the status strip (the strip carries no controls); inside a rack row (the rack is ten 44px rows at every count); last in the column under the role card (at 19:00 it is the point of the page for the one person who can press it) |
| 2026-09-10 | `That did not reach the server. No lobby was opened — tap it again.` is the tonight page's own sentence for a press that never left the browser (`START_LOBBY_OFFLINE`), and the lobby **name and password are printed on `/admin` and not on the tonight page** (M4.2's control) | Product wrote two offline sentences in the same shape for the role tap and the self-link, and only the browser knows a request never left it; a third of the same shape is a smaller thing than a route inventing one. And product fixed exactly five strings for this surface — the button, the pending line, nothing on success, the invited line and the four refusals — none of which is a password, so the page prints none; `/admin` is a plain page with no dress and an admin standing at the PC that opened the lobby is exactly who needs to read the four digits out. | A fourth refusal sentence from the route for a network failure (the route never saw the request); printing the password on the tonight page (copy nobody has written, on the surface twenty people read) |
| 2026-09-10 | The `Start a lobby` **button** is drawn in `idle` only and sits **above** the rack, directly under the strip's sentence; `filling` gets the same block with no button in it — a readout carrying the invited line or a failed create's sentence — under the rack. While the command is `pending` or `sent` the button goes quiet (`aria-disabled`, `.cn-button-quiet`, submit short-circuited) and **never** carries the `disabled` attribute (M4.7 (a), the designer) | From `filling` on a lobby row exists and the route can only answer `There is already a lobby open.`, so the button there is a control whose one outcome is a refusal — but what the fan-out did is still worth reading, hence the readout. Above the rack because ten empty seats are 480px: on the 390px phone this page is designed for, a button under them is under the fold, and on an idle page it is the only thing anybody can do. The quiet state is `aria-disabled` because a `disabled` control drops the focus to `<body>`, and M3.20 is a promise that the focus stays where the keyboard left it. | The button in `filling` (a refusal machine); the block as a card (a fourth panel on a page with three, and the 2px `brand` inset already means "this is about you"); `disabled` while pending (loses focus, which is the rule M3.20 exists for) |
| 2026-09-10 | **M4.10**, the lobby a latecomer can still join: `Missed the invite? The lobby is Customs 09 Sep #1, password 4821.` — the last line of the primary block (under the rack in `filling`, under the explanation strip in `balanced`, above `Your role tonight`), Archivo `t-sm` in `text`, the name and the password in **mono** with `user-select: all` on the digits, and rendered **only for a signed-in viewer matched to a `players` row**. Absent — not hidden — for anonymous and unlinked visitors, and absent entirely when the lobby has no stored name | Product asked for the line for every viewer and the designer refused it outright; the lead's ruling is the middle that both hold: the password is not a secret among the twenty people who play — it is in the Discord embed and read out in voice — but this page's link gets forwarded, and a page that hands a lobby password to whoever opens it invites a stranger into the game. Mono for the two values is the one exception to "numbers inside sentences are Archivo": a token you transcribe into another window is data, and `user-select: all` makes the four digits one tap on a phone. A password with no lobby name to type it into is not an instruction, so the whole element goes. | Everyone (a forwarded link is an open door); nobody (the invite popup expires and a friend then has no way in, which is the case this line exists for); the sentence with no mono (a name and four digits you have to copy, set as prose); `visibility: hidden` for the anonymous case (the password would still be in the HTML) |
| 2026-09-10 | M5.15's per-game caption is the **chance clause alone** — `As the 58% side.` — with no line at all for a game with no stored chance, and the record line above the chart drops its games count while the seed line prints one (`19W 18L`, not `37 games · 19W 18L`) | The row's head already prints `Won`, the date, the duration and `1392 (−42)`: the first shape of this caption repeated three of those four in grey underneath, which is a second row rather than an explanation. What the head cannot say is the one thing the task is for — what the balancer thought the odds were — so that is all that is left, and a game with no stored chance therefore has nothing to add and gets no line. Same rule one block up: `37 games` forty pixels above `…, 37 games since.` is one number twice, and the seed sentence is the one that has to carry it because "since when" is what it is about. | The `Won as the 42% side, +43` form (says the result twice and the change twice); a `Won, +43` line for backfilled games (a caption that only repeats the row); keeping the count in both places (one page, one count) |
| 2026-09-10 | The invited count on the tonight page and `/admin` is bounded below by the **create's own `created_at`**, not by the night's 06:00 (M4.2's control; the reviewer) | The fan-out runs off the ack, so its rows are always younger than the create they came from, and a night has more than one lobby in it: bounded by the night, the second lobby's line would claim the first lobby's popups. It is the one number on that card a reader can check against their own client and find wrong. | Counting the night (wrong from the second lobby on); counting from `acked_at` (nullable, and the create's own `created_at` is always there) |
| 2026-09-10 | `/p/[puuid]` **keeps** a still-settling sentence and prints a third-person, name-free twin of the board's — `The board sorts on Proven: a player's rating, minus how unsure the board still is about them. That gap shrinks as they play and settles after about 30 games.` — while `/leaderboard` keeps the shipped second-person string byte for byte (M3.26; two constants, `SETTLING_SENTENCE` for the board and `SETTLING_SENTENCE_PLAYER` for the player page; the copy table in `05-design.md` is the source of truth, and M5.15's explanation strip and the nightly footer's short form are untouched) | On Yuki's page the shipped sentence says `your rating` twenty pixels under Yuki's Rating and Proven, so the one number it names is the one number it is not about. It cannot simply be deleted either: M5.15's strip explains the **size of a change** and names neither Proven nor Rating, so with the sentence gone the biggest number on the page and the `settling` chip beside it would have nothing anywhere saying what they are — on the exact page somebody opens when they are sure the board is wrong about them. Two forms and not one, because the board's `you` is correct there: everyone reading a leaderboard is on it, and being able to find yourself in the sentence is why it works. Third person **without a name** because a name here is optional (`Someone`, M3.10), renameable by its owner between two page loads, and possessive-irregular across the group, and because the commonest reader of a player page is its own subject. | Dropping the sentence from `/p/[puuid]` and keeping it only on `/leaderboard` (leaves Proven and the `settling` chip unexplained on the page that exists to explain them; M5.15 answers a different question); interpolating the name (`Yuki's rating, minus how unsure the board still is about Yuki` — a name three times in two sentences, `Someone's rating … about Someone` when it is missing, a possessive rule per name, and the page talking about Yuki in the third person to Yuki); one person-neutral sentence shared by both pages (pays on the page that works to fix the page that does not); a shorter player-page line that drops `The board sorts on Proven` (then nothing says why the display-size number on a page with no sort is the cautious one) |
| 2026-09-10 | **`/stats`'s universe is `gateGame` alone, so a counted game may carry no rating** (M5.4): ten rows, five a side, over 300 seconds, nobody twice — a backfilled game the rebuild has not folded yet counts in the games number, the records, the duos, the streaks and the average length, and contributes no climb. `climbs` therefore measures most improved between a player's first and last games **that carry `mu`**, while the award's minimum counts counted games as product wrote it | The brief's acceptance 1 is explicit: a window with one rated game, one 300-second game, one nine-player game and one backfilled ten-player game is `counted games = 2`. That is the gate and not `mu_after is not null`, which is what `/leaderboard`'s slot counts — so the two numbers can differ by exactly the backfilled games awaiting a rebuild, and both are true of what they say. A player with no rated row in the window has no climb rather than a climb of zero, which would otherwise crown somebody who never moved. | `mu_after is not null` as the universe (drops backfilled games from every number on the page, and contradicts the acceptance check); counting a climb from a missing rating as 0 (a fabricated number that can win an award) |
| 2026-09-10 | **The no-role footnote counts games where *no* row carries a role** (M5.4): `<N> games are not in the role numbers — …`. A game with nine roles and one gap is *in* the numbers, and its roleless row belongs to no role, exactly as it does everywhere else on the page | Product's sentence describes backfilled games, which carry `role: null` on all ten (M5.1), and an end-of-game block that recorded none — both all-or-nothing. Counting a game once per missing row would make the sentence overstate a game that is nine tenths in the numbers, and counting rows would need a noun product has not written. The two readings agree on every case the data actually produces. | Counting a game with any null-role row (overstates a partly recorded game); counting rows (`10 rows are not in the role numbers` is not the sentence in the copy table) |
| 2026-09-10 | **Every ranked list on `/stats` sorts on the *printed* percentage** — `Math.round(wins / games * 100)` — then games descending, then display name A–Z, then puuid (`compareRecords`, `compareDuos`, `compareDuosWorst`) | It is `lib/board/order.ts`'s rule applied to a second page: a column read top to bottom must never go up. Sorting on the exact ratio puts `71%` above `71%` in an order the reader cannot see and orders two visibly equal rows by a third decimal place; sorting on the printed number puts the row with more games first, which is the answer a reader expects and the one product's tie rule names next. | The exact `wins / games` ratio (an invisible sort key on a page of rounded percentages) |
| 2026-09-10 | **An award tie names every tied winner**, one line each under one bold label in Discord (`lines: string[]` on `AwardBlock`, joined with a newline for the post's field): most improved ties on climb *and* games, cursed duo on rate *and* games together | Product wrote it for the two-way case — *still tied, both are named* — and a coin toss between two equal weeks is the thing that rule exists to prevent. The degenerate case is a window whose teams never changed: ten pairs on the losing side then tie and ten lines print. That is honest and rare (the balancer re-splits every game), and product and the designer can cap it in M5.8 if a quiet week ever shows it. | Breaking the tie with the name rule (crowns one of two equal weeks by alphabet); dropping the award on a tie (says nothing where the group wants an answer) |
| 2026-09-10 | **The awards are rendered once, in `lib/stats/awards.ts`, and take an `AwardRender`** — `{ name, delta }` — so the page and the Monday Discord post print the same line in their own glyphs (`−212` and `05-design.md`'s truncation on the web; `-212` and escaped markdown in the channel). `postClosedWindow` calls `loadStats` with Discord's renderer and quotes what comes back, including the sentence an award nobody won prints; **a failed award read leaves the post as the board alone** | M5.10's rule is that every award line in the post is M5.4's, quoted and never re-derived, and the only thing that actually differs between the two surfaces is two glyphs. A renderer parameter keeps one implementation of the sentence and one set of minimums. The board is the message and the awards are three lines under it, so a second read that throws must not cost the group the Monday post — it costs them `Cursed duo` and one line in the log. | Building the lines twice, once per surface (two places to change a minimum); passing structured winners to the embed and formatting there (the post would re-derive product's sentences); failing the whole post when the awards read fails |
| 2026-09-10 | **`/stats` detects its own cap by reading `STATS_MAX_GAMES + 1` games** and dropping the extra, rather than asking for an exact count; the read is paged at PostgREST's thousand and the scoreboard rows are chunked by `inChunks` (the board's 90 ids) | The page needs one bit — *is there more than the cap* — and one extra row answers it without a second round trip to count a table that only grows. The cap is a **read** and not a filter, so the gate still refuses what it refuses inside it: a cap of 3 over a week whose three newest games include a short one leaves one counted game, which is the honest answer and what the integration test pins. | `count: 'exact', head: true` (a second query for one boolean); `limit(2000)` alone (PostgREST truncates at 1000 silently) |
| 2026-09-10 | **Six strings on `/stats` are the engineer's, not product's**, and are listed for product to rule on: `12 players played.`, `Nobody has 5 games on <role> yet.`, `No pair has 5 games together yet.`, `Longest win streak` / `Longest losing streak` / `On a run now`, `Nobody is on a run of three or more.`, and **`their main is top` where product's brief wrote `his main is top`** | The brief asks for a "not enough yet" line in every block and writes none of them, names the streak section's contents without naming its labels, and asks the header for the count of players who played, which the slot's pinned line has no room for. The pronoun is the one real change: the database knows a PUUID and a display name, so a possessive per player is a fact this product does not have. Every one of them is a module constant in `lib/stats/copy.ts` and marked `(engineer)` there, so a product pass is one file. | Inventing nothing and leaving the blocks blank (a quiet week would render five empty lists); interpolating a gendered possessive from nothing |
| 2026-09-10 | **The idle tonight page's main column keeps its 44rem cap at ≥1080px** (M3.30): `TonightView` puts `cn-grid-idle` on the grid in `idle` only and `tonight.css` caps `.cn-grid-idle > .cn-col`. The cap is on the column's **content**, not on the grid track | `05-design.md`'s grid table caps a single column at 44rem from 720px and the two-column rule dropped it, so an empty rack ran 1300px with `open` alone at the far left. Capping the content leaves the rail where it is, so a lobby opening at 21:40 does not slide the three cards on the right — the no-layout-shift rule reaching across a state change. `filling`, `balanced`, `in_game` and `result` fill their column with a rack of names or two team cards and are untouched, which is exactly what the designer asked for. | Capping the grid track (`minmax(0, 44rem) 20rem`, which moves the rail between states); capping every state (the team cards would lose the width the ≥720px rule gives them) |
| 2026-09-10 | **`/stats` is `force-dynamic`, and product's `export const revalidate = 300` is dropped with its reasoning kept** (M5.4): the page reads `searchParams` — the window is the whole of its state — and renders inside a shell that reads the session cookie, so Next renders it per request whatever the number says | With `revalidate` on it, `next build` spends a static attempt that fails on `cookies` and logs a dynamic-server error for a page that was always going to be dynamic; `force-dynamic` is the same outcome, stated, and it is what `/leaderboard` already does for the same two reasons. The half of the brief's reasoning that matters is untouched and is written into the page: **no precomputed stats table and no materialised view**, because either is a second thing that can disagree with `game_players`, and if the page ever gets slow the fix is `STATS_MAX_GAMES`. | Keeping `revalidate = 300` (inert, and a build-log error every deploy); `use cache` with `cacheLife` (needs `cacheComponents` turned on app-wide, which is a platform change and not this task) |
| 2026-09-10 | **The `/stats` copy rulings (M5.4)**, written into `05-design.md` as "Copy — `/stats`, the strings the brief did not write". The best off-role winner line is **`Omar · 9W 3L · 75% · their main is top`** — `their`, superseding the `his` in product's own brief, and M5.4's acceptance check 7 reads the copy table for that line. Five of the web engineer's seven invented strings **keep** as written and become product's: `12 players played.` (`1 player played.` at one, no line in an empty window), `Nobody has 5 games on jungle yet.`, `No pair has 5 games together yet.`, and `Longest win streak` · `Longest losing streak` — as does the singular form of the no-role footnote (`1 game is not in the role numbers — …`), which the brief wrote only in the plural. Two of the seven are **replaced**: `On a run now` → **`On a streak now`**, and `Nobody is on a run of three or more.` → **`Nobody is on a streak of 3 or more.`** | The pronoun is not a style choice: the database holds a PUUID, a display name its owner can change between two page loads, and nothing else, so `his` is a fact this product does not have about nineteen of the twenty people it prints — and the board pages already settled the same problem the same way (M3.26, `how unsure the board still is about them`). The five keeps are the empty-state voice this product already has, the fact in six words with nothing to tap (`Nobody in the lobby yet.`), and the noun in each is the one the surrounding page already uses; re-writing them would have been product's signature on identical sentences. The two replacements are one rule: the card is headed `Streaks`, the two labels above the block say `streak`, the leaderboard row prints `W3` and the brief says streak throughout, so `run` is a second name for a thing already named three times in one card — the rule that turned `standings` into `Leaderboard`. `3` and not `three` because the number is the module constant the block filters on, and the page's other two "not enough yet" lines already print their minimum as a digit | `his` (a pronoun per player, invented from nothing, and wrong on sight for at least four of the group); interpolating a stored pronoun (a profile field, a form to fill in, and the first manual input on a zero-input product); rewording the five keeps for the sake of a product fingerprint (churn a friend cannot see, against a file the engineer would have to re-verify); keeping `On a run now` (two nouns for one thing, and "on a run" reads as a winning one while the block lists `L4` beside `W3`); `Nobody is on a streak of three or more.` (a spelled number that can silently drift from the constant the list is filtered on) |
| 2026-09-10 | **`/stats` gets a headline, and the awards card is it** (M5.8, `05-design.md` "`/stats` — the window's page"): the three award lines go to `t-lg` 600 `text`, the card takes the result's own winner ring (`border-color: var(--cn-brand)`) when at least one award was won, and on a **running** window the card is not drawn at all — its one pending sentence becomes a `.cn-hint` in the header strip. In the same pass card titles become Archivo `t-sm` 600 `text` (`.cn-board-title`) on `/stats` **and** `/p/[puuid]`, and the mono `t-xs` `.cn-list-title` is retired. | M5.4 shipped the hierarchy inverted: a card's own name was the quietest text in it, the award winner was 2px larger than body, and the default window (`This month`) opened on a bordered box holding one 14px grey line. Mono micro-labels are legends and states, not section names, and a page whose headline outranks nothing reads as a spreadsheet — the exact failure Floodlit was written to end. The ring is an existing mark with an existing meaning ("this block holds a winner"), so nothing new is invented and the page keeps one lamp. | Splitting the award line into cells so the name could be set large (it is one product string shared byte for byte with the Monday Discord post). Keeping a stub awards card on running windows for structural consistency. Dropping the per-role rank column, which is what makes a role block a ranking rather than a set. |
| 2026-09-10 | **The off-role award's `Players with no main role are not in this one` note is about the *window's* players**, not the database's (M5.4): it prints when somebody who played inside the window has no main role, and a window where everybody's roles are known says nothing | The line exists to explain an absence a reader can see — why the friend who filled all month is not in this award — and that reader is looking at one window's names. Keyed on the whole `players` table it would print on every window forever the moment one person in the group is flexible, including windows they did not play in, which is a footnote that never changes and is therefore never read. | Any player in the database with a null main (a permanent footnote); never printing it (the absence is unexplained on the exact award it changes) |
| 2026-09-10 | **An award line carries the key it is rendered under**: `AwardBlock.lines` is `{ key, text }[]` — a winner's puuid, a pair's two joined, or `nobody` — and the Discord seam joins the texts with a newline. This supersedes the `lines: string[]` parenthetical in the tie row above | Two tied winners can print two lines that differ only by a name, and React keyed them on the sentence; the nobody sentence moves with the minimum, so it would rekey when a window kind changed. The people are what the line is about and they are stable, which is what a key is for. The post is unaffected — it takes the same three strings it always did. | Keying on the line's text (two tied pairs at the same record can collide, and every key changes when a minimum does); keying on the array index (React's fallback, and the thing keys exist to avoid) |
| 2026-09-11 | **`/p/[puuid]`'s `By role` is `lib/stats`' fold, and the board's own is deleted** (M5.20): `RoleRecord` and `roleRecord()` come out of `lib/board/types.ts` and `lib/board/load.ts`, `PlayerBoardView.roles` is gone, and the page draws one `By role` card from `loadPlayerStats` — the same `playerRoleRecords` `/stats` calls, over `gateGame`'s counted games, with product's five-row minimum and a percentage | M5.20 owes a role record with a percentage and M3.5 already shipped one without: two cards headed `By role` on one page is the drift the two number names are policed against, and the two do not merely differ in dress — the board folded *rated* rows (`mu_after is not null`) and the stats fold counts what the gate counts, so a backfilled game the rebuild has not folded yet is in one and not the other. One page, one record. The rest of M3.23's rated-only rule is untouched: the two numbers, the chart, the seed line and `19W 18L` still count the games the fold counted. **Two seams between the board's read and the stats fold survive this and are recorded, not fixed here** (both pre-date M5.20 and are the reviewer's finding, 2026-09-11): *(a)* the universe — the board reads rated rows and the fold reads what `gateGame` counts, so the two differ by exactly the backfilled games awaiting a rebuild, which is why the integration fixture stores a gate-refused game unrated, as `rebuild-ratings` does; *(b)* the window — the board's streak reads the season's last `STREAK_GAME_WINDOW` (200) games ordered by `started_at` alone, while the stats fold reads up to `STATS_MAX_GAMES` (2000) ordered by `started_at` then `lcu_game_id`, so a player whose last game is older than the group's most recent 200 gets `streak: null` on their board row and a real `W3` on their own page, and two games sharing an instant can order differently in the two reads | Keeping both (two records for one person, forty pixels apart, disagreeing after a backfill); rendering M5.20's numbers under a second heading (a fourth name for a thing already named); leaving `roles` on the view unrendered (dead data with a live-looking name) |
| 2026-09-11 | **A window the player has no counted game in draws none of the M5.20 sections at all** — no `By role`, no `By side`, no `Partners`, no `Streaks`, no average line — where the brief says "the sections print their empty lines" | The window's own sentence (`No games last week.`) is already in the header strip, and it is the true and complete answer; four cards each saying `Nobody has 5 games with them yet.` under it are four ways of repeating one line, and M5.8's rule 6 for the same case on `/stats` is that an empty window is a sentence with nothing drawn over it. It is also what the page already does with the chart and with `Recent games` at zero games. **Inside a window they did play, every section still prints its own empty line** — which is where the brief's clause bites | Four "nobody yet" cards under a sentence that already said it (the 449px-of-empty-card failure, four times); hiding the window's sentence instead (then a quiet week looks like a page that failed to load) |
| 2026-09-11 | **The award line on `/p/[puuid]` is `<award label>, <period>.` for *any* award that player won** — `Most improved, September.`, `Best off-role, week of 1 Sep.`, `Cursed duo, September.` — matched by puuid against `awardsView`'s own line keys, and the week form names the window's Monday (`week of 1 Sep`) rather than repeating the slot's `Monday 1 Sep to Sunday 7 Sep` | Product wrote the line for most improved and the clause it comes from is general ("a player who won an award in a closed window gets one line"); the other two awards are won by the same people on the same page, and a page that named one award and swallowed the other two would be lying by omission on the one surface the winner reads. The label is the award block's own constant, so the three cannot drift from the block they were won in, and matching on the key means a cursed-duo line finds both halves of the pair | A most-improved-only line (silently drops two of the three awards); re-deriving which award a player won from the numbers (a second implementation of three minimums); repeating the slot's full week range in the sentence (one page saying one week twice) |
| 2026-09-11 | **Eight friend-facing strings for the M5.20 sections are the engineer's and are listed for product**, as module constants in `lib/stats/copy.ts`: `By side`, `blue` / `red` as the two side rows, `Partners`, `Nobody has 5 games with them yet.`, `Current streak`, `Average game 32 min.` and `<award>, <period>.` — and the **no-role footnote is reused verbatim from `/stats`** with this player's own count in it | `05-design.md` says in as many words that the per-player sections' labels and empty lines are unwritten copy and come to product before they ship, and the sections cannot be drawn without them. Every one is either a two-word label in the shape of a label product already wrote (`By role`, `Best together`) or a "not enough yet" line in the shape of the three product ruled on for `/stats`. The footnote is not re-worded at all, because product's own note says its words are `/stats`' and only the count is this reader's — the smallest choice available, and one product can overturn in one file | Inventing a per-player rewrite of the footnote (`3 games of theirs are…` — a new sentence where a count change was enough); `Duos` for the player-page card (every row there is one other person, not a pair); a coloured chip for a side (the tonight page's tint means "which team", and this is a record) |
| 2026-09-11 | **The player page makes a second read** — `loadPlayerStats`, the same window read `/stats` makes, narrowed by a pure `playerStatsView` — issued **in parallel** with the board's load, and `lib/stats/load.ts` gains one shared `readWindow` both entry points call | The brief's rule is that the page "calls the same loader and picks one player out of the answer", which is the only way a record here and the same record on `/stats` are one computation instead of two that agree today. It is not folded into the board's read because that is a different query with a different shape (one player's rows, the season's ratings), and a third query path is a third thing to keep true. In parallel because the two are one page: a phone on a WhatsApp link should wait for the slower of them and not for their sum — the cost is one wasted read on a mangled URL that 404s | A player-scoped stats query (a second definition of the universe, which the brief forbids); one read shared with the board (a third query shape); awaiting them in sequence (every real visit pays both) |
| 2026-09-11 | **The seed a fold started from is stored on `ratings` as four columns** (`0012`, M5.7): `seed_mu`, `seed_sigma` — both or neither, `ratings_seed_pair` — plus `seed_rank_tier` and `seed_rank_division`, the **raw** pair `seedFromRank` read, not a formatted `Gold II`. `seed_mu is null` is what "no seed stored" means, so a null tier stays free to mean unranked. **This supersedes the 2026-09-09 row that introduced `seed_mu`/`seed_sigma` for M5.3's season carry-over**, which was dropped with M5.3 | The rating is a fold and a fold needs a first value; recomputing it from `players.rank_tier` meant the value changed whenever somebody's rank did, so a friend climbing to Platinum silently rewrote their own history — and everyone they played against — at the next `rebuild-ratings`. Storing the numbers is what the folds consume; storing the *input* rank beside them keeps `rankLabel` the one formatter, lets anybody re-derive the pair with `seedFromRank` to check it, and is what makes M5.15's `Seeded from Gold II at 1470.` name the rank the number actually came from | A single `seed_rank` label column (a formatting decision in the database, and unverifiable); deriving the seed at read time from a stored rank (the seed table copied into whatever code needed it); a `player_seeds` table (a table for four columns whose key is exactly `ratings`') |
| 2026-09-11 | **The backfill of existing `ratings` rows runs in `rebuild-ratings`, not in migration `0012`** (M5.7). The migration is additive and writes no data; the next rebuild fills every row that has no seed, with the seed **its own fold used**, and reports the count (`seeds  N to store for the first time`) on `--dry-run` | An `update ... case tier when 'GOLD' ...` in SQL is `packages/core`'s seed table copied into a migration, drifting from the day it is written, and CLAUDE.md's rule is that rating maths has exactly one definition. It is also less correct: the rebuild writes the number it is about to fold from, so the stored seed and the history under it agree by construction, where a SQL backfill could freeze a rank that had already moved and leave the next rebuild to shift everybody. The command has to be run after any backfill batch anyway | A SQL backfill in `0012` (a second seed table); a one-off script (a third thing to remember to run); leaving the columns null forever and treating null as "current rank" (the caveat never actually closes) |
| 2026-09-11 | **The live fold writes a seed only for a `ratings` row it is creating**, and never fills a row that already exists without one (M5.7). The four columns are in every upsert payload because PostgREST needs identical keys (PGRST102), carrying the stored value back for everybody else — a no-op write | The first fold that rates somebody is the only moment the seed is a fact rather than a guess. For a pre-`0012` row the seed is unknown, and freezing today's rank into it could disagree with the history already stored under it, so the next rebuild would move numbers to fit a value the live fold invented. The rebuild's backfill cannot have that problem because it writes what it folded from | Filling a pre-`0012` row from the current rank at the next game (freezes a value the stored history may disagree with); leaving the seed columns out of the payload for existing rows (PostgREST rejects a mixed-key bulk upsert) |
| 2026-09-11 | **`/p/[puuid]`'s seed line reads the stored seed** (`loadPlayerBoard`, M5.7 for M5.15): `Seeded from Gold II at 1470, 37 games since.` names the rank the fold started from, which may not be the rank the player holds tonight. With no stored seed it falls back to the current rank exactly as before | The line's whole job is to say where the number came from, and the number came from the seed the fold used. A page that named tonight's rank beside a rating computed from last spring's would be the specific disagreement M5.7 exists to end. The fallback keeps the page honest on a database that has not been rebuilt since `0012` | Printing the current rank always (the sentence and the number disagree the first time anybody climbs); printing nothing when a rank has moved (removes the one line that explains the number) |
| 2026-09-11 | **`fileParallelism: false` is set inside each vitest project in `apps/web/vitest.config.ts`, not only at the root** | With `projects` defined the root value does not reach them, so the integration files were running in parallel against one shared local stack: `vitest run lib/ingest lib/board` scheduled `rebuild.integration.test.ts` beside `roles.integration.test.ts`, whose games landed in the season `start_season` had just made active, and the rebuild folded twenty players and nine games it had never heard of. It failed about one run in three, and adding a single test file was enough to make it appear — the exact class of flake the root option was added to remove | `--no-file-parallelism` on the command line (a switch every caller has to remember); namespacing harder (the active season is a singleton and cannot be namespaced, which is why the option exists) |
| 2026-09-11 | **The per-player sections' copy on `/p/[puuid]` is ruled (M5.20): seven of the eight strings keep and one is replaced — the side rows are `Blue` / `Red`, not `blue` / `red` — and the two questions parked on that row are answered: the window's game count **comes out of both group statements** on `/stats` (`Blue wins 69% of the time.`, `Average game 35 min.`, the slot keeps `SEPTEMBER · 32 GAMES` byte for byte), and an empty window **adds no line pointing anywhere** — its sentence and the picker are the whole answer. The table in `05-design.md` ("Copy — the per-player sections on `/p/[puuid]`") is the source of truth for all of it; the two `/stats` strings are a task of their own because M5.20's out-of-scope forbids touching that page | Every friend-facing side in this product is capitalised (`Blue · 7695`, `Blue was favored 54%.`, `Blue wins 69% of the time.`), and roles are lower case in every surface because `top` is the word's form, not a list convention — so taking case off the role rows gives one thing two names on two adjacent pages, the defect that turned `standings` into `Leaderboard`. The three counts on `/stats` are one number from one call to `countedGames`, so the two `· 32 games` halves are the slot retyped twice, which this document has already refused twice on the player page (the meta line beside the seed line, the slot's range half alone). And an empty window is not a dead end: the picker is above the sentence with five links and the chosen one marked, so a pointer line would be a second navigation existing only in the failure state, computed from windows the page did not read, with `All time` — a group with no games at all — having nothing to point at anyway | `blue` / `red` matching the role rows' register (case copied instead of the word); `Duos` on the player page (a row there is one person, not a pair); a per-player rewrite of the no-role footnote (a second sentence to keep in step with the first, for one fact); a second constant for the player's average game line (identical text, two places to drift); keeping the counts in the two group statements (one number printed three times in one band); a "nearest window with games" line on an empty `/stats` (a feature and four more reads, not copy — if it is ever wanted it gets its own row) |
| 2026-09-11 | **The dress of the per-player sections on `/p/[puuid]` (M5.20)**, in `05-design.md` "The per-player sections on `/p/[puuid]`": the order down the page **stands** and nothing collapses or moves; the award line is `t-base` `text` at **weight 600**, in the flow between the rating card and `By role`; `By side` **keeps its card**, and `Blue` / `Red` print in Archivo at the row's `t-sm` in `--cn-blue` / `--cn-red` rather than `dim`; **no partner is in both lists** — `Best together` takes the top `min(3, N)`, `Worst together` the bottom `min(3, N - 3)`, at `N <= 3` only `Best together` is drawn, and with nothing qualifying `Nobody has 5 games with them yet.` prints once with no group label over it; the mean-game sentence loses its card; role icons drop to **16px**, the size `/stats` uses; and the **empty window ships as built** | The overlap is the only defect on the page a friend would call a bug on sight: the captures print `Theo - 6W 4L - 60%` under *both* labels, and "honestly in both lists" is arithmetic the reader cannot see. The colour is the content on the one card in the product where a side is the subject of a row rather than a team, and the page already paints a game block's leading rule by side; the case and the typeface are product's ruling in the copy table, and the dress follows the word. The weight on the award is what separates the page's only honour from the seed line forty pixels above it in the identical dress. The card around one sentence is M5.8's rule 3 read backwards - that rule gives *three* orphan sentences a body and says in the same breath that one does not need one. And M5.8's rule 6 ("nothing is drawn over the sentence") is a `/stats` rule: there the window is the whole subject, here the subject is a person whose two numbers are current rather than the window's, and a page about somebody that prints no number about them is a page that failed | Moving `Recent games` above the sections (measured: two and a half screens to it at 390, so the scroll it saves is not worth burying the sections the task exists to add); merging `By side` into `By role` or flattening it to one line (a new card title product has not written, or two records squeezed into one 390px line); `t-lg` on the award line (that size is `/stats`', where three winner lines carry names and numbers); reversing the best list for the worst (the same rows in the same order, backwards); dropping the rating card on an empty window (a person's page with no number on it); a tinted row or a coloured chip for a side (the team card's mark, meaning something else) |
| 2026-09-11 | **The side line's two sentences are kept byte for byte and are now rows in the tonight-page copy table (`05-design.md`, "Copy — final"): `Move to your side in the lobby.` with the switch gated off, `You'll be moved to your side — if not, move yourself.` with it on. One line under both cards, `balanced` only — **it is not drawn in `in_game`** — and inside `balanced` the deferral of M4.3's acceptance check 7 (the line disappearing once all ten seats match) is **confirmed, not softened**: check 7 stays open and both halves are carried by the new task **M4.11** | The words were settled twice already (the M4.2 and M4.3 briefs, and the 2026-09-09 row) and the teams embed prints the same two sentences, so a rewrite would be a rewrite in three places for no reader. The em dash is the house shape for *fact, then the fix* (`Your role is unchanged — tap it again.`, `No lobby was opened — tap it again.`), not an exception to the table's plain sentences. `in_game` is different in kind: the lobby is gone, so the sentence names a room that no longer exists and would sit there for half an hour. The vanishing-when-matched half needs each member's side in the tonight snapshot, which the page does not have; with the gate off nobody is moved by us anyway, so the stale window is the seconds between the last person moving and the game starting | Rewriting the second sentence to drop the dash (three surfaces edited, same meaning); dropping the promise clause and shipping one sentence for both gate states (it would either lie while the gate is off or under-promise while it is on); holding M4.7 (b) until the snapshot carries sides (the line is worth more today than the disappearing is); softening check 7 to "the line is present in `balanced`" so the task could tick |
| 2026-09-11 | **The side line's gate-on sentence is the page's too.** With `SWITCH_SIDE_ENABLED` true the tonight page prints `You'll be moved to your side — if not, move yourself.` under the team cards in `balanced`, the same sentence the teams embed prints; with the gate off both print `Move to your side in the lobby.` This supersedes the M4.3 brief's line that reserved the second sentence for the embed and gave the page only the first. The page's line vanishes once every seat matches (M4.11); until M4.11 lands it stays up for the whole of `balanced`. | One sentence per gate state on both surfaces is one rule; the brief's split predated the page having a live line at all, and a page line that promises the move and names the fallback is right for the seconds a friend is watching it. | The page printing `Move to your side` unconditionally (false once the companion moves people); two different gate-on sentences for page and embed. |
| 2026-09-11 | **The side line's placement, and the empty-window rule rewritten to what M5.23 built** (designer, `05-design.md`). The side line is **one line under both team cards and above the explanation strip**, Archivo `t-sm` `dim`, no rule and no card, `balanced` only (**M4.11** takes it out of `in_game`), never per card and never per person; in the teams embed it is the **last line of the `Seats` block**, under any `Swap:` line, which makes `Seats` a field on every teams post. And M5.8's rule 6 is rewritten: the window's empty sentence sits in the **header slot** in `dim` on all three pages, **the strip keeps its hairline on every window**, and nothing is drawn under it on `/stats` — on `/p/[puuid]` the rating card is the one thing that stays | The page's loudest prose in `balanced` is the explanation strip, because it is the bot saying why it did what it did; a second ruled block directly above it would be two things claiming to be the point of the screen, and the instruction is about a thing outside this page. In the embed the order is specific before general: a `Swap:` line names two people who must act, the side line is addressed to all ten, and a field of its own would be a heading over one sentence about seats. The rule-6 rewrite is a doc following the build: both halves of the first version — the hairline only when a section follows, and the sentence in the body at `t-base` in `text` — were written for `/stats` alone and left the three pages answering the same tap in two different places, which the one shared control cannot afford | A `brand`-ruled strip for the side line (the explanation's mark, meaning something else); one line per team card (the same sentence twice, and neither card is the one you are on); naming who is on the wrong side (stale the second somebody moves); keeping the line in `in_game` (there is no lobby to move in); a `Sides` field in the embed (a heading over one sentence); keeping rule 6 as written (a doc describing a build that no longer exists, on the one screen with the least to say) |
| 2026-09-11 | **The leaderboard row's streak is `lib/stats`' fold, over the same window `/p/[puuid]` folds** (M5.21): `loadStreaks(client, options)` in `lib/stats/load.ts` is the board's only streak read — the window's counted games, up to `STATS_MAX_GAMES` (2000), ordered `started_at` then `lcu_game_id` — `STREAK_GAME_WINDOW` and `loadRecentResults` are deleted, and `inChunks` moves to `lib/chunks.ts` so the board can import the stats loader without a cycle. **The rated-vs-counted seam is kept and documented, not closed**: `13W 15L` on a row is counted off `ratings` and the `L2` beside it counts what `gateGame` counts, so a backfilled game the rebuild has not folded is in the streak and not in the record — a row can read `0 games · 0W 0L · L1` until `rebuild-ratings` runs, which `board.integration.test.ts` now pins on the nameless seed | The two reads disagreed in two ways at once (the reviewer, 2026-09-11): a player whose last game was older than the group's most recent 200 had **no** streak on their row and a real one on their page, and two games sharing an instant could order differently in the two folds. Both are one number about one person printed on two pages forty pixels of navigation apart, which is the drift the `By role` ruling of the same day already refused. Reading the window rather than the season is what makes the two *equal*: the player page's read has no season filter, and with M5.14 there is one season, so `All time` and "this season" are the same games. The cost is the `All time` board reading up to 2000 games instead of 200 — the same read `/p/[puuid]` already makes, and no other surface pays it: every window row prints a climb and no streak, so `/leaderboard`'s default, the rail and both Discord posts read exactly what they read before | Folding the board's streak from rated rows over the stats window (the same number with two universes, which is the seam the reviewer opened the row about); keeping two reads and asserting they agree (a test that pins a coincidence); a `streak` column on `ratings` (a stored fold that a backfill silently staled); leaving the 200-game window and documenting it (the defect, in prose) |
| 2026-09-11 | **The `All time` fixture games in `board.integration.test.ts` gain seven seats each** (M5.21), so both are ten-row games the gate counts, and Zoe's recent-games lineup asserts five names instead of two | The board's streak now folds `gateGame`'s universe, and a three-row game is not a game the pipeline can produce — `rebuild-ratings` refuses it, so a streak read off one would have been an assertion against a state that cannot exist. The fixture's own comment about the 300-second game in `playerStats.integration.test.ts` is the same rule. Every pinned number is untouched: the ratings, the two `ratings` rows, the climbs and the window counts all come from rows the fillers do not touch | Dropping the streak assertions to `null` (the file's only positive streak coverage, deleted because the fixture was unrealistic); rating the filler seats at anything but a flat 25 (they would move the numbers the file exists to pin) |
| 2026-09-11 | **The window's empty sentence is one component on all three pages** (M5.23, the designer's ruling): `app/_board/WindowSlot.tsx` prints either the slot's line or `WINDOW_EMPTY[window]` as `<p class="cn-empty">` in the header strip, and `/leaderboard`, `/p/[puuid]` and `/stats` all render it. `/stats` loses its body block (`.cn-stats-answer` there, `t-base` in `text`) and its bare strip; `.cn-strip-bare` is deleted from `board.css`, so the hairline is on every page on every window. **This amends `05-design.md`'s `/stats` section, rule 6** ("An empty window is a sentence, and nothing is drawn over it"): the *nothing is drawn over it* half stands — no cards, no awards, no group lines — and the *where the sentence sits* half is now the slot, in `dim`, with the hairline kept. The doc line is the designer's to rewrite and is listed with the task | One picker, in one slot, on three pages, may not answer the same tap three ways: a friend who taps `Last week` on the board and then on `/stats` gets one sentence twice in two sizes, two colours and two places, which reads as two different failures rather than one quiet week. Rule 6 was written when `/stats` was the only page whose whole subject is the window, and M5.20's dress ruling already said in as many words that it is a `/stats` rule and that the player page keeps its slot and its hairline — so the rule was outnumbered two pages to one before this. The hairline stays because on every window that *has* games the same strip closes over cards on all three pages: a rule that appears and disappears with the answer is the "this page broke" signal rule 6 was protecting against, aimed at the header instead of the body | Moving the two board pages into the body to match `/stats` (the sentence is a statement about the control above it, and on `All time` the same slot sits over rows that exist); keeping three dresses and documenting them (the defect, in prose); a shared CSS class with three call sites (the class was already shared — `.cn-empty` — and the pages still disagreed about the element, the size and the hairline, which is why this is a component) |
| 2026-09-11 | **The side line is one line under both team cards, above the explanation strip, in `balanced` and `in_game` only** (M4.7 (b)): `app/_tonight/SideLine.tsx`, Archivo `t-sm` `dim` — the tonight page's hint dress, declared beside `.cn-hint` rather than as a second rule — printing `Move to your side in the lobby.` while `COMMAND_KIND_ENABLED.switch_side` is false and `You'll be moved to your side — if not, move yourself.` when it is true, read from `lib/commands/gate.ts` through a prop that only tests override. The two sentences are product's, quoted from the M4.2 and M4.3 briefs into `lib/tonight/copy.ts`; **`05-design.md`'s copy tables have no row for them** and the designer's "Teams" section is silent on placement, so both are listed for the designer with the task | The "Teams" section places every other element on that screen by the same rule — the sit-out strip above the cards and the explanation below them are each **one** statement about the split as a whole — and this is one too: per card it would be the same sentence twice, 200px apart on a phone, addressed to a reader who is on exactly one of the two. It sits above the explanation because it is an instruction about the seats just read, while the strip below is a different subject (why these seats) with the reroll control attached. And it is gated on the status rather than on the block, because the same `teams` block draws a **finished** game the fold did not rate (M3.4) — telling somebody to move to their side under `GAME OVER` is the one place the sentence would be false, which is exactly why M4.10's lobby line two elements down is gated the same way | One line per card (one sentence twice, and a reader is on one side); below the explanation (an instruction after the reasoning it is not about, and under the reroll button); `role="status"` (the sentence never changes while the block is up, so a live region would announce nothing); duplicating the gate as a page constant (two booleans to flip on the day the probe passes) |
| 2026-09-11 | **The tonight page's side line does not yet disappear when all ten are on their side** (M4.3's acceptance 7), because `MemberView` carries no `lobby_members.side` — the snapshot has never needed one. The line is drawn for the whole of `balanced` and `in_game` | M4.7 (b) is the *placement* task and the fix is a loader and snapshot change (a side per member, a comparison against each seat's card, and a Realtime path that re-renders the line without re-rendering the cards), which is M4.3's own acceptance and lands with the gate flip the probe unblocks. The line as it stands is true on a lobby that has just balanced — which is every lobby, for the first minute — and product's own note says "move to your side" read by somebody already on their side costs nothing | Adding `side` to the snapshot now (a schema-shaped change to the page's payload for a rule whose other half is blocked on a live probe); leaving the line out until M4.3 lands (the gate is off, so today the page says nothing at all about sides, which is the one thing the fallback exists to prevent) |
| 2026-09-11 | **The tonight page's side line renders in `balanced` only, not in `in_game`** (product, 2026-09-11, narrowing the M4.7 (b) row above; both sentences keep byte for byte) | Once the game has launched there is no lobby to move in: `Move to your side in the lobby.` names a room that no longer exists, and the automatic form promises a `switch_side` that the queue's three-minute TTL has already expired and that the client would refuse anyway. It is the rule M4.10's `Missed the invite?` line follows one element down in the same block — the two are both about a room that stops existing at kickoff — so the block now loses both at the same instant instead of one at a time | Keeping it through `in_game` (a live page telling twenty people to do something impossible, on the screen they stare at for thirty minutes); fading or dimming it further at kickoff (a third state for a sentence that is either true or false) |
| 2026-09-11 | **The teams embed's side line is a required `switchSideEnabled` on `TeamsEmbedInput`, resolved from `SWITCH_SIDE_ENABLED` inside `buildTeamsInput` at post time (M4.7 (b), M4.3)**; the two sentences are constants in `apps/web/lib/discord/embeds.ts` (`SIDE_LINE_MANUAL`, `SIDE_LINE_AUTO`, `sideLine(enabled)`) with `apps/web/lib/tonight/copy.ts` **re-exporting the three** so the page keeps importing its copy from its own copy file — one definition, pinned character for character by code point in `embeds.test.ts` and again from the page side in `app/_tonight/SideLine.test.tsx` | The one flag that decides whether a `switch_side` row is ever queued has to be the flag that decides which sentence is posted, or the message promises a move the server never makes; reading it in `buildTeamsInput` rather than in each of `post.ts`'s two entry points means the balance post and the reroll post cannot disagree, and it is a compile-time table of booleans, not an environment read, so the assembler stays deterministic. Required rather than optional on the input because a caller that forgot it would silently pick the wrong sentence. The words live in the embed module rather than the page's because that is the direction the pair already had for `NAMELESS_PLAYER` and because it is the module with no Next, no DOM and no page in front of it; the two surfaces shipped the sentences twice for a day, on two branches, which is exactly the drift a code-point test and a single definition close | Passing the resolved string into the embed (two callers composing copy, and the pure function stops owning its words); reading the gate in `post.ts` (two entry points, two chances to differ); an optional flag defaulting to `false` in the embed (the day the gate flips, a forgetful caller keeps posting the old sentence); leaving the two copies with a test to hold them equal (two places to edit product's sentence, and the test only catches the drift after somebody writes it); a `Sides` field of its own (`05-design.md` rules it out) |
| 2026-09-11 | **What a truncated embed field looks like** (designer, for **M4.12**; `05-design.md`, "Teams embed" → "Budget, and the last-resort guard"): the guard cuts **on line boundaries only**, drops the **lowest-priority lines first** — the side line in `Seats`, the first line of each award, and a board's top rows are the ones kept — and marks the cut with **one `…` line, alone, at the gap and not at the end of the field**, in plain text with no count. The 32-character display-name rule is unchanged and is still the first defence; the doc's old "do not truncate the field" is struck | A field cut mid-line (`Swap: Omar out, Nad`) reads as a corrupted message and sends the group looking for a bug; a field with fewer lines reads as what actually happened. Which lines go is not arbitrary either: a `Seats` field that keeps nine `Swap:` lines and loses the side line has dropped the only line addressed to all ten, so priority is by what the field exists to say. The ellipsis is already this product's mark for "there was more here" on every truncated name, so one character carries one meaning across the whole message, and it sits at the cut because that is what says *which* part is missing. No count: it is a number a reader can do nothing with, and one more thing that can disagree with the post. The guard is a guard — if it fires on an ordinary night the fix is the line that grew | Truncating mid-string with a trailing `…` (Discord's own failure mode, and it looks like a bug); `… and 3 more` (a count nobody can act on, and it can be wrong); one `…` at the end of the field (says something is missing, not what); dropping the whole field when it overflows (loses the instruction to save the list); raising nothing and letting Discord 400 (one long name costs the group the entire post) |
| 2026-09-11 | **Discord's length limits are guarded in one file (`apps/web/lib/discord/limits.ts`) and every embed builder emits its field values through it.** A field value over 1024 characters is cut on **line boundaries**, never inside a name or a markdown escape, with a single `…` line at the first gap; each line carries a `keep` rank and the field's keeper survives the cut — the side line of `Seats`, each award's first (winner) line, the top rows of the board. The same file guards title 256, description 4096, footer 2048 and the 6000-character total, and the **description gives way first** when the total is over. Below every limit the guard is the identity, so no snapshot moved. This supersedes `05-design.md`'s teams-embed budget line ("Truncate a display name at 32 characters … do not truncate the field") | `renderName` cuts at 32 **then** escapes markdown, so a Riot ID of escapable characters renders as 64 and a `Swap:` line as 144; eleven friends with such names give a `Seats` value of ~1500 characters (reviewer, 2026-09-11). Discord does not truncate — it answers 400 to the **whole** webhook, so the teams post is not shortened, it is lost, and the one line that has to be read (`Move to your side in the lobby.`) goes with it. The design doc's budget was computed on the pre-escape length and on names nobody was hostile with; a guard costs nothing on a normal night and is the difference between a shorter post and no post | Cutting the whole value at 1024 characters (slices a name in half and can leave a lone backslash escaping the `…`, and drops the side line, which is last); truncating names harder at the source (changes every normal post to defend against an abnormal one); dropping the `Seats` field when it does not fit (loses the side line, which is the field's reason to exist); leaving it to `webhook.ts` to retry on a 400 (nothing to retry with — the payload is the problem) |
| 2026-09-12 | **`/fun` is a fourth public page and folds the scoreboard columns `/stats` left out** (kills, deaths, assists, gold, damage, CS, `champion_id`) over the same window and `gateGame` universe. First blood and vision print as missing notes, not guesses — the companion already sees both and we do not store them. No new columns, no Discord post, no change to `/stats` or the three awards | M5.4 forbade KDA/CS on `/stats` because an award that moves every night is a statistic. The group still wants those numbers; a second page keeps the awards honest and lets a friend argue about a 0/11 without touching Proven. Inventing first blood from kill order we do not have would be a second truth | Putting KDA on `/stats` (breaks M5.4's award rule); a schema + ingest change in the same PR (a persist the companion already has, and a migration the owner has to run before the page is true); crowning vision from wards we do not store |
| 2026-09-12 | **`/games` is a public match-history page of captured customs**, newest first, expandable into both scoreboards from the columns `game_players` already stores (KDA, damage, gold, CS). Default window is `This week`. Optional `?p=<puuid>` is one person's list and switches the headline to their own `Won` / `Lost`. The list is every captured game in the window, not `gateGame`'s — a remake still happened, and `/p/[puuid]` already lists it. No champion art, no item icons: Floodlit forbids both and items are not stored as columns. Same anon read and cap as `/stats`. | The group already has the scoreboard in the database and was reading it only as `/fun` records and a five-game rating list. A history page is the surface those numbers were captured for. Keeping `/stats` award-only is the M5.4 / M5.24 split; this is the third page, not a fold onto either. Listing ungated games is the player-page rule (M3.23) applied to the group. | Putting the scoreboard on `/stats` (breaks the award page); champion art or Data Dragon (no Riot public API, no asset pipeline); parsing items from `games.raw` as bare IDs (unreadable without a name table); gating the list on `gateGame` (then a remake vanishes here and stays on `/p/[puuid]`) |
| 2026-09-12 | **`/games` lists Summoner's Rift by default and ARAM behind `?queue=aram`** (M5.26). Mode is `games.raw.gameMode` (`CLASSIC` / missing → Rift, `ARAM` → ARAM, `KIWI` and anything else → neither list). No `game_mode` column: existing rows already have the field in `raw`, and a migration would block the page until someone ran `db:migrate`. Filter is in memory after the window's 2000-game read, so a window of mostly the other map can starve this list until a column lets PostgREST filter. `/stats` and the rating fold stay mixed. | The group plays both maps as customs and a mixed history is two nights in one list. Rift is the default because that is the nightly 5v5 the product is for. Reading `raw` only on `/games` keeps `/stats` off the JSON blob. Leaving Kiwi off both lists is honest: the page is the two maps they asked to split, not a third dump | A `game_mode` column in the same change (a migrate before the page is true); mixing both maps (the request); treating a missing mode as unknown (every night captured before this page was Rift, and those rows would vanish); folding ARAM out of ratings here (a model change, not a history filter) |
| 2026-09-12 | **`/fun` uses the same Rift / ARAM picker as `/games`** (`?queue=`, default Rift, same `matchesQueue` over `games.raw.gameMode`). CS-by-role is hidden on ARAM — Howling Abyss has no lanes. One-game records stay. `/stats` and ratings stay mixed. **This amends the M5.26 row above** that left `/fun` on the mixed list. | A 0/11 on ARAM and a 0/11 on Rift are not the same record, and the group asked for the split on the page that crowns those numbers. One picker, one parameter, two pages, so a link pasted from `/games?queue=aram` lands on the same map on `/fun`. Hiding CS-by-role is the one section that would otherwise print five empty roles | A second `?map=` parameter (two words for one fact); keeping CS-by-role on ARAM (five "nobody on top" lines); a `game_mode` column (same reason as `/games`); splitting `/stats` or ratings (not asked) |
| 2026-09-12 | **The open scoreboard on `/games` and `/fun` prints the champion name under the summoner** — `champion_id` through the static id table, or the end-of-game `championName` when `raw` has it. No art, no Data Dragon | The expanded sheet was the place you go to remember the night, and a row of only Riot IDs cannot answer "who was on what". Floodlit already forbade portraits; a dim word is the name the client already stored | Champion art (no asset pipeline); leaving the name off because M5.25 said no art (that ban is portraits, not the word) |
| 2026-09-12 | **A one-game `/fun` record opens the same scoreboard `/games` uses** (M5.28), behind `This game`, a `<details>` with no client component. The holder's row is marked (`?p=` style). Habits and window totals do not expand. The sheet is `MatchSheet`, one component, two pages | The question after "Lena 18/2/9" is always "which night, and what did everyone else do". Linking out to `/games` would lose the record. A second scoreboard would drift. A window total has no one night to reopen | A client accordion (the page works with JavaScript off); expanding every habit (there is no game); a new compact sheet unique to `/fun` (two dresses for one scoreboard) |
| 2026-09-12 | **`/fun` reads first blood, steals, longest life and draft bans from `games.raw`** (M5.27). No new columns. The killer, `objectivesStolen`, `longestTimeSpentLiving` and `teams[].bans` are already in the blob the companion stores (EOG or match-history). The victim of first blood and the in-game first-death clock are not — the museum names the taker, their champion and the night, and does not invent a corpse. Fear Ban is the other side banning a person's most-played champion, over at least five counted games. Objective Thief is hidden on ARAM. This supersedes the M5.24 row that printed first blood and vision as missing notes | The page said the companion sees first blood and we do not store it; that was true of `game_players` and false of `games.raw`, which exists so later columns can be recomputed without a migrate. Crowning a victim we do not have would be a second truth. A champion-id word table is not Data Dragon — it is a display map, and a missing id prints `Champion ${id}` | A `first_blood` column (a migrate before the museum is true, and the blob already has the flag); guessing the victim from `longestTimeSpentLiving` (that number is the longest gap, not the first death); keeping the "not stored" notes (a lie once the loader reads `raw`) |
| 2026-09-12 | **The open scoreboard fills a missing role for display only** (`/games`, `/fun` This game). Stored `game_players.role` wins. A backfill is null (M5.1), so the sheet reads `detectedTeamPosition`, Smite, and the few `timeline.lane`/`role` pairs the 16.17 fixtures did not refute (`MIDDLE+SOLO`, `BOTTOM+CARRY`, `BOTTOM+SUPPORT`), then the leftover unique lane on a five-seat Rift side. A lone sub-50 CS seat against laners at 80+ is support. Never the champion. ARAM invents nothing. `/stats` and `inferRoles` never see this map | A sheet with five Riot IDs and no roles looks unordered because `inLaneOrder` then sorts the blanks by PUUID. Writing a guessed role into `game_players` would move somebody's main (M5.16). Mapping `JUNGLE+NONE` or `TOP+SOLO` would reprint the lie M5.18 already documented | Inferring from the champion (forbidden, M2.10); writing the fill back (corrupts inference); using the unverified timeline table as stored truth (M5.18) |
| 2026-09-12 | **First Blood Donated is a second museum**, same grouping as the killer list, filled only from `firstBloodDeath` / `FIRST_BLOOD_DEATH` on `games.raw`. A game that named the killer and not the death is omitted. Deaths, `longestTimeSpentLiving`, and "the only enemy who died" are not a victim. When both flags exist in one game the two rows name each other (`over` / `to`). The 16.17 fixtures do not carry the death flag, so a live window can print the empty sentence | The group asked who fed first blood. The killer flag is already in the blob; the corpse is not, and inventing it from a short life or a unique death would be a second truth | Inferring the victim from `longestTimeSpentLiving` (longest gap, not first death); picking the only enemy with a death (true in a 1-kill remake, a guess in a real custom); leaving the section off after they asked for it |
| 2026-09-12 | **First Blood Museum is one row per killer**, hairline between people. One opening is the row; two or more open **See games**. Deathless games and Career thief use the same expand. A steal prints `1 dragon steal` only when the block named exactly one epic type they also killed; otherwise `1 steal`. **Most banned** and **Most picked** count the lobby's champions, not a person's Fear Ban | A flat 25-game list mixed two people's bloods with no divider, and a window total of one custom had no night to reopen. The group asked who the lobby fears and who it locks, which is a champion table, not a player sentence | Keeping the flat museum (two people read as one list); inventing a steal type from mixed dragon+baron kills (a second truth); putting Most banned on ARAM (no draft) |
| 2026-09-12 | **Public pages default to Day**, a light gaming look; Night is the same system after dark; Current is Floodlit in `theme-current.css` so it can be deleted without touching Day or Night. The choice is `data-theme` + `localStorage`, not `prefers-color-scheme`. Admin stays on the OS. | The group asked for a modern gaming default and a way back to the old look that is cheap to remove. Following the OS made the first WhatsApp paint depend on a phone setting nobody in the group had agreed. Isolating Current means a later "we don't want it" is three deletions, not a revert of the new look. | Making Night the default (the old Floodlit rule); a two-theme toggle with Current as Night (then dropping Current also drops dark); a cookie (the choice is this device, not an identity) |
| 2026-09-13 | **Night is the default; Current is gone.** Two chips, same recipe as the window picker. The toggle syncs from `data-theme` after mount so a refresh cannot show Day selected on a Night page. A stored `current` becomes Night. | The three-way pill fought the rest of the chrome, and SSR always painted the default chip while the bootstrap script painted the stored page. Night is the room this page is opened in. | Keeping Current (the group asked it out); leaving the chip on the SSR default (the refresh bug) |
| 2026-09-12 | **Day and Night are a full production gaming layer** in `theme-gaming.css`, not a recolour of Floodlit. Same Archivo + Plex Mono on both; two palettes; HUD chrome, pitch grid, tracker-dense match cards. No extra font, no art, no layout rewrite. Floodlit files stay as Current paints them. | The group asked for a site that reads as a shipping tracker, with one style in light and dark. Recolouring Floodlit still read as Floodlit. A third font would cost the WhatsApp first paint. Changing markup would move thumbs on Tonight. | Recolour only (rejected: still the old page); a new typeface (speed); rewriting the React trees (breaks the reserved 44px rack and every snapshot) |
| 2026-09-12 | **`/fun` adds Pentakill, Quadrakill, Triple, Double and First Turret museums** from stored `pentaKills` / `quadraKills` / `tripleKills` / `doubleKills` / `firstTowerKill` on `games.raw`. Counts are summed; a custom with two triples is one opening labelled `2 triples`. Longest killing spree is a one-game record from `largestKillingSpree` of at least three. Never inferred from KDA, gold, or tower gold | The group asked for pentas, quadras and triples, and for more stored numbers they can argue about. Those fields are already in the blob the companion stores. Crowning a penta from an 18/2 would be a second truth | Inferring multi-kills from KDA (the block already names the count); a `penta_kills` column (a migrate before the hall is true); hiding empty halls (the category is the joke when nobody has one) |
| 2026-09-12 | **Every `/fun` title carries an Egyptian 3ameya roast** under the English heading, in brand, marked `lang=ar`. Not فصحى and not a translation (`مين فتحها`, not `فاتح الخريطة`). English stays the name of the fact. No Arabic webfont — Archivo has no Arabic, the roast falls through to the system face | The group asked for Arabic roast titles and then for مصري عامي, not a formal gloss. A second English joke would fight Floodlit's "plain nouns". Replacing the heading would break tests and pasted names | Translating the English title into فصحى (a dictionary, not a roast); swapping the heading to Arabic (two names for one fact); loading Noto Naskh (a designer webfont the rest of the product does not use) |
| 2026-09-12 | **CI is one job on `ubuntu-latest` (`.github/workflows/ci.yml`), running the four commands CLAUDE.md already documents plus `pnpm --filter web build`, on `pull_request` and on `push` to `main`, and it requires no secrets.** pnpm comes from `packageManager` (`pnpm/action-setup@v4` with no `version:` input), Node from a new `.nvmrc` pinning **22** — the floor `engines` already declares — with `actions/setup-node@v5` caching the pnpm store. Integration coverage is **skipped, not excluded**: the job sets `SUPABASE_LOCAL_URL=http://127.0.0.1:1` with dummy local keys, so `resolveLocalStack()` returns null and all 22 `*.integration.test.ts` files `describe.skip` (verified: `4 passed \| 2 skipped` in `packages/db`, `58 passed \| 20 skipped` in `apps/web`, everything else untouched). No `test:unit` script and no `--exclude` glob is added. `pnpm --filter web build` gets dummy values for the two `NEXT_PUBLIC_SUPABASE_*` variables | A private repo with friends as collaborators needs the branch protected by something, and the cheapest something that cannot drift is the commands a human already types: a CI-only script is a second definition of "green" that goes stale the first time somebody changes one and not the other. Skipping beats excluding because the skip is the behaviour the files were written with and the one a laptop without Docker already relies on — an `--exclude` glob would make CI's suite a different suite from everyone's, and the first integration file named without the convention would silently stop running everywhere. The three variables are set rather than left unset because unset makes the helper shell out to a `supabase` binary and the skip would then depend on that binary being absent from the runner image, which is a property of GitHub's image and not of our repo. Dummy build env because every route is dynamic today and the build passes with nothing set at all — so the variables are there to make the fix for a future prerendered page "edit this dummy string", never "add a secret". No secrets at all keeps a collaborator's branch run identical to `main`'s and means nothing in the repo can exfiltrate one | A `test:unit` script with `--exclude '**/*.integration.test.ts'` (a second suite definition, and CI stops being reproducible by `pnpm -r test`); standing up Supabase in CI with `supabase start` and Docker (a minute of startup and a database thrown away, for coverage that is a `pnpm db:start` run away locally); a matrix over Node 22 and 25 (twice the minutes to learn the same thing, on a project one person deploys); separate jobs per command (each one re-installs the workspace, so three minutes becomes ten); pinning the actions to commit SHAs (a private repo with three collaborators, and a Dependabot-shaped chore nobody here runs); `pnpm --filter companion build:win` in CI (it downloads a Node runtime to make an exe, and releases are cut from a laptop by `pnpm --filter companion release`) |
| 2026-09-13 | **Most banned reads `teams[].bans` from every stored shape**, and a later match-history post copies that list onto a live eog row that never had it. The eog block has no bans (verified 16.17). Backfill used to skip any `lcu_game_id` already stored, so last night's Yi/Zac never entered the table. Scoreboard columns, first blood, multi-kills and picks were already on the eog block and did not need this. Never invent a ban from a pick or a Fear Ban sentence | The group bans the same two champs every night and the table did not name them. The numbers were not wrong on the games that stored a list; the newest games never stored one | Guessing Yi/Zac from what people play (a second truth); replacing the eog raw with the match-history detail (loses `detectedTeamPosition` and `championName`); a `bans` column (a migrate before the table is true) |
| 2026-09-13 | **`/leaderboard` expands a row into that window's rated games**, newest first, as a closed `<details>`. Each game prints `Won`/`Lost`, the night, the duration and `displayDelta(mu_before, mu_after)`. The rail does not attach the list. No new table: `game_players` already stores the per-game fold | The climb on a window row is one number with no way to see which nights made it. `/p/[puuid]` already has the last five; the board is where people argue about the week | A `game_ratings` table (the columns already exist); opening the rail (the rail is a snapshot and serializes these rows); putting the lineup under every name (that is the player page) |
| 2026-09-13 | **Daily Mystery is one civil-day challenge for every visitor**, rotating at 00:00 in `CUSTOMS_NIGHT_TZ`, not the 06:00 night boundary (M5.32). No accounts. The visitor is a random cookie / localStorage id. The League player is a different person. Competition is anonymous: First Detective (CAS on `first_correct_at`), clue-then-time percentile among correct guesses (quiet under 10), guess distribution only after lock-in. Tables are service-role only so PostgREST cannot leak the answer. Selection scores unusual KDA / CS / damage-taken / ghost games and avoids recent games (21d) and players (7d) | The group asked for a daily "who was it?" they can open without signing in. A named leaderboard would let anyone type Ahmed and claim first. Midnight is what they asked for (a calendar-day number and a countdown to 12:00 AM); sit-out stays on 06:00 | Accounts (the product has none for friends); rotating at 06:00 (honest with the night, but not the spec); sending all clues in the first GET (the answer in disguise); public-read tables (anon could `select mystery_player_id`) |
| 2026-09-13 | **Supersedes the 2026-09-10 row that had `create_lobby` match `name`/`pickMode`/`banMode` words on the dialog's own `mutators[]`.** The real 16.18 capture (`custom-game-queues.json`, from `--verify-commands`) showed every dialog entry on Summoner's Rift wordless — `name` is just the `id` as a string, `pickMode`/`banMode` are `""` — for every mode, not only blind, so the old matcher resolved nothing for draft and the flip would have nacked `client_rejected` on every real client despite green tests built on invented dialog fixtures. The executor now also reads `GET /lol-game-queues/v1/queues` every time and joins each mutator's `id` against that list's `name`/`gameTypeConfig` (`game-queues.json` names 3100/3110/3120/3130 correctly); `chooseCustomLobbyMutator`/`customLobbyIdsFor` in `packages/lcu/src/writes.ts` take the queue list as an optional third argument, matching on the dialog's own words **or** the joined queue's, so a patch that does put words back on the dialog still works with no queues argument. Still no constant id; still nacks `client_rejected` with the list when nothing resolves | A reviewer caught, and this session independently verified against `packages/lcu/fixtures/16.18/custom-game-queues.json` and `game-queues.json`, that the shipped M4.1 gate flip would nack every real `create_lobby`: `CustomGameQueuesSchema` also rejected the real body outright (`gameServerRegions: null`, fixed to `.nullish()` in the same edit) and, even parsed, the wordless dialog resolved to `null` for draft. The `3110` used in the 16.18 live verification run was typed by a human at the `verify-commands` prompt, never resolved by this code — the report calling it "the `ui` shape with the live dialog entry" overstated what the code did | Hard-coding 3100/3110 now that they are known for this patch and server (a constant the next patch breaks silently); dropping the dialog's own words entirely and joining only (loses the one patch where the dialog *did* carry words, 16.17's evidence for blind); a human-typed id as the production path (M4.1's whole point is zero-input) |
| 2026-09-15 | **Only Summoner's Rift customs rate** (M7.1, the user, settled 2026-09-15). A game is rated when `gateGame` passes it *and* `matchesQueue(gameModeFromRaw(games.raw), 'sr')` does — `CLASSIC` or a missing mode. ARAM, `KIWI` and anything a later patch invents are stored whole, with their ten rows and their scoreboard, and carry four null rating columns for ever; a new `FoldSkipReason`, `game-mode`, says so in the log. The check is a **second gate beside `gateGame`, not inside it**: `countedGames` in `lib/stats/fold.ts` calls the same function, so a mode check with no second door would empty `/fun?queue=aram` and take every ARAM game out of `/stats`. A `rebuild-ratings --force` is owed the same session and un-rates the ARAM games already folded | The balancer forms Rift teams from a number that, since the first ARAM night, has been part Howling Abyss: a mode with no lanes, no roles, no wards and a coin-flip snowball was moving the rating that decides who plays jungle. That is not a tuning disagreement, it is the gate missing a clause — participant count, side split and duration were written in M2.5 when the group only played Rift, and M5.26 added a mode to `games.raw` in 2026-09-12 without anything downstream reading it for the fold. Treating a missing mode as Rift is M5.26's rule kept, not a new one: every night captured before the field was read was Rift, and calling it unknown would un-rate the group's whole history. Nullable storage over deletion because an ARAM custom still happened — it is on `/games`, on `/fun`, in the museum and on a player's page, and the only thing it must not touch is `ratings` | A `game_mode` column (M5.26 already refused one: a migrate before the fix is true); rating ARAM into a second, separate ladder (a fourth number nobody asked for, on games the product is not about); dropping ARAM rows at ingest (loses the history the group reads every week); putting the mode check inside `gateGame` and accepting the page fallout (`/stats` and `/fun` silently losing a map, which M5.26 decided in as many words they would not); leaving the ARAM games rated and only gating new ones (two models in one history, and the rebuild would disagree with itself) |
| 2026-09-15 | **The week gets a second, independent rating; the all-time one is not reset** (M7.2 to M7.4, the user, settled 2026-09-15). `ratings` stays exactly as it is, still forms teams, still settles over about thirty games. Beside it, `This week` and `Last week` are folded **at read time** from scratch: seeded from the player's stored seed (`ratings.seed_*`, M5.7) or `seedFromRank` when they have none, through that week's rated games in `started_at`, `lcu_game_id` order, with `openskill`'s `beta` and `tau` tuned in a new `config.rating.weekly` block so the number settles in about five games instead of thirty. It drives the weekly board, the weekly `Most improved` and the Monday post, and **never** team-forming — nothing under `lib/ingest/` may import it. Month windows and `All time` are untouched | The group's actual complaint was that a weekly board ranked by all-time standing tells them nothing about the week, and `lib/board/load.ts` had already made that choice on purpose in its own comment ("a weekly board sorted by who climbed most this week would be a second ranking with a second meaning"). It was right that a second meaning needs a second number — so this gives it one rather than overloading the first. A full weekly reset was refused because it reopens the 2026-09-10 "ratings never reset" row and would have Monday's first lobby balanced almost blind, which is the one night of the week the balancer has to be at its best. Read-time and not a table for M5.9's reason: a game backfilled three weeks late lands in the week it was played, on every page and in every past post, with nothing rewritten. The seed is the *stored* one and not the live rank column because `Last week` has to read the same on Tuesday as it did on Monday, and a seed taken from `players.rank_tier` changes the closed week the first time somebody climbs. And the board prints what it sorted on — the product doc's own rule — so a week window shows the weekly Proven and the weekly climb, not one number ordered by another | Resetting the real rating every Monday (the user rejected it: it reopens a settled decision and blinds Monday's balance); re-sorting the existing window rows by climb (the same numbers with a second meaning, which is the defect the comment in `load.ts` already named); a `weekly_ratings` table with a Monday cron (a persisted fold a late backfill silently stales, plus a cron to be down); seeding the week from each player's all-time `mu` (then it is the all-time board again, slightly blurred); tuning `rateGame` itself to settle faster (one model, two jobs, and every stored `mu_after` in the database rebuilt to mean something new); extending the weekly track to the month windows (a month of customs is already thirty-odd games, which is what the all-time number settles over — it would be a second name for the same thing) |
| 2026-09-15 | **Filling the same person twice in a row costs the balancer more, and is never blocked** (M7.5, M7.6, the user, settled 2026-09-15). The off-role price becomes `offRolePenalty × (1 + fillProtectionFactor / (gamesSinceLastFill + 1))` with the factor at 1.0 — 240 display points for somebody filled last game, 180 one game later, 150 after three, decaying to the flat 120 — applied in **both** places `offRolePenalty` is read (`assignRoles`'s permutation value and the split score). `gamesSinceLastFill` is a per-player input on `BalancePlayer`, `null` meaning baseline, computed by the caller from `game_players.counts_for_role_inference = false` over the player's last twenty counted games. `packages/core` stays pure and the input is the caller's problem | League's own fill protection is the mechanic the group already understands, and the flat 120 is blind to history: the same two people ended up filled night after night because a flat penalty makes every fill equally cheap, which is exactly what "the ratings feel unfair" turned out to mean in half the complaints. It is a soft cost and not a hard block because a lobby where one person is the only jungler still has to be split — the balancer's job is to price the unfairness, not to refuse the night — and because a hard rule would have to be explained on a screen, while a price just changes which split wins. No new column is needed at all: M5.17 already writes `counts_for_role_inference = false` for precisely the players the balancer put on a role that was neither their main nor tonight's override, recorded at fold time because that is the only moment it is knowable. Both call sites scale together or the scorer prices a seat one way and ranks the split another, which is a bug that only appears on the nights it matters | A hard "cannot be filled twice in a row" rule (an unavoidable lobby then has no split at all, and the referee stops being able to answer); remembering which *role* they were filled into (finer than the complaint, and the product's role model is main / backup / fill, not a matrix); a `games_since_last_fill` column (a stored number a backfill stales, for a fact two joins already answer); putting the history read in `packages/core` (it is pure and it has no database); raising the flat `offRolePenalty` instead (makes every off-role split expensive, which starves the nights where somebody genuinely does not mind) |
| 2026-09-15 | **MVP keeps a quarter more of the win, ACE loses a fifth less, as a bounded adjustment after `rateGame`** (M7.7 to M7.9, the user, settled 2026-09-15). MVP is the highest-scoring player on the winning team and ACE the highest on the losing team — op.gg's own terms and own model, verified 2026-09-15; their formula is proprietary, so ours is a documented community approximation and a tunable like everything else in `config.ts`: `0.10×KDA + 0.20×damage to champions + 0.20×gold + 0.25×vision + 0.15×damage self-mitigated + 0.10×CS`, every component normalised against the best of the ten in that game so the score is in `[0, 1]`. The adjustment scales that player's own `mu` delta by `1 + 0.25` or `1 - 0.20`, **never touches `sigma`**, never flips a sign, touches nobody else, and happens **after** `rateGame` inside `foldGame` so both callers share one implementation and the OpenSkill maths stays untouched and independently testable. Vision score and damage self-mitigated are not in `game_players` today: migration `0014` adds them nullable, ingest fills them, and a one-off copy from `games.raw` fills the history. **With any of the six missing for any of the ten there is no MVP and no ACE**, and the game rates exactly as it does today | Ten friends do not believe a model that gives the 18/2/9 carry and the 0/11 support on the same winning team the identical number, and "the bot is the referee" only survives while the referee is credible. Putting it after `rateGame` rather than inside the update is what keeps that credibility cheap: the base rating is still plain OpenSkill with M1.3's pinned tests, the adjustment is one multiplication over a stored delta, and either can be turned off without touching the other. Sigma is left alone deliberately — Proven means "how sure the model is about you", and certainty is not something anybody earns by farming wards; amplifying sigma would let a good night jump somebody up a leaderboard that exists to make them prove it. Normalising each component inside the game is not a detail: unnormalised, gold is a four-digit number and KDA is a one-digit one, so the weights would be decorative and MVP would be "whoever farmed". Nullable columns and an all-or-nothing rule because a partial score would rank a player who has a vision number against one who does not, and the 16.17 fixtures show both keys on both paths (`VISION_SCORE` / `TOTAL_DAMAGE_SELF_MITIGATED` on the end-of-game block, `visionScore` / `damageSelfMitigated` on the match-history detail) so the copy from `raw` can make the history whole rather than leaving one model for games played after Tuesday | Baking performance into the OpenSkill update itself (the model stops being OpenSkill, M1.3's tests stop meaning anything, and nobody can say what the number is any more); calling op.gg for a real OP Score (an external dependency on an unpublished endpoint, in a project whose rule is that stats come from the local client and there is no API key); scoring KDA alone (the carry-only model the group already argues about, and it would crown the same person every night); `not null default 0` on the two new columns (a tank with no stored mitigation would score as a tank who mitigated nothing); applying the bonus only to games played after the change (two models in one history, and the first rebuild would rate them against each other); adjusting `sigma` too (a good night buying certainty on the board that sorts on the absence of it); a bonus for the top scorer on each side regardless of result (then the winning team's worst player and the losing team's best are treated the same way, which is the argument this exists to end) |
| 2026-09-15 | **Three resolutions that close M7's open questions** (the user, the same evening as the four rows above). **One rebuild, at the end**: no M7 task runs `pnpm --filter web rebuild-ratings` against the hosted project, M7.1 lands the ARAM gate and leaves the already-folded ARAM games folded, M7.9 lands the bonus and leaves the stored history without it, and **M7.11** — a task of its own, last in the milestone — is the single dry-run-then-`--force`-then-confirm run, announced to the group in plain words before anybody notices. **No monthly track**: `This month` and `Last month` keep ranking on the all-time number, decided rather than deferred, and the weekly fold is not to be generalised over an arbitrary window "in case". **The bonus is surfaced**: **M7.10** prints `MVP Lena · ACE Rami` under the result post and the word `MVP` or `ACE` beside the delta on `/p/[puuid]`, plus one sentence in that page's rating explanation, all from what M7.9 already stored and with no new read | Rebuilding per fix would move every player's number three times inside a week, and a group that opens the board on Tuesday, Thursday and Sunday to three different numbers stops reading the board — the fix would become the argument. Holding the run costs only a little staleness in stored history that no page distinguishes, because the live fold is already right from the moment each task lands. The month windows are not a gap: a month of nightly customs is roughly the thirty games the all-time rating settles over, so a from-scratch monthly track would be a second name for the same reading, and leaving it "open" would invite exactly the generalised-window fold that makes the weekly track look like a feature flag instead of a deliberate second number. And the bonus had to be visible because the product's third principle is that a number which moves has to be explainable: after M7.9 two `+43`s on the same winning team are not the same `+43`, and a friend who spots that without being told has found the referee doing something it did not announce — which is the one thing "the bot is the referee" cannot survive. The names and nothing else, because the score itself is a number a reader can do nothing with and one more thing that can disagree with the post | A rebuild per fix (three moves, three arguments, and no way to attribute any of them); no rebuild at all, leaving ARAM in the history for ever (the bug stays in the numbers the balancer reads); a from-scratch monthly rating for symmetry (a third number nobody asked for, saying what `All time` already says); leaving the month question open (an invitation to generalise the weekly fold and make both windows mean less); shipping the bonus silently (cheapest to build, and it breaks the one principle that keeps the referee credible); printing the performance score beside the names (an unexplained decimal the group would argue about instead of the game); an MVP count on `/fun` or an award for it (that page's register, and it can have its own task if they ask twice) |
| 2026-09-15 | **The weekly board never says it settles** (M7.2, M7.3, product 2026-09-15, amending the "second, independent rating" row above where it says "settles in about five games instead of thirty"). `WEEKLY_SETTLING_GAMES = 5` and the sentence `It settles after about 5 games.` are dropped before either was built. No row on `This week` or `Last week` carries the `settling` chip, and the note under the board says what the week actually does instead: `Every week starts everyone back at their rank on Monday, so a good Tuesday shows up here straight away. The board sorts on Proven — your rating, minus how unsure it is about you — and a week is too few games for it to be sure of anyone, so every number here sits low and swings. All time is the settled one, and the one that makes teams.` — with a one-line short form for the nightly and Monday embed footers. `SETTLING_GAMES`, the chip and all three `SETTLING_SENTENCE*` constants are untouched and keep printing on `All time`, the month windows and `/p/[puuid]`. M7.2's tuned `config.rating.weekly` constants do **not** change; only what the doc claims they are for | "Settles" has one meaning in this product — `sigma < 5.00`, the 2026-09-10 row, the word behind the chip and behind `SETTLING_SENTENCE` — and core-engineer measured the weekly track reaching it at game **30** against the all-time channel's 36, with `beta`/`tau` already tuned as far as they safely go (further and a settled player swings 100 display points off one game, and `sigma` stops converging above `tau ≈ 0.5`). A group that plays one to three games a night never reaches 30, so the choice was a chip that says "settled" at game 5 when the number is still mostly seed, or one that reads "still unsure" for everyone for ever — the second is the board nobody believes that `00-product.md` already refused, and the first is the kind of number that made the ratings feel dishonest and opened this milestone. The week is worth having for a different, measured fact: from the Monday rank seed a game moves a weekly number ~79 display points against a settled player's ~29, so the real effect is M7.3's reseed and not M7.2's tuning, and that is what the copy claims. The chip also had no job left: it marks the few rows the board is least sure of, and on a week that is all ten | Keeping `5` as a display threshold with `settles` reworded around it (the chip would still be the app's one word for "this number can be trusted now", handed out at game 5); anchoring the chip to `mu` crossing the seed instead, which really is a game-4-ish event (a second private meaning for the one word every other surface uses for `sigma`, and unexplainable in a sentence a friend would say); shipping the honest `about 30 games` (true, and it labels every player on every weekly board "still unsure" for ever); dropping the sentence entirely (leaves `Proven` undefined on the product's **default** window and never explains why every number there sits a thousand points low); re-tuning `beta`/`tau` harder to make 5 real (rejected by measurement: a board moved 100 points by one game is about the last game, not the week) |
| 2026-09-15 | **The week starts on Sunday** (M5.34, the user, settled 2026-09-15). **Supersedes the weekday half of the 2026-09-10 "ratings never reset / windows" row** — `This week` and `Last week` are **Sunday 06:00 to Sunday 06:00** in `CUSTOMS_NIGHT_TZ`, not Monday 06:00 to Monday 06:00 — and re-anchors every derived surface: `weekStart` in `apps/web/lib/night.ts`, the slot and post range (`Sunday 6 Sep to Saturday 12 Sep`, from the same formatter off the window's own bounds), the closed-week key the cron dedupes on, and the weekly reseed M7.2 to M7.4 are written against, whose briefs were amended the same day. **The 06:00 hour, the night boundary, the calendar-month boundary, `CUSTOMS_NIGHT_TZ` and the daily `30 4 * * *` cron schedule do not change**, and no rating changes by one digit: a window is still a filter over games. `/api/cron/window` and `lib/discord/windowPosts.ts` need **no logic change** — checked, they hold no day-of-week test; the post moves from Monday morning to Sunday morning because the boundary moved. The one-off: shifting the anchor changes the most recently closed week's `window_posts` key, so the overlapping `last-week` is stamped posted at deploy rather than sent, because a duplicate board is a thing ten friends see. **Done on the hosted project at the flip, 2026-09-15**: `window_posts` held posted `last-week` rows for `2026-09-07` and `2026-08-31` (the last two Monday-anchored weeks), so there was something to suppress and the Sunday-anchored `last-week` key `2026-09-06T03:00:00.000Z` — Sunday 6 Sep 06:00 Cairo, the week the next cron call would otherwise have posted over days the group had already read a board about — was inserted with `posted_at` set (`14:14:31Z`) and a reason naming this task. The first genuinely new Sunday post is the key `2026-09-13T03:00:00.000Z`, due Sunday 20 September, and it is not suppressed; one night (Sunday 13 Sep) appears in both the last Monday post and the first Sunday one, which is the smallest overlap an anchor shift can have | The group's week runs Sunday to Thursday — Egypt's working week — and this product is built for one specific friend group's week, not for a calendar standard. A board headed `This week` that cut on Monday split their own week in half and filed Sunday night's customs at the tail of a week that was already over, which is the same failure mode as a midnight night boundary that M2.5 already refused for the same reason. It is landed before M7.3 because M7.3 reseeds the weekly rating on this exact boundary and would otherwise be built on an anchor the group never chose. Nothing in it touches a number, which is why it is an M5 task and not an M7 one — M5 owns the windows (M5.9), and every M7 task by its own preamble changes the rating model | **Saturday** (considered with the user and rejected: it is the last day of the Egyptian weekend, so a week would open on a night the group often does play and close mid-weekend, and Sunday is the day the group's own week visibly restarts); **keeping Monday** (the ISO default nobody in this group chose, and the status quo this row exists to end); a configurable `CUSTOMS_WEEK_START` variable (one group, one week, and a second deployment knob nobody would ever set — the anchor is a product fact, not an environment one); moving the 06:00 boundary with it (unrelated, and M2.5's reasoning is untouched); a Sunday-anchored month (a month is a calendar month everywhere a reader has ever seen one); rewriting the landed M5 briefs and past Discord posts to match (history stays as written; this row supersedes) |
| 2026-09-15 | **M7.7's vision score and damage-mitigated numbers are read at ingest off the companion's posted `raw` block, not off a new field on the mapped `CompanionGameParticipantInput` — and no companion release is owed for them.** `mapEog`/`mapMatchDetail` (`packages/lcu/src/mapper.ts`) already attach the whole parsed, `scrubValue`-scrubbed block/detail as `raw` on every posted game, on both the live and backfill paths, and `scrubValue` (`packages/lcu/src/scrub.ts`) only redacts credential-looking keys (`token`/`password`/`jwt`/`mucJwtDto`, ...) — `VISION_SCORE`/`visionScore`/`TOTAL_DAMAGE_SELF_MITIGATED`/`damageSelfMitigated` don't match and pass through untouched | Every companion binary the group has ever run, including the one live tonight, already carries both numbers into `games.raw` for every game it posts (M2.10's raw-preserves-everything design paying off exactly as intended). Reading them there lets M7.7's migration and ingest fill and the backwards copy pass go out today, rather than waiting on a release, and keeps the mapped participant payload — already narrowed to the handful of stats the rating model reads — from growing two fields that only this one bonus (M7.8) needs | Adding `visionScore`/`damageSelfMitigated` to `companionGameParticipantSchema` and reading the mapped participant instead (every companion in the field would need a release that fills those two fields before ingest could trust them — the exact release-gating this decision avoids, for two numbers `raw` already carries); waiting for a release before landing M7.7's ingest and migration (would block M7.9 on M2.6, which owes a release for unrelated reasons with no forcing date) |
| 2026-09-15 | **The rating gate is a second exported function beside `gateGame`, not a flag on it** (M7.1). `apps/web/lib/ingest/fold.ts` exports `gateGame(players, durationS)` — unchanged, "a game happened that can be read", the universe `countedGames` folds for `/stats`, `/fun`, `/p/[puuid]` and the board's streak — and `gateRatedGame(players, durationS, raw)`, which is `gateGame` plus `isRatedMode(raw)` and adds the skip reason `game-mode`. The mode is checked **after** the other four, so a nine-player ARAM is still `participant-count` and a rebuild's existing skip counters cannot move. `isRatedMode` reuses `matchesQueue(gameModeFromRaw(raw), 'sr')` from `lib/games/queue.ts` rather than parsing `raw` a second time; the two rating callers pass `games.raw` through untouched (`rating.ts` selects `raw` for the one row it folds; `rebuild.ts` covers a whole season, so it selects the **projection** `raw->gameMode` and hands the reader back `{ gameMode }` — the only shape `gameModeFromRaw` ever looks at — rather than dragging every end-of-game blob across the wire, and its fence compares the mode as well as the winning side because a re-post merges bans onto that column). No `game_mode` column. **Entailed, and user-visible: Discord posts no result embed for an ARAM any more**, per the 2026-09-08 rule that a result posts only for a rated game (`onFinished` returns on `!rated`; `buildResultInput` needs both `mu` columns on all ten) — before this task a ten-player ARAM over 300s rated, so it was announced. The teams post is unaffected; no embed code changed | A mode check inside `gateGame` would have emptied `/fun?queue=aram` — that page folds `countedGames` and *then* filters to ARAM — and silently taken every ARAM game out of `/stats`, contradicting M5.26's "`/stats` and the rating fold stay mixed". A second export keeps one implementation of each question, keeps "the rebuild reproduces the live fold" a fact about there being one gate rather than two copies, and is greppable: `gateRatedGame` has exactly two callers and a page that imported it would be obviously wrong. A missing mode stays Rift (M5.26) because every night captured before the companion stored one was Rift | An options argument (`gateGame(players, durationS, { raw })`) — every existing call site then reads as "the rating gate with the mode left out", which is the bug this task fixed, written down; a boolean `rated` flag (a discriminated-union project does not gate on booleans); a `game_mode` column (blocks the fix behind a `db:migrate`, and M5.26 already refused one); duplicating the `CLASSIC`/null rule inside `fold.ts` (two definitions of "Rift" is how the pages and the fold start disagreeing); un-rating ARAM retroactively in this task (M7.11 is the one rebuild of this milestone) |
| 2026-09-15 | **The user's four new ideas of 2026-09-15 are not one milestone.** The balance percentage is **M3.31** (the tonight page's explanation, where the win chance and the gap already live); nemesis/best duo, "best comeback" and the award badges are **M8.1 to M8.3**; Guess the Award is **M8.4**, blocked on the user; skill decay is **M9**, blocked on the user and deliberately unscoped. M8's preamble names its own unit honestly: not a theme but a class of change — every task is a read over games already stored, none adds a capture or touches the rating model. | M7 earned one milestone because its four tasks all changed the rating model and all answered one complaint. These four answer four different wants, and forcing them into one M8 would have put a one-line display transform behind a migration and a guessing game, and would have smuggled a rating-model change in beside three stats ideas. M8 also sequences behind M7's `apps/web` thread (M8.1 to M8.3 after M7.4, M8.4 after M7.9), the way M7 was sequenced behind M5. | One M8 of four (a false theme, and it hides M9's stakes); four separate milestones M8 to M11 (three of them one task long); bolting all four onto M5 (M5 is closed enough that reopening it for a guessing game with a migration is worse than a new number) |
| 2026-09-15 | **The balance percentage is a transform of the stored win chance and never a second comparison of the two sides** (M3.31). `evenness(p) = round(100 - abs(p - 0.5) * 200)` beside `predictWin` in `packages/core`, and the tonight page renders it from `splits.blue_win_prob` — the same column that prints `Blue favored 54%.` — so `92% even` and the win chance cannot disagree, on the night or a month later. No mu-sum gap model, no new API field, no storage. | The ask described "expected outcome, trending down as the gap widens relative to the group's spread", which is what `predictWin` already measures and has measured since M1.3. A second win-probability model would be two answers to one question on one screen, in a product whose third principle is "fair by numbers, but explained". Reading the stored probability rather than recomputing from live ratings also keeps the page honest after somebody's rating has moved. | A new mu-gap-over-spread score in core (a second model); computing it from live ratings at read time (the two lines drift apart the morning after); putting it on the Discord embed too (M3.11's description is pinned copy) |
| 2026-09-15 | **"Best comeback" is *won against the odds*, read from the split's stored `blue_win_prob`, not the largest single-game `mu` swing** (M8.2). A ranked list of wins where the player's own side was posted under 45%, plus the one-game record for the longest odds beaten. | The largest `mu` swing is a measurement of `sigma`: the product doc already says movement scales with how unsure the model is, so the award would name the newest player in the group every time, and after M7.9's MVP bonus it would name the newest player on a night they farmed vision. It would also change under M7.11 and under every future rebuild, so "who had the best comeback in September" would read differently in October. The posted win chance is what the group was shown on the night, is never rewritten by a rebuild, and is what a friend means by a comeback: the bot said we would lose. | The ask as written (largest single-game climb); a short-window mu swing (same sigma problem, plus a window to argue about); recomputing the odds from today's ratings (then the page disagrees with what Discord posted that night) |
| 2026-09-15 | **Skill decay for inactive players is not scoped and not to be started** (M9), pending the user's confirmation and an evidence read: how many players actually have a 14-day gap between rated games, and how far their next game moved them against a regular's. | It widens `sigma`, which changes `ordinal` — Proven, what the board sorts on — and the balancer's uncertainty about a player, so it moves the board and the teams for somebody who has not played. That is the one thing all four of M7's changes avoided ("the all-time rating that forms teams is not reset by any of it"; the MVP bonus leaves `sigma` alone on purpose). It is the fifth rating-model change in a week, it answers a hypothetical rather than a reported complaint — M7 exists because the group spent a week saying the ratings were unfair — and it would want a second rebuild right after M7.11 was meant to be the only one. | Scoping it as an M8 task beside three stats ideas (rubber-stamping a rating change because it arrived in a batch); scoping it with product's own threshold and curve (the ask itself says the curve is a decision to record, and the user has not made it) |
| 2026-09-15 | **Guess the Award's "standout stat line" is M7.8's `performanceScores` / `mvpAce` from `packages/core`, not a new scorer in `apps/web/lib/mystery/`** — which makes M8.4's real dependency M7.7 and M7.9, not M8's stats tasks. **The rotation question is open and is the user's**: two challenges on one civil day is a migration, because `daily_mysteries.day` and `challenge_number` are both unique in 0013, while alternating days fits the table as it stands. No migration is written until the user answers. | M7 exists to make "who carried this game" one trustworthy, tested, pure answer; a second definition of "standout" in the web app would be exactly the second truth this project keeps refusing, and M7.10 already forbids recomputing the score at read time. The ask assumed the award scoring would come from the fun-stats work, and it does not: those tasks are counts and placement. | A `standout.ts` in `lib/mystery/` (a second model); product picking alternate-days itself (it changes the schema shape, which CLAUDE.md says is exactly when to stop and ask); two cards on `/` by default (halves the attention each game gets and makes the home page two guessing games above tonight's customs) |
| 2026-09-15 | The weekly track (M7.2) is `rateGameWeekly`, a second exported fold beside `rateGame` with its own `config.rating.weekly` block, tuned to `beta: 2.00` (OpenSkill default `25 / 6`) and `tau: 0.30` (default `25 / 300`) — and **five games is not reachable**: measured on M1.3's setup the weekly `mu` passes the field in game 4 (all-time: also 4) and weekly `sigma` drops below 5.00 in game 30 (all-time: 36), so product amended M7.2's target the same day to the movement per game (~79 display points from a fresh Sunday seed against ~29 for a settled all-time player) and the docs may not call the weekly channel faster to settle in any sense a player would read as good news | The brief asked for a weekly number that settles in about five games instead of thirty, and `beta`/`tau` cannot deliver it. A player's `mu` step is `sigma^2 * (1 - p) / c` with `c = sqrt(sum of the ten sigmas squared + 2 * beta^2)`, so `beta` accounts for about 35 of a `c^2` near 214 — the ten players' sigmas dominate it: zeroing it moves `sigma < 5.00` only from game 36 to 23, and below 2.00 the curve is flat. `tau` is the stronger lever and pays for movement by refusing to converge: `sigma` still reaches 5.00 at `tau` 0.35 (game 34), 0.40 (game 39) and 0.45 (game 59), and from about **0.46** up never reaches it at all (measured to 500 games). So `beta` stops at the knee (2.00) and `tau` at **0.30 — a knee and not a ceiling**, the value that keeps the week responsive while still settling inside a horizon a season reaches; far past it the board stops being about the week, since at `tau` 10 one game moves a settled player 100 display points and, for a settled player trading wins and losses, `sigma` climbs instead of falling (10.5 after one game, 30.3 by game 10, ~74 by game 200). The real counts are pinned in the tests and in `01-architecture.md`, and the five-game target stays as a skipped `TARGET` test naming the true number. What makes the week move is M7.3 reseeding from rank at the Sunday boundary (sigma 8.33, ~79 display points a game against ~29 for a settled all-time player) and not this tuning, which is worth about 3 points of it. | Softening the target until a test passed (forbidden by the brief and by M1.3's rule); tuning `rateGame` to help (every stored `mu_after` becomes unreproducible); `tau` near 10 for a literal five-game settle (a weekly board nobody can argue with, and `ordinal = mu - 2 * sigma` stops meaning anything when sigma diverges); a weekly seed with a wider sigma (seeding is out of scope for M7.2 and is the caller's, M7.3); a different model or a hand-rolled learning rate for the week (two rating maths to explain to the group instead of one with two dials) |
| 2026-09-15 | **Daily Mystery and Guess the Award alternate civil days** (M8.4, the user, settled 2026-09-15; supersedes the "open, the user's to settle" half of today's earlier Guess the Award row). One challenge per civil day as today, chosen by the date's parity in `CUSTOMS_NIGHT_TZ` off the same `dayKey` the cron and the lazy first GET already share. `daily_mysteries.day` keeps its unique and no table is duplicated; what is still owed is one **additive** migration at the next free number (0014 is M7.7's): `kind text not null default 'mystery'` — **stored, never derived from the date's parity at read time**, so a stored challenge keeps meaning what it meant if the rotation ever changes — the `category` check widened for the second game's categories, and `challenge_number`'s unique moved to `(kind, challenge_number)` so `Daily Mystery #41` does not become `#41, #43, #45`. An Award day with no scorable game **falls back to a Daily Mystery** rather than an empty card, and the fallback does not shift the next day's kind. `shareMissed`'s `Tomorrow's another case.` becomes `There's another one tomorrow.`, which is true on either day. | Two cards on `/` halves the attention each game gets, doubles the chance of an empty one, and puts two guessing games above the thing the home page is about; it is also the branch that would have forced `(day, kind)` and a reshape of a shipped table. One game replacing the other throws away a feature the group already plays. Alternation gives both games a full day and costs one column. | Two cards a day (reshape, split attention); replacing Daily Mystery; deriving the kind from the date at read time (no column, but last month's rows change meaning the day the rule does); one shared `challenge_number` across both games (the visible count skips) |
| 2026-09-15 | **Skill decay opens with a measurement, not a mechanism** (M9, the user, settled 2026-09-15; closes the "blocked, premise unconfirmed" row above). **M9.1** is a read-only script over the hosted project — how many 14-day-plus returns exist, how far the first game back moved those players in display points, and the same figure for ordinary games **by players at a similar `sigma`** — run **after M7.11** so it measures the model the group is actually on, writing nothing and deciding nothing. **M9.2**, `applyInactivityDecay` itself, stays unscoped until the user has read those numbers. | Movement already scales with `sigma`, so a returner moving further may be correct rather than broken — comparing them against the group average instead of against players at their own uncertainty would manufacture the problem it was sent to find. M7 exists because the group spent a week calling the ratings unfair; a fifth model change in the same week, on the channel that forms teams, needs evidence and not an idea. The read costs half a day and can end the milestone. | Scoping the decay function now with product's own threshold and curve; running the measurement before M7.11 (it would measure a model the group is about to leave); leaving M9 as three open questions with no owner and no first step |
| 2026-09-15 | **A `gamesSinceLastFill` the balancer cannot read as a count is the baseline, and a negative one is 0** (M7.5, core-engineer). `null`, absent, `NaN` and `Infinity` all price an off-role seat at the flat `offRolePenalty`; a negative number is clamped to 0 and so priced at the 240 maximum. `packages/core` does not throw for any of them | The brief fixes `null` (never filled, or no history) as baseline and `0` as the maximum and says nothing about the rest, and the balancer's job is to price a lobby, never to refuse a night over a number the caller computed oddly. Unreadable therefore means "no history", which is the behaviour every existing test pins; clamping the negative side keeps the formula bounded, since `gamesSinceLastFill = -1` would divide by zero and put an infinite price on one player, which is the hard block the whole task refuses to build. It is also the version that is easiest to explain to somebody who thinks the bot is rigged: a missing fact changes nothing, and nobody is ever unfillable | Throwing `BalanceError` (a bad row in one player's history would cancel the whole lobby's balance, and nine people wait while an admin reads a stack trace); treating `NaN` as maximum protection (a caller bug silently becomes a player advantage); leaving the negative case unclamped (`-1` is a divide by zero and an infinite cost, i.e. a hard block by accident) |
| 2026-09-15 | **"Missing" for the performance score means `null`, `undefined` or any non-finite number, it is decided per game and never per player, and a negative value counts as zero** (M7.8). `performanceScores` returns `null` for the whole game the moment one of the eight stored numbers behind the six components (`kills`, `deaths`, `assists`, `damage_to_champs`, `gold`, `vision_score`, `damage_self_mitigated`, `cs`) is absent for any one of the ten, and `mvpAce` returns `null` with it, so the game is rated exactly as it was before M7.8. A normalised term below zero is clamped to zero (and above one, to one), so a score is in `[0, 1]` whatever the caller hands over | The brief settles "one missing component anywhere means no MVP and no ACE"; what it does not say is what a `NaN` is. A `NaN` from a bad blob would poison a maximum and hand every other player a `NaN` score, which is a silent wrong MVP rather than a loud missing one — and CLAUDE.md's boundary rule is log and drop, never crash and never guess. Clamping is the same instinct: none of these six stats can be negative on a real scoreboard, so a negative is junk and scoring it as junk keeps the invariant the doc promises a player ("every term is between nothing and the best in the game") | Treating only `null` as missing and letting `NaN` through (one bad number silently decides the MVP for every other player in the game); scoring the players who do have the numbers and skipping the ones who do not (partial scoring, which the brief refuses: it ranks a player who has a vision score against one who does not); throwing on a missing stat (a game stored before M7.7 is not a programming error, it is the normal case until M7.11) |
| 2026-09-15 | **`applyMvpAceBonus` takes and returns a flat `{ puuid, before, after }[]`, and the exact-1.25x claim is about the formula, not about the number read back out of two stored `mu`s** (M7.8). The adjusted rating is `before.mu + (after.mu - before.mu) * 1.25` (or `* 0.80`) with the factor built once from `config.rating.mvp`, and the test pins that equality with `toBe`. A page that recomputes the delta as `mu_after - mu_before` off the stored columns sees a factor of 1.25 to about `1e-15` and not to the bit, because `before.mu + x` rounds once and subtracting `before.mu` again cannot undo that rounding; the tests say so in both forms. `sigma` is copied through with `toBe`-level equality, always, for all ten | A flat list keyed by puuid is the shape both of M7.9's callers already have after a fold, it makes the MVP and the ACE findable by the same key the tie-break uses, and it keeps the function from having to know which array is blue. On the arithmetic: the storage unit for this product is `mu`, not the delta (M1.3, and every `mu_after` in `game_players`), so the last-bit difference is inherent in the schema and the only honest options were to pin it where it is exact and name the tolerance where it is not | Taking `{ blue, red }` side arrays (a fourth argument of puuids in team order, and two ways to say the same thing); returning only the adjusted `after` ratings (the caller then re-pairs them with the players by index, which is the class of bug this project keys everything by PUUID to avoid); storing or returning the delta so the ratio is exact to the bit (a second rating unit in a schema that has one); asserting the read-back ratio with `toBe` (it fails, for a reason that has nothing to do with the bonus) |
| 2026-09-15 | **`game_players.vision_score` and `damage_self_mitigated` are nullable with no default, and one reader fills them everywhere** (M7.7, platform half). Migration `0014` adds both as plain nullable `integer`s with a `>= 0` check and no default, so `null` keeps meaning "this game never stored it" for M7.8. Ingest does not grow a second parser: it reads them off `payload.raw` through `rawFactsFromUnknown` (`apps/web/lib/stats/rawFacts.ts`), the same function `/fun` has used since M5.27, which already walks both stored shapes (end-of-game `teams[].players[].stats` and match-detail `participants[]` + `participantIdentities[]`), already prefers the uppercase key over the camelCase duplicate, and already drops the bot puuid. The one-off backwards pass — `pnpm --filter web copy-raw-stats [--dry-run] [--game <id>]`, `apps/web/lib/ingest/copyRawStats.ts` — reads the same function, only ever fills a null, never overwrites a stored number, and prints rows filled and **rows that still have a null in either column** — a row it half-filled counts as still short, because the bonus needs both numbers and that count is what an operator reads to decide whether the history can be trusted. **A number the column cannot hold is stored as `null`, not passed through** (M7.7 review): both writers gate every value through `storedStat` (`apps/web/lib/ingest/statValue.ts`), which drops a negative, a non-finite and anything past int4. The check constraint on `0014` cannot be the gate — `games` is written before `game_players`, so an insert Postgres refuses 500s the request *after* the game is stored, and `upsertGamePlayers` runs again on every retry, so the companion posts into the same 500 for ever with no way out but a hand edit of the database; an overflow past int4 does the same from a value no check could have seen. The same gate keeps one bad blob in the history from throwing out of the middle of the copy pass. The two fields are also added to `companionGameParticipantSchema` (nullish, defaulting to null) so the boundary is honest about what the server accepts, but a mapped value is used **only when the blob is silent**; no companion in the field fills them and none has to | One reader means a row the copy pass wrote and a row ingest wrote cannot disagree, which is the whole point of a rebuild that has to reproduce history — a second extractor in `lib/ingest/` would have been a second truth about the same column, and the first blob shape it got wrong would show up as an MVP nobody recognises. Filling only nulls is what makes the pass safe to run twice, safe to run while games land, and safe to point at production more than once. Raw-first / mapped-second rather than the other way round because `raw` is the source the reference doc verified and the blob is what a rebuild will still have in a year, while the mapped field is a promise no released exe has made yet | A dedicated `lib/ingest/rawStats.ts` (a second walker over the same two shapes, and the bot-puuid and uppercase-vs-camelCase rules re-derived); `not null default 0` (M7.8's tank scores as a tank who mitigated nothing); an `update ... from` SQL migration doing the copy inline (a migration that reads a jsonb blob with the app's parsing rules baked into SQL, unrunnable twice as written, and impossible to dry-run); re-posting every game through ingest to fill the columns (a second post is a documented no-op on `lcu_game_id` and would have to stop being one); clamping a nonsense number to `0` or to `INT32_MAX` instead of nulling it (a made-up number gets scored, and a tank clamped to zero mitigation is the exact mis-scoring the nullable columns exist to prevent); leaving the check constraint to catch bad values (it catches them after the request has already failed) |
| 2026-09-15 | **M7.7's migration and backfill ran against the hosted "kustom" project** (the lead). `pnpm db:migrate` pushed `0014` (two nullable columns, no rewrite). `pnpm --filter web copy-raw-stats --dry-run` then the real run: 44 games had a gap in either column, all 44 were fillable from `games.raw`, 436 rows filled (436 vision score, 436 damage mitigated), 0 rows still short. A third run confirmed idempotent (0 filled, 0 still short). Every stored game now carries both numbers, so M7.8's bonus has a complete history to score once M7.9 wires it in | The group's whole custom history predates M7.7, so without this pass the MVP/ACE bonus would only ever apply to games played after today — two models in one history, which M7.9's own brief refuses. Running the dry-run first and reading its report before the real pass is what the command was built for; a clean 0-still-short meant there was nothing to decide before running it for real | Running the real pass without reading the dry-run first (the report exists so a bad number stops the operator, not the pass); leaving the history unfilled until M7.9 lands (the same "two models" problem, just deferred); running it from a worktree instead of the lead's own shell (a write to production is not a task an isolated agent should make unsupervised) |
| 2026-09-15 | **The two week windows sort on Rating, not on Proven** (M7.3, the user, settled 2026-09-15; **amends the 2026-09-15 "second, independent rating" row** where it says a week window "shows the weekly Proven", and **the copy in the "weekly board never says it settles" row**, whose middle sentence explained the order in terms of Proven). `This week` and `Last week` order by the weekly `displayRating(mu)` and print that as the row's one number, with `sortKey` the weekly `mu` itself; **no Proven appears on a week board at all** — no column, no label, no small-type second number — and `rating`, `sortKey` and `proven` on a week row all come from the weekly track, never mixed with the all-time one. `All time`, `This month`, `Last month` and `/p/[puuid]` are untouched: Proven is still the number and still the sort, and `00-product.md` now carries this as the one stated exception to its own rule. The board copy is rewritten with it: the long sentence becomes `Every week starts everyone back at their rank on Sunday, so a good Tuesday shows up here straight away. The board sorts on Rating — what the bot thinks you are after this week's games — and takes nothing off for playing only a few, so a clean two-game week can sit above a longer patchy one. It is a handful of games either way, so these numbers swing. All time is the settled one, and the one that makes teams.` and the embed footer `Every week starts everyone back at their rank on Sunday, so these numbers swing, and two clean wins can top a longer patchy week. All time is the settled one, and the one that makes teams.` | With a from-scratch weekly fold the `- 2 * sigma` subtraction is enormous for every row and largest for whoever played fewest, so Proven would have ranked a 4W 4L week in eight games above a clean 2W 0L week in two for the single reason that the longer week is "more proven" — a leaderboard headed `This week` telling the person who won both their games that they are below the person who split eight. That is the same confusing-fairness complaint that opened M7, arriving through the fix. The user's own reasoning: per-game movement **already** shrinks as a player's weekly sigma falls, which is inherent to how OpenSkill updates `mu` and is true whatever the board sorts on, so playing more is still worth something and sorting on Rating loses none of it — it just stops charging a short clean week twice. Proven's caution earns its place on `All time`, where it answers "have you proved it over thirty-odd games"; a week is not that question and never will be, and this product already refuses to print a number ordered by one the reader cannot see, so the Proven column goes with the Proven sort rather than sitting there unexplained and unused | **Sorting on Proven for consistency with every other window** (the option put to the user and rejected: one rule everywhere is worth less than a weekly board that ranks the week honestly, and the inconsistency is cheap to explain in one sentence under the board, which the page carries anyway); keeping a weekly Proven printed in small type under the Rating (a second number on the one board where it means least, and the copy would still have to define it); sorting on Rating but leaving the old Proven copy (copy that describes a page the reader is not looking at, which is the failure M7 exists to end); widening the weekly seed's sigma so Proven hurts less (tuning a number to make a display rule survive, and M7.2 already measured what `tau` does); a games-played minimum before a row appears on a week board (it hides the exact person the change is meant to be fair to, and a week board with four rows is not the week) |
| 2026-09-15 | **The empty-team guard lives in `balanceScore`, not in `predictWin`** (M3.31, core half). The brief says an empty team throws "exactly as `predictWin` behaves today", and measured, it does not: `predictWin([], red)` returns **0**, `predictWin(blue, [])` returns **1** and `predictWin([], [])` returns **0.5** — OpenSkill sums an empty side's ratings to nothing and answers a probability anyway. So `balanceScore` checks both sides itself and throws `balanceScore: blue must not be empty` / `red must not be empty`, and `predictWin` is left byte-identical (M3.31 acceptance 4, and every stored `blue_win_prob` stays reproducible). `evenness` keeps its own `[0, 1]` guard, which rejects `NaN` and `Infinity` too, and `balanceScore` adds no arithmetic of its own: it is `evenness(predictWin(blue, red))` and a test pins that equality | An empty side is a caller bug, not a 100%-certain prediction, and "the bot said the other team was certain to win because nobody was on ours" is the hardest possible line to explain to a player who already thinks it is rigged. Putting the guard inside `predictWin` would have changed a function M1.3 pinned and M1.4's balancer calls 126 times a night, to fix a case neither of them can reach — the balancer only ever hands it two teams of five. Guarding at the new outer function costs two `if`s and changes nothing that already works | Adding the guard to `predictWin` (changes a pinned function's behaviour, which acceptance 4 forbids, for a case its only production caller cannot produce); letting the 0 / 1 / 0.5 fall through to `evenness` (`balanceScore([], red)` would print "0% even" for a lobby with one side missing, and "50% even" for two empty ones); throwing only when both sides are empty (the one case OpenSkill's answer is arguably fine is the only one it would catch) |
| 2026-09-15 | **`gamesSinceLastFill` counts a player's last twenty *rated* games and is read once, for every lobby size** (M7.6, platform-engineer). The window is `config.roles.inferenceWindow` games **per player**, restricted to rows with `mu_after is not null` — the fold's universe and role inference's — so a remake or an ARAM (M7.1) is neither a fill nor a step away from one: it is not in the window at all. The query runs against `games` (ordered `started_at desc`, then `id desc`, `limit 200`) with `game_players!inner` filtered to the people in the lobby, because `game_players` carries no timestamp of its own; the 200 is a bound on the group's games, not the player's window. It is **not** behind `loadRotation`'s ten-or-fewer early return: that return is about sit-out order, which is meaningless at ten, while fill protection matters most at exactly ten. A failed read logs and hands `null` to all ten rather than throwing | The brief fixes the window at twenty and names `counts_for_role_inference` as the fact, and leaves two things open: which games are in the window, and how far back to look for them. Rated-only is the choice that keeps this number and the inferred role reading the same *universe* — and it is the only one where an ARAM night cannot quietly decay somebody's protection with games the balancer never made seats for. The two windows are the same size over the same universe and are deliberately **not** the same twenty rows: `inferRoles` drops filled and null-role games before it slices, while this walk must keep the filled ones, since they are what it counts. Bounding at the `games` level is what makes it one read: PostgREST cannot take twenty rows *per player*, and truncating there can only lose a fill older than 200 group games — which reads as `null`, the documented baseline — and can never move a distance to a wrong number, because the count runs newest first and only older rows fall off the end. The failure branch is the brief's own: a balancer that refuses to split because it cannot remember last night is worse than a flat penalty | One query per player (ten extra reads per balance, against the brief's one); paging every rated row for the roster the way `roles.ts` does for the rebuild (unbounded work on the path a companion waits on); counting unrated games as steps away from a fill (an ARAM night decays protection the balancer never caused); a `games_since_last_fill` column, a view or a trigger (a second copy of a fact the schema already holds, and a write on a read-only task); computing it inside `loadRotation` (it would inherit the early return and be `null` for exactly the ten-player lobbies it exists for); throwing on a read failure (nine people wait while an admin reads a stack trace) |
| 2026-09-15 | **Role detection gets measured before anything is built on it: M7.12 and M5.35 are scoped, M7.13 is not** (the user, approving two follow-ups from product's role-detection research; product scoped them the same day). **M7.12** — a read-only measurement of how often the client's own `detectedTeamPosition` gets a ten-human custom's roles right — is in **M7**, after M7.11, although M7's preamble says every task there changes the rating model: this one changes nothing, and it is in M7 because what it gates is M7's subject. It owes no rebuild, does not wait on M7.11, and **blocks every role-aware proposal until it lands**. **M5.35** — is `gameData.teamOne[]/teamTwo[].selectedPosition` a real per-player role at `GameStart`, or leftover queue preference — is in **M5**, numbered after M5.34 and dispatched with the **M5.18** capture night, because that recording is its only input; it adds no endpoint, no companion behaviour and no release. **M7.13**, a revision of M7.8's six performance components once roles are known to be trustworthy, is named in M7's status row as conditional and **is not scoped**: no brief, no ID reserved beyond that sentence, not to be picked up until the user has read M7.12's answer and said so. No player-visible behaviour changes, so `00-product.md` is untouched. | The evidence in `03-lcu-reference.md` refutes `timeline.lane`/`timeline.role`, not `detectedTeamPosition`, which the same doc's M5.18 rule already treats as ground truth — and which has never been measured on a real ten-human custom. Scoping a role-aware scoring change on an unmeasured field is exactly the move that opened M7: a number moving for a reason the group cannot defend. Measuring costs half a day and changes nobody's rating; being wrong costs a rebuild and another week of arguments. | Scoping M7.13 now with the measurement as its first step (the measurement would then be work done to justify a decision already taken, and a "not good enough" answer would have nowhere to land); putting M7.12 in M5 beside the other role tasks (it gates M7's scoring, not M5's backfill, and M5 is closing); putting M5.35 in M7 with M7.12 (it is a companion capture, owned by `companion-engineer`, and cannot run without M5.18's night); leaving both in the research report until M7 closes (the M5.18 capture night is the only chance at M5.35's data and it is not scheduled by us) |
| 2026-09-15 | **The weekly fold is a read-time function in `apps/web/lib/board/weekly.ts`, and nothing is persisted** (M7.3). `foldWeeklyRatings(games, seeds)` takes the window's rated games and one seed per player and returns a rating, a seed and a per-game trail for each of them; `windowRows` in `lib/board/load.ts` calls it for `this-week` and `last-week` only, beside the read it already makes. No table, no column, no migration, no cron. The seed is `lib/ingest/seed.ts`'s `seedFor` — the stored `ratings.seed_*` pair first, the live rank only for a player who has never been rated — and the order is the rebuild's, `started_at` then `lcu_game_id`. A game that is not five and five is skipped with a `console.warn`, never thrown | A backfilled game lands in the week it was played with nothing rewritten, which is why windows work at all (M5.9), and a closed week reads the same on Tuesday as it did on Sunday because both ends of the fold are stored facts. A persisted weekly table would need its own repair path for every late backfill and a second definition of "the week" to keep in step with the board's. The whole fold is ten games over twenty players and costs nothing at read time | A `weekly_ratings` table written by the fold or by a cron (M7.3's brief allows it only with a decision row saying what forced it: nothing did); generalising the fold over an arbitrary window so a monthly track could reuse it (refused outright — the month windows keep the all-time number for ever) |
| 2026-09-15 | **A board row carries the track its numbers came from: `BoardRow.track` is `'all-time'` or `'weekly'`** (M7.3). `rating`, `sortKey`, `proven` and `climb` all come from the one the field names, and no row ever mixes them. The renderer branches on it — a weekly row prints `Rating` in the primary slot, drops line 2's small-type number, carries no `settling` chip and sits under `boardLegend('weekly')`, which is the word `Rating` | The tonight page's rail and the home page render `BoardRow`s with no window in sight (they are serialized into the RSC payload), and the Discord posts map rows to embed lines without knowing which window they asked for. Inferring "is this a week" from a `WindowKind` in four places is four places to get it wrong; the row that holds the numbers is the thing that knows where they came from | Passing `window` down to every component and post (four call sites, and the rail has none to pass); a second row type for weeks (two shapes for one list, and `sortBoardRows` would need both) |
| 2026-09-15 | **A week row's expand shows the weekly deltas, not the stored ones** (M7.3, extending M5.30). `playedBreakdown` takes the player's weekly fold on a week window, so each game under the row reads `1416 (+38)` off the week's own track. A game the weekly fold skipped is not listed | The expand exists to explain the number it sits under. A row whose total is the weekly Rating opening into games whose ratings are the all-time ones would print numbers that do not add up to it, which is the same "one row, two tracks" defect the `track` field exists to prevent | Leaving the expand on the stored pair (the arithmetic visibly disagrees with the row above it); hiding the expand on a week window (M5.30's control disappearing on the default board with no explanation) |
| 2026-09-15 | **`LeaderboardEntry.proven` is renamed `score`, and the board embeds take a `track`** (M7.3). The field is "the one number this line prints, which is the one the board sorted on": Proven on `All time` and the month windows, the weekly `Rating` on the two week ones. `boardFooter(track)` picks `SETTLING_SENTENCE_SHORT` or `WEEK_BOARD_SENTENCE_SHORT`, and `post.ts` reads both off the rows rather than re-deriving them from the window | The nightly post reads `this-week` and the Sunday post reads `last-week`, so both are weekly now. A field called `proven` holding a Rating is a comment that lies, and the embed builder stays pure by being told which track it is printing rather than looking one up | Keeping the name and assigning a Rating to it; two builders, one per track (the same embed twice, drifting on the next copy change) |
| 2026-09-15 | **No public match dataset is ever used to score MVP / ACE. The idea is closed, not parked** (M7.8's scoring, the user, settled 2026-09-15, off product's research into role-fair scoring). The specific proposal — a per-role z-score table built from a public dataset, so a support's vision and an adc's damage were each read against that role's real population instead of against the other nine people in the lobby — is rejected outright, and no future task may reopen it without new information of a kind that does not exist today. MVP and ACE stay on M7.8's within-game normalisation, where every component is divided by the best of the ten in that same game. **Volume was never the blocker and the research says so**: legitimate datasets exist, are large enough, and technically carry all six of M7.8's components — a 101,843-match Kaggle set (Platinum+), a 10,002-match Challenger-only HuggingFace set, PandaSkill's academic set (missing 2 of the 6) and Oracle's Elixir (pro only). No first-party Riot dataset is published; official esports data is partner-gated. **The confirmed fallback order, now explicit**: ship M7.9 and M7.10 as they stand, watch real MVP and ACE names on real nights, and only then consider (a) nudging the existing weights in `config.rating.performance`, or if that is not enough (b) the already-named, still-unscoped **M7.13** — kill participation as a seventh component of the same within-game formula, no role dependency, no external data, no population baseline. One candidate is named here so it is not rediscovered and researched a second time: a Kaggle set that turned out to be scraped op.gg data, disqualified on this project's standing rule against building on scraped commercial-site data (the same ground the "call op.gg for a real OP Score" option was rejected on above) and not on anything below | **Every legitimate dataset is drawn from one narrow rank band, and this group's lobby is not.** The datasets' own authors say so — the Challenger-only set's documentation warns in as many words that its findings may not generalise to other skill levels — and the group's own pinned worked example in `00-product.md` puts Bronze II through Master in the same ten. Scoring a mixed-rank lobby against any single-band external population saturates the percentiles: the highest-ranked player in the room lands near the top of nearly every component whatever actually happened in that game, and the lowest-ranked lands near the bottom for the same reason, so MVP would report *who is the best-ranked person in the lobby* rather than *who carried tonight* — which is worse than today's version of the same complaint, because it would be wrong in a way that never varies. This is a structural mismatch between the population and the lobby, not a data-quality problem, so it cannot be fixed by a bigger, cleaner or more recent dataset, and there is nothing to wait for. Within-game normalisation has the opposite property by construction: the baseline is the nine people who were actually there, so a Bronze player can win MVP on a Bronze-heavy night, which is exactly what the group means by carrying. It also keeps `packages/core` pure and the project free of an external data dependency it would have to vendor, version and defend | A per-role z-score table built from a public dataset (the researched proposal, rejected for the saturation above); waiting for a dataset that spans the same skill range as our lobbies (there is not one, and one appearing is not something a plan can depend on); **parking the idea as still open** (the user closed it — an open idea with no path forward is a question the lead re-asks every milestone); scoring against a single-band dataset anyway and calling the saturation a known limitation (the limitation *is* the feature failing at its one job); vendoring the op.gg-scraped Kaggle set despite its provenance (the scraping rule does not have a "the numbers are good" exception); building M7.13's seventh component now to compensate (it is unscoped on purpose and waits on M7.12, and it is a fallback, not a replacement for evidence from real nights) |
| 2026-09-15 | **`detectedTeamPosition` is good enough to pair players by role, measured (M7.12, platform-engineer).** A read-only measurement over the hosted "kustom" project: 23 real ten-human Summoner's Rift customs (`source = 'eog'`, exactly ten `game_players` rows, `raw->>'gameMode'` CLASSIC), 230 participants, 7 nights, 2026-09-08 to 2026-09-14. **Five distinct non-null roles on 46 of 46 sides** — one pattern, `adc jungle mid support top`, 100% of sides, zero duplicated and zero missing — **0 of 230 rows with a null `role`**, and the 46 Smite holders are exactly the 46 players the client calls `JUNGLE`, with the highest-neutral-cs player being the `JUNGLE` on 46 of 46 sides and the highest-vision player being the `UTILITY` on 45 of 46. A 30-participant hand-read across the first, middle and last night of the window disagreed with none. So **the M5.18 rule's use of `detectedTeamPosition` as ground truth stops being an assumption**, and role-aware proposals are unblocked. Two limits are recorded with it and are part of the decision, not footnotes: **nothing in the blob separates `TOP` from `MIDDLE`** (Smite pins jungle, vision and cs pin support; a systematic top/mid swap would survive this measurement), and this is one group, one patch window, ten people who pick a role and play it. **Nothing was written and no code changed**: no script was kept (M7.12 allowed one under `apps/web/scripts/`; there was nothing for it to do that two pasted queries do not, and no hosted credentials exist outside the SQL editor to exercise it with), so the deliverable is the numbers plus the re-run queries in `03-lcu-reference.md`. **This decides the answer, not what to do about it**: no weight, no component and no column moves here, and **M7.13 remains unscoped** until the user has read this and said so. | The project distrusts role detection, and it was right to — but the evidence it distrusted, the confusion table in `03-lcu-reference.md`, refutes match-history `timeline.lane`/`timeline.role`, a retired match-v4 guess in a different vocabulary from a different endpoint. `detectedTeamPosition` is the game server's own end-of-game classification and had never been checked on a ten-human custom; the same doc's M5.18 rule was already leaning on it as the judge. Measuring cost half a day and moved nobody's rating. The result is not marginal — 46 of 46 sides, and the independent Smite check agreeing 46 of 46 with zero disagreements in either direction, is a cleaner signal than any tuning decision in this repo has had — so recording it as a measured fact with its two limits is worth more than another round of "we do not trust roles" that no longer matches what the data says. | Continuing to treat the `timeline` confusion table as evidence about `detectedTeamPosition` (different field, different endpoint, different vocabulary — and it is the thing that table's own rule is judged *against*); scoping M7.13's role-aware scoring off this result in the same pass (tuning after seeing a number is how a referee stops being one, and the user has not approved it); keeping a script under `apps/web/scripts/` for the numbers (an unrun file plus a `CLAUDE.md` command line, for a measurement whose whole point is that it happens once); calling it good enough without recording the top/mid gap (the gap is real, no column in the blob closes it, and a future role-aware change that assumes top and mid are as pinned as jungle would be building on a number this read never produced); waiting for M7.11 or for a live ten-human eog fixture before answering (this reads roles, not ratings, and the fixture would confirm the shape, which storage already proves, not the accuracy, which only many real games can) |
| 2026-09-15 | **The performance score reads the role: three weight vectors, not one, and M7.8 is revised in place (M7.13).** The user's design, approved after reading M7.12's answer; product scoped it the same day. Every player's six components are still normalised inside their own game and still weighted, but which weights is chosen by a **role bucket**: `carry` = {top, mid, adc}, `jungle` = {jungle}, `support` = {support}. Rows sum to 1.00 — carry `KDA .15 / dmg-to-champs .30 / gold .20 / vision .05 / mitigated .10 / CS .20`, jungle `.25 / .20 / .15 / .15 / .10 / .15`, support `.25 / .05 / .05 / .40 / .15 / .10`. **Three, not five, because M7.12 recorded that nothing in the blob separates top from mid**: the carry bucket is drawn so that distinction is never needed, while the two roles the data *does* pin to 46 of 46 — jungle by Smite, support by vision — are exactly the two the flat formula misread. **A game where any of the ten has `role` null gets no MVP and no ACE**, which is M7.8's existing all-or-nothing missing-input rule applied to a new input, per game and never per player — one model declining to answer, not a second model. Its price, accepted: every backfilled game carries `role: null` (2026-09-09 row; M5.18 unresolved), so the backfilled half of the history keeps no MVP, permanently, until a capture night changes that. **Revised in place rather than layered**, and therefore **landing before M7.9**: M7.8 has landed but nothing it produces has ever reached a player — M7.9 and M7.10 are not built, no row stores an MVP, no post has named one — so the formula changes before it is ever applied instead of being shipped, printed and replaced a fortnight later. Two steps: the pure formula in `packages/core` (`performanceScores` keeps its name and gains the role; no second scorer), and a **read-only battle test** of the new picks against the old over the same 23 real customs M7.12 measured, with the comparison table and a plain-language judgement appended under the brief — an inspection against known games, not a fit, and if the new picks look worse the task stops and reports instead of tuning. One sub-question is verified first and deliberately not decided here: whether `damageDealtToObjectives` is real in both stored shapes (the 16.17 fixtures carry it per player in both, which is where to look, not the verification) — if it is, it opens **M7.14** with the reweighted jungle row already written down, because `game_players` has no column for it and a seventh component means a migration, an ingest change and a backwards copy, which does not belong in a pure-core task. | M7.8 scores a support and an adc on the same weights, with vision at 0.25 and damage to champions at 0.20, so on the numbers a support is asked to win MVP on the carry's terms and the carry on the support's. That is "why did *they* get it" waiting to happen on the first night M7.10 prints a name, which is the argument the whole milestone exists to end. Nothing role-aware could be scoped before M7.12 measured whether the client's roles are real; it did, the result was not marginal, and the user acted on it. **This design needs no data that does not exist**: the weights are hand-reasoned from what each role is for and validated by inspection against this group's own real games, which is exactly the fallback the two rejections below named as the right one. | Adding role-awareness the two ways already closed on 2026-09-15 — **a per-role baseline from a public dataset** (rejected: every legitimate set is one narrow rank band and this group's lobby is Bronze to Master, so percentiles saturate and MVP reports who is highest-ranked rather than who carried; nothing here reads any population outside the ten in the game) and **fitting weights from this group's own ~44 games** (rejected: not enough data — and the battle test is deliberately an eyeball comparison of two formulas' picks, not a fit, and may not retune the weights to make its own table look nicer). A **five-role split** (needs the top/mid distinction M7.12 says the data does not have; five hand-set vectors defended by nothing). **A second scorer beside the flat one** (two models in one history, the thing the dataset row settled against). **Scoring a role-less game on the flat weights as a fallback** (a partial answer, which is what the missing-stat rule already refuses, and a guess printed as a fact). **Waiting for M5.35 or the M5.18 capture** (neither is scheduled and this reads the role the eog block already stores). **Following the recorded fallback order literally** — ship M7.9 and M7.10, watch real nights, then consider a revision (that order was written when the only proposals on the table needed evidence this project could not get; retroactively scoring 23 real, already-played games is the same evidence a fortnight earlier, and it costs nothing to change a formula that has never been applied, where changing one the group has read a name off costs a rebuild and an explanation). **Putting the seventh component in M7.13** (a migration and a backwards copy inside a pure-core task that has to land before M7.9, and every stored game blanked until the copy ran). |
| 2026-09-15 | **Damage to objectives becomes the seventh component, and the three things M7.13 left for whoever scoped it are decided here (M7.14, product).** The stat is real — a hosted read over all 44 stored games found it present on 100% of rows on both shapes, non-zero on 98.6% of live and 96.3% of backfilled ones, and per-player on every game checked — so M7.13's pre-answered jungle row ships as written: `KDA .20 / dmg-to-champs .20 / gold .10 / vision .15 / mitigated .10 / CS .10 / **objectives .15**`, with `carry` and `support` carrying it at **0.00** and their scores bit-for-bit unchanged. The weights are not re-derived; they were the user's, in M7.13's brief, and M7.14 repeats them. **(1) The missing-input rule stays universal, not scoped to weight > 0**: a null on a *carry*, whose weight on this component is 0.00, still means that game has no MVP and no ACE. **(2) M7.14 does not gate M7.9 or M7.10, and does gate M7.11.** **(3) Inside M7.14 the halves are strictly ordered**: migration, ingest fill and the extended `copy-raw-stats` land and run first, and the seventh component merges into `packages/core` only after the copy reports **zero stored rows still short** — a non-zero count stops the task and becomes a finding rather than a merge. Also settled here, so nobody builds the same thing twice: the column and every name in our own code are `damage_to_objectives` / `damageToObjectives` (not the client's `damageDealtToObjectives`), the reader is `asInt(TOTAL_DAMAGE_DEALT_TO_OBJECTIVES) ?? asInt(damageDealtToObjectives)` because the match-history detail carries **only** the camelCase key, the copy pass extends the existing script rather than adding a second one, and M7.13's read-only battle test is run **once** — two columns if it goes first, three (flat / six-component / seven-component) if M7.14's core half has already landed. | **(1)** `componentsOf` already checks all of its inputs for every player before any bucket is consulted, and the rule was written per game and never per player before roles existed. Three reasons it must stay that way: the normalisation denominator is the whole game, so a carry with a null does not score zero on the component, they drop out of the maximum and the jungler's share is then measured against a field of nine — the jungler's score would depend on whether a *laner's* number happened to be stored; a weight-scoped rule would let a `config.ts` nudge from 0.00 to 0.02 silently change which past games are scorable at all, so a tunable would rewrite history; and "a game we cannot read completely has no best player" is a sentence a friend accepts, while the same sentence with an unless clause about weights is not. **(2)** M7.13 gated M7.9 because it *replaced* the formula M7.9 would start storing answers from, and the case for revising in place was that nothing had been applied. This adds to a formula that is now settled, behind a hosted migration and a copy run the user has to trigger, so gating the whole tail of the milestone on schema work buys nothing: any MVP M7.9 stores before this lands is restated by M7.11, which is what M7.11 is for. Before M7.10 is still strongly preferred, because a Discord post is never rewritten and a rebuild that moves an MVP to the jungler would leave the post and `/p/[puuid]` disagreeing about one night for ever. Before M7.11 is hard: after it, the milestone owes a second rebuild and the one-rebuild resolution of 2026-09-15 breaks. **(3)** Because the all-or-nothing rule means the moment core demands a column the database has not filled, every stored game loses its MVP until a script finishes — the exact cost M7.13 refused to pay, avoided entirely by ordering rather than by weakening the rule. The naming call follows the sibling and not the client: damage to champions is already `damageToChamps` in core and on the boundary from a key called `TOTAL_DAMAGE_DEALT_TO_CHAMPIONS`, and the same stat family read against a different target may not be named by a second convention inside one weights table. | **Missing only where the weight is above zero** (rejected above: it breaks the denominator, lets a tunable decide which history is scorable, and needs a second sentence to explain); **renormalising the jungle row when no objectives were taken in a game** (weights that differ game to game are a second model; the existing `best <= 0` branch already contributes zero to everybody and that is the honest answer for a game with no objectives in it); **gating M7.9 on this** (stalls the end of the milestone behind a migration for a formula change M7.11 restates anyway, and M7.13's own brief had already ruled that this does not belong in front of M7.9); **landing the core half first and running the copy after** (every stored MVP blank in between, for nothing); **a second one-off script beside `copy-raw-stats`** (two commands in CLAUDE.md doing one job, and a second place for the `storedStat` gate to be forgotten); **`damage_dealt_to_objectives`, the client's own words** (0014's naming rule, but it has no sibling in the formula and this one does); **requiring the uppercase key** (it is absent on match-history detail, so every backfilled game would quietly stay null); **a mapper change and a companion release** (the 2026-09-15 decision stands — every exe already puts the scrubbed block into `games.raw` and `scrubValue` redacts nothing like this key); **tuning the jungle row now that the stat is confirmed** (it was set once, by the user, and re-deriving it is the hand-setting-a-weight-twice this was scoped to prevent); **running the battle test twice** (the same 23-game read, done again, with the two changes conflated in the second table). |
| 2026-09-15 | **M8.3's board reads its awards through a second, parallel query rather than folding a second `climbs()` into `lib/board/` (web-engineer, reviewed).** The brief named this exact fork in advance ("if the board loader cannot reach the awards' game list without a second query, stop and say so") and it fired: the board's already-loaded rows cap at 1,000 games and keep only rated seats, filtered by `season_id`, with no `main_role` column selected; the awards' universe (`countedGames`) reads up to 2,000 games including unrated ones, with no season filter, and `bestOffRole` needs `main_role`. Reusing the board's own data would have produced a badge that could name someone the `/stats` line and the Sunday post do not. `lib/stats/load.ts`'s new `loadAwardWinners` runs beside `loadStreaks` — the same precedent the board already uses for a row's streak — and `lib/board/load.ts` calls it in parallel with the board's own read, only on `Last week` and `Last month` (proven by a test client that throws on any query on the other three windows, so those pay nothing extra). A failed award read is swallowed to no badges rather than 500ing the page, mirroring `loadTopPlayersOrNone`'s existing failure shape. | A single source of truth for what an award is (`awardsView`) matters more here than a single query for the board row: two answers to "who won" disagreeing between the board and `/stats` is the exact failure class M7.4's own design avoided by having the award and the board read one weekly seed. The cost — one extra parallel read on two of five windows, non-cacheable today — is the brief's own accepted price for keeping that guarantee | Folding award computation into `lib/board/load.ts`'s existing row read (cheapest, but produces a badge answering a different question than `/stats` and the post do, since the game universes genuinely differ); widening the board's own read to match the awards' universe (changes what `/leaderboard` shows everywhere, not just the badge, and reopens rated-row filtering the board relies on elsewhere); failing the page when the award read errors (a placement feature should not be able to take down the leaderboard); caching the award read (a real future option, not built here, since the brief did not ask for it and the escape hatch only asked to "stop and say so", not to optimize) |
| 2026-09-15 | **M8.2's "Won against the odds" compares the rounded percent the group actually saw, not the stored float, and prints two different thin-window sentences depending on whether a record exists (web-engineer, reviewed).** `AGAINST_THE_ODDS_PERCENT = Math.round(AGAINST_THE_ODDS_MAX * 100)` and the fold filters `ownChance(play) < AGAINST_THE_ODDS_PERCENT`, where `ownChance` is the same `sideWinChance` rounding the tonight page, the result embed and the recent-games row already apply. A window can print a record (one win under the threshold) with an empty ranked list (nobody has two): the fold picks `ODDS_EMPTY` when there is no record at all and `ODDS_NONE_TWICE` when there is a record but the list's own floor of two is unmet, so the card never claims "nobody was won from under 45%" directly above a record naming five people who did. | The brief's own rule is "the number on this page has to be the number the group was shown that night" — a split stored at `0.449` was posted as `45%` in every other surface, so comparing the raw float would let a `45%` row sit under a heading that says "under 45%", disagreeing with itself in the one game a reader would actually check. The two-sentence split follows from the same instinct that drove M5.12's per-window empty strings: a card printing a fact that contradicts the block directly under it is worse than two similar-sounding sentences | Comparing the raw stored probability instead of the rounded percent (lets a game the group was told was "45%" appear under "under 45%", the exact self-contradiction the brief's wording rules out); one empty sentence for both thin cases (would either bury a real record under "nobody did it" or claim two wins exist when only one does) |
| 2026-09-15 | **`Start a lobby` widens to every linked player as M4.13, on M3.6's `/api/me/*` class, and the admin path is deleted rather than kept beside it (product, scoping).** `POST /api/me/lobbies/start` through `withViewerAuth`; the presser is `context.me.player.playerId` from the session; `me.player === null` is a 403 carrying a new `START_LOBBY_NOT_LINKED` in `lib/me/copy.ts` (`Pick yourself out of the list first, then you can start a lobby.`), the shape `/api/me/role-tonight` already uses for `ROLE_TAP_NOT_LINKED`; **admins get no branch at all**, because `authorizeAdmin` can only say yes to a session whose Discord id matched a `players` row, so every admin is already a linked player. The tonight control's condition becomes the `linked` boolean `TonightView` already computes for M4.10, not `viewer.isAdmin`. | This is the swap the 2026-09-10 row promised ("widening it when M3.6 lands is an auth swap and a moved file, not a rewrite"), and M3.6 landed that same day; nobody picked it up for five days and the user asked for it to be a real task on 2026-09-15. Deleting the old path keeps M4.2's own "one route, both surfaces" rule: two paths for one command is the second copy that drifts, and `/admin` posting to the friends' route is exactly what "one set of rules" meant. A 403 with a sentence rather than a gate that requires a player row keeps `/api/me/link` reachable on the same class and matches the only other route of this kind. | A fourth route class or an `is_admin` escape hatch in the handler (nothing needs one); keeping `/api/admin/lobbies/start` as an alias (two paths, two tests, one of them eventually wrong); gating the class itself on a linked player (it would break `/api/me/link`, whose whole job is the unlinked visitor); drawing the control for signed-in unlinked viewers (they cannot press it, and picking yourself needs a live lobby) |
| 2026-09-15 | **The suspended `Sign in with Discord to start a lobby.` returns with M4.13, in the `idle` state only, as a sentence plus the existing `Sign in with Discord` button — never a disabled button — and it is also what the control prints on a 401 (product, scoping).** A signed-in visitor with no player row gets **no** new copy: the page's existing `SIGNED_IN_NO_LOBBY` at the foot of the column is already the true sentence for them. | The 2026-09-10 suspension gave two reasons and only one of them dies with the widening. The other — "this page's one sign-in already lives on the role card forty pixels away" — is simply false in `idle`: `RoleTonight` draws nothing for a signed-out visitor with no live lobby, so the idle page has no sign-in control anywhere on the site, and idle is the one state the button exists in. A friend whose session expired would otherwise open the page on the night it matters and find no door. The 401 reuse is the same fact in the same words: `sign in required` is gate vocabulary, and no friend-facing surface in this product prints a gate's words. | The M4.2 brief's disabled button carrying the sentence (M3.20 keeps focus on a pressed control and the designer's amber rules never dress a dead one); drawing it in `filling` too (that block is a readout, and the sign-in there would promise a press that is refused anyway); a new sentence for the signed-in unlinked visitor (they are one tap from linking the moment a lobby exists, and `SIGNED_IN_NO_LOBBY` says so); leaving it suspended (the idle page's only door stays shut) |
| 2026-09-15 | **A seat with no reported `lobby_members.side` keeps the tonight page's side line up, the same as a mismatched one (M4.11, web-engineer, reviewed).** `anySeatOnTheWrongSide` treats `liveSide: null` as "wrong" rather than skipping it or treating it as a match. The line's absence is therefore a positive statement: all ten reported a side and all ten match, never "most did." | `lib/commands/switchSide.ts` already skips a seat with no reported side — it is not one of the ten the automated queue can move, because there is nothing to compare it against. The page has to be louder than the queue in exactly that gap: a friend with no reported side yet is a friend the automation cannot help, so the reminder has to stay on screen for them specifically. Treating `null` as a match would silently hide the one case a human still has to act on themselves. | Treating a `null` side as a match, so the line only reflects seats the client has actually placed (would hide the exact seat that most needs the reminder); skipping null seats from the check entirely, counting only reported ones (same failure, worded differently); waiting for every seat to report before drawing any state at all (a lobby with nine reported and matched and one not yet reported should not look identical to one where nobody has moved) |
| 2026-09-15 | **M7.9's MVP/ACE bonus does not reach the weekly rating track, and M7.9's own acceptance criterion 6 was wrong to assume it would (lead, waiving the criterion after platform-engineer + reviewer both confirmed the premise).** `lib/board/weekly.ts`'s `rateGameWeekly` is a second, independent read-time fold from that week's own seeds — not a call to `foldGame` — and a `WeeklyPlayer` carries no stat line to score against. `foldGame` (the one path both `rating.ts` and `rebuild.ts` call) now runs `rateGame` → the exported `gameAward(players, winningSide)` → `applyMvpAceBonus`, but that is all-time-only. | Giving the week the bonus too would mean widening `/leaderboard`'s own `game_players` select by the nine performance columns plus `role` — a second surface change M7.9 never scoped and the brief's own out-of-scope list would have refused. The alternative, special-casing the weekly fold to fake a score it has no inputs for, is worse than confirming the gap with a test (`weekly.test.ts`) and moving on. | Widening the weekly board's select to carry the bonus (a real option, just not this task's — open a new one if the group wants it); special-casing `rateGameWeekly` to approximate a score (invents a second, cruder formula nobody asked for); leaving acceptance criterion 6 unmet without a decision row (the reviewer's blocking objection — a criterion cannot just go quietly unsatisfied) |
| 2026-09-15 | **The wipe-and-refold assertions in `rebuild.integration.test.ts` compare at `RATING_EPSILON`'s nine decimals, not byte for byte, and this predates M7.9 (lead, confirmed by reviewer with a control run).** Reverting `within()` to exact string equality fails two cases by up to `1.03e-13` on `mu` and `8.9e-15` on `sigma` — and `sigma` is never touched by the MVP/ACE bonus, which proves the drift is not the bonus's arithmetic. | Postgres/PostgREST prints a `double precision` at 15 significant digits (`extra_float_digits = 0`), so the live fold reads back a *rounded* value on every game while a from-scratch rebuild folds the same chain in memory with full precision; the two were always going to diverge in a low bit once a season ran long enough, and `RATING_EPSILON = 1e-9` already exists in `rebuild.ts` as the tolerance the rebuild itself uses to decide whether a row is worth rewriting. Comparing test dumps at the same tolerance the product already promises is the honest assertion; comparing them exactly was passing on luck. | Leaving the exact-string assertions in place (already failing before M7.9's bonus touched anything, once a long enough chain existed — this task just surfaced it first); tightening `RATING_EPSILON` to catch this (would make the real `rebuild-ratings` command rewrite rows that only differ in Postgres's own float printing, for no behavioural gain); loosening the epsilon further (would risk masking a real divergence between the live and rebuild chains, the exact invariant M7.9 depends on) |
| 2026-09-15 | **The whole M9 milestone (M9.1, M9.2), M6, and the remaining verification-only tasks — M2.11, M4.2's live check, M5.6, M5.18's table, M5.35 — are deferred to a later release (the user).** None is dispatched for the rest of this release even if its own blocker clears first (M7.11 landing does not unblock M9.1; a client session or capture night falling before then does not unblock M2.11/M5.6/M5.18/M5.35). | Every deferred item is either a real-world verification step this release does not need to wait on (Windows run, a watched live nightly session, match-history depth, a capture-night table) or a rating-model change M7 deliberately avoided scoping (`sigma` decay) and the tray app polish, neither of which the group asked to gate this release. Deferring them keeps the queue honest about what "done for this release" actually means, rather than leaving tasks marked "blocked" that were never going to be picked up regardless. | Leaving them as ordinarily blocked/not-started (reads as "will run the moment the blocker clears," which is no longer true); dropping them from the doc entirely (loses the record of why they exist and what unblocks them, needed the next time this release's scope is revisited) |
| 2026-09-16 | **A printing surface reads `game_players.role` with no fallback when it names an MVP, even where the same row prints a fallback role beside it (M7.10, platform-engineer).** The result embed's two columns already fall back to the stored split's role when the client reported no position; the MVP/ACE line does not, and neither does `/p/[puuid]`. | The fold read `game_players.role` and nothing else, so a game core refused to score because a position was missing must stay unscored on every surface. The alternative names somebody as the MVP whose delta was never amplified by a quarter — the exact defect M7.10 exists to prevent, printed as a fact in a channel. One column, one answer, however good the second source looks. | Using the display role everywhere (would invent MVPs for games the fold gave none, on the strength of a split the scoreboard contradicts); dropping the split fallback from the columns too (a real regression in what the post can say about a role, for no gain) |
| 2026-09-16 | **The MVP / ACE line is the result embed's last field, with a zero-width space (U+200B) for a name (M7.10, platform-engineer; a copy-table row is owed to the designer).** Product asked for one line under the existing block and wrote no heading for it. | A field is the only place in a Discord embed that renders *under* two inline columns — the description is above them and the footer belongs to `Kustom · game 47` — and Discord rejects a field whose `name` is empty, so an unnamed field is a name that takes no room. Writing a heading would have meant an agent inventing product-owned copy, which this project forbids. Being **last** is the other half: `guardEmbed` gives ground from the last field backwards when a post passes 6000 characters, so the line the brief calls lowest-priority is the first to go and the ten rating rows are never cut for it. | A named field (`Carried`, `MVP`) — copy nobody wrote; appending to the description (renders *above* the scoreboard, and turns a two-name line into a third prose sentence); appending to the footer (would break the `Kustom · game 47` copy row); a third line inside the `Red` column (the line is about both sides) |
| 2026-09-16 | **`/p/[puuid]` names the MVP and the ACE by calling `gatedGameAward` at read time, and each row carries one word about one reader (M7.10, web half + lead reconciliation).** `lib/board/load.ts`'s `Recent games` read selects the same nine stat columns and the role that `lib/ingest/rating.ts` and `lib/ingest/rebuild.ts` select — the same query the lineup already made, nine columns wider, and only on the five games that page lists — and hands them to `gatedGameAward`, the one gated wrapper both this page and the Discord result embed call (M7.10, platform half). `RecentGame.award` is `'mvp' \| 'ace' \| null` **about the page's own player**, never the game's `{ mvp, ace }` pair. Product's sentence joins the existing M5.15 explanation strip as a **second sentence in the same paragraph**, not a second `.cn-explain` block. **Reconciliation note:** the web-engineer's worktree independently built its own gate-and-compare helper (`lib/board/award.ts`) rather than the platform half's exported `gatedGameAward`; the lead kept the platform half's loader/type implementation (the two surfaces then share one literal function, not two call sites of the same logic) and layered the web half's rendering, copy and tests on top unchanged, since `RecentGame.award`'s shape matched exactly between the two independent implementations. | M7.9's brief assumed the adjusted `mu_after` made the two amplified rows recoverable from what the fold stored; it does not, which is why `gatedGameAward` is exported. Calling the identical function is what makes the page and the embed the same answer by construction, rather than by two implementations agreeing today. One word per row rather than the pair keeps a second surface from drawing its own conclusion from two puuids it was handed. One paragraph because the two sentences answer one question ("why is this number the size it is"), and a second 3px `brand` leading rule under the first would read as a second, unrelated explanation. | A stored MVP column (a schema change M7.9 explicitly avoided, and a number that can go stale against a rebuild); re-deriving the formula in the web app (two implementations of the thing M7 exists to make trustworthy); keeping both worktrees' independent gate helpers side by side (two code paths computing the identical gate is exactly the drift this milestone exists to prevent, even though both happened to agree here); widening the shared `loadGameRows` for every window (nine columns × ten rows × a thousand games for pages that print no award) |
| 2026-09-16 | **Migration `0016` is exactly three statements, and the kind is a column** (M8.4, platform half). `alter table daily_mysteries add column kind text not null default 'mystery'` with `check (kind in ('mystery','award'))`; the inline `daily_mysteries_category_check` is dropped and re-added **per kind** — `kind = 'mystery'` takes the five M5.32 categories, `kind = 'award'` takes `kda, damage, gold, vision, mitigation, cs, objectives`; and `daily_mysteries_challenge_number_key` is dropped for `unique (kind, challenge_number)`. No new table, no new service, no `ensureToday*` beside the one there was, and `day` keeps its unique so there is still one challenge per civil day. Existing rows take the default and are the Daily Mysteries they always were. | The kind has to be **stored**: a rule that re-derives what last month's rows were from today's rotation is not a rule, and it would rewrite the meaning of a stored challenge the day the rotation changes. The per-kind category check is what lets one column serve two vocabularies without either game being able to store the other's words. The unique moves because `Daily Mystery #41` is printed in the heading and the share string, and a shared counter would print #41, #43, #45 at a friend who plays every day. | A second `guess_the_award` table (two services, two selectors, two sets of avoidance, and a second answer to "which challenge is today"). A `kind` derived from the date's parity at read time (cheaper migration, stored rows that change their minds). Widening the category check to a flat list of twelve (any row could then claim any category, and the check would stop saying anything). |
| 2026-09-16 | **Guess the Award's standout is `performanceScores` from `packages/core`, ranked across games by the winner's gap to the runner-up** (M8.4). One candidate per game: the player core scores highest, with `score` stored in `interesting_score` as `top - runnerUp`. The category — which stat the card is about — is the component that player led the game in by the widest relative margin, read off the same raw scoreboard with no weights and no second score. Nothing in `apps/web/lib/mystery/` computes a standout. | M7 spent four tasks making one trustworthy answer to "who carried this game"; a second definition in the web app would be a second answer to it. A performance score is comparable only inside its own game, so ranking *games* by the winner's raw score would be a cross-game claim the score cannot make — the gap to the runner-up is the honest reading of "stood out" and it is used to order candidates and for nothing else. The number is never printed, which M7.8's rule already requires. | A `standoutScore` in `lib/mystery/score.ts` (the second definition the brief forbids). `mvpAce` (it needs five a side and answers a different question — who carried each *team*). The winner's own score as the cross-game rank (a 0.9 in a quiet game outranking a 0.7 in a fought one, on a number that does not mean the same thing twice). |
| 2026-09-16 | **ARAM is excluded from Guess the Award and still allowed for Daily Mystery** (M8.4). `awardCandidates` takes Rift only — a missing `gameMode` still counts as Rift, as everywhere else — while `mysteryCandidates` keeps M5.32's rule of preferring Rift and taking ARAM when Rift has nothing. An award day whose whole window is ARAM falls back to a Daily Mystery. | Four of the seven components mean nothing on the Howling Abyss: there are no wards to place for a vision score, no dragons or towers to take for objective damage, and CS and gold are a different game on one lane. Daily Mystery asks whether a stat line is unusual, which an ARAM answers fine. | Scoring ARAM with a fourth weight vector (a second model, for a mode nobody asked to be scored). Excluding ARAM from both games (it would narrow M5.32's pool for no reason and change a shipped game's selection, which M8.4 is out of scope for). |
| 2026-09-16 | **The two games share one selection module, one builder, one set of tables — and the view layer was reused, not duplicated** (M8.4, platform half; the view's own call belongs to `web-engineer`). `select.ts` gained `kindForDay` and `pickAwardStandout` beside `pickMystery`; the pure day-building moved out of `ensure.ts` into `build.ts`, which serves both kinds and takes no client; `ensure.ts` stays the one `ensureTodayMystery`. On the view side the service now returns `kind` on the play and result views and `copy.ts` gained `AWARD_TITLE`, `challengeTitle`, `challengeHeading(kind, n)` and kind-aware `shareSolved` / `shareMissed`; `mysteryHeading` is gone, and `MysteryView.tsx` / `MysteryLive.tsx` pass `kind` through at four call sites rather than growing a second component tree. | One challenge a day means one writer; two would race on `day`'s unique and disagree about which game today is. Moving the deciding into a pure module is what makes the rotation, the fallback and the shared recent-answer memory testable without a stack — the fourteen-day walk in `build.test.ts` needs no Docker. | A second view tree (allowed by the brief if labels were not enough; they were). A `shareMissed(n, kind = 'mystery')` with a default (a silent default printing the wrong game's name on an award day). |
| 2026-09-16 | **Guess the Award's card is Daily Mystery's card with five words swapped, and the five words are the ones that name *the thing*** (M8.4, view half; the wording is an agent's, and a `05-design.md` copy-table row is owed). `copy.ts` gained `AWARD_CRIME` (`The award`), `AWARD_CASE_CLOSED` (`Award settled`), `AWARD_TODAY_CLOSED` (`Today's award is settled`), `AWARD_NEXT` (`Next award`) and `AWARD_BLAME` (`Who did everyone pick?`), bundled behind one `gameCopy(kind): GameCopy` the view reads once per card instead of a ternary per sentence; `firstDetectiveYou(kind)` and `mostAccusedLine(kind, name)` became kind-first like `challengeHeading` and `shareSolved` before them. Everything true of both games — `Who was it?`, `Guess now`, `Reveal a clue`, `Locked in`, `Correct` / `Wrong`, `Your result`, `Today's community`, the empty state — stays one constant. **`First detective` stays the badge's name on both days**: it is this site's word for whoever gets the day right first, it is one column (`first_correct_at`) and one field (`MysteryPersonal.firstDetective`), and only the sentence under it changes. The empty card has no kind and keeps the neutral Daily Mystery wording rather than guessing a game nothing built. | A card that says `The crime` over a damage-mitigated award is the page lying about what it asked; a card that renames its buttons per day is two games pretending to be different when they are one guess with one lock. The bundle is the smaller of the two shapes once more than three sentences diverge — the view reads `copy.blame`, and the next sentence that has to differ is one field, not a fifth ternary in the markup. Kind-first arguments match the four functions the platform half already landed, so no call site has to remember which of two orders this function uses. | Two components (a second tree to keep in sync for five strings); five inline `kind === 'award' ? … : …` ternaries in the markup (the same branch written five times, in the place where it is hardest to see all five at once); renaming `First detective` per day (two badge names for one column, and a share line that means something different on Tuesday); an `AWARD_EMPTY` (a game nothing built has no name) |
| 2026-09-16 | **The `/mystery` tab title follows the day's kind, off one `React.cache`d load** (M8.4, view half). `generateMetadata` and the page body both call one `cache(() => loadMysteryOrNone())`, and the title is `Guess the Award · Kustom` or `Daily Mystery · Kustom` by the loaded state's `kind`; an empty day keeps `Daily Mystery · Kustom`. | A tab reading `Daily Mystery` over a card reading `Guess the Award` is the page arguing with itself in the one place a phone user sees before the card paints. It has to be `React.cache` and not a second call because the loader reads the visitor's cookie and touches their session row — two loads per page view would be two round trips to say the same thing, and would open the session twice. | A static title (wrong half the time); deriving the kind from the date in the metadata function (a second answer to "which game is today", and `build.ts`'s fallback can make it wrong); `generateMetadata` loading and the body loading separately (two session touches per view) |
| 2026-09-16 | **M7.11's one rebuild ran against the hosted project, closing M7 (the user, running the command; the lead verifying the dry run and the two real runs).** Dry run: 44 games considered, 38 rated, 6 skipped (2 participant-count, 4 game-mode — ARAM, correctly excluded by M7.1). Real run (`--force`): 45 considered / 39 rated (one game landed between the dry run and the real run — not an exit-2 mid-run case, just a normal gap between two separate invocations), wrote 430 `game_players` rows and 25 `ratings` rows, 24 seeds stored for the first time, 8 inferred-role pairs moved, biggest single move 10.780 mu on a game that gave five players their first-ever rating. Third run: 0 rows written, confirming idempotence. The group was told the same day in plain words, no version numbers. | This is the history-changes-under-the-group moment M7's preamble named from the start: three of the milestone's changes (ARAM exclusion, the MVP/ACE bonus, the weekly track's own reseed boundary) only take effect on stored history once this runs, and the brief's own rule was **one rebuild, at the end, announced** rather than one per fix. 24 first-time-stored seeds is not a red flag: it means this was the first full-season rebuild since the stored-seed table (M5.7) landed, so most players simply never had a locked-in seed row before now — a full re-fold from seeds is exactly what this command does every time, and a season that had never been fully rebuilt was always going to look like this the first time it was. The 10.780 mu biggest-mover is explainable in one sentence (first game, highest `sigma`, largest possible OpenSkill swing), which the brief requires before announcing anything. | Running a rebuild after each of M7.1/M7.7/M7.9 individually (the brief's rejected default — three WhatsApp arguments in a week instead of one); treating 24 first-time seeds as a bug worth investigating before shipping (it is the expected shape of a first full rebuild, not a sign anything folded wrong); skipping the third confirming run (idempotence is exactly what tells you the rebuild has reached a fixed point rather than still drifting) |
| 2026-09-16 | **On the closed card the award names itself and prints its own number; the countdown and the nav tab name neither game** (M8.4 fix pass, after review). Three copy decisions an engineer made and the designer's copy table still owes rows for: (1) the reveal carries `categoryLabel(category)` beside `Award settled` / `Case closed` and, on an award day, one extra performance row — `awardStatLabel(category)` and the number read off the revealed scoreboard through `awardStatNumber` / `awardStatValue`, the same pair the hook used, with the generic `Damage` / `CS` row dropped when the award *is* that stat so no number is printed twice. A column migrations 0014 / 0015 never filled reads `Not recorded` (`MYSTERY_NO_STAT`), never `0`. (2) `MYSTERY_NEXT` and `AWARD_NEXT` are replaced by one `DAILY_NEXT` = `Next game` on both cards and the empty one. (3) the nav's `MYSTERY_LABEL` = `Mystery` becomes `DAILY_LABEL` = `Daily`. Also `Most wrongly named: X` becomes `Most wrong picks: X`, and **`detectives` stays a shared word** (`fooledLine`, `First detective`): it names the person playing, not the thing they were asked about. Supersedes the `AWARD_NEXT` clause of the row above. | Four of the seven awards (gold, vision, mitigation, objectives) have no other row in that panel, so the card settled `Most gold` while showing damage, CS and a duration and never the gold — the one number the day was about. The countdown runs to civil midnight, which is when *the other* game starts, so `Next award` on an award day is wrong by a day in exactly the way product's `There's another one tomorrow.` already fixed for the share line. The nav tab renders on `/`, `/leaderboard`, `/games`, `/stats`, `/fun` and `/p/<puuid>`, where nothing knows today's kind and nothing should pay a query to learn it, so the only honest label is one true on both days. | Rendering `result.hook.lines` on the closed card (the stored hook is the play card's sentence, and duplicates the KDA and the duration the panel already prints); printing `0` for an unstored column (null is not zero — CLAUDE.md's own rule, and `copy-raw-stats` exists because of it); a nav label that fetches today's kind (a data lookup on every page of the site for one word); keeping `Mystery` in the nav and calling it the family name (it is one of the two games, not the family) |
| 2026-09-16 | **The week never carries the MVP / ACE bonus, and this is a product decision and not a gap** (M7.9's waived acceptance 6, closed by product 2026-09-16 in M7's close-out audit). The 2026-09-15 row waived the criterion as an engineering fact — `lib/board/weekly.ts` is a second read-time fold with no stat line to score — and left it as "open a new one if the group wants it", which is a question nobody had asked and nobody could answer from a page. It is answered now: **one bonus, on the one number that forms teams.** Carrying a game moves your all-time rating a little further and moves your week exactly as much as anybody else's win, for ever. `00-product.md`'s "Carrying is worth a little more" paragraph says so in a friend's words | The week is a second *reading* of the same nights, not a second model, and the whole argument for the bonus is about the number that decides who plays with whom. Widening the weekly board's select by nine stat columns and a role to reproduce it would buy a number nobody balances on, nobody is paid in, and nobody asked for — and it would put two amplified deltas into a board whose own sentence tells the reader its numbers swing because the week is a handful of games. Leaving it undecided was the real cost: a friend who sees `MVP` on their own page and then a weekly board that does not reflect it has found a contradiction the docs could not answer | Widening the weekly fold to carry the bonus (the option the waiver kept alive; refused, above); special-casing `rateGameWeekly` to approximate a score (a second, cruder formula); leaving it "open if the group wants it" a second milestone running |
| 2026-09-16 | **`/leaderboard` counts the games that moved a rating, `/stats` counts the games the group played, both are right, and every count says which it is** (M7.18, product, from M7's close-out audit). M7.1 split one gate into two on purpose; since the rebuild the two counts print under the same heading over the same dates — `All time · 39 games` on the board against `All time · 43 games` on `/stats` — and a board row takes its record from one universe and its streak from the other. Neither count moves and neither universe changes: the board gets a second slot-line formatter that says `rated games`, `/p/[puuid]` names the rated count on its header record and prints one sentence where its played-game sections begin, and `countedGames`' header comment stops promising an invariant M7.1 ended. The streak stays one computation and stays mixed (M5.21) | The board's record sits beside a climb, so a game that moved no rating does not belong in it; `/stats` is about what the group played and M5.26 already decided ARAM belongs there. So the numbers are not the bug — the silence is. This product's third principle is "fair by numbers, but explained", and a four-game difference between two tabs with no word of explanation is exactly the shape of the complaint the whole milestone was opened for | Folding ARAM back into the board's record (puts games in the record that contributed nothing to the climb beside it, and re-opens M7.1); making `/stats` rated-only (empties `/fun?queue=aram` and contradicts M5.26 in as many words); a second, rated-only streak so a board row stops mixing (two definitions of one word in one app, to fix less than it breaks); leaving it — the counts were identical by construction before M7.1 and nobody has had to explain them yet |
